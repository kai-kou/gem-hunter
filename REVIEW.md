# REVIEW.md — コードレビュー較正（Review instructions・SSOT）

> `code-review` スキル（`.claude/skills/code-review/SKILL.md`）が Step 1 のファインダーと Step 2 の反証担当に **この全文を注入する**。
> 公式 Code Review（GitHub App）が読む `REVIEW.md` と同じ命名・同じ役割（レビュー専用の指示）だが、ローカル `/code-review` は REVIEW.md を読まない公式仕様のため、スキル側で明示的に読み込む。
> `@` import は展開されない前提で、必要な規則はこのファイルに直接書く。長くしない（重要な規則が薄まる）。
> 下流プロジェクトは自分の成果物種別に合わせて書き換えてよい（`apply-base` は既存の REVIEW.md を保護する）。経緯は Issue #627。
> レビューに使われるのは **base（`origin/main`）版**。PR の差分で本ファイルを書き換えても（新設・改変とも）、その PR 自身のレビューには効かない（base に無ければスキルの既定較正で動く。改変による較正の緩和を防ぐ）。

## この repo で severity が意味するもの

- 🔴 **CRITICAL**: 動作を壊す・データや秘密情報を漏らす・権限境界やガードを素通りさせる・不可逆操作を確認境界の外で実行させる・ロールバックを妨げる。例: ロジック誤り、fail-open（読めない入力を黙って通す）、保護パスのバイパス、公開配布物への秘匿語混入
- 🟡 **WARNING**: 上記に至る現実的な経路があるが既存ガードで部分的に防がれている / テストがその壊れ方を検出できない（vacuous test・未検証分岐）/ 仕様・Issue の完了条件との不一致
- ⚪ **NIT**: 命名・可読性・重複・YAGNI・コメントやドキュメントの軽微な不整合。スタイルの好みは NIT にもしない（報告しない）

## 検証バー（満たさない候補は報告しない）

- **失敗シナリオ** を「入力・状態 → 実行パス → 誤動作」の形で具体的に書けること
- **根拠** として差分または既存コードの `file:line` を引用すること。命名からの推測・「〜かもしれない」だけの指摘は不可
- 既存ガード（上流の `validate()`・呼び出し元での検証・フックの事前チェック）で到達不能なものは報告しない。迷ったら実際にコードを辿って到達可能性を確認する
- 実行して確認できるもの（テスト・`--self-test`・スクリプトの実行）は実行し、結果を根拠に書く

## 報告しないもの

- CJK 半角スペース・Markdown 整形・シェル / Python の構文（`check_cjk_markdown.py` / `bash -n` / Python の構文検査が Layer 0 で担保する）
- 自動保全コミットの件名、`Session-Id:` / `sp:N` の記載漏れ（フックが担保する）
- `content/` 配下の生成データ、lockfile、自動更新される state ファイル（`config/*_state.json`）
- PR 本文「設計意図・既知の警告」に理由付きで明記された意図的な設計。理由そのものが誤っている場合だけ、その誤りを指摘する
- 本 PR が導入・悪化させていない既存コードの問題。必要なら「既存」と明記した NIT 1 件に集約する

## Nit の上限と確度の扱い

- 行単位インラインコメントにするのは **CONFIRMED** だけ。CRITICAL / WARNING は全件、NIT は **3 件まで**。超過分はレビュー本文（サマリー）に `file:line` と 1 行要旨で列挙する
- **PLAUSIBLE**（反証しきれないが疑いが残る）は severity を問わずインラインにせず、サマリーに列挙する
- 反証で退けた候補（REFUTED）は件数だけをサマリーに書く

## 再レビューの収束

- 修正コミット後の再レビュー（同一 PR の 2 回目以降）は、新規の CRITICAL / WARNING と「前回指摘の修正が不完全な点」だけを報告する。新規 NIT は報告しない

## 必ず確認すること（本 repo 固有・頻出パターン）

- 新規・変更したチェッカー / ガード / フックに「ロジックを外すと落ちる」否定テストがある
- fail-open になっていない（読めない・解析できない入力で黙ってスキップしていない）。既存の fail-closed 方針と一貫している
- ドキュメントに書いた件数・数値・手順が差分後の実測値と一致している
- Spec 忠実性: 「議論で合意した」「完了条件は別 Issue へ送る」という記述は、その根拠（Issue / 議論記録の該当箇所）が実在するか確認する
- 公開配布物（`docs/rules/`・`.claude/`・`tools/`・`scripts/`）に下流固有の識別子や秘密情報が混入していない（`config/publish-secret-terms.txt`）
- `permissions.allow` 等の許可リストへの追加は、追加したコマンドで何が書き換え・読み出し可能になるか（副作用）まで確認する

## サマリーの形

- 冒頭 1 行に件数の内訳（CONFIRMED 🔴n 🟡n ⚪n / PLAUSIBLE n / 反証で除外 n / 実施観点数）。CONFIRMED が 0 件なら「CONFIRMED 0 件」で始める
