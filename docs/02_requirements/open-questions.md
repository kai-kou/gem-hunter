# 要件定義に入る前の現状認識と論点リスト（Open Questions）

- **作成日**: 2026-08-17 JST
- **目的**: 既存ドキュメント（コンセプト・競合調査・MVP 最低要件・技術リサーチ 2 本）を突き合わせ、要件定義書を書き始める前に **解消が必要な論点** を洗い出す。
- **使い方**: 各項目に付いた ID（`D-n` / `Q-n` / `R-n`）で回答・決定を返してもらい、決まった順に本ファイルへ決定内容を追記する。全 `D-n` が埋まった時点で要件定義書（PRD / SRS）の執筆に入る。

---

## 1. 現状認識

### 1.1. 揃っているもの

| ドキュメント | 位置づけ | 状態 |
|---|---|---|
| [`00_concept/initial-concept.md`](../00_concept/initial-concept.md) | プロダクトビジョン「IndieGems」（B2C・Gem Score・目的別タグ・週刊ダイジェスト・Freemium $5-9/月） | 記述あり。ただし仮説段階で検証データなし |
| [`01_research/market/20260817-...competitive-analysis.md`](../01_research/market/20260817-github-repo-search-competitive-analysis.md) | 競合 30 数社の棚卸しとホワイトスペース 8 件の評価 | 密度が高く一次情報の出典も明示。ただし **結論がコンセプトと逆方向** |
| [`02_requirements/minimum-requirements.md`](./minimum-requirements.md) | MVP の機能要件 FR-1〜7・非機能・テスト・受け入れ基準 | 単体としては完成度が高い。ただしコンセプトのコア機能は全て対象外 |
| [`03_design/architecture/20260817-nextjs16-...md`](../03_design/architecture/20260817-nextjs16-architecture-research.md) | Next.js 16 の汎用アーキテクチャ指針（815 行） | 網羅的。MVP への適用メモあり |
| [`04_development/api/20260817-github-api-research.md`](../04_development/api/20260817-github-api-research.md) | GitHub API 全域の指針（1287 行） | 網羅的。ただし「GitHub App + Webhook 中心」の SaaS 前提で、MVP 想定と乖離 |

### 1.2. 揃っていないもの

- **実装物ゼロ**: `package.json` もアプリケーションコードも未作成（現状はドキュメントのみのリポジトリ）。
- **プロジェクトミッション未記入**: [`docs/project-mission.md`](../project-mission.md) がベース雛形の `{{...}}` プレースホルダのまま。KPI・判断基準・品質ゲートが未定義のため、「迷ったときに何を優先するか」の拠り所がない。
- **空ディレクトリ**: `01_research/user/`（ユーザーリサーチ）、`02_requirements/user-stories/`、`03_design/ui-ux/`、`03_design/data-model/`、`adr/`、`meeting-notes/` がすべて空。
- **Issue / PR による議論履歴ゼロ**: 意思決定の経緯がドキュメントのコミットにしか残っていない。

---

## 2. 決定が必要な論点（`D-n`・ユーザー判断）

> 私（Claude）が仮定で決めると成果物が根本から変わってしまうもの。**`D-1` が全体の分岐点** で、他はそれに従属する。

### D-1【最重要】どの製品の要件定義書を書くのか

既存ドキュメントは **3 つの異なる製品** を指している。

| 候補 | 出典 | 内容 | 主要リスク |
|---|---|---|---|
| **(a) 検索 Web アプリ（素朴版）** | `minimum-requirements.md` | キーワード検索 → 一覧 → 詳細ページ。認証・DB・スコアリングなし | 差別化がない（GitHub 純正で足りる）。ただし確実に完成する |
| **(b) IndieGems（B2C ディスカバリー）** | `initial-concept.md` | Gem Score・目的別タグ・週刊ダイジェスト・Pro 課金 | データ基盤（ETL・永続化・スコア計算）が必須で MVP 規模を大きく超える |
| **(c) エージェント向けライブラリ選定 API / MCP** | `competitive-analysis.md` の Recommendations | 意図ベース検索 + 信頼シグナル（Scorecard / deps.dev / Socket）でリランクして MCP で返す | UI を持たない B2D 製品。コンセプトの B2C 像とペルソナが別物になる |

**推奨**: (a) を **Phase 1 の確定スコープ** として要件定義書を書き、(b)/(c) は「将来拡張として要件定義書の付録に方向性だけ書く」。理由は、(a) が唯一「完成」を定義できるスコープであり、(b)(c) はどちらもデータ基盤の設計判断（`D-5`）を先に固めないと要件が書けないため。

> ⚠️ これは「(b)(c) を諦める」という意味ではなく、**1 つの要件定義書に 3 製品を混ぜない** という提案。

### D-2 `minimum-requirements.md` は「不変の与件」か「自分たちの暫定案」か

同ドキュメントは、受け入れ基準チェックリスト形式・「Star 数 / Watcher 数 / Fork 数 / Issue 数」の表示項目指定・「開発に AI を利用した場合はその利用方法と範囲を README に記載」といった記述から、**外部から与えられた課題仕様（技術選考課題等）の体裁** を持っている。

- **(a) 外部与件（変更不可）** の場合 → 要件定義書は「与件を満たすことを保証する構造」で書き、独自要件は与件を侵さない追加分としてのみ足す。受け入れ基準はそのままトレーサビリティ表になる。
- **(b) 自分たちの暫定案（変更可）** の場合 → コンセプト・競合調査の知見を反映して要件そのものを組み替えられる。

**この違いで要件定義書の書き方が根本から変わる** ため、最初に確定させたい。

### D-3 事業目的（何のために作るか）

- ポートフォリオ / 学習 / 選考課題 → 「完成度と説明可能性」が最優先。運用コスト・法務リスクは軽視してよい。
- 実際に公開して収益化する → GitHub の利用規約・スクレイピング可否（`R-8`）、運用コスト（`R-6`）、継続的な鮮度維持が要件に入る。

**推奨**: 現時点では前者を前提に要件を書き、後者に必要な項目は「Phase 3 の前提条件」として分離しておく。

### D-4 ターゲットと UI 言語

コンセプトの GTM は Product Hunt / X での世界展開（＝英語圏）だが、既存ドキュメントは全て日本語。UI を日本語にするか英語にするか、i18n を要件に含めるかが未定。

**推奨**: MVP は **UI 英語・ドキュメント日本語**（対象が世界中の OSS 開発者であり、i18n 基盤は MVP では持たない）。

### D-5 データアーキテクチャ（GitHub API 直参照 か 自前 ETL + DB か）

これは (b)(c) を目指す場合の **最大の技術的分岐**。

| 方式 | 実現できること | 制約 |
|---|---|---|
| **GitHub Search API 直参照**（`minimum-requirements.md` の前提） | キーワード検索・詳細表示 | 1 クエリ最大 1000 件・検索 30 req/分（認証時）・「dependents 数」「コミット密度」等は取得不可 |
| **GH Archive / BigQuery でバッチ ETL + 自前 DB** | Gem Score・活性度の時系列・偽 star 検出・目的別タグ | BigQuery $5/TB スキャン、DB 運用、鮮度維持の継続コスト。MVP 要件の「DB なし」と両立しない |

**Gem Score・活性度予測・依存数ランキングは、GitHub Search API 単体では原理的に実装できない**（`competitive-analysis.md` §3 の制約と一致）。この事実を要件定義書に明記した上で、Phase 境界をどこに引くかを決める必要がある。

**推奨**: Phase 1 は API 直参照、Phase 2 で ETL 導入。ただし Phase 1 の段階で「API 呼び出しを 1 箇所に隔離する（データアクセス層の分離）」を非機能要件に入れ、Phase 2 の差し替えコストを下げる。

### D-6 GitHub API の認証方式

> ⚠️ **本節は 2026-08-17 時点の検討内容である。`D-20`（2026-08-18）で GitHub App 方式へ改訂済み**（実測検証の結果は §6 の `D-20` と「`D-6` の決定から従属的に確定する事項」を参照）。以下は当時の比較表として残す。

| 方式 | レート制限 | 適性 |
|---|---|---|
| 未認証 | 60 req/h（検索 10 req/分） | 実質不可。デモですら詰まる |
| Fine-grained PAT（サーバー側で保持） | 5,000 req/h（検索 30 req/分） | **MVP に最適** |
| GitHub App | インストール単位で枠を確保 | 一般公開・多ユーザー運用時に必要 |

**推奨**: MVP は **サーバーサイド保持の Fine-grained PAT**（`minimum-requirements.md` §4.1 の「秘匿情報をクライアントへ露出させない」と整合）。GitHub App は D-3 が「実運用・収益化」の場合のみ Phase 2 以降で検討。

### D-7 デプロイ先

`nextjs16-architecture-research.md` は Vercel と AWS/GCP 等を両論併記のまま。Cache Components / ISR / Image Optimization の挙動と運用コストがここで変わる。

**推奨**: **Vercel**（Next.js 16 の機能を追加設定なしで使え、MVP 規模なら無料枠で収まる）。

### D-8 要件定義書の体裁と粒度

| 候補 | 向いている場面 |
|---|---|
| PRD（プロダクト要求仕様） | 事業目的・ペルソナ・成功指標を含む。関係者への説明用 |
| SRS 風（機能要件を ID 付きで網羅） | 実装・検収のトレーサビリティ重視。`minimum-requirements.md` は既にこの形式 |
| ユーザーストーリー + 受け入れ基準（Given/When/Then） | アジャイルに開発を回す場合。テストへ直結する |

**推奨**: **PRD（§1〜3）+ SRS 風の機能要件表（§4 以降）のハイブリッド**。既存 `minimum-requirements.md` の FR-ID 体系をそのまま引き継ぎ、上位にプロダクト文脈を足す形にする（既存資産を捨てずに済む）。

### D-9 スケジュールと投下可能な工数

MVP の期限、1 日あたりに使える時間、コスト上限（月額）が未定。要件のスコープ判断（どこまでを Phase 1 に入れるか）に直結する。

---

## 3. 不明瞭・矛盾している点（`Q-n`・要明確化）

> ユーザー判断が要るとは限らないが、**放置すると要件定義書が自己矛盾する** 箇所。

### Q-1【重大】コンセプトのコア機能を、自プロジェクトの競合調査が否定している

`initial-concept.md` のコア機能「マイクロ・スイートスポット検索（star 数 10〜500 に限定）」に対し、`competitive-analysis.md` は次の 2 点を報告している。

1. **star は品質シグナルとして崩壊している**: 約 600 万個の偽 star が検出され（He et al., ICSE 2026）、2024 年 7 月ピーク時は 50 star 超リポジトリの 16.66% が偽 star 活動に関与。同調査の「やめるべきこと」に **「star 単純ランキング（差別化不能）」** が明記されている。
2. **star レンジ絞り込みは GitHub 純正で 1 行で書ける**: 検索構文 `stars:10..500` で同等のフィルタが可能。**「star レンジで絞る」こと自体は差別化にならない。**

→ **「Hidden Gem とは何か」を star 以外の観測可能な指標で再定義しない限り、プロダクトの存在理由が成立しない。** 要件定義の前に決着が必要。

### Q-2 「Hidden Gem」の操作的定義が存在しない

コンセプトは「過小評価された名作」と述べるが、機械的に判定できる定義がない。候補となる指標と、その入手可否は以下。

| 候補指標 | 入手元 | GitHub Search API 単体で取得可能か |
|---|---|---|
| 被依存数（dependents） | deps.dev / Ecosyste.ms / Libraries.io | **不可**（外部 API 必須） |
| コミット密度・最終更新 | GitHub API / GH Archive | 一部可（`pushed_at` のみ） |
| Issue クローズ率・応答時間 | GitHub API（追加リクエスト）/ GH Archive | 一部可（コスト大） |
| README の構造品質 | Contents API + 解析 | 可（ただし解析ロジックは自作） |
| セキュリティ健全性 | OpenSSF Scorecard API | **不可**（外部 API 必須） |
| star 増加率 | GH Archive（時系列が必要） | **不可**（スナップショットのみ） |

→ 「注目度（star）に対して実利用（被依存数）が不釣り合いに大きいものが Gem」という定義は筋が良いが、**被依存数が GitHub API から取れない** ため、`D-5` の判断と不可分。

### Q-3 Gem Score が式レベルで未定義

`initial-concept.md` §3.3 は 3 つの構成要素（Maintenance / Doc Quality / Dependency Signal）を挙げるのみで、重み・正規化方法・スコアレンジ・再計算頻度・欠損値の扱いが未定。**このままではテスト可能な受け入れ条件が書けない。**

> ✅ **2026-08-20 解決済み**: 算出方法（エコシステム内でのパーセンタイル順位の差）は [ADR 0009](../adr/0009-hidden-gem-score-definition.md) で正式決定した。算出方法そのものは再議論しない。未決なのは同 ADR §2.3 が挙げる **5 点のみ**（① 母集団定義 ② しきい値・足切り基準の具体値 ③ 配信件数 ④ 対象エコシステムの最終確定〔`D-10`〕⑤ 名前解決）。詳細は下記「決定ログ」`Q-3` および「`Q-3` の決定から従属的に確定する事項」を参照。

### Q-4 「目的別タグ付け（#決済 #CLI軽量化）」の生成方法が未定

人手キュレーション / GitHub topics の流用 / LLM による自動分類 のいずれかで、コスト・精度・鮮度・要件の複雑度がまったく変わる。タグ体系（オントロジー）の管理主体も未定。

### Q-5 MVP 要件とコンセプトの「対象外」範囲が食い違う

`minimum-requirements.md` §1.2 は、認証・お気に入り・独自スコアリング・通知・課金を **すべて対象外** としている。これは `initial-concept.md` の Pro 機能（アラート・お気に入り・分析レポート）と週刊ダイジェストを丸ごと外すことを意味する。「MVP で意図的に落とした」のか「別製品として書かれた」のかが不明。

### Q-6 技術リサーチ 2 本の適用範囲が MVP と乖離している

`github-api-research.md` の結論は「GitHub をイベントソースとするアプリを作れ（GitHub App + Webhook 中心）」だが、MVP は **他人の公開リポジトリを検索するだけで、Webhook を受ける対象リポジトリが存在しない**。同様に GraphQL / Octokit / Queue / ETag の各章も MVP には過剰。**どの章を採用し、どの章を将来用として棚上げするかの仕分けが未了**（`nextjs16-...md` にある「本プロジェクトへの適用メモ」に相当する表が、GitHub API 側には存在しない）。

### Q-7 非機能要件が定性的で検証できない

`minimum-requirements.md` §4 は「不要な再取得を避ける」「見やすさ・操作しやすさ」等の定性表現に留まる。数値目標（LCP / INP の予算、検索応答の目標時間、レート制限到達時の具体的挙動、同時利用者数の想定）が未定のため、**受け入れ判定ができない**。

### Q-8 アクセシビリティの達成基準レベルが未定

「キーボード操作可能」「代替テキスト付与」とあるが、WCAG のどのレベル（A / AA）を満たすのかが未定義。テスト要件（axe 等の自動チェックを CI に入れるか）にも未接続。

### Q-9 エラー種別の切り分け要件が実装可能性と未照合

§3.2 は「通信失敗・API エラー・レート制限超過を区別して伝える」と要求するが、GitHub API のレート制限は **403 と 429 の両方で返り、`x-ratelimit-remaining` / `retry-after` ヘッダの読み取りが必要**。二次レート制限（secondary rate limit）は別扱いになる。要件として書くなら、この判別ロジックを仕様に落とす必要がある。

### Q-10 詳細ページの取得方式が未定義

FR-4 の表示項目（Watcher 数など）は `GET /search/repositories` の結果にも含まれるが、詳細ページを **URL 直打ちで開ける**（FR-5）ためには `GET /repos/{owner}/{repo}` を個別に呼ぶ必要がある。検索結果からの遷移時にデータを引き回すのか毎回取得するのかで、キャッシュ設計とレート消費が変わる。

> 補足: GitHub の `watchers_count` は実際には star 数と同値を返し、真の Watcher 数は `subscribers_count` である（REST API の既知の仕様）。要件で「Watcher 数」と書く場合、どちらを指すかの明記が必要。

### Q-11 プロジェクトミッション（`docs/project-mission.md`）が未記入

CP-5 の実体が空のため、判断に迷ったときの優先順位・KPI・品質ゲートが存在しない。要件定義書の「成功指標」節と表裏一体なので、同時に埋めるべき。

### Q-12 リポジトリ名（`gem-hunter`）とプロダクト名（`IndieGems`）が不一致

READMEでは両方併記されているが、正式名称が未確定。ドメイン取得可否も未調査。

---

## 4. リサーチが不足している点（`R-n`）

| ID | 不足している調査 | なぜ必要か | 推奨手段 |
|---|---|---|---|
| **R-1** | **ユーザーリサーチ（n=0）** | ペルソナ「個人開発者」もペイン（「巨大フレームワークは学習コストが高い」）も **著者の仮説のみで一次情報がない**。`01_research/user/` が空 | HN / Reddit / X の既存スレッド収集（デスクリサーチ）で代替可。5〜10 人へのヒアリングができれば理想 |
| **R-2** | **GitHub 純正検索で「どこまで代替できるか」の実測** | Q-1 のとおり `stars:10..500 pushed:>2026-01-01 language:python` 等で相当部分が再現できてしまう。**差別化の根拠がここに懸かる** | 実際にクエリを叩き、純正で満たせない要件を列挙する（半日規模）。**§4.1 で 1 ドメイン実施済み（n=1）。残: 言語・分野を変えた 3〜5 ドメインでの追試** |
| **R-3** | **既存の類似スコアとの差分検証** | OpenSSF Scorecard（18+ チェック・0-10 点）、`criticality_score`（OSSF）、Ecosyste.ms の各種指標が既にある。**Gem Score が車輪の再発明になるリスク** | 各スコアの算出項目を並べ、Gem Score の独自性を 1 表にする |
| **R-4** | **被依存データの入手経路と利用条件** | Q-2 の中核。deps.dev / Ecosyste.ms / Libraries.io の **カバレッジ・レート制限・ライセンス・商用利用可否** が未調査 | 各 API のドキュメント確認（`competitive-analysis.md` に存在は書かれているが条件は未確認）。**§4.1 で実施済み。残: ① deps.dev v3alpha に `GetDependents` 相当があるかの一次確認 ② Ecosyste.ms の商用ライセンス条件の確認（`R-8` と統合）** |
| **R-5** | **レート制限の実測と必要枠の逆算** | 想定同時利用者数 → 必要 req/h の試算がない。「PAT 5,000 req/h で何 DAU まで捌けるか」が未知 | 1 検索あたりの API 呼び出し回数を設計し、キャッシュヒット率の仮定を置いて試算 |
| **R-6** | **MVP の実運用コスト試算** | `competitive-analysis.md` の試算は埋め込み・ベクトル DB 中心で、**MVP（埋め込み不要）のコストは未試算** | Vercel 無料枠の上限、超過時の課金、画像最適化のコストを確認 |
| **R-7** | **Next.js 16 の該当機能の一次確認** | `use cache` / `cacheLife` / `cacheTag` / `updateTag()` は比較的新しく、**本 MVP のユースケース（外部 API レスポンスのキャッシュ）での実挙動が未検証** | context7 MCP で最新ドキュメントを取得し、小さな検証実装で確認 |
| ~~**R-8**~~ | ~~**GitHub 利用規約・法務の一次確認**~~ | ✅ **完了（2026-08-20）**: GitHub ToS「H. API Terms」/「D.8」・AUP「Information Usage Restrictions」「Services Usage Limits」を一次確認し、[ADR 0013](../adr/0013-public-operation-under-github-terms.md) に制約 `T-1`〜`T-4` として固定した。アバターは `next/image` を使わず GitHub のサイズパラメータで直接参照する実装（`INF-11`）のため **再配信に当たらない** ことを確認済み | ⚠️ Ecosyste.ms（CC BY-SA 4.0）の商用ライセンス条件は **Phase 2 で当該データ源を採用する時点** まで開いたまま（本 ADR の射程外・§6 再決定条件） |
| **R-9** | **競合調査の「未確認」項目のクローズ** | 同 Caveats に Bloop.ai の現状、grep.app の動向、Tavily 買収額、Pinecone 最新料金、DeepWiki のコストが「未確認」として残っている | 要件定義書には影響が小さいため **優先度低**。(c) を選ぶ場合のみ再調査 |
| **R-10** | **UI/UX の参照設計** | `03_design/ui-ux/` が空で、画面要件は表形式の記述のみ。**カード 1 件に何を出すか**（説明文・言語・最終更新・トピック）が未定 | 競合（GitHub 検索結果・Libraries.io・OSS Insight）の画面を 3 つ比較し、表示項目を決める |
| ~~**R-11**~~ | ~~**テスト戦略の具体化**~~ | ✅ **クローズ済み（2026-08-18）**: 手段とレイヤー分担を確定し [テスト戦略](../04_development/testing-strategy.md) を正本として作成した（Vitest 4 + RTL / ACL のみ MSW 2 / E2E は Playwright + axe）。🔴 **`async` Server Component はユニットで描画できない**（Next.js 公式が E2E を推奨）ため、データ取得を伴う画面の検証は E2E に置き、ロジックはユースケースへ出す | — |

---

## 4.1. R-2 / R-3 / R-4 の調査結果（2026-08-17 実施）と Q-1 / Q-2 の推奨解

### R-2: GitHub 純正検索での代替可能性（実測）

`mcp__github__search_repositories` で以下のクエリを実行した。

```
pdf parser language:python stars:10..500 pushed:>2026-05-01 archived:false
```

**結果: `total_count = 31`。** 「Python の PDF パーサで、star 10〜500、直近 3 ヶ月以内に更新、アーカイブ済みでない」という条件が、**このドメインでは GitHub 純正検索の 1 クエリで完全に再現できた**（上位は py-pdf-parser 426★ 等、いずれも妥当な候補）。

> ⚠️ **サンプル数 n=1・要追試**。実行したのは 1 ドメインのみで母数も 31 件と小さい。言語・分野を変えた 3〜5 ドメインでの追試を `R-2` の残タスクとする。ただし後述の Hidden Gem 定義の主要根拠は「被依存数が GitHub API に存在しない」という **API 仕様側の事実** であり、本サンプル数とは独立に成立する。

→ **`initial-concept.md` のコア機能「マイクロ・スイートスポット検索（star 10〜500 に限定）」は、単独では差別化にならないことが実測で確定した**（`Q-1` の懸念は事実）。

### R-3: 既存スコアとの差分（車輪の再発明リスク）

OpenSSF `criticality_score` は既に 10 シグナルの加重和で 0〜1 のスコアを算出しており、**公開 BigQuery データセット** (`openssf.criticality_score_cron.criticality-score-v0-latest`) として全世界のリポジトリ分が配布されている。

| シグナル | 重み | `initial-concept.md` の Gem Score との重複 |
|---|---|---|
| created_since | 1 | — |
| updated_since | -1 | Maintenance Score と重複 |
| contributor_count | 2 | — |
| org_count | 1 | — |
| commit_frequency | 1 | **Maintenance Score（コミット密度）と重複** |
| recent_releases_count | 0.5 | — |
| closed_issues_count | 0.5 | **Maintenance Score（Issue クローズ率）と重複** |
| updated_issues_count | 0.5 | 同上 |
| comment_frequency | 1 | — |
| dependents_count | 2 | **Dependency Signal と重複** |

→ **Gem Score を「メンテナンス健全性スコア」として設計すると、`criticality_score` の再実装になる**（`R-3` の懸念は事実）。ただし重要な差異が 1 つある: **`criticality_score` が測るのは「重要度の絶対値」であり、「注目度に対して過小評価されているか」ではない。**

### R-4: 被依存データの入手経路

| データ源 | 被依存数 | 認証 | レート制限 | データライセンス |
|---|---|---|---|---|
| **deps.dev (Google)** | **v3 API のエンドポイント一覧には無い**（v3alpha は未確認。Scorecard・ライセンス・脆弱性・依存グラフは提供） | ドキュメントに記載なし | ドキュメントに記載なし | 生成データは CC-BY 4.0 / Google API ToS。**キャッシュは明示的に許可** |
| **Ecosyste.ms** | **提供あり**（packages / repos の 293M リポジトリ・14.4M パッケージ） | **不要** | **5,000 req/h（IP 単位）** | コード AGPL-3 / **データ CC BY-SA 4.0**。別途「商用ライセンス」の窓口あり |
| OpenSSF criticality_score | 代理指標（コミットメッセージ内の言及数） | BigQuery アカウント | BigQuery 課金 | 公開データセット |

> ⚠️ **要注意（`D-3` が「収益化」の場合）**: Ecosyste.ms の **データが CC BY-SA 4.0（継承ライセンス）** である点。これを取り込んで作った派生データセットには同ライセンスの継承義務が生じうるため、商用プロダクトのコア資産にする場合は商用ライセンスの取得可否を先に確認する必要がある（`R-8` に統合して扱う）。deps.dev（CC-BY 4.0・継承義務なし）とは条件が異なる。
>
> ✅ **2026-08-20 確認済み（「要確認」から更新）**: 上記の CC BY-SA 4.0 は公式ドキュメント（`docs.ecosyste.ms/docs/usage/licences/` および `packages` リポジトリ README「Data from the API is licensed under CC BY-SA 4.0.」）で逐語確認済み（[調査補遺](../../content/research/20260820-zero-query-discovery_supplement.md) §4.1）。ShareAlike の継承条件（「全部または相当部分」の線引き）を公式は数値で示していないため、保守的に UI へ出典表示（`Data via Ecosyste.ms（CC BY-SA 4.0）` + ライセンスへのリンク + 改変の明示）を必須とする。帰属表示の運用ルールと ShareAlike の最終判断時期は決定ログ `D-28` を参照。

### Q-1 / Q-2 の推奨解: 「Gem = 実利用に対する注目度の不足（残差）」

上記 3 点から、Hidden Gem の操作的定義として以下を推奨する。

> **Gem とは、「実際に使われている度合い（被依存数）」に対して「注目度（star 数）」が不釣り合いに小さいリポジトリである。**
>
> `Gem Index = 実利用シグナルの順位 − 注目度シグナルの順位`（または対数回帰の残差）

この定義を推す根拠は以下の 4 点。

1. **star 単純ランキングを回避できる**（`competitive-analysis.md` の「やめるべきこと」に抵触しない）。star は分子ではなく **分母** として使う。
2. **GitHub 純正検索で原理的に再現できない**（被依存数は GitHub API に存在しない）。`R-2` で判明した差別化の欠如を、**唯一直接的に解決する軸**。
3. **`criticality_score` の再実装にならない**。criticality は「重要度の絶対値」、Gem Index は「重要度に対する注目度の不足＝残差」。**既存に同等品が見当たらない**。むしろ criticality_score を実利用シグナルの入力としてそのまま利用できる。
4. **偽 star に対して構造的に頑健**。star を水増しすると Gem Index は **下がる**（＝ランキングから外れる）ため、`competitive-analysis.md` が指摘した偽 star 問題が攻撃面にならない。

**この定義から導かれる要件上の制約（重要）**:

- 被依存数はパッケージレジストリ単位でしか取得できないため、**Phase 2 の対象は「パッケージとして公開されているリポジトリ」に限定される**（アプリ・設定集・学習用リポジトリは対象外になる）。これは同時に、`initial-concept.md` が排除したがっていたノイズ（チュートリアル・課題提出用リポジトリ）を **自動的に除外できる** という利点でもある。
- 対象エコシステムの選定（npm / PyPI / crates.io / Go 等のどれから始めるか）が新たな決定事項になる（→ `D-10` として追加）。
- **Phase 1（検索 Web アプリ）はこの定義を実装しない**。与件（`D-2`）のスコープを侵さず、Phase 2 の中核として設計する。

### D-10（追加）Phase 2 の対象エコシステム

**推奨**: **npm と PyPI の 2 つから開始**（Ecosyste.ms のカバレッジが厚く、`initial-concept.md` のペルソナ＝個人開発者の利用言語と一致するため）。

---

## 5. 推奨する進め方

1. **`D-1` と `D-2` に回答をもらう**（この 2 つで要件定義書の骨格が決まる。他の `D-n` は推奨案どおりで進めても手戻りは小さい）。
2. **`Q-1` / `Q-2` を先に決着させる**（Hidden Gem の操作的定義。これが決まらないとプロダクトの存在理由が書けない）。ここだけは `R-2` / `R-3` / `R-4` の調査結果が判断材料として要る。
3. `docs/project-mission.md`（`Q-11`）を埋め、要件定義書の「目的・成功指標」と一致させる。
4. 要件定義書（`docs/02_requirements/prd.md` 想定）を執筆。`minimum-requirements.md` の FR-ID 体系を引き継ぐ。
5. 決定した技術判断を `docs/adr/` に ADR として記録する（Next.js 16 採用・認証方式・データアーキテクチャの 3 本が最低限）。

---

## 6. 決定ログ

> 回答をもらい次第、ここに追記する。

| ID | 決定内容 | 決定日 |
|---|---|---|
| **D-1** | **(a) → (b) の二段構えで 1 本の要件定義書に書く。** Phase 1 = 検索 Web アプリ（`minimum-requirements.md` のスコープ）、Phase 2 = IndieGems（Gem Score・目的別タグ等）とし、フェーズ境界を明示する。(c) エージェント向け API / MCP は採用しない（将来の選択肢として付録に言及するに留める） | 2026-08-17 |
| **D-2** | **`minimum-requirements.md` は外部与件（記載された受け入れ基準は必達の下限として固定・変更しない）。ただしその上への独自要件の上乗せは自由。** 与件の FR-1〜7・受け入れ基準チェックリストはトレーサビリティ表として要件定義書に引き継ぎ、Hidden Gem 定義・Gem Score 等は「上乗せ要件」として別 ID 体系で追加する | 2026-08-17 |
| **D-3** | **主目的は選考課題 / ポートフォリオ。ただし有用であればそのまま運用を継続したい**（副次目標）。したがって「与件を確実に満たす + 設計判断を説明できる」を最優先しつつ、**運用継続を阻害しない設計**（無料枠に収まる・レート制限で破綻しない・規約違反をしない）を制約として課す。収益化は現時点では目的としない | 2026-08-17 |
| **Q-1 / Q-2** | **Hidden Gem = 「過小評価度 × 健全性」の 2 軸で定義する。** ① 過小評価度（`Gem Index` = 被依存数の順位 − star の順位の残差）で候補を抽出し、② 健全性（メンテナンス活性度）でフィルタする 2 段構え | 2026-08-17 |
| **D-9** | **期日は存在するが未確定。「MVP を先に完成させ、期日まで機能・改善を積み上げられる状態にする」を方針とする。** 要件定義書では各要件に **優先度ラベル（`P1-MVP` / `P1-積み上げ` / `P2`）** を付与し、`P1-MVP` を最短で到達可能な最小集合として切り出す | 2026-08-17 |
| **D-4** | **i18n 対応（日英切替）。** ただし **ロケール基盤（ルーティング + メッセージ抽出）は MVP 時点で入れ、翻訳の作り込みは積み上げフェーズに回す**（後付けコストが最も高い部分だけ先に確保する） | 2026-08-17 |
| **D-5 / D-7** | **インフラ（キャッシュ基盤・デプロイ先）は現時点で決定しない。** ただし **外部キャッシュを後から差し込めることを設計制約とする**。MVP は GitHub API 直参照で動かし、DB は持たない | 2026-08-17 |
| **D-6** | **サーバー側の単一 Fine-grained PAT を既定とし、加えて「任意の GitHub OAuth ログイン」を MVP に含める。** 未ログインでも全機能が使え、ログインは **レート枠を各自のものに切り替えるだけ**。あわせてキャッシュ・request coalescing・直列化・ETag・残量監視による degraded 表示を要件化する | 2026-08-17 |
| **D-5 追補** | **「DB を持たない」を MVP の都合ではなく設計原則に格上げする。** 状態はクライアント側（`localStorage` / Cookie / URL）に寄せ、サーバー側ストアを持たない構成を Phase 2 まで可能な限り貫く | 2026-08-17 |
| **UI-1**〜**UI-4** | 要件定義書の執筆中に確定した UI レベルの決定（① 一覧カードに説明文 / 言語 / star / 最終更新 / topics を表示 ② ソートは既定 = 関連度 + 切替 UI あり ③ i18n は全ロケールにプレフィックス・既定 `ja` ④ 表示件数は利用者が切替可能）。**内容の正本は [`prd.md`](./prd.md)**（`AR-1` / `AR-2` / `AR-3` / `AR-4`） | 2026-08-17 |
| **Q-11** | **`docs/project-mission.md` を記入済み。KPI は完成度 KPI のみを置く**（与件充足 11/11・Lighthouse・Core Web Vitals・主要フローのテスト・ADR の網羅）。利用実績 KPI は運用開始を判断した時点で追加する | 2026-08-17 |
| **Q-5** | **与件 §1.2 の対象外リストを Phase 1 の境界として尊重し、コンセプトの「Pro 機能」という枠組み自体を廃止する**（`D-3` で収益化しないため）。個々の機能は下記のとおり再配置する | 2026-08-17 |
| **Q-6** | **`github-api-research.md` の適用範囲を仕分けし、採用章 / Phase 2 章 / 不採用章を要件定義書側に表として持つ**（原本は編集しない） | 2026-08-17 |
| **Q-9** | **エラー種別の判別ロジックをレスポンスヘッダに基づいて仕様化する**（下記の対応表）。レート制限エラー時には `D-6` のログインによる緩和を案内する | 2026-08-17 |
| **Q-10** | **詳細ページは常に `GET /repos/{owner}/{repo}` を単独で呼ぶ**（検索結果からデータを引き回さない）。「Watcher 数」は **`subscribers_count`** を用いる | 2026-08-17 |
| **Q-4** | **目的別タグは自前生成せず、GitHub topics の活用に絞る。** LLM による自動分類・人手キュレーションは行わない。差別化は「topics で絞り込み × Gem Index で並べ替え」の組み合わせで出す | 2026-08-17 |
| **Q-3** | **Gem Index はパーセンタイル順位の差で算出する。** エコシステム内で被依存数と star をそれぞれパーセンタイル化し、その差を指標とする。閾値・件数などの具体値は実データを見てから決定する。🔴 **2026-08-20 追記**: 算出方法は [ADR 0009](../adr/0009-hidden-gem-score-definition.md) で正式決定済み。未決なのは同 ADR §2.3 の 5 点（母集団定義・しきい値の具体値・配信件数・対象エコシステムの最終確定・名前解決）のみ | 2026-08-17 |
| **D-10** | **Phase 2 の Gem ランキング対象は npm + PyPI から開始する。** エコシステムを後から追加できる構造とし、対象拡張は同じ仕組みの適用で済ませる。**Phase 1（キーワード検索）は全 GitHub が対象であり、この絞り込みの影響を受けない** | 2026-08-17 |
| **Q-12** | **正式名称を `gem-hunter` に統一する。** `IndieGems` は初期コンセプト由来の旧称として扱い、以降のドキュメント・UI・OAuth App 名はすべて `gem-hunter` で揃える | 2026-08-17 |
| **Q-7** | **Core Web Vitals（LCP 2.5s / INP 200ms / CLS 0.1）とキャッシュ TTL を数値要件として置き、さらに Lighthouse を CI ゲート化する。** ゲートの偽陽性対策（本番相当ビルド・複数回実行の中央値・スロットリング固定・カテゴリ別閾値）を同時に要件化する | 2026-08-17 |
| **Q-8** | **WCAG 2.2 レベル AA を目標として宣言する。** 自動検証可能な範囲は axe を CI に組み込んで常時担保し、自動では拾えない項目は手動チェックリスト化する（「完全準拠」とは名乗らない） | 2026-08-17 |
| **D-8** | **要件定義書を「正本（SSOT）」と位置づける。** 体裁は PRD + ID 付き要件表 + 主要フローの Given/When/Then。今後作成する派生ドキュメント（リーンキャンバス / インセプションデッキ / ユーザーストーリー / ロードマップ）とは **責務境界を定め、同じ事実を二重に書かず ID 参照で結ぶ** | 2026-08-17 |
| **D-11** | **スプリントごとの PR にプレビューデプロイの URL を必ず貼る。** そのため **プレビュー環境のデプロイ先だけは先行して決定する**（`D-7` の一部前倒し）。本番デプロイ先とキャッシュ基盤は引き続き未決。`NFR-21`（特定 PaaS 固有機能への依存を最小化する）は **アプリのランタイムに対する制約として維持** し、**プレビュー機能（PR ごとの自動デプロイ）への依存は許容** する（開発フローの道具であり、アプリコードを PaaS に縛らない） | 2026-08-17 |
| **D-12** | **開発中の確認の発火ラインを「仕様解釈が 2 通り以上あり、選択で成果物が変わるとき」に限定する。** 実装手段・ライブラリ選定・命名・ファイル分割は確認せず自律実行し、採用した仮定を Issue コメントか最初のコミットメッセージに 1 行記録する。`CP-6` と A-1〜A-6（既約境界外）の扱いは **変更しない** | 2026-08-17 |
| **D-13** | **アプリの実行モデルを「サーバーレス関数前提」とする。** 事業者は引き続き決めないが、インスタンスが使い捨てである前提で設計する（プロセス内メモリに状態を持たない）。移植性は Next.js 標準機能と Adapter / OpenNext 経由で確保し、`NFR-21` は維持する | 2026-08-18 |
| **D-14** | **アプリケーションからは個人と結びつきうる情報を一切ログ出力しない。** IP・User-Agent・検索キーワード・閲覧対象・GitHub ユーザー識別子・トークンを記録せず、出力してよいのはエラー種別コード・HTTP ステータス・レート残量・処理時間に限る。匿名集計もサーバー側には持たない | 2026-08-18 |
| **D-15** | **インフラ設計の第 4 の評価軸として「運用の手離れ」を置く。** デプロイは git push / マージのみをトリガーとし、手動デプロイ経路を作らない。人手が残る定常作業は一覧化し、増やす変更には自動化の手当てを同時に付ける | 2026-08-18 |
| **D-16** | 🔴 **インフラを Cloudflare に確定する（プレビュー・本番とも Cloudflare Workers）。** ユーザー明示決定（2026-08-18）により `D-5` / `D-7`（デプロイ先未決）と `D-11`（プレビュー環境の事業者未決）をクローズする。`M-4` は廃止せず「事業者を選定するゲート」から「**第三者へ公開するか否かを決めるゲート**」へ性質を変えて維持する（独自ドメインの要否・GitHub 利用規約の一次確認・コスト撤退ラインの確定はここに残す）。運用の一次経路は **wrangler CLI**（Cloudflare MCP は読み取り 4 ツールのみ許可・書き込み系は使わない）。CI は **GitHub Actions + `cloudflare/wrangler-action`** とし Workers Builds は採用しない | 2026-08-18 |
| **D-17** | **ランタイムは `@opennextjs/cloudflare` アダプタとし、`next` を 16.2.11 以上にピンする。** `Node.js Middleware` と `proxy.ts` は一切使わない（アダプタ未対応・動作未確認のため、`/` → `/ja` のリダイレクトはルート Server Component の `redirect()` で実装する）。`next/image` の最適化も使わない（`INF-11`） | 2026-08-18 |
| **D-18** | **MVP のキャッシュは HTTP `Cache-Control` + Workers Caching のみとし、永続ストア（R2 / D1 / Durable Objects / KV）を採用しない。** `NFR-17` Cache Port は維持し、実装位置を **`src/infrastructure/platform/`** と定める（`open-next.config.ts` ではない）。L3 の導入条件は [インフラ設計](../03_design/infrastructure/infrastructure-design.md) §6.2 の観測条件を変更せずに維持する | 2026-08-18 |
| **D-19** | **Workers Free の実測で上限（p95 CPU 10 ms / Worker バンドル 3 MB gzip）を超えた場合は Workers Paid（$5/月）へ上げる（事前承認済み）。** Claude は `limits.cpu_ms` の設定と監視の準備まで確認なしで進め、**支払い方法の登録と Paid 切替の操作だけ** を `A-6` としてユーザーへ即時依頼する（Free → Paid に CLI / API 経路は存在しない）。あわせて **コストの撤退ラインを月額 $10** とし、Billable Usage API の監視閾値に用いる（`infrastructure-design.md` §14 の未決事項をクローズ） | 2026-08-18 |
| **D-20** | 🔴 **サーバー側の GitHub 認証を「GitHub App の installation token」に改訂する（`D-6` の方式部分を上書き）。** Fine-grained PAT を既定とした `D-6` の決定を、**実測検証（2026-08-18・Cloudflare Workers 上）** に基づいて置き換える。採用理由は ① Core 枠が **8,800 req/h**（PAT の 5,000 req/h の約 1.76 倍・公式式 `5,000 + 20 リポジトリ超過分 × 50`・上限 12,500 と整合）② トークンが **1 時間で自動失効** するため、`INF-4`（定常運用ゼロ）を壊す「PAT の有効期限更新」が発生しない ③ 秘密として保持するのが **署名鍵** であり、漏洩時の影響範囲を権限セット（`contents:read` / `issues:read` / `metadata:read`）に閉じ込められる。**`D-6` のうち「任意の GitHub OAuth ログイン」「ログインで変わるのはレート枠だけ」「複数トークンのローテーションを行わない」は変更しない** | 2026-08-18 |
| **D-21** | **リリースサイクルは trunk-based（PR ごとのプレビュー + `main` = 本番）を維持し、常設の dev ブランチ / dev 環境を持たない。** `minimum-requirements.md` §4「プロダクション運用を想定した実装とする」は §4.1〜4.4 でアプリ実装品質（信頼性・性能・a11y・保守性）を列挙しており、環境構成・ブランチ戦略は **与件充足の判定範囲外**（禁止でも要求でもない任意の上乗せ）。単一開発者 + 完全自走という体制では dev を見る人間も実トラフィックも存在せず、dev は CI 以上の検証を追加しないまま「誰が・いつ dev→main を昇格させるか」という未定義の判断点だけを増やす。代わりに ① **`main` マージ後のテストゲート**（合成状態の検証・#39）② **README への判断根拠の明記** ③ **`M-4` での再判定** の 3 点で「プロダクション運用を意識している」ことを担保する。⚠️ 仮に将来 dev 相当の環境を持つ場合は **`[env.dev]`（別 Worker）一択**（`versions` + 固定 preview alias 方式は、シークレットが Worker 1 本の版チェーン全体で線形継承されるため dev 専用シークレットの分離を構造的に保証できない）。**理由と却下案の正本は [ADR 0004](../adr/0004-release-cycle-trunk-based.md)**（本行はサマリー）。README「設計上の判断」にも要点を転記済みのため、改訂時は ADR・README の両方を更新する | 2026-08-18 |
| **D-22** | **アプリケーションのディレクトリ命名をクリーンアーキテクチャの層名に統一する**（`src/domain/` / `src/usecases/` / `src/infrastructure/` / `src/ui/` / `src/composition/` / `src/shared/`）。旧表記の `lib/infra/` は **`src/infrastructure/platform/`**、`lib/data/` は **`src/infrastructure/github/`** へ読み替える。理由は ① [ユーザーストーリーマップ](./user-story-map.md) §5.2 の 3 層判定と `tools/self_review_check.py` が既に `src/` 系で実装されており、インフラ設計 2 本だけが `lib/` 系で **同一概念に 2 つの名前がある状態** だった（`SD-4` の権威解決対象） ② 層名がそのままディレクトリ名になっていれば依存規則を grep で機械検査できる。🔵 **命名は `D-12` により確認不要の自律判断**（実装手段・命名は確認しない）。境界の効力（`NFR-21` / `INF-5` の bindings 限定）は名前が変わるだけで **変更しない**。正本は [アプリケーションアーキテクチャ](../03_design/architecture/application-architecture.md) | 2026-08-18 |
| **D-23** | 🔴 **GitHub Actions が制限中のため、CI とデプロイをセッション実行へ切り替える。** 根拠は飼い主の逐語指示 2 件（2026-08-19）: ①「GitHub Actions について、制限中なので自前でチェックする仕組みに切り替えてください。」②「本番デプロイについて自動で行えるように許可リストに追加してください。」（後者を受けて `.claude/settings.json` の `permissions.allow` に wrangler の deploy 系を追加済み）。 起動不能なワークフローは撤去し、品質チェックは `tools/run_checks.sh`、プレビュー・本番デプロイは `wrangler` の直接実行で行う。`INF-20`（デプロイのトリガーは git push / マージのみ）は **暫定的に緩和** し、制限解除後に原則へ復帰する（復帰手順は [`cloudflare-infrastructure.md`](../03_design/infrastructure/cloudflare-infrastructure.md) §8） | 2026-08-19 |
| **D-24** | 🔴 **`D-18` の L2 実装を改訂する（`D-18` の実装方式部分を上書き）。** MVP キャッシュ L2 の主役を「HTTP `Cache-Control` + Workers Caching」から **アプリ内 `CachePort` の実装（`InMemoryCache`）** に改訂する。理由: エッジキャッシュが HIT すると Worker 自体が実行されず、`SP-5`（[`user-story-map.md`](./user-story-map.md) §5.3）の受け入れ条件「2 回目は GitHub API を呼んでいないことを外から検証できる」に必要な `X-Cache-Status` ヘッダを動的付与できないと実機検証（`wrangler dev --local` + スタブ GitHub API）で判明した（`AsyncLocalStorage` 経由の代替案も不成立）。検証可能性を優先し L2 の主役を差し替える。あわせて **`NFR-7`（request coalescing）を「補助から主要な防波堤へ格上げ」した部分を撤回** し、当初案（[インフラ設計](../03_design/infrastructure/infrastructure-design.md) §4）どおり補助に戻す（L2 をエッジキャッシュに依存させない本改訂と矛盾するため）。`D-18` のうち「永続ストア（R2 / D1 / Durable Objects / KV）を採用しない」「Cache Port を維持し実装位置を `src/infrastructure/platform/` とする」は **変更しない**。根拠は [ADR 0005](../adr/0005-cache-port-yagni-exception-and-ttl.md) と [議論記録](../../content/discussions/sp5-cache-design-20260819/whiteboard.md)（round 3・争点 A） | 2026-08-19 |
| **D-25** | 🔴 **`NFR-27` の Lighthouse ゲートは Accessibility = 100 のみ blocking とし、Performance = 90 以上は目安値として計測・記録する（未達でもゲートしない）。** `run_lighthouse.mjs`（`tools/run_checks.sh` 配線）の実装には「Performance 90 以上」の閾値をそもそも持たせない。ユーザー確定事項（2026-08-20）に合わせて `prd.md` `NFR-27` を改訂。あわせて **Lighthouse Accessibility = 100 は a11y の担保を意味しない** ことを確認した（axe-core は `:focus-visible` を発火させず、#179 クラス＝フォーカスリングの非テキストコントラストは検出不能。現状の未達状態でも 3 画面すべて 100 だった実測に基づく）。三層防御（Lighthouse/axe = 一般違反 / `check_contrast.py` = トークン値の 3:1 判定 / E2E = 消失と到達不能）で分担する。根拠は [議論記録](../../content/discussions/sp10_a11y_20260820/whiteboard.md)（round 3・lead 判定） | 2026-08-20 |
| **D-26** | 🔴 **プレビュー環境（preview alias）を「削除」する CLI・REST API は存在しないことが実測で確定した。代わりに『退役（retire）＝完了したスプリントの alias に本番と同じビルドを再アップロードして内容を置き換える』を採用する。** alias URL は生き続けるが、古いスプリントのコードを配信し続ける状態は解消される（version オブジェクト自体は削除されず残る）。能動的に効く唯一のレバーである Worker 全体の `previews_enabled` トグルは、並行中の他 PR のプレビューも一括無効化してしまい `SD-1`（プレビュー URL 必須）と両立しないため、通常運用では使わない（緊急時の手動 killswitch としてのみ想定する）。**あわせて本番デプロイの発火点を改訂する**: `Sprint Goal:` 行のあるスプリント PR は、マージ直後ではなく **Sprint Review 判定が `accepted`（または `accepted_with_conditions` かつ `deploy: yes`）になった時点** でデプロイする（`rejected` の間はデプロイも退役もしない・fail-closed）。非スプリント PR は従来どおりマージ直後にデプロイするが、main 上に判定未確定 / rejected のスプリント Issue が残っている間はデプロイを待機する（直列化）。`main` の巻き戻し・revert は導入せず trunk-based の 1 ホップ構成は維持する（[ADR 0004](../adr/0004-release-cycle-trunk-based.md) の再判定ではなく追記）。実装の正本は [Cloudflare インフラ設計](../03_design/infrastructure/cloudflare-infrastructure.md) §6.1 / §8.2。根拠は [議論記録](../../content/discussions/sprint-env-lifecycle-20260820/whiteboard.md)（5 レンズ・3 ラウンドの敵対的相互検証・lead 判定） | 2026-08-20 |
| **D-27** | 🔴 **`M-5`（Phase 2 着手判断ゲート）を「着手する」で通過させ、スライス `S-3` を「構想・実装しない」から実装対象へ格上げする。** あわせてキーワード非依存の発見体験（日次ダイジェスト）を導入し、飼い主が `AskUserQuestion` で選択した 3 分岐を確定する: ① 入口はトップページ（`/{locale}`。検索窓は同ページ上部に残し MVP の検索動線を壊さない）② 更新粒度は日次（日付を唯一のシードとする決定論的生成）③ `S-2`（お気に入り・検索履歴・topics 絞り込み）より先に着手する。🔴 **`D-3`（主目的は選考課題 / ポートフォリオ、収益化は目的としない）は変更しない**。Phase 2 着手はこの目的と矛盾せず、運用継続を阻害しない設計という制約もそのまま維持する。🔴 **本決定により [`inception-deck.md`](../00_concept/inception-deck.md) Q4.2 の『Phase 2（Gem Index）の実装』を撤回し、『通知・アラート・週刊ダイジェストの配信』はメール / push 通知に限定する**（静的な日次ダイジェストと RSS 配信は対象内）。設計判断の詳細（リマインド機能の各案の採否表・専門チームの議論経緯）は [議論記録](../../content/discussions/post-mvp-strategy-20260820/decisions.md) を参照 | 2026-08-20 |
| **D-28** | Gem データのバッチ集計は **Cloudflare の外**（セッション / Routine の cron）で回し、生成した静的 JSON を git commit → デプロイで **Static Assets** として丸ごと差し替える。API 経由の全件走査はせず、Ecosyste.ms の S3 バルクダンプ（約 1,310 万パッケージ・2〜4 ヶ月毎更新）を用いる。**`D-18`（永続ストアを採用しない）との境界は「Worker がそのファイルへ実行時に書き込むか」で判定する**（No なら `D-18` の対象外。バッチ生成 → デプロイ差し替えは No 側であり `D-18` に抵触しない。誤読リスクがあるため下記「`D-16`〜`D-19` の決定から従属的に確定する事項」に注記を追加した）。SPOF（バッチ停止時に更新が止まる）は無害化 + 検知して自己修復で対処し、配信自体は止めず鮮度のみ劣化させる | 2026-08-20 |
| ↳ **D-28 訂正（`SP-14` 着手時実測・2026-08-20）** | 🔴 **本文の「S3 バルクダンプを用いる」は方針転換した。** `SP-14` 着手直後の実測で Ecosyste.ms の S3 バルクダンプ（`https://ecosystems-data.s3.amazonaws.com/`）は `AccessDenied` で技術的に取得不可と判明した。代わりに **Ecosyste.ms REST API（`packages.ecosyste.ms`）の `rankings` フィールド**（各指標のパーセンタイル順位を Ecosyste.ms 側で日次計算済み・値域 0〜100・0 が最上位）を Gem Index の入力として信頼する。候補プール規模分の API 呼び出しのみで完結し、全件走査（約 122 日）も 778GB ダンプDL も不要になる。`rankings` の母集団が npm 単独スコープであることは `ecosyste-ms/packages` の実装ソース（`registry.rb#top_percentage_for`）で確認済みで、ADR 0014 §2.6 の不変条件（エコシステム内で閉じて計算）と整合する。🔴 **符号の向き（実装の正本）**: `Gem Index = rankings.dependent_packages_count − rankings.stargazers_count` で算出し、**値が小さいほど過小評価度が高い（昇順が上位）**。`rankings` は 0 が最上位の順位なので、被依存数が上位（値が小さい）かつ star が下位（値が大きい）ほど強い負値になる。ADR 0009 §2.1 の式（「被依存数のパーセンタイル順位 − star のパーセンタイル順位」）と同じ並びで、同 §3.1 が言う「star を水増しするとランキングから外れる」性質も保たれる（水増しで `stargazers_count` の順位が上がる = 値が小さくなる → 差が大きくなり昇順で下位へ落ちる）。**§3.1 の「値が下がる」という文言は、パーセンタイルを「大きいほど上位」と読む前提で書かれたもので、Ecosyste.ms の順位（0 が最上位）を入力にする本実装では「値が上がる」が正しい**（結論であるランキングからの脱落は同じ）。「Cloudflare 外の cron で生成 → 静的 JSON を差し替え」という配信方式（`D-28` 本文・`D-18` 境界）は不変。実装の正本は [ADR 0014](../adr/0014-zero-query-daily-digest.md) §5 | 2026-08-20 |
| **D-29** | Ecosyste.ms のデータ利用における帰属表示を、配信 JSON のトップレベルに `source` / `license` / `source_license_url` / 改変の明示を含むメタデータとして必須化し、UI にも出典表示を出す運用とする。🔴 **Ecosyste.ms の生テキスト（description 等）は再配信せず、数値・識別子・自作の派生値に限定する**（生テキストの転載は "substantial portion" の解釈を保守側へ押すため）。ShareAlike（CC BY-SA 4.0）の適用範囲の最終判断は、`R-8`（GitHub 利用規約の一次確認）と同じタイミング — **第三者へ公開するかを決める時点** に合わせる（`D-3` の「運用継続を阻害しない設計」と整合）。データが CC BY-SA 4.0 であること自体は公式ドキュメントで確認済み（下記 `R-4` 節の注記を参照） | 2026-08-20 |
| **D-30** | 🔴 **`SP-16`（キーワード検索結果の Gem Index 順ソート）の仕様分岐 2 件を確定する**（`SD-3` 第 2 系統・`D-12` の発火ラインに合致）。`SP-14` / `SP-15` が生成した静的 JSON を実測（候補 294 件 / ユニークリポジトリ **227** / `repositoryFullName` を全件保持 / gemIndex レンジ -6.734〜0.003）したうえで飼い主が選択した。**① Gem Index を持たない検索結果は「上位に集めたうえで末尾に残す」**（絞り込みは 227 件という被覆率ではほとんどのキーワードで 0 件になり検索機能として成立しないため不採用。件数が変わらないので `AC-6` / `AC-7` を壊さない）。**② 並べ替えの範囲は「最大 1,000 件を取得してから並べ替える」**（GitHub 検索 API は Gem Index を `sort` として受け付けないため、1 ページ内のローカル並べ替えでは*全該当リポジトリ中の Gem Index 順* にならない。飼い主は素直な挙動を優先した）。🔴 **② の代償**: 1 検索あたり最大 10 リクエスト（`per_page=100` × 10 ページ）に増え `NFR-5` のレート予算を大きく圧迫する。既存キャッシュ層（[ADR 0005](../adr/0005-cache-port-yagni-exception-and-ttl.md)）に全ページ取得の結果を載せて同一キーワードの再ソートで再取得しないこと、総ヒット件数に応じた取得上限を設けることで吸収する。決定の全文は Issue #285、反映先は [`user-story-map.md`](./user-story-map.md) §5.3 `SP-16` | 2026-08-21 |
| **D-31** | 🔴 **本番デプロイの発火点を Workers Builds（Cloudflare native の Git 連携）へ移す。`D-16` のうち「CI は GitHub Actions + `cloudflare/wrangler-action` とし Workers Builds は採用しない」という部分** だけ **を上書きする**（Cloudflare を使う選択・wrangler を運用の一次経路とする点は不変）。上書きの理由は `D-16` 当時に想定した 2 経路が **どちらも塞がった** こと: ① GitHub Actions がプラットフォーム側の制限で起動できない（`D-23`）② クラウドセッションからの `wrangler deploy` が Claude Code の auto mode classifier に阻まれる（**実測ブロック 5 回・成功 2 回**。`permissions.allow` では解除されず、本番デプロイは分類器の組み込み保護対象として公式に明示されている。🔴 **毎回ブロックされるわけではないが分かれ目は未確定** で、非決定的な経路に本番リリースを預けないという判断は変わらない・Issue #288 / #300 / `L-130`）。検討した他の 4 案（ユーザー設定 `~/.claude/settings.json` の `autoMode` に例外追加 / managed settings /ブロックのたびに手動承認 / 飼い主が毎回手で実行）に対し、**Workers Builds だけが Claude の権限を広げずに「マージ = 本番反映」を回復する**（`INF-20` の原則へ復帰し、`D-23` の暫定緩和が不要になる）。🔴 **前提**: Cloudflare GitHub App の接続はダッシュボードでの対話的認可が必須で API 経路が無いため、飼い主の 1 回の操作が要る（`A-6` 相当）。🟢 **本決定が確定させたのは「発火点の移行先」だけだったが、デプロイの *タイミング*（`D-26` のゲートをどう維持するか）は `D-32` で決着した**（2026-08-21）。🔵 `D-16` が Workers Builds を却下したもう 1 つの理由（PR コメント機能が beta で URL の安定性が低い・[議論記録](../../content/discussions/cloudflare-infra-20260818/whiteboard.md)）も本決定で意図的に上書きする（PR コメントは `SD-1` の要件ではなく、プレビュー URL は既存の `--preview-alias` 運用が担うため）。🔴 **非本番ブランチビルドは有効化しない**（既存の preview alias 運用と衝突し、退役対象外の orphan version が蓄積するため）。手順・設定値・接続前の前提条件・停止手順の正本は [Cloudflare インフラ設計](../03_design/infrastructure/cloudflare-infrastructure.md) §8.2.3 | 2026-08-21 |
| **D-32** | 🔴 **Workers Builds へ移行しても `D-26` のデプロイゲートを維持する。方式は「Deploy command をゲート込みにする」（案 (a)）。** Workers Builds の本番ブランチは `main`（リポジトリの default branch）のままとし、Deploy command に `npm run deploy:ci`（= `bash tools/workers_build_deploy.sh`）を指定する。このスクリプトはビルド環境で `python3 tools/check_deploy_gate.py` を実行し、**exit 0 のときだけ `npm run deploy` へ進む**。🔴 **ゲートが閉じているときは exit 0 で握り潰さず、その終了コードのままビルドを失敗させる**（Workers Builds が「Deploy command が exit 0 でデプロイしなかった場合にビルドを成功扱いにするか」を公式に明記しておらず未確認のため、その未文書の挙動に依存しない）。赤いビルドは「デプロイ保留中」の可視化であって故障ではない。検討して **却下した 2 案**: ① 「Sprint Review 判定が出るまで `main` にマージしない」— 判定の滞留が `main` への統合全体を止め、非スプリント PR まで巻き込む（Sprint Review 未実施のまま open で滞留する Issue は実在する — #288 / #308。現行のゲートはデプロイのみを止めるためマージは滞っていないが、この案を採るとその滞留がそのままマージの停止に変わる）② `deploy-live` 専用ブランチを本番ブランチに指定してゲート判定をセッション側に残す — 分類器は **non-default branch のうち名前がデプロイ先を示すもの**（`production` / `release` / `gh-pages` 等）への push を独自に本番デプロイと判定し、しかも分類器は CLAUDE.md とドキュメントを読むため **正しく文書化するほど検知されやすくなる**（名前を隠して回避するのは `L-130` の迂回禁止に触れる）。ビルドイメージに Python 3.13.3 と git がプリインストール済みであること、secrets が `wrangler deploy` で削除されないことは公式ドキュメントで確認済み。実装・設定値の正本は [Cloudflare インフラ設計](../03_design/infrastructure/cloudflare-infrastructure.md) §8.2.3。根拠は [議論記録](../../content/discussions/prod-deploy-gate-20260821/whiteboard.md)（4 レンズ・2 ラウンドの敵対的相互検証・lead 判定） | 2026-08-21 |
| **D-33** | 🔴 **`sort=gem-index`（キーワード検索結果の Gem Index 順ソート・`SP-16` / `US-34` / `GR-4` の一部）を撤去する。** 初見ユーザーのフィードバック 10 件（Issue #308）を受けた専門チームの議論（[議論記録](../../content/discussions/first-impression-20260821/whiteboard.md)・争点 E）で全会一致となった「機能として成立していない」という判定を飼い主が承認した。**根拠（実測）**: `SP-14` / `SP-15` が生成した候補プールはユニーク **227** リポジトリしかなく、検索上位 100 件との一致は `react`=8 件 / `test`=7 件 / `cli`=6 件、非 JS 系を含む一般語ではほぼ 0 件だった。候補プールは Ecosyste.ms の **npm registry 限定**（`generate_gem_digest.mjs`）であるため、Python / Go / Rust 等の非 JS 系キーワードは **候補プールの規模をいくら拡大しても一致 0 件のまま** という構造的限界がある（母集団が単一エコシステムに閉じているため）。**代償**: `D-30` ②（最大 1,000 件を取得してから並べ替える）により 1 検索あたり最大 10 リクエスト逐次取得（体感 3〜8 秒）を要していたが、撤去により常に 1 リクエスト（体感 0.5〜1 秒）に短縮する。フィードバック⑨（体感時間への不安）・⑩（検索一覧に Gem Index の数値が出ず不安）は原因ごと解消する。**却下した代替案**: ① 理由入りローディング文言で待たせたまま様子を見る案 — 待たせた末に大半が「Gem Index 情報なし」に落ちる失望を文言では埋められない（提唱者の `newcomer_ux` 自身が round 3 で撤回） ② 候補プールを 10 万件へ拡大する案 — 概算では一般語ヒット率が 60〜75% まで改善しうるが、npm 限定である以上 非 JS 系キーワードには効かず、かつ現行の `static-gem-digest.ts` は JSON を JS バンドルへ `import` で焼き込む設計のため 10 万件（gzip 後 4〜6MB 級）は Workers Free の バンドル 3MB 上限（`INF-2` / `INF-3`）を単独で圧迫する（ランタイム fetch 方式への設計変更か Workers Paid が要る）。**再導入の条件**: 🔵 **候補プールが npm 以外のエコシステムを含み、かつ一般語の検索上位 100 件で 30% 以上に Gem Index が付くこと**（構造的限界の解消を客観的に確認できる基準として専門チームの lead が裁定）。**撤去の範囲**: `SortPicker`（UI）と URL 受理（`SortOrder` 値オブジェクト）から `gem-index` を外し、`search-repositories.ts` の gem-index 経路・`list-gem-facets.ts`（削除）・`repository-list.tsx` の `gemFacets` 経路を撤去する。🔵 **`GemIndex` 型・`computeGemIndex`・`Gem` 型・候補プールは撤去しない**（「今日の Gem」＝日次ダイジェストが使い続けるため）。反映先は [`user-story-map.md`](./user-story-map.md) `SP-16` / [`prd.md`](./prd.md) §11 `GR-4` / [ADR 0009](../adr/0009-hidden-gem-score-definition.md) §2.3 | 2026-08-21 |
| **D-34** | 🔴 **トップ末尾の日次ダイジェスト RSS 配信（`US-33`・`SP-15` の一部）を撤去する。** 初見ユーザーのフィードバック（Issue #334・F-5）を受け、専門チームの議論（[議論記録](../../content/discussions/feedback334_detail_readme_20260821/whiteboard.md)・争点 E・lead 判定）を経て飼い主が承認した。**理由**: RSS の実利用が見込めない一方、`resolveLandingHost` による Host header cache poisoning 対策・XML エスケープ・RFC 822 日付変換など専用の実装・テスト資産（`digest-rss.ts` / `route.ts` とそのテスト）を維持するコストに見合わない。**撤去の範囲**: `app/api/digest/rss/`（ルート + テスト）・`src/infrastructure/feed/`（ディレクトリごと）・`src/composition/digest-feed.ts`（`DAILY_DIGEST_LIMIT` は `src/composition/container.ts` へ移設して存続）・`daily-digest.tsx` の購読リンクブロックと `rssLink` ラベル型・`messages/{ja,en}.json` の `digest.rssLink`・`e2e/sp-15.spec.ts` の手順 4。🔵 **日次ダイジェスト本体（`US-30`〜`US-32`・トップページ表示・出典表示・差分表示）は撤去しない**（RSS は配信経路の 1 つに過ぎず、ダイジェストの生成・表示機能とは独立）。`FALLBACK_META` / `resolveLandingHost` は RSS 専用実装ではないため残す。**再導入の条件**: 🔵 **RSS リーダー経由のダイジェスト購読に対する具体的な利用需要が確認できること**（構造的な不要判定を覆す客観的根拠が出た場合に専門チームの lead が再裁定する）。反映先は [`user-story-map.md`](./user-story-map.md) §5.3 `SP-15` / [`prd.md`](./prd.md) `US-33` / `AR-10` / [ADR 0014](../adr/0014-zero-query-daily-digest.md) §2.5 | 2026-08-21 |
| **D-35** | 🔴 **プロダクト紹介のランディングページ（LP）を GitHub Pages で公開する。ソースは `site/`、公開は `gh-pages` ブランチのルートへ同期する方式とする。** 理由: ① GitHub Actions はプラットフォーム側の制限で起動できない（`D-23`）ため CI デプロイを採れない ② Pages は `gh-pages` ブランチ作成時に自動で有効化され、独自ドメインもビルドも不要でコストがゼロ ③ アプリ本体（Cloudflare Workers）と配信を分けることで、LP の更新が本番アプリのデプロイゲート（`D-26` / `D-32`）に影響しない。🔴 **`D-32` との関係**: `D-32` は「auto mode classifier が `production` / `release` / `gh-pages` 等の名前のブランチへの push を本番デプロイと判定しうる」ことを理由に `deploy-live` 専用ブランチ案を却下したが、**それは Workers 本番デプロイの文脈** であり、本決定は静的 LP の配信レーンとして別に置く。実測（2026-08-22）では `git commit-tree` + `update-ref` + `git push origin gh-pages` の経路で **ブロックされずに公開できた**。🔵 **将来ブロックされた場合の代替**: `mcp__github__push_files` で `gh-pages` を更新する（迂回のためのブランチ名変更はしない・`L-130`）。手順とチェックリストの正本は [`site/README.md`](../../site/README.md)、機械検査は `tools/check_site.py`（`run_checks.sh` に配線済み） | 2026-08-22 |
| **D-36** | 🔴 **Gem Index の母集団を Ecosyste.ms の 12 レジストリ・10 万リポジトリ級へ拡大し、`D-33` が撤去した検索経路への適用を「バッジ + 検索語連動の Gem 一覧」という形で再導入する。** 実測（2026-08-22・[リサーチ記録](../01_research/data/20260822-dependency-data-sources.md) / [議論記録](../../content/discussions/gem-index-feasibility-20260822/whiteboard.md)）: 12 レジストリ × 被依存数降順 15,000 件 = 18 万パッケージを **180 リクエスト・約 10 分** で収集でき、ユニーク **109,469 リポジトリ**（npm は 9.2% にすぎず、検索ヒットの **52% が非 npm**）。検索上位 100 件の被覆は **react 32% / test framework 36% / image processing 19%**（`D-33` 時点は 6% / 3% / 0%）。🔴 **`D-33` の 2 つの撤去根拠はいずれも解消した**: ① npm 限定という構造的限界 → レジストリ追加で解消（検索ヒットの 52% が非 npm） ② 10 万件はバンドル 3MB(gzip) を圧迫する → **Static Assets はバンドル枠に計上されない**（20,000 ファイル・25MiB/ファイル・リクエスト無料無制限）ため配信方式の変更で解消。🔵 **ただし `sort=gem-index`（検索結果の主ソート軸）は復活させない**（`D-33` のこの部分は維持）。理由は被覆率ではなく **ソート軸としての体験破綻**: 実測で 100 件中 68 件に値が無く、値の無い結果に順位を与えられない。加えて Gem Index は定義上「有名なものを下げる」ため、`react` 検索の先頭が `react/react` でなくなり検索の正しさを壊す。全体順を作るには最大 1,000 件取得（`D-30` ②・体感 3〜8 秒）が要り、`D-33` が撤去した経路に戻る。**採用する形**: ① 検索結果カードに **Gem バッジ**（並び順は変えない注釈・付かないことが低評価を意味しない旨を添える） ② 検索語をそのまま引き継ぐ **「この検索語の Gem を見る」導線** と Gem 絞り込み一覧（一覧の中では全件が Gem なので Gem Index 順ソートが成立する）。**却下した代替案**: `sort=gem-index` の全面復活（上記の体験破綻）／ブラウズ専用の発見面を主軸にする案（キーワード起点を失いミッション「個人開発者が **見つけられる**」に届かない・提唱した `product` 自身が round 2 で撤回）／現状維持（10.9 万リポジトリの母集団を活かさない）。反映先は [`prd.md`](./prd.md) §11 `GR-4` / [`user-story-map.md`](./user-story-map.md) `SP-16` / [ADR 0009](../adr/0009-hidden-gem-score-definition.md) §2.3 🔵 **`SP-17`（#387）実測追記（2026-08-22）**: 被覆率を **一般語 24 件**（レトロ申し送りの「3 クエリしかない」への対応）で測り直した。母集団 62,483 リポジトリ・関連度順の上位 100 件に対し **平均 34.5% / 中央値 34.5% / 最小 5.0%（`machine learning`）/ 最大 57.0%（`database driver`）・0 件ヒットのキーワードなし**。`D-36` の 3 件は react 32% → **35%** / test framework 36% → **38%** / image processing 19% → **22%** で、**3 件サンプルは全体傾向（平均 34.5%）を大きく外していなかった**。分布の示唆: ライブラリ的な語ほど高く（database driver 57% / http client 56% / orm 50% / web framework 49%）、非ライブラリ語ほど低い（machine learning 5% / encryption 16% / compression 17% / state management 18%）。`machine learning` は検索上位が awesome リスト・チュートリアル・モデル本体で占められパッケージが上位 100 に入らないための外れ値。測定は `python3 tools/measure_gem_coverage.py`（クラウドでは `mcp__github__search_repositories` で取得した結果を `--search-results` に渡す・`L-114`）で再現できる（**入力と結果のスナップショットを [`docs/01_research/data/20260822-gem-coverage-search-results.json`](../01_research/data/20260822-gem-coverage-search-results.json) と [`20260822-gem-coverage.json`](../01_research/data/20260822-gem-coverage.json) にコミット済み**。次に測り直したとき、差が自分の変更によるものか GitHub 検索側の変動かを切り分けられるようにするため）。🔴 **候補プールの失敗判定条件（本行を SSOT とする）**: Gem Index 上位 20 件を目視し、`stars=0` の自動生成ミラー・repo 誤紐付けが 3 件以上混ざっていないこと（出典: Issue #387 の完了条件 / [議論記録](../../content/discussions/gem-index-feasibility-20260822/whiteboard.md) の lead 裁定 `fail_criteria`）。🔵 `--min-stars` の既定により `stars=0` は構造的にプールへ残らないため、目視の主対象は **repo 誤紐付け**（star が閾値以上のもの）である。⚠️ この条件は **`D-36` 本文が定めたものではない**（「`D-36` の失敗判定条件」という出典表記は誤り。本追記で出典を明示して一本化する） 🔵 **`SP-19`（#389）追記（2026-08-22・導線ラベルの逐語を確定）**: 本行の「**採用する形**」② が書いた導線ラベルの逐語 **「この検索語の Gem を見る」** を、**「この検索語の Gem 候補を一覧で見る」** に確定する（実装 `home.gemListLink.label` / [`user-story-map.md`](./user-story-map.md) `SP-19` 操作レビュー手順 2 / `e2e/sp-19.spec.ts` が参照する逐語もこれに揃っている）。🔴 **置き換える理由は `D-36` 自身の語調**: 本行は Gem の印を「限定的なデータに基づく参考値であり、付かないことが低評価を意味しない」と定めているのに、当初表記の「Gem を見る」は一覧に載るものを断定的に「Gem である」と述べてしまう。「Gem 候補」「一覧で」まで含めた逐語が本決定の意図に合う。⚠️ **当初表記へ差し戻さないこと**（差し戻すと E2E が参照する逐語と不一致になり、`D-36` が守らせたい語調も失われる） | 2026-08-22 |
| **D-37** | 🔴 **Gem Index の母集団は「レジストリ別成層化 + 汚染フィルタ + repo 単位 dedupe」を必須とする。** (1) **レジストリ別成層化**: **各レジストリから被依存数降順で同数（既定 15,000 件）を取る固定枠** とし、母数比例枠は採らない（母数比例では npm が枠の大半を占め、`D-37` が潰そうとしている 1 レジストリ支配を再現するため）。順位は自前プール内でレジストリ別に再計算する。母数が枠に満たないレジストリ（CPAN・CRAN 等）は取れるだけでよい。枠の値は `SP-17`（#387）の実測で見直してよいが、**見直すときは被覆率を測り直して決定ログに追記する**。(2) **汚染フィルタ**: `stars=0` を無検証で「過小評価の証拠」として採用しない。🔵 **欠損判定の規則（2026-08-22 に実 API で確認）**: 一覧 API のレスポンスには **① `repo_metadata` 自体が無い**（例 `@types/json5`）**② `repo_metadata` はあるが `stargazers_count` キーが無い**（Maven の実測で 100 件中 22 件）**③ `stargazers_count: 0` が明示されている**（例 `is-color-stop`）の 3 状態があり、**① と ② を「欠損」、③ のみを「真の 0」として扱う**（`repo_metadata` の有無だけで判定すると Maven の 2 割を取りこぼす）。欠損は Gem 候補から外し、真の 0 でも高被依存（被依存数の上位帯）と組み合わさるものは repo 誤紐付けの疑いとして除外する。(3) **repo 単位 dedupe**: 代表は **被依存数最大の flagship パッケージ**。**根拠（実測）**: レジストリ内順位を素で混ぜると上位 100 件が Maven 87 / hex 13 となり、その実体は `stars=0` の Maven WebJars ミラー（自動生成の再配布パッケージ）。star 下位帯の **83.2% が `stars=0`** で、過小評価上位は構造的に `stars=0` の塊に支配される。**却下**: **素の絶対値によるグローバルパーセンタイル再計算**（実装して確認したところ上位 100 件中 **90 件が npm** になり、Maven 支配を npm 支配に入れ替えるだけ）／`sum` 集約（同一レジストリ内の workspace 分割が 95% を占め被依存数の水増しになる）／`min`・`max` による代表選定（`arkworks-rs/algebra` でベンチマーク用付属クレートが代表になり、実際に 1,829 件から依存される `ark-ff` を取りこぼす）／deps.dev BigQuery への乗り換え（無料枠 1TB を一撃で超える事故報告・運用複雑化に見合わない）。**絞り込みの照合は repo 名・パッケージ名の単語境界一致のみ**（🔵 **複数語クエリは全語 AND 一致を既定とし、0 件になったときだけ「最も選択的な 1 語」へ緩める**。`image processing` のような概念語は AND では 1 件しかヒットしない実測があるため、**Gem 一覧の 0 件率を `SP-19`（#389）の失敗判定条件に含める**） とし、`topics` は配信データに載せない（`orm` が `normalize` に一致する部分一致ノイズは単語境界で消える。10.9 万件の topics 収集は GitHub API 30 req/分では非現実的で、Ecosyste.ms の repos API は欠落がある）。Ecosyste.ms の生テキストは再配信しない（`GR-6`） 🔵 **`SP-17`（#387）実測追記（2026-08-22）**: 枠 15,000 件は据え置き（12 レジストリ × 15,000 = 180,000 パッケージを **180 リクエスト・3m24s** で収集）。🔴 **汚染フィルタは「閾値の調整」ではなく (2) の適用範囲そのものを拡張した**: (2) の本文は除外対象を **「真の 0（`stars: 0` が明示）× 高被依存」** と定めているが、採用した既定（`minStars=5` × 全帯）は **`stars < 5` の候補を全帯で一律除外する規則へ拡張** したものである（`stars` が 1〜4 の候補も落ちる。判断の前提として、**本プールは各レジストリの被依存数上位 15,000 件だけの集合なので全件が構造的に高被依存帯にあり、「高被依存」という限定が絞り込みとして機能しない**）。⚠️ **拡張の代償**: 除外される「被依存が多いのに star が 1〜4 しかない」帯は、本プロダクトが最も拾いたい真正 Gem 候補の帯と重なるため、**実測 29,557 件の除外には巻き添えが含まれうる**（救済策 — 例えば repo 誤紐付けを star 以外の特徴で判別する — は後段の検討課題として残す）。閾値をスイープして上位 20 件を目視した結果は `minStars=1` / 上位帯 10%（88,981 件・汚染 5 件）→ `minStars=1` / 全帯（76,454 件・汚染 3 件）→ `minStars=2` / 全帯（70,951 件・汚染 1 件）→ `minStars=3` / 全帯（67,280 件・汚染 0 件）→ **`minStars=5` / 全帯（62,565 件・汚染 0 件・採用）** で、観測されたノイズ帯の上端（star=2 の `rizkychi/discorudo`・被依存 1,817）との余裕を持たせて **5** を既定にした（このスイープは下記の段順修正より前に実施したもので、修正後の同一設定でのプールは **62,483 件**）。🔴 **(1) の「順位を自前プール内で再計算する」の解釈を確定した — 順位は汚染フィルタと dedupe を通した *生き残り* だけで再計算する**（段順: 欠損 star 除去 → 被依存 0 件除去 → 暫定 `dependentRank`（汚染フィルタの上位帯判定用）→ repo 単位 dedupe → 汚染フィルタ → **最終順位の再計算**）。**根拠（実測）**: 順位をフィルタ前に計算すると、除外率がレジストリごとに 4.8%（npm）〜86.9%（CPAN）と大きく違うため、除外率の高いレジストリは生き残りの `starRank` が上側へ切り詰められて `gemIndex` が正へ寄り、**プール全体は均衡している（npm 15.5%）のに Gem Index 上位帯だけが偏る**（`daily-digest.json` の上位 300 件が npm 128 + Maven 123 = 84%、6 レジストリがゼロ。上位 60 件では Maven 42 + npm 18 で 10 レジストリがゼロ）。生き残りで再計算すると **上位 20 件が 9 レジストリ・上位 60 件が 11 レジストリ・上位 300 件が 12 レジストリ全て** にまたがる。あわせて dedupe をフィルタより前に置く（そうしないと `highDependentRankPercentile < 100` のとき flagship だけが汚染判定で落ち、被依存数の小さい兄弟パッケージが代表として生き残る）。**結果**: 最終プールは 1 レジストリ支配なし（npm 15.49% / PyPI 14.93% / Go 13.49% … CPAN 1.55%）で、`D-37` が却下した 2 案（レジストリ内順位の素の混在＝ Maven 87%・素のグローバル再計算＝ npm 90%）のどちらの偏りも起きていない。除外内訳は star 欠損 13,750 / 被依存 0 件 14,344 / 低 star の汚染疑い 27,311 / repo 重複 29,639（repo 重複には **case 違いの同一 repo 65 件**（`perl/perl5` と `Perl/perl5` 等）を畳んだ分を含む） 🔵 **`SP-19`（#389）実測追記（2026-08-22）**: 本行が `SP-19`（#389）の失敗判定条件に含めた **Gem 一覧の 0 件率** を実測した。**計測経路**: 画面と同じ `makeSearchGems`（[`src/usecases/search-gems.ts`](../../src/usecases/search-gems.ts)）→ `StaticGemIndex#search`（[`src/infrastructure/platform/static-gem-index.ts`](../../src/infrastructure/platform/static-gem-index.ts)）を `public/data/gem-index/` のファイルシステム reader で駆動した（照合規則は写経せず [`src/domain/model/gem-keyword.ts`](../../src/domain/model/gem-keyword.ts) の実装をそのまま通し、母集団 62,483 件・**全語 AND → 0 件なら最も選択的な 1 語へ緩和** という意味論まで含めて測っている）。🔴 **結果: 実在の技術キーワード 42 語（1 語のみ 24 語 + 2 語以上 18 語）で 0 件は 0 語 = 0 件率 0.0%**。内訳は ① **1 語のみ 24 語**: 0 件率 **0.0%**・ヒット件数は 最小 **13**（`migration`）/ 中央値 **65.5** / 最大 **388**（`cli`）・緩和の発動 0 語 ② **2 語以上 18 語**: 0 件率 **0.0%**・最小 **1** / 中央値 **4** / 最大 **107**・**緩和が発動したのは 5 語（27.8%）で、5 語すべてが救済された**（`template engine` → `template` 73 件 / `state management` → `management` 23 件 / `http client library` → `library` 107 件 / `message queue` → `message` 45 件 / `password hashing` → `hashing` 4 件）。別に測った 29 語の追加セットでも 0 件は 0 語で、**実在しない語（`zzzznotexist`）だけが 0 件** になった（0 件経路が生きていることの確認＝計測の妥当性）。🔴 **判定**: 本行は 0 件率の **数値の閾値を定めていない** が、実測 **0.0%** はどの水準を置いても失敗にならないため、**`SP-19` を止める失敗条件には該当しない**。⚠️ **ただし 0 件率という指標だけでは見えない事実が 2 つある**（いずれも `SP-19` の停止事由ではなく、扱いを決めるならフォローアップ）: ① **「1〜2 件しか出ない薄いヒット」が 42 語中 4 語（9.5%）ある**（`image processing` 1 件 / `object mapper` 1 件 / `web framework` 2 件 / `log formatter` 2 件）。本行が緩和規則の根拠に挙げた「`image processing` は AND では 1 件しかヒットしない」は **再現した**（1 件ヒットするので緩和は発動せず、薄いまま出る）。0 件率が 0% でも、体験としては空に近い結果が残る ② **日本語だけの検索語は 0 件ではなく全件（62,483 件）になる**（`画像処理` `テスト` `キャッシュ` `日本語` `暗号` `ログ出力` の 6 語で確認）。`tokenizeQuery` が非 ASCII を区切りとして落とすためトークン列が空になり、`search()` が「絞り込みなし＝全件」と解釈する（`gem-keyword.ts` の JSDoc が明記している設計どおりの挙動で、バグではない）。ASCII を含む `JSON パーサー` は `json` だけが残り 423 件。**この 2 点はどちらも 0 件率では検出できない** ため、指標を増やすかどうかは別途の判断が要る 🔴 **上記 ② は本 PR（`SP-19`）で解消済み（2026-08-22）**: [`src/usecases/search-gems.ts`](../../src/usecases/search-gems.ts) が **照合不能な検索語**（生値はあるが `tokenizeQuery` の出力が 0 語）を「絞り込みなし＝全件」と区別し、**0 件 + 専用の案内**（`gems.unmatchableQuery`）へ倒す。したがって上の計測経路で日本語だけの検索語をいま測り直すと **全件ではなく 0 件** が返る。⚠️ **これを回帰（バグ）と誤認して照合不能判定を削らないこと** — 削ると本追記自身が「端的に誤った表示」と呼んだ「『画像処理』の Gem 62,483 件」が復活する。🔵 **`GemIndexPort#search` のポート契約は変更していない**（「`tokens` が空配列なら絞り込みなし＝全件」のまま）。区別はポートの外側（ユースケース）で行っており、ポートを直接使う次の呼び出し口は同じ判定を自前で持つ必要がある。🔵 **残るフォローアップは ①「薄いヒット」（1〜2 件しか出ない検索語が 42 語中 4 語・9.5%）のみ** で、② に対する新規 Issue は不要 | 2026-08-22 |
| **D-38** | 🔴 **Gem Index の配信は「レジストリ別シャードの静的アセット + isolate 内メモリキャッシュ」とする。** レジストリ別の静的 JSON を Static Assets に置き、isolate の cold start で `Promise.all` により並列取得して単一 `Map` にマージし、以降は `Map.get()` で join する。🔵 **初期化は singleton promise パターンにする**（起動中の Promise をモジュールスコープに保持し、cold start 直後に同一 isolate へ並行到達したリクエストは同じ Promise を await するだけにする）。これをしないと並行リクエストがそれぞれ 12 本ずつ取得を走らせ、cold start の実測コストが歪む。`limits.cpu_ms` は暫定 **50ms** を設定し、`wrangler tail` の p95 実測後に 20〜30ms へ締める。**却下**: **D1 の `IN` クエリ**（`D-18` が D1 を明示列挙して不採用と確定しており、`D-27` の境界注記は **静的アセットファイルの判定基準** であって D1 を救済しない）／ハッシュ分割シャード（シャードを増やすほど転送量は減るが subrequest が増える二律背反で、Free の 50/invocation に収まる分割数が存在しない）／Static Assets の Range 二分探索（Range 未対応の報告があり未検証）／バンドル焼き込みの継続（データ更新のたびに JS の再ビルドが要り `D-28` の設計意図と逆行）。⚠️ **cold start は「デプロイ直後の 1 回」ではなく isolate ごとに継続的に発生する**（発生率は Cloudflare 非公開）。`limits.cpu_ms` の妥当性は実測でしか確かめられない 🔵 **`SP-18`（#388）実測追記（2026-08-22）**: `limits.cpu_ms` を暫定 **50** から **200** へ引き上げた。**根拠（ローカル実測・Node / V8）**: cold start の内訳は `index.json` 取得 **1.4ms** / 12 シャード読み込み **10.5ms** / `JSON.parse`（3.47 MiB）**39.9ms** / `Map` 構築（62,483 件）**40.7ms** = 合計 **92.5ms**、うち I/O 待ちを除いた **CPU 相当は約 81ms**。warm 時の `Map.get()` × 30 件は **0.012ms** で、コストは cold start 1 回に閉じている。🔴 したがって **本行が当初置いた「p95 実測後に 20〜30ms へ締める」という見込みは実測により否定された**（cold start は isolate ごとに継続的に発生するため、暫定 50ms のままでは超過しうる）。⚠️ **Workers 上の p95 は未測定**: `wrangler tail` の WebSocket が実行環境のプロキシを通らず、Cloudflare Observability のクエリでもプレビュー版のイベントが返らない。プレビューへ 14 リクエスト投げて全て 200・CPU 超過（**Error 1102** / invocation outcome `exceededCpu`）ゼロという間接証拠のみで、cold start が実際に起きたかは確認できていない（Error 1027 は Workers **Free** の使用量到達による停止であり、本アカウント（Paid）では発生しない・[Workers Limits](https://developers.cloudflare.com/workers/platform/limits/#cpu-time)）。🔵 **締め直せる見込みはある**: 所属判定に必要な `fullName` だけを持つ軽量アセットへ分ければ **81ms → 14.5ms**（実測）・subrequest **13 → 2** にできる → **Issue #434**。本番で p95 を測れるようになった時点、または #434 の完了時に改めて締める 🔵 **`SP-19`（#389）実測追記（2026-08-22）**: `limits.cpu_ms` を **200 → 400** へ引き上げた。**根拠（ローカル実測・Node / V8・別プロセスで 5 回計測）**: `D-36` が採用した「検索語連動の Gem 絞り込み一覧」は、所属判定用の `Map<repo, gemIndex>` では足りず **全レコード（`packageName` / `stars` / `dependentCount` / `registry`）と照合用トークン列** を **同じ 1 回の parse から** 作る必要がある（アセットを増やさない・2 回目の parse をしないという `D-38` の前提は維持）。cold start の内訳は `JSON.parse` 約 33ms / レコード生成 約 23ms / `toLowerCase` 約 6ms / dedupe 約 20ms / **トークン計算 約 91ms** / **Gem Index 昇順の並べ替え 約 31ms**。計測結果は ① `lookup` だけのリクエスト（`SP-18` のバッジ経路）**96〜110ms** ② Gem 一覧の初回リクエスト **237〜277ms** ③ warm の絞り込み 1 回 **8〜12ms**（62,483 件の線形走査） ④ heapUsed 増分は `lookup` のみ **+12.5 MiB** / 一覧構築後 **+31.6 MiB**（`SP-18` 実装は +5.1 MiB）。**② が 200ms を超えるため引き上げた**（超過時は `SP-18` 追記と同じ **Error 1102** / `exceededCpu`）。🔵 **一覧専用のコスト（トークン計算 + 並べ替え = 約 122ms）は初回の一覧リクエストまで遅延させる 2 段構成にした** ため、`SP-18` で出荷済みのバッジ経路に回帰を持ち込まない（`SP-18` 実装の同条件は 84〜90ms で、差は全レコードを保持する分の +約 14ms のみ）。🔴 **値を 400 にした理由**: 観測した最大は 277ms で、300 では余裕が **約 8%** しか残らない。Workers 上の p95 は `SP-18` から引き続き未測定（同じ `wrangler tail` の制約）であり、そこが枯れる保証がないため **実測最大に対して約 44% の余裕** を取って **Error 1102** を潰す側に倒した。🔵 `limits.cpu_ms` は **上限のキャップであって予約ではない**（課金は実使用 CPU に対して起きる）ので、上げても平常時のコストは増えず、増えるのは「暴走したリクエストが打ち切られるまでの最大 CPU」だけである。🔴 **#434（軽量アセット化）は本引き上げの代替にならない**: あれは `lookup` 側を軽くする施策で、一覧側の 237〜277ms は残る | 2026-08-22 |

> ⚠️ **`D-27`〜`D-29` の採番についての注記**: [議論記録](../../content/discussions/post-mvp-strategy-20260820/decisions.md) §4 は本来 `D-25`〜`D-27` を割り当てる想定だったが、`D-25` が別セッションの決定（`NFR-27` Lighthouse ゲート・同じ 2026-08-20 付）に既に使用されていたため、**一度振った ID を変更・再利用しない** という原則（`docs/02_requirements/prd.md` 系のルール）に従い `D-26`〜`D-28` へ 1 つずつ繰り下げて採番した。議論記録側の表記（`D-25`/`D-26`/`D-27`）と本ファイルの実際の ID（`D-26`/`D-27`/`D-28`）は 1 対 1 でずれている点に注意すること。

### D-16 / D-17 / D-18 / D-19 の決定から従属的に確定する事項

- 🔴 **Cloudflare 前提の構成の正本は [Cloudflare インフラ設計](../03_design/infrastructure/cloudflare-infrastructure.md)**。`INF-n` 契約（[インフラ設計](../03_design/infrastructure/infrastructure-design.md)）は引き続き契約の正本であり、新ドキュメントは「その契約を Cloudflare のどの機能で満たすか」だけを持つ。
- **`D-16` の帰結**: `INF-2`（超過時は課金ではなく停止に倒す）は **Workers Free を維持すること自体** で構造的に満たされる（超過は HTTP Error 1027 で停止し課金されない）。逆に Paid へ上げるとハードキャップは消滅し、`limits.cpu_ms` と Billable Usage API による後追い監視しか残らない。
- **`D-17` の帰結**: Free の **CPU 10 ms / Worker バンドル 3 MB（gzip）** に収まるかが `INF-3`（与件の技術スタックが動くこと）の成否を決める。`SP-1` で実測し、閾値超過が確定した場合のみ Workers Paid への切替を `A-6` としてユーザーへ依頼する（Free → Paid に CLI / API 経路は存在しない）。
- **`D-18` の帰結**: 🔴 **`D-24`（2026-08-19）により L2 の主役はアプリ内 `CachePort` の実装へ改訂され、`NFR-7`（request coalescing）の「補助から主要な防波堤へ格上げ」は撤回されて当初案どおり補助に戻った**（詳細は `D-24` を参照。以下は撤回前の記述であり履歴として残す）。~~`NFR-7`（request coalescing）が補助から主要な防波堤へ格上げされる（Workers Caching のリクエスト合体はエッジで効くため、`D-13` の「インスタンス内でしか効かない」という前提が変わる）。~~ 一方で **キャッシュヒットも Worker のリクエスト枠を消費する** ため、「キャッシュヒット率向上 = コスト削減」は Cloudflare では成立しない（効くのは CPU-ms と GitHub レート枠の 2 軸のみ）という帰結は `D-24` 後も変わらない。
- 🔴 **`D-18`（および `D-24` が上書きした部分）の境界注記（`D-27`・2026-08-20）**: **ビルド時に生成しデプロイで丸ごと差し替わる読み取り専用の静的アセット**（Gem データのバッチ生成 JSON 等）は本決定（永続ストアを採用しない）の **対象外** である。境界は「**Worker がそのファイルへ実行時に書き込むか**」で判定し（`D-27`）、No 側（バッチ生成 → デプロイで丸ごと差し替え）は `D-18` の禁止対象に含まれない。既存の `D-18` / `D-24` の決定文そのものは変更しない（本注記は境界の明確化のみを追加する）。
- **`D-24` の帰結**: `SP-5` の検証手段（`cloudflare-infrastructure.md` §4.5）が「`GET /api/search` の応答ヘッダ `X-Cache-Status`」に確定し、画面（SSR 応答）では観測できないことが明確になった（[`user-story-map.md`](./user-story-map.md) §5.3 `SP-5` の操作レビュー手順もこれに合わせて改訂済み）。
- **`INF-5` の帰結**: Cloudflare bindings へのアクセスを **`src/infrastructure/platform/` 配下に限定** し、grep 2 本（bindings の直接参照 / Cloudflare 環境変数による実行時分岐）で機械検出する。目視判断だった `NFR-21` の遵守がセルフレビューで自動判定できるようになる。
- **`D-19` の帰結**: 実測が閾値超過を示したとき、セッションは `status:waiting-user` で止まらずに「支払い方法を登録してください」という **1 手だけの依頼** を出して待つ（判断は事前に済んでいるため A/B/C/D 分類をやり直さない）。撤退ライン $10 は Billable Usage API（`GET /accounts/{id}/billable-usage`）のポーリング閾値として実装する。⚠️ Budget alerts は通知のみで停止しないため、これに依存しない。
- **人間に残る作業**: ① Cloudflare API トークンの初回発行 ② その値を GitHub Actions Secrets と Claude.ai 環境変数の 2 箇所へ貼る、の 2 つだけ（いずれも一度きり）。以降のリソース作成・デプロイ・プレビュー URL 生成・シークレット投入は Claude が非対話で実行する。

### D-31 / D-32 の決定から従属的に確定する事項（追記・2026-08-23 JST 実測・Issue #451）

> 🔴 **`D-31` / `D-32` の決定文そのものは撤回しない**（上の決定ログの本文は書き換えていない）。ここは「移行後に前提の一部が成り立っていなかった実測」と「それを埋めた対策」の追記のみ。

- **`D-31` の帰結の訂正**: `D-31` は「Cloudflare 側が `main` への push ごとにビルド・デプロイする」ことを根拠に「マージ = 本番反映が回復する」としていたが、**Git 連携自体は実測どおり正常に機能していた一方で、`D-32` のゲート方式（Deploy command 内で `check_deploy_gate.py` を実行し、閉じていれば非 0 で終了させる）が想定より広く効いていた**。Cloudflare Builds API で直近 24 件のビルドを確認したところ、**全件が `build_outcome: "fail"`**（ゲート判定によるビルド失敗）だった。原因は Git 連携の不具合ではなく、**ゲートが開いた後に再ビルドを起こす経路が存在しなかったこと**（push 時点でゲートが閉じていたコミットは、後でゲートが開いても Cloudflare 側からは自動的に再試行されない）。「発火点の移行」自体は `D-31` の設計どおり成立しており、訂正が要るのは「移行すればマージ = 本番反映に戻る」という結論部分だけ。
- **`D-32` の帰結の補完**: `D-32` が定めた fail-closed（ゲートが閉じていればビルドを失敗させ本番を更新しない）は実測でも正しく機能していた（`build_outcome: "fail"` の全件で本番は更新されていない）。ただし `D-32` はゲートを **閉じたまま維持する** 設計だけを持ち、**ゲートが開いた後にそれまで塞がれていた push 分をどう本番へ届けるか** を決めていなかった。この欠落を埋める対策として `tools/trigger_workers_build.py`（Cloudflare Builds API で `main` 最新コミットのビルドを再トリガーする。実行前にデプロイゲートを自前で確認し、閉じていれば何もせず exit 1 で終わる）を追加した。Sprint Review 判定コメント投稿後の一次経路を、手動 `npm run deploy`（§8.2.2 の分類器ブロック対象）からこの再トリガーへ切り替え、スプリント自走ルーティンのプリフライトにも `tools/check_prod_drift.py` の本判定を配線して滞留を検知する。
- **`tools/check_prod_drift.py` の運用漏れ**: 本番と `main` HEAD の乖離を検出する専用スクリプトが、`tools/run_checks.sh` からは `--self-test`（自己診断）としてしか呼ばれておらず、**本判定モードがどの定期ルーティンからも呼ばれていなかった**。滞留が静かに進行する一因だったため、上記の再トリガー対策とあわせてプリフライト配線を追加した。
- 詳細な手順・API エンドポイント・実測ログは [Cloudflare インフラ設計](../03_design/infrastructure/cloudflare-infrastructure.md) §8.2.3 が正本。

### D-13 / D-14 / D-15 の決定から従属的に確定する事項

- 🔴 **インフラに求める契約の正本は [インフラ設計](../03_design/infrastructure/infrastructure-design.md)**（`INF-n`）。事業者名を持たないまま、ランタイム契約・個人情報規定・環境構成・選定の評価軸を確定させたもの。
- **`D-13` の帰結**: プロセス内メモリのキャッシュが当てにできないため、`NFR-17`（Cache Port）は「あると便利」から **「無いと成立しない」に格上げ** される。あわせて `NFR-7` の request coalescing は **補助** と位置づけ、レート制限対策の主役をキャッシュヒット率に置く。
- **`D-14` の帰結**: 利用者からの削除要求に対してサーバー側で行う作業が存在しない（Cookie と `localStorage` の削除で完結する）。この性質を README に明記する（`NFR-30`）。アナリティクス・エラートラッキング SaaS は ADR なしに導入しない。
- **`D-15` の帰結**: `main` へのマージのみを本番反映のトリガーとし、手動デプロイ手順を運用に含めない。事業者選定の評価軸に「ロールバックの手数」「環境変数のローテーション時に再ビルドが要るか」が入る。
- ✅ **事業者の選定は完了した**（`D-16`・2026-08-18）。プレビュー・本番とも Cloudflare Workers であり、記録は [ADR 0002](../adr/0002-cloudflare-workers-infrastructure.md)。構成の正本は [Cloudflare インフラ設計](../03_design/infrastructure/cloudflare-infrastructure.md)。

### D-11 / D-12 の決定から従属的に確定する事項

- 🔴 **4 規律の正本は [`docs/rules/sprint-development-rules.md`](../rules/sprint-development-rules.md)**（`SD-1` 動作確認 / `SD-2` TDD / `SD-3` 確認の発火ライン / `SD-4` ドキュメント自律参照）。Hot 層に常駐させ、全セッションで自動読み込みされる。
- **`SD-1` の帰結**: スプリントの完了条件に「PR のプレビュー URL 上で操作レビュー手順を完走できること」が加わる（[`user-story-map.md`](./user-story-map.md) §7 運用ルール 2）。URL を出せない場合は理由とローカル起動手順を PR に書く（沈黙禁止）。
- **`SD-2` の帰結**: [`user-story-map.md`](./user-story-map.md) §5.3 の各スプリントの「操作レビュー」手順は **E2E テストに写す**。手動確認と自動テストを一致させ、以降のスプリントでの回帰を自動検知する。テストのスキップ・無効化による緑化は禁止。
- **`D-12` の帰結**: `user-confirmation-minimization.md` §3 item 0 の 4 分岐のうち **(c) の適用範囲が「仕様解釈の分岐」まで広がる**（従来は不可逆リスクのあるものに限っていた）。逆に (a)(b) は実装手段の領域に限定される。
- ✅ **プレビュー環境のサービス選定は完了した**（`D-16`）。Cloudflare Workers の **version + preview alias**（`wrangler versions upload --preview-alias pr-<N>`）で PR ごとの URL を出す。Worker を増やさないため Free の Worker 数上限を消費しない。`NFR-21` は維持され、差し替え時に書き換えるのは `src/infrastructure/platform/` と設定ファイルのみ。

### D-1 / D-2 の決定から従属的に確定する事項

- 要件定義書は **1 本**（Phase 1 / Phase 2 を章で分ける）。`minimum-requirements.md` は与件原本として **編集せず残す**。
- 上乗せ要件は与件の FR-ID と衝突しない別体系（例: `GR-n` = Gem 系要件）で採番する。
- Phase 1 の受け入れ判定は、与件チェックリスト（`minimum-requirements.md` §7）を **そのまま** 用いる。

### D-3 の決定から従属的に確定する事項

**主目的（課題・ポートフォリオ）から**:

- **Phase 2（IndieGems）は「構想として要件定義書に記述するが、Phase 1 では実装しない」**。Phase 1 の完成度を Phase 2 に食わせない。
- **「説明可能性が最優先」＝ ADR の重要度が上がる**。技術選定 1 件ごとに「なぜその選択をしたか / 何を捨てたか」を `docs/adr/` に残すことを、要件定義書の完了条件に含める。
- 非機能要件の数値目標（`Q-7`）は **「説明できる設計判断の根拠」** として設定する（Core Web Vitals の目標値を置き、達成手段を ADR に紐付ける）。

**副次目標（有用なら運用継続）から** — これらは「後で足す」と破綻するため、Phase 1 の設計時点で織り込む:

- **`R-5`（レート制限の必要枠逆算）は Phase 1 の要件に残す**。GitHub Search API は認証済みでも 30 req/分で、キャッシュなしの公開運用は数人の同時アクセスで破綻する。キャッシュ戦略（`D-5` / `D-7` と連動）の設計根拠になるため、除外しない。
- **`R-6`（コスト試算）は「無料枠に収まること」を非機能要件として残す**。試算そのものは軽量でよいが、無料枠を超える設計（常時稼働ワーカー・マネージド DB 等）を Phase 1 で選ばないための制約として機能させる。
- **`R-8`（GitHub ToS）は「API 経由のみ・スクレイピングしない」という設計制約に圧縮して残す**。この制約を守る限り、一次確認は **「第三者へ公開する時点」まで先送りできる**（Phase 番号ではなく公開の有無で判断する。規約が効くのは公開して他者に使わせた瞬間であり、Gem Index の実装有無とは無関係）。
- **Ecosyste.ms の CC BY-SA 4.0 は現時点でブロッカーではない**（非商用運用のため）。ただし **出典表示（BY）の義務は運用時に発生する** ため、Phase 2 の構想記述に「UI 上でのデータ出典明記」を要件として含める。収益化に踏み出す場合のみ商用ライセンスを再検討する。

### Q-1 / Q-2 の決定から従属的に確定する事項

- **2 軸の役割分担を混ぜない**: 過小評価度は **ランキング（並び順）** に、健全性は **足切り（フィルタ）** に使う。1 つのスコアに合算しない（合算すると「健全だが有名」なリポジトリが上位に戻り、`competitive-analysis.md` が否定した star 追随ランキングに退化する）。
- **健全性は自作しない**。OpenSSF `criticality_score`（公開 BigQuery データセット）と OpenSSF Scorecard を可能な限りそのまま流用する。`R-3` で判明した「Gem Score の構成要素の大半が既存スコアと重複する」問題を、再実装ではなく **依存** で解消する。これは「車輪の再発明をしなかった」という説明可能な設計判断にもなる（`D-3` 主目的と整合）。
- **データ源が 3 系統になる**: 被依存数 = Ecosyste.ms / star = GitHub API / 健全性 = OpenSSF。3 者を突き合わせるキー（リポジトリ URL ↔ パッケージ名）の解決が Phase 2 のデータモデル設計の中心課題になる。
- **対象はパッケージとして公開されたリポジトリに限定される**（被依存数が取得できる範囲）。副作用として、コンセプトが排除したがっていたノイズ（チュートリアル・課題提出用リポジトリ）が自動的に除外される。
- 残る未決事項: `Q-3`（Gem Index の正規化方法・閾値）と `D-10`（対象エコシステム）。

### D-9 の決定から従属的に確定する事項

- 要件定義書は **「MVP の線」と「積み上げバックログ」の 2 部構成** にする。`P1-MVP` は次の 2 要素で構成する。

  1. **与件チェックリスト（`minimum-requirements.md` §7 の 11 項目）を満たす最小集合** — 必達の下限であり、削る選択肢を作らない（`D-2` と整合）
  2. **後から入れると既存コードの書き換えが発生する上乗せ要件** — ロケール基盤（`D-4`）/ Cache Port（`D-5`）/ 任意 OAuth（`D-6`）

  ⚠️ ①だけを `P1-MVP` の定義にしてはならない。②の 3 件はいずれも「後付けコストが高いから先に入れる」という理由で MVP に置いたものであり、①だけを定義にすると実装時に②が積み上げ側へ落とされ、この原則自体が空文化する。

- **`P1-MVP` から削ってよいのは②のうち、削っても①を割らないものだけ**。①は削らない。
- **工数見積もりは書かない**（期日が未確定なため根拠のない数字になる）。代わりに **要件間の依存関係** を明記し、着手順だけ決められる状態にする。
- 積み上げ側の各項目に **「入れると何が良くなるか / 落とすと何を失うか」を 1 行** 添える（期日が見えた時点で切る判断ができるように）。
- 🔴 **「積み上げていける状態」は要件そのものになる**。以下を `P1-MVP` に含める（後から入れると既存コードの書き換えが発生し、積み上げ速度がそこで落ちるため）:
  - **データアクセス層の分離**（`D-5` の推奨と同一。API 呼び出しを 1 箇所に隔離し、Phase 2 の ETL 差し替え時に UI 層を触らない）
  - **テストと CI の土台**（回帰を検知できない状態で機能を積むと、積むほど壊れる。与件 §5 のテスト要件がそのまま土台になる）
  - **型定義と Lint / フォーマッタ**（与件 §4.4 に既にある）
  - **後付けコストが高い構造の先行確保**: ルーティング設計（locale セグメント含む・`D-4`）とキャッシュ境界。これらは後から変えると全 URL / 全データ取得箇所に波及する
- 逆に **後からいくらでも足せるもの**（表示項目の追加・フィルタ UI・ソート順・Gem Index）は MVP に入れない。

### D-4 の決定から従属的に確定する事項

- **ロケールを URL に含める**（`/[locale]/...`）。与件 §4.3 の「検索条件（キーワード・ページ）を URL に反映する」と同じ URL 設計上で解決するため、**ルーティング設計を実装の最初に確定させる**（後から locale セグメントを足すと既存 URL がすべて変わり、共有リンクも壊れる）。
- **MVP で入れるのはロケール基盤まで**: ルーティング・ロケール検出・メッセージカタログの仕組み・UI 文言のハードコード禁止（すべてカタログ経由）。**日本語カタログの完成度は積み上げフェーズで上げる**。
- **翻訳対象は自前の UI 文言のみ**。GitHub 由来のデータ（リポジトリ説明文・topics・言語名）は翻訳せず原文表示とする（機械翻訳は正確性を損ない、`D-3` の「説明可能性」に反する）。
- **テスト要件への影響**: 主要フローの E2E は **既定ロケール 1 つで実行** し、ロケール切替そのものを独立したテストケースとして 1 本持つ（全フローを 2 ロケール分回すとテスト時間が倍になり、積み上げ速度を落とす）。
- ライブラリは `next-intl` を第一候補とするが、Next.js 16 App Router での最新の推奨構成を実装着手時に一次確認する（`R-7` に統合）。

### D-5 / D-7 の決定から従属的に確定する事項

> ⚠️ **本ブロックは `D-5` / `D-7` 決定時点（2026-08-17）の記録である。** デプロイ先そのものは `D-16`（2026-08-18）で Cloudflare に確定してクローズ済み。以下のうち **Cache Port など「決めないを成立させるために作った構造」の帰結は引き続き有効**（`D-18` で実装位置が `src/infrastructure/platform/` に確定した）。

**「決めない」を成立させるために、決めておくべきこと**（先送りは無条件では成立しない）:

- 🔴 **キャッシュ抽象（Cache Port）を設ける**。MVP は Next.js のキャッシュ機構（`use cache` / Data Cache）を実装として使い、外部キャッシュ（Upstash Redis 等）へ差し替えられるインターフェースを挟む。
  - ⚠️ **YAGNI の意図的な例外**: CLAUDE.md は「1 箇所しか使わない抽象化を先回りで追加しない」を禁じている。本件は **「インフラを決めない」という決定を成立させるための必要条件** であるため例外とし、その理由を ADR に記録する。ただし **抽象の面積は最小に絞る**（`get` / `set` / `invalidate` + TTL 指定のみ。汎用キャッシュライブラリを自作しない）。
- 🔴 **キャッシュキーの命名規約を先に確定させる**。キー設計は後から変えると全キャッシュが無効化され、外部キャッシュへの移行時にも移植できない。最低限、検索結果（クエリ + ページ + ソート条件）と単一リポジトリ（`owner/repo`）で名前空間を分ける。
- 🔴 **TTL 方針を要件として明記する**。検索結果と詳細情報で更新頻度の要求が異なるため、TTL は 1 つの値に統一せず用途ごとに定める（具体値は `R-5` のレート制限逆算と合わせて実装着手時に決定）。
- **特定 PaaS 固有機能への依存を最小化する**。デプロイ先が未定である以上、Vercel 固有の機能に強く依存すると選択肢を狭める。Next.js 標準機能（App Router / `use cache` / `next/image`）の範囲で組み、それを超える依存が必要になった時点で ADR を起票する。
- **DB は MVP で持たない**（`P2` 送り）。Gem Index（`Q-1`/`Q-2`）はバッチ集計と永続化を要するため、DB の導入判断は Phase 2 の着手時に、そのとき決まっているインフラ前提のもとで行う。
- **環境変数の一覧を要件定義書に持つ**。インフラが後から決まる以上、「何を外部から注入する必要があるか」（GitHub トークン・OAuth クライアント情報・キャッシュ接続情報・ロケール既定値）を先に列挙しておかないと、移行時に漏れる。

### D-6 の決定から従属的に確定する事項

> 🔴 **認証方式は `D-20` で GitHub App に改訂済み。** 下記の枠は改訂後の実測値に更新してある（ログイン設計・与件との整合は `D-6` のまま変更なし）。

**根拠となる実測値**（GitHub App の installation token で実測・2026-08-18。未認証と PAT の値は公式ドキュメント・2026-08-17）:

| 枠 | GitHub App（採用・実測） | Fine-grained PAT | 未認証 |
|---|---|---|---|
| 検索 API | **30 req/分** | 30 req/分 | 10 req/分 |
| Core API（詳細取得） | **8,800 req/h** | 5,000 req/h | 60 req/h |
| Code search | 10 req/分 | 10 req/分 | — |
| GraphQL | 8,800 req/h | 5,000 req/h | — |
| 二次制限 | 100 並行リクエスト / 900 ポイント毎分 / 直列実行を推奨 | 同左 | 同左 |

> ⚠️ GitHub App の Core 枠は **インストール先のリポジトリ数・ユーザー数で変動する**（公式式 `5,000 + 20 を超えた分 × 50` / 上限 12,500）。**8,800 は 2026-08-18 時点の実測値であり固定値ではない**（縮小したときの扱いは [ADR 0003](../adr/0003-github-app-authentication.md) §7 の撤回条件 2）。

- **304 が返る条件付きリクエストは primary rate limit を消費しない**（公式明記。**2026-08-18 に実測で確認済み** — 正規名への `If-None-Match` で枠の消費はゼロ）。ただし検索結果は変動しやすく 304 になりにくいため、**ETag が効くのは詳細取得側**。
- 🔴 **リポジトリ改名による 301 リダイレクトは枠を 1 消費し、ETag も効かない**（実測）。旧名で叩くと 200 は 2 消費・304 でも 1 消費になるため、**詳細取得は必ず検索 API が返した正規の `full_name` で行う**（`Q-10` 従属事項を参照）。
- 律速は **検索の 30 req/分**（GitHub App でも検索枠は PAT と同じ）。詳細取得（8,800 req/h）は実質ボトルネックにならない。
- **installation token は TTL 1 時間**。取得コスト（`POST /app/installations/{id}/access_tokens`）を毎リクエストで払わないよう、**サーバー側でトークンを失効前まで再利用する**（有効期限に安全マージンを引いて再取得する）。

**ログイン設計**:

- 🔴 **ログインは完全に任意で、機能差を作らない**。ログインで変わるのは **レート枠だけ**（未ログイン = アプリ共有枠、ログイン = 各自の 30 req/分）。「ログインしないと使えない機能」を作った瞬間、`D-6` の目的（不特定多数が気軽に使える）が壊れる。
- 🔴 **OAuth スコープは要求しない（no scope）**。公開リポジトリの検索・閲覧しか行わないため、追加権限は不要。「最小権限」を実証できる設計判断として ADR に残す。
- 🔴 **セッションは暗号化した httpOnly Cookie で持つ**（`D-5` で DB を持たないため）。ユーザーの OAuth トークンを **クライアントへ露出させない**（与件 §4.1 と同じ原則をユーザートークンにも適用する）。
- **CSRF 対策として OAuth の `state` パラメータを必須にする**。ログアウト（Cookie 破棄）も要件に含める。
- **OAuth の callback URL はデプロイ先に依存する** ため、`D-5`/`D-7`（インフラ未定）と直結する。**必ず環境変数化** し、ホスティング先が決まっていなくても構成が壊れないようにする。

**与件との整合（明示的に説明する）**:

- 与件 §1.2 は認証を「対象外」としているが、これは「実装しなくてよい」であって禁止ではないと解釈し、**レート枠という技術的必然を根拠に上乗せする**（`D-2` の「上乗せは自由」に沿う）。
- ただし **解釈を書き残さないと「要件を読んでいない」と誤読されうる** ため、README と要件定義書の双方に「与件では対象外だが、なぜ入れたか」を明記することを完了条件に含める。
- **認証を入れても、与件が対象外とした他の項目（お気に入り・通知・課金・独自スコアリング）は MVP に入れない**。ログインを足したことを口実にスコープを広げない。

**キャッシュ・レート対策の要件化**（`D-5` の Cache Port 上に実装する）:

- 同一クエリのキャッシュ、同時同一クエリの request coalescing、GitHub API 呼び出しの直列化、詳細取得での ETag 利用、`x-ratelimit-remaining` 監視による degraded 表示（キャッシュのみ返す / 混雑を明示）。
- 🔴 **複数トークンのローテーションは採用しない**（レート制限の回避とみなされる恐れがあるため）。この判断も ADR に残す。

**テスト要件への影響**: OAuth フローは外部依存のためモック化する（与件 §5「外部 API はモック化」と同じ扱い）。未ログイン / ログイン済みの両方で主要フローが成立することを検証対象に含める。

### D-5 追補（DB レス原則）の決定から従属的に確定する事項

- ✅ **`D-6` と矛盾しない**。暗号化 httpOnly Cookie によるステートレスセッションは、まさに「サーバー側ストアを持たない」ための定石であり、本原則を **強化する** 方向にある（セッションストアを立てる選択肢が消える）。
- **状態の置き場所を要件として明示する**:

| 状態 | 置き場所 | 理由 |
|---|---|---|
| 検索キーワード・ページ・ソート | **URL**（`searchParams`） | 与件 §4.3 の「リロード・共有で同じ結果」を満たす |
| ロケール | **URL のパスセグメント**（`D-4`） | 共有時に言語が保たれる |
| ログインセッション（OAuth トークン） | **暗号化 httpOnly Cookie** | サーバー側ストア不要。クライアント JS からは読めない |
| UI 設定・検索履歴・お気に入り | **`localStorage`** | サーバー不要で実現できる。端末ローカルで完結 |
| API レスポンス | **キャッシュ層（Cache Port）** | 永続ストアではなく破棄可能なキャッシュとして扱う |

- 🔵 **副次的な発見**: 与件 §1.2 が対象外とした「お気に入り・プライベートリスト管理」は、**`localStorage` なら DB なしで実現できる**。積み上げバックログの有力候補として要件定義書に記録する（`P1-積み上げ`）。
- 🔵 **Phase 2 の Gem Index も DB レスで貫ける可能性がある**: バッチで事前計算した結果を **静的データ（JSON 等）として配信** すれば、クエリ時に DB を必要としない。「対象を上位 N 件に絞る」前提と相性が良い。**Phase 2 の第一候補案として要件定義書に記録し、DB 導入は最後の手段とする**。
- ⚠️ **受け入れる制約を明記する**（後から「できない」と言わないため）: 端末間で同期されない / ブラウザのデータ削除で消える / `localStorage` の容量上限（数 MB 程度）。これらを許容する旨を要件に書き、同期が必要になった時点で初めて DB を検討する。
- 🔵 **副次効果**: サーバーに個人データを保持しないため、プライバシー・データ保護上の考慮が最小化される。これも説明可能な設計判断として ADR に残す。

### D-8 の決定から従属的に確定する事項

**要件定義書が正本として持つもの**: 要件 ID・要件内容・受け入れ条件（主要フローは Given/When/Then）・優先度ラベル・要件間の依存関係・スコープ境界。

**派生ドキュメントとの責務境界**（同じ事実を 2 箇所に書かず、必ず ID 参照で結ぶ）:

| ドキュメント | そこが正本として持つ情報 | 要件定義書との関係 |
|---|---|---|
| **要件定義書**（本体） | 要件 ID / 内容 / 受け入れ条件 / 優先度 / 依存関係 | **正本**。他はここを参照する |
| リーンキャンバス | 課題・価値提案・顧客セグメント・主要指標（事業仮説） | 要件定義書の「目的・ペルソナ・成功指標」節は **ここを要約参照** する |
| インセプションデッキ | やらないことリスト・トレードオフスライダー・リスク・エレベーターピッチ | 要件定義書の「スコープ外」節は **ここを参照** する |
| ユーザーストーリー | 実装単位への分解（1 要件 → 1〜n ストーリー） | **必ず要件 ID を参照する。受け入れ条件を再掲しない**（重複するとドリフトする） |
| ロードマップ（[`roadmap.md`](./roadmap.md)・作成済み） | 時間軸・マイルストーン（`M-n`）・リリース単位 | 要件定義書の優先度ラベルを到達順序へ射影したもの。🔴 期日が未確定のため絶対日付を持たない（`D-9`） |
| ADR | 技術的意思決定の理由と却下案 | 要件定義書からは ADR 番号を参照するだけにする |

**ID 体系の規約**（正本である以上、ID の安定性が最重要）:

- `FR-n`: 与件由来の機能要件（`minimum-requirements.md` の番号を **そのまま引き継ぐ**）
- `NFR-n`: 非機能要件
- `GR-n`: Gem 系の上乗せ要件（Phase 2 中心）
- `AC-n`: 与件の受け入れ基準 11 項目（トレーサビリティ用）
- 🔴 **一度振った ID は変更・再利用しない**（欠番が出ても詰めない）。派生ドキュメントからの参照が壊れるため。
- 各要件に **`P1-MVP` / `P1-積み上げ` / `P2`** の優先度ラベルを付ける（`D-9`）。

**ドキュメント作成順の推奨**: 要件定義書（正本）→ インセプションデッキ（やらないことの明文化）→ ユーザーストーリー（実装単位）→ ロードマップ（時間軸）。リーンキャンバスは要件定義書の前後どちらでもよいが、`initial-concept.md` と `competitive-analysis.md` から抽出できるため **要件定義書と並行** で作れる。

### Q-8 の決定から従属的に確定する事項

- 🔵 **与件 FR-7 の選択に答えが出る**。与件は「ページネーション **または** 無限スクロール」を選択制にしているが、**AA 目標を採るならページネーションが明確に有利**（無限スクロールはキーボード操作・スクリーンリーダー・フォーカス管理・「現在位置」の提示すべてで不利。加えて `D-4` の URL 状態保持とも噛み合う）。**ページネーションを採用し、その理由を ADR に残す**。
- 🔴 **配色はデザイン着手時に確定させる**（コントラスト比 4.5:1）。色を後から変えると全画面に波及するため、`D-4` のルーティングと同様「後付けコストが高い先行確保項目」に含める（`D-9`）。
- **状態表示（与件 §3.2）とアクセシビリティを接続する**: 読み込み中・0 件・エラーは視覚表現だけでなく **`aria-live` で支援技術にも伝える**。与件の「利用者が判別できる」を、支援技術利用者にも適用する。
- **画像（オーナーアイコン）の代替テキスト方針を決める**: リポジトリ名が隣接して読み上げられる文脈では、アイコンは装飾として `alt=""` にする方が冗長にならない。この判断も明文化する。
- **テスト要件に追加**: axe による自動チェックを CI に組み込む（`@axe-core/playwright` 等。実装着手時に構成を確認 → `R-11` に統合）。
- **手動チェックリストを `docs/` に持つ**: キーボードのみでの検索 → 一覧 → 詳細 → 復帰の完走、フォーカスの可視性と順序、見出し階層とランドマーク、200% 拡大時の破綻有無。

### Q-7 の決定から従属的に確定する事項

**採用する数値目標**:

| 指標 | 目標値 | 根拠 |
|---|---|---|
| LCP | 2.5 秒以下 | Google が定める Core Web Vitals の "good" 境界（外部基準の引用） |
| INP | 200ms 以下 | 同上 |
| CLS | 0.1 以下 | 同上 |
| Lighthouse Accessibility | **100**（ゲート） | axe ベースで決定論的に判定されるため、閾値化しても揺れない |
| Lighthouse Performance | **90 以上**（ゲート） | 下記の安定化条件とセットで運用する |
| キャッシュ TTL | 検索結果と詳細で別値（**暫定**: 検索は分オーダー / 詳細は時間オーダー） | 具体値は `R-5`（レート枠の逆算）と合わせて実装着手時に確定する |

🔴 **CI ゲートの偽陽性対策**（これを付けずにゲート化すると、`D-9` の「積み上げ速度」を落とす）:

- **本番相当のビルドに対して計測する**（開発サーバーでは測らない）。
- **複数回実行の中央値を採る**（Lighthouse CI の複数実行機能を使う）。単発の実行結果でゲートしない。
- **CPU / ネットワークのスロットリング設定を固定する**（実行環境差を減らす）。
- **Accessibility と Performance を同じ厳しさで扱わない**。Accessibility は決定論的なので満点ゲート、Performance は 90 のゲートに留め、**しきい値近傍の揺れで頻繁に落ちるようなら warn 運用へ落とす**（この判断基準もあらかじめ要件に書いておく）。
- **静的指標（バンドルサイズ）を併用する**。Lighthouse の揺れに左右されない補助指標として、初期 JS バンドルサイズの推移を CI で可視化する（ゲート化は必須としない）。
- ⚠️ **サーキットブレーカー**: Lighthouse ゲートが原因で実装が 2 回以上ブロックされたら、ゲートの設定自体を見直す（ゲートを守ることが目的化しないように、この撤退条件を要件に明記する）。

### Q-12 の決定から従属的に確定する事項

- ルート `README.md` と `docs/README.md` の呼称を `gem-hunter` に統一する（要件定義書の執筆と同じ PR で行う）。
- **`docs/00_concept/initial-concept.md` は編集しない**。`docs/README.md` の運用ルール（完了したドキュメントは削除せず残す）に従い、初期コンセプトの原本として保存する。呼称が変わった事実は **要件定義書側で「旧称: IndieGems」と 1 行明記** して橋渡しする。
- OAuth App 名（`D-6`）・デプロイ時のドメイン候補も `gem-hunter` を基準にする。

### D-10 の決定から従属的に確定する事項

- **エコシステムは設定値として扱う**（コードに npm / PyPI を直書きしない）。追加が「対象リストへの追記 + バッチ再実行」で済む構造にする。
- 🔴 **名前解決（リポジトリ URL ↔ パッケージ名）が Phase 2 の中核課題**。1 リポジトリが複数パッケージを公開する（モノレポ）、パッケージのリポジトリ URL が未設定・古い、といったケースの扱いを構想段階で明記する。**ここを甘く見ると Gem Index の分母が壊れる**。
- **エコシステムをまたいだ順位の比較可否を決める必要がある**。npm と PyPI では被依存数の絶対値のスケールが異なるため、**順位はエコシステム内で正規化してから統合する**（`Q-3` の正規化方法と直結）。
- 静的データ配信案（`D-5` 追補）の実現可能性は **上位何件を配信するかで決まる**。件数は Phase 2 着手時に、実データのサイズを測ってから決定する。

### Q-3 の決定から従属的に確定する事項

- 🔵 **スコアをそのまま UI に出さず、パーセンタイルを言葉で提示する**。「Gem Score 73 点」より **「被依存数では上位 5%、star では下位 40%」** の方が、根拠が読み手に伝わる。パーセンタイル方式を採った利点をそのまま UI に活かす（`D-3` の説明可能性と整合）。
- **`D-10` のスケール差問題が同時に解ける**。パーセンタイル化はエコシステム内で閉じるため、npm と PyPI の絶対値の違いが指標に混入しない。
- **母集団の定義を要件に明記する**（パーセンタイルは母集団が変われば値が変わるため）。「どの範囲のリポジトリを 100% とするか」を決めずにパーセンタイルを出すと、数値の意味が定義できない。
- 🔴 **算出方法は確定済み・残るのは 5 点のみ（2026-08-20・[ADR 0009](../adr/0009-hidden-gem-score-definition.md) §2.3）**: パーセンタイル順位の差という算出方法自体は確定しており再議論しない。実データ待ちで残っているのは ① 母集団定義 ② しきい値・足切り基準の具体値 ③ 配信件数 ④ 対象エコシステムの最終確定（`D-10`）⑤ 名前解決（リポジトリ URL ↔ パッケージ名）の 5 点のみ。要件定義書には **「実データで決定する」と明記** し、決めたつもりにしない。
- **偽 star 耐性の根拠を書き添える**: star のパーセンタイルが上がると Gem Index は下がるため、star の水増しはランキングから外れる方向に働く（`competitive-analysis.md` の偽 star 問題への構造的な回答）。

### Q-4 の決定から従属的に確定する事項

- ⚠️ **差別化軸は Gem Index 1 本に集約される**。topics 単体は GitHub 純正検索の `topic:` 修飾子で代替できるため、それ自体は差別化にならない（`R-2` と同じ構図）。**価値は「topics で絞る × Gem Index で並べる」の組み合わせにあり、この点を要件定義書に明記する**（純正検索は Gem Index を持たないため、この組み合わせは再現できない）。
- 🔵 **topics は追加コストなしで使える**。GitHub の検索・詳細レスポンスに既に含まれるため、追加リクエストが発生しない。**Phase 1 の一覧カード・詳細ページに表示する案は `P1-積み上げ` の有力候補**（`D-9`）。
- **表記揺れの正規化は最小限に留める**（`cli` / `command-line` / `command-line-tool` 等）。シノニム辞書は人手メンテになるため、**上位の頻出タグに限って手当てし、網羅を目指さない**。網羅しようとした時点で「人手キュレーション」に戻ってしまう。
- 🔴 **スコープ外を明記する**: `initial-concept.md` の「#決済 #CLI軽量化」のような **独自語彙・日本語の目的タグは提供しない**。コンセプトからの意図的な後退であり、その理由（自前タグ体系の維持コストが運用継続と両立しない）をインセプションデッキの「やらないことリスト」に記録する。
- **topics が付いていないリポジトリの扱い** を要件に含める（付与率は高くない）。タグで絞り込めない対象が一定数残ることを前提に、キーワード検索との併用を保証する。

### Q-5 の決定から従属的に確定する事項（コンセプト機能の再配置）

`D-3` で収益化しないと決めたため、「無料 / Pro」という区分自体が消える。`initial-concept.md` の Pro 機能は以下へ再配置する。

| コンセプトの機能 | 再配置先 | 理由 |
|---|---|---|
| お気に入り・プライベートリスト | **`P1-積み上げ`** | `localStorage` で DB なしに実現できる（`D-5` 追補）。与件では対象外だが、上乗せ要件として無理なく足せる |
| 詳細なリポジトリ分析レポート | **Phase 2 に吸収** | Gem Index の内訳表示（パーセンタイル提示・`Q-3`）がこれに相当する。独立機能として持たない |
| カスタムアラート（Discord / Slack 通知） | **`P2` かつ条件付き** | 通知には常駐処理か外部サービスが要り、**DB レス原則（`D-5` 追補）と正面から衝突する**。原則を崩す判断をした場合にのみ着手する |
| 日次ダイジェスト（旧称: 週刊ダイジェスト） | 🔵 **実装対象**（`D-27`・2026-08-20 により `P2` 条件付きから格上げ） | 当時の懸念（配信先の管理に永続化が要る）は、RSS（Pull 型・購読者リストをサーバーに持たない）を採用したことで解消された。更新粒度は週次ではなく **日次**（`D-27` で確定）。トップページの静的な日次ダイジェストとして実装する（[ADR 0014](../adr/0014-zero-query-daily-digest.md) §2.1 / §2.5） |
| Pro サブスクリプション（課金） | **スコープ外** | `D-3` により目的としない |

### Q-6 の決定から従属的に確定する事項（GitHub API リサーチの適用範囲）

| 区分 | 該当する内容 |
|---|---|
| **Phase 1 で採用** | REST を基本とする / Octokit / Pagination / Rate Limit（一次・二次）/ ETag・条件付きリクエスト / Retry 戦略 / API バージョンの明示 / Secret 管理・最小権限 |
| **Phase 2 で採用** | GraphQL の併用（複数リポジトリのバッチ取得で効く）/ Cache 設計の作り込み |
| **不採用** | GitHub App / Webhooks / Idempotency / Queue / Mutation の作法 / AI Code Review アーキテクチャ |

- **不採用の理由を 1 行で書く**: 本プロダクトは **他人の公開リポジトリを読むだけ** で、監視対象リポジトリもイベント購読関係も存在しない。したがって同リサーチの中心命題「GitHub をイベントソースとするアプリを作れ」は、**本プロダクトの形には当てはまらない**。
- この表は `nextjs16-architecture-research.md` が持つ「本プロジェクトへの適用メモ」と同じ役割を果たす（GitHub API 側には無かったもの）。

### Q-9 の決定から従属的に確定する事項（エラー判別の仕様）

| 判別条件 | 種別 | 利用者への提示 |
|---|---|---|
| fetch 自体が失敗 | ネットワーク到達不可 | 「接続できません」+ 再試行 |
| 403 / 429 かつ `x-ratelimit-remaining: 0` | 一次レート制限 | `x-ratelimit-reset` から復帰時刻を算出して提示 + **ログインで緩和できる案内**（`D-6`） |
| 403 / 429 かつ `retry-after` あり | 二次レート制限 | `retry-after` 秒後に再試行可能と提示 |
| 401 / 403（上記以外） | 認証・権限の問題 | **内部情報を出さず汎用エラー** として扱う（サーバー設定の問題であり利用者は対処できない） |
| 422 | 検索クエリが不正 | 入力の修正を促す |
| 404（詳細ページ） | 対象なし | Not Found 表示（与件 §3.2） |
| 5xx | GitHub 側の障害 | 再試行を提示 |

- 🔵 **レート制限エラーがログイン導線になる**。`D-6`（任意 OAuth）の存在価値が、エラー体験の中で自然に伝わる設計にする。
- 与件 §3.2 の「通信失敗・API エラー・レート制限超過を区別して伝える」は、この表で機械的に判定可能になる。

### Q-10 の決定から従属的に確定する事項

- 🔴 **技術的に単独取得が必須である** ことが確認できた: 与件 FR-4 が求める「Watcher 数」の実体は **`subscribers_count`** であり、**これは検索結果のレスポンスには含まれず `GET /repos/{owner}/{repo}` でのみ得られる**（GitHub 公式ドキュメントで確認済み・2026-08-17）。`watchers_count` / `watchers` は star 数のミラーであり Watcher 数ではない。
- したがって **「検索結果のデータを引き回して詳細を描画する」案は成立しない**。FR-5（URL 直接アクセス可能な独立ページ）とも整合し、経路によって表示内容が変わる問題も起きない。
- **レート影響は軽微**: 詳細取得は Core API（GitHub App の実測で 8,800 req/h・`D-20`）で、律速の検索枠（30 req/分）とは別枠。加えて **ETag が効く** ため、304 が返る限り枠を消費しない（実測確認済み）。
- 🔴 **`{owner}/{repo}` は検索 API が返した正規の `full_name` をそのまま使う**（自前で組み立てたり、利用者の入力・古いキャッシュの値を使わない）。改名されたリポジトリを旧名で叩くと **301 リダイレクトが枠を 1 消費し、ETag も効かなくなる**（2026-08-18 実測・`D-20`）。詳細ページを URL 直接アクセスで開いた場合（`FR-5`）に旧名が渡りうるため、**301 を受けたら正規名へ寄せて以降のキャッシュキーを揃える**。
- **要件定義書には `subscribers_count` を使う旨を明記する**。与件が「Watcher 数」としか書いていないため、**どちらを指すかの解釈を残さないと実装者が誤る**（`watchers_count` を使うと star と同じ数字が 2 つ並ぶ）。
