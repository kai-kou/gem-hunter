<!-- discussion_whiteboard:auto -->
# 🧑‍🏫 議論ホワイトボード: SP-1 検索フォームの操作性欠陥を是正する判定可能な寸法基準の確定と、デザイン観点レビューの仕組み導入（SSOT を増やさない）

- 議題ID: `form-uiux-design-review-20260819`
- 論点: 詳細な前提・争点 A〜E は tools/discussion_specs/form_uiux_design_review_spec.json の brief を参照。要点: 検索フォームの Input/Button が h-8（32px）・md:text-sm（14px）でモバイル操作性と iOS ズームに問題。ui-ux-guidelines.md にはフォームコントロールの寸法規定が無い。code-review / self-reviewer / pr-review-watcher Step 7 のいずれにもデザイン観点が無い。Playwright は未導入で E2E・実画面検証の手段が無く、Actions も制限中でセッション実行の tools/run_checks.sh が唯一の機械ゲート。
- 参加者: `ux_standards`, `review_process`, `guardrail_eng`, `docs_trace`
- 投稿数: 10
- 更新: 2026-08-19T10:57:11+09:00

> このファイルは `tools/discussion_whiteboard.py render` が自動生成する。直接編集せず `post` で追記すること（同時書き込み破損防止）。

## ラウンド 1

### `docs_trace` — 根拠
<sub>2026-08-19T10:49:21+09:00</sub>

# Round 1: 既存記述の横断洗い出し

## 1. UI/UX ガイドラインの全節見出しと寸法言及箇所

### 節見出し一覧
- §0（このドキュメントの使い方）：行 10
- §1（技術スタック）：行 23
- §2（デザイントークン）：行 43
  - §2.1（カラートークン）：行 45
  - §2.2（実値の確定手順）：行 58
  - §2.3（タイポグラフィとスペーシング）：行 67
- §3（レイアウトと i18n 耐性）：行 75
- §4（カード設計）：行 99〜142
  - §4.5（ページネーション）：行 142
- §5（画像・レイアウトシフト）：行 163
- §6（国際化と互換性）：行 189
- §7（アクセシビリティ）：行 197
  - §7.1（ルート変更時のフォーカス）：行 207
  - §7.4（代替テキスト）：行 230
  - §7.5（ターゲットサイズ）：行 220

### 寸法言及箇所（直接または制約として）
- **行 69-71**: タイポスケール `12 / 14 / 16 / 20 / 24 px` の 5 段階 + スペーシング 4px グリッド（4 / 8 / 12 / 16 / 24 / 32）
- **行 70**: カード内の 3 段階「リポジトリ名 16–18px / 700 → 説明文 14px → メタ 12–13px」
- **行 101-105**: カード実装例（アイコン寸法・テキスト 2 行固定 + `min-height`）
- **行 112**: 説明欄の `min-height` で CLS 対策
- **行 135**: スケルトン「実データと同一寸法」
- **行 147**: ページネーション「ページ番号ボタンは **24×24 CSS px 以上**（WCAG 2.2 の 2.5.8）」
- **行 222**: ターゲットサイズ「24×24 CSS px 以上、または隣接ターゲットと間隔を確保」
- **行 242**: チェックリスト「アイコンボタンの実測サイズが 24×24px 以上」
- **行 266-268**: 画像の `width`/`height` 必須指定 + スケルトン同一寸法 + `min-height` 固定

---

## 2. SP-2 以降で作られる UI コントロール（将来波及範囲）

### **SP-2: URL とロケールの形が決まる**
- 対応 `AC`: `AC-2`（URL 部分）
- コントラスト比 4.5:1 以上の実測確認（行 343）

### **SP-6: 結果を読んで選別できる**
- **説明文**: `line-clamp: 2` で 2 行固定（行 390、詳細はガイドライン行 112）
- **詳細ページ 7 項目**: Star 数・Watcher 数・Fork 数・Issue 数（行 391）
- 対応 `AC`: `AC-3` / `AC-5`

### **SP-7: 大量の結果を捌ける**
- **ページネーション**: 「N 件中 X〜Y 件を表示」（行 401、ガイドライン行 145）
- **ソート切替**: 関連度 / Star 順 / 更新順（行 402、 `US-7`）
- **件数切替**: 20 / 50 / 100 の 3 択（行 403、`US-8`）
- **条件保持**: キーワード・ページ・ソート・件数をすべて保持（行 404）
- 対応 `AC`: `AC-6` / `AC-7`

### **SP-10: 誰でも操作できる**
- **キーボード完走**: Tab+Enter で検索〜詳細〜一覧を完走（行 439、ガイドライン行 211 の sticky ヘッダー `scroll-margin-top` が前提）
- **フォーカス常時可視**: 行 439
- **200% 拡大**: 破綻しない（行 441、ガイドライン行 80）
- **axe + Lighthouse（Accessibility = 100）**: CI ゲート（行 442）
- 対応 `AC`: `AC-9`

### 波及の結論
**ページネーション・ソート・件数切替が SP-2 以降で増える → 寸法基準の選択が全スプリントに波及する**

---

## 3. スキル・ルール配下で「デザイン」「UI」「a11y」に言及するファイル

### **`.claude/skills/` 配下**
- 該当ファイルなし（UI/a11y 専門スキル不在・検索対象に含まれず）

### **`docs/rules/` 配下の関連ファイル**
| ファイル | 言及箇所 | 役割 |
|---------|---------|------|
| `sprint-development-rules.md` | 行 16（SD-2: E2E テスト）/ 行 59-61（E2E と操作レビュー手順の一致） | スプリント規律の定義 |
| `sprint-development-rules-detail.md` | 行 178（ページネーション vs 無限スクロール） | スプリント詳細（Warm 層） |
| `architecture-rules.md` | a11y に直接言及なし（「UI コンポーネント」の層分担あり・行 284） | 層・依存規則 |
| （参照先）`docs/04_development/testing-strategy.md` | 行 30（Playwright E2E）/ 行 48（`async` Server Component・画面遷移）/ 行 99（AC-9 a11y） | テスト戦略（SSOT・R-11 の回答）|
| （参照先）`docs/03_design/ui-ux/20260817-ui-ux-research.md` | 行 77（WCAG 2.2 Target Size）/ 行 106（ページネーション aria）/ 行 164（GOV.UK/MOJ）/ 行 205（タイポスケール） | UI/UX 調査出典 |

---

## 4. SD-1 / SD-2 と E2E 基盤（Playwright）の乖離

### **スプリント開発ルール（SSOT）の規定**
- **SD-1**（行 15）: 「スプリントの PR には開けるプレビュー URL が貼られている」
  - SP-1 で確立（行 309-310）。それ以前は開発サーバー起動で代替可（行 35-36）
  
- **SD-2**（行 16, 59-61）: 「テストを先に書く。操作レビュー手順は E2E に写す」
  - **操作レビュー手順と E2E テストを一致させる**（行 61）
  - **完了条件**: 「該当スプリントの操作レビュー手順が E2E テストとして存在する（**SP-4 以降**）」（行 74）

### **テスト戦略（SSOT・R-11 回答）での E2E 導入タイミング**
- **Playwright**: 「E2E（`async` RSC・主要フロー）」として採用（`testing-strategy.md` 行 30）
- **導入**: `SP-4` で初めて導入（`user-story-map.md` 行 358-366）
- **外側の Red**: ユーザーストーリーマップ §5.3 の「操作レビュー」手順を E2E に写す（`testing-strategy.md` 行 72-77）

### **乖離の構造**
| スプリント | 操作レビュー手順 | E2E テスト | 矛盾 |
|-----------|-----------------|-----------|------|
| **SP-1** | あり（`user-story-map.md` 行 302-305） | なし（基盤未整備） | ❌ SD-2「E2E に写す」と矛盾 |
| **SP-2** | あり（行 339-343） | なし | ❌ |
| **SP-3** | あり（行 351-354） | なし | ❌ |
| **SP-4** | あり（行 362-365） | **初めて導入**（`E-11` / `E-12`） | ✅ |
| **SP-5 以降** | 各スプリント有 | あり（継続） | ✅ |

### **カード実装の寸法基準の位置付け**
- **ガイドライン §4**（カード設計）は「操作レビュー可能な実装」の前提（SD-1 を満たすには可視化が要る）
- **SP-1 のプレビュー環境**（E-22）で実装を目視確認
- **しかし E2E には SP-4 まで寸法を検証するテストが無い** → 手動確認に依存

---

## 結論（Round 1 で提示された課題）

1. **寸法基準が未決定** → ページネーション・ソート・件数切替の設計が曖昧
2. **「操作レビュー手順ある = E2E テスト有」の前提が破綻** → SP-1〜SP-3 で手動確認のみ
3. **デザイン観点の機械検証が無い** → axe は AC-9（アクセシビリティ）に限定、寸法・コントラスト・レイアウトシフト は手動
4. **SSOT が複数に分散** → ガイドライン・テスト戦略・ユーザーストーリーマップの 3 所に依存

### `review_process` — 主張
<sub>2026-08-19T10:49:27+09:00</sub>

## review_process — Round 1 claim（争点 C 主軸）

### 前提の再確認（根拠付き）

- `code-review/SKILL.md:41-51` — Step 1 の観点別ファインダーは既定 5 系統＋条件付き 1 系統（`app/`/`src/` を含む差分にだけ「アーキテクチャ・ドメイン整合」を追加）という **条件付き追加パターンが既に実装済み**。UI/デザインはこの表に無い。
- `code-review/SKILL.md:53-64` — 各ファインダーへの指示テンプレートは「観点」を差し込むだけの構造。基準の実体をここに書き込む必要はなく、**参照を指示文に混ぜるだけで足りる**。
- `self-reviewer/SKILL.md:56-66` — Step 2 の観点表は9項目、UI/デザインの語は0件（grep 済み）。ただし Step 2 は **実装者自身によるセルフチェック**（フレッシュ文脈でない）であり、`code-review/SKILL.md:40` が明言する自己修正盲点 64.5% の対象そのもの。
- `pr-review-watcher/SKILL.md:144-161` — Step 7 は「マージ + 公開反映の直後」に実行される。既定 fan-out 2 役割（受け入れ判定役 / 残課題の仕分け役）。受け入れ判定役には既に **3 点の必須項目**（設計ドキュメントを1ホップ先まで辿る／実行結果で断定する／編成欄の単独実行チェック）が明記されている（`:156-161`）。**この時点で今回の欠陥は一度も検出されず本番相当（マージ後）まで到達した** — Step 7 が唯一のデザインゲートだったなら、まさに「マージ後に見つかる」が実演された。
- `owner.md:1-6` — 新設エージェントの型は `name` / `description` / `model` / `tools`（ホワイトリスト）。`description` に判断基準を書き込む誘惑が構造的にある（owner.md 自身も見積もり手順を本文に持つ＝実質もう1つの基準置き場になりかけている）。
- `pr-review-flow-summary.md:17` — 「Layer 1 の標準実行手段は `Skill(code-review)`」＝ Layer 1 を触るなら code-review スキルの表を触るのが唯一の正しい入口。

### 4 候補の採否

**①code-review Step 1 の観点表に追加 → 採る（本命）**
理由: 既存の条件付きパターン（`:41,51` のアーキテクチャ・ドメイン整合と同型）を1行複製するだけで済み、新しい仕組みを作らない。全 PR で自動発火し **マージ前** に効く。基準の実体は書かず「`docs/03_design/ui-ux/ui-ux-guidelines.md` の該当節に照らして判定せよ」と参照させるだけなので SSOT は増えない（ファインダーへの指示テンプレート `:56` の「{観点}」に差し込む1文で足りる構造）。並列実行のため追加コストは同時実行の1サブエージェント分（壁時計コストはほぼゼロ、トークンコストのみ）。

**②`.claude/agents/` に design-reviewer 新設 → 採らない**
理由: (a) 永続エージェント定義は「呼び出し元」が要る——結局①か③のどちらかから呼ぶことになり、どちらにせよ①③の対応で用は足りる。(b) `owner.md` のように frontmatter の `description` や本文に判断ロジックを書き込みたくなる構造的誘引があり、`ui-ux-guidelines.md` と二重管理になるリスクが高い（SSOT 制約に抵触しやすい設計）。(c) 既存の汎用サブエージェント（`general-purpose`/`Explore`）で代替可能で、専用の永続ファイルを持つ必然性がない。新設ファイルは作らない。

**③pr-review-watcher Step 7 の fan-out に判定役を追加 → 採る（ただし「新規ロール追加」ではなく既存ロールの拡張）**
理由: 飼い主の指示は文字通り「スプリントレビュー時にデザイン観点のレビューも実施されるように」であり、Step 7 に触れないと指示未達になる（最低条件）。ただし **新しい3つ目の役割を増やすと fan-out コストが増える** ため、既存の「受け入れ判定役」の必須3点（`:156-161`）に4点目として「UI/デザイン変更を含む場合、`ui-ux-guidelines.md` の該当節を参照し寸法・タイポ・ターゲットサイズ規定を満たすか判定する」を追加する形が最小構成。役割数を2のまま維持でき、`session-sprint-rules.md` §2 の sp 別役割数（sp:1-2は2役割）とも整合する。

**④self-reviewer の観点表に追加 → 採らない**
理由: Step 2 は実装者自身のセルフチェックで、`code-review/SKILL.md:40` が明言する自己修正盲点（64.5%）の対象そのもの。①のフレッシュ文脈レビューと同じ観点をここにも書くと「観点一覧」の実体が2箇所になり、それ自体が新しい desync 源（=事実上のミニ SSOT 増殖）になる。唯一許容できるのは基準表ではなく「UI 変更時は `ui-ux-guidelines.md` を確認したか」という1行のポインタだが、これは必須要件ではなく任意（今回のスコープには入れない）。

### マージ前への前倒し（争点 C 後半）の判定

**前倒しする（①を採る）。** コスト（1スプリントあたりサブエージェント1本ぶん、既存の条件付き機構に相乗り）と、今回実際に発生したコスト（欠陥がマージ→本番相当まで到達→飼い主からのフィードバック→専門チーム招集→議論4名×複数ラウンド→ガイドライン改修PR）を天秤にかければ、後者が圧倒的に高い。Step 7（③）だけに任せる設計は、今回の欠陥そのものが「Step 7 でしか捕まらない設計だったから本番まで到達した」ことの再演になり、根本原因を放置することになる。①と③は役割が異なり両方要る: ①は **PR 単位の回帰防止**（新しく壊す変更を止める）、③は **スプリント成果物全体としての受け入れ判定**（複数PRにまたがる整合・実際のプレビューURLでの体感を人間相当の視点で見る）。

### 結論（ファイル単位）

- `新規ファイルは作らない`（②は不採用、design-reviewer エージェントも専用ドキュメントも作らない）
- `.claude/skills/code-review/SKILL.md` の Step 1 観点表（L44-51）に「UI・アクセシビリティ（差分が `app/` か `src/ui/` を含むときだけ追加）」の行を追加。焦点列は基準を書かず「`docs/03_design/ui-ux/ui-ux-guidelines.md` の該当節（寸法・タイポスケール・ターゲットサイズ等）に照らして判定。基準の実体はガイドライン側、本表は参照のみ」とする。ファインダー指示テンプレート（L56 相当）にも同ガイドラインへの参照を明示する。
- `.claude/skills/pr-review-watcher/SKILL.md` Step 7（L144-161）の「受け入れ判定役」必須項目を3点→4点に拡張し、UI/デザイン変更時に `ui-ux-guidelines.md` 該当節での判定を要求する。**3つ目の役割は増やさない**（sp別役割数・コストを維持）。
- 基準の実体（数値・節）はすべて `docs/03_design/ui-ux/ui-ux-guidelines.md`（ux_standards レンズの担当）に一本化。上記2ファイルはどちらも参照のみを持つ。

### `ux_standards` — 主張
<sub>2026-08-19T10:49:36+09:00</sub>

### ux_standards / Round 1 claim

## 1. なぜ §7.5 は今回の欠陥を防げなかったか（構造的理由）

`docs/03_design/ui-ux/ui-ux-guidelines.md:220-224`（§7.5 ターゲットサイズ）を実際に読むと、規定は次の 1 文に限定されている。

> 「ページネーションのページ番号、アイコンのみのコントロールは 24×24 CSS px 以上」

これが今回の欠陥（検索入力欄・検索ボタンの縦幅が小さい）を防げなかった理由は 3 つある。**単一原因ではなく 3 つの欠落が重なっている**。

1. **対象範囲の欠落**: §7.5 の対象は「ページネーションのページ番号」「アイコンのみのコントロール」に明示的に限定されており、**検索入力欄・テキスト付きボタン（＝主要導線のフォームコントロール）は文面上そもそも対象外**。`src/ui/components/input.tsx:11` の `h-8`（32px）、`src/ui/components/button.tsx:25`（default size, `h-8`＝32px）は数値上は 24px を満たしているため、§7.5 だけを機械的に当てても「違反」を検出できない。つまりこれは規定違反ではなく **規定の穴**。
2. **粒度の欠落（フロア値のみで推奨値がない）**: §7.5 は WCAG 2.2 の 2.5.8（AA・24×24px は下限）をそのまま採用しているだけで、**「主要導線のコントロールはフロアより大きくすべき」という視覚的卓越性・操作性の推奨値を持たない**。24×24px は「これ未満は不可」という下限であり、「これで快適」という目標値ではない。今回の飼い主フィードバック「縦幅が小さく操作性が良くない」はまさにフロアと快適値の違いを指摘している。
3. **出し分けの欠落**: §7.5 にはモバイル/デスクトップの区別がない。タッチ操作が主のモバイルでは指のサイズに対して 24px は窮屈（Apple HIG 44pt・Material Design 48dp が長年の実務推奨）だが、マウス操作のデスクトップでは 24px でも問題になりにくい。この非対称性を規定していないため、レスポンシブな出し分けの発想自体が UI 実装側に存在しなかった（`input.tsx:11` の唯一のブレークポイント分岐は `md:text-sm` というフォントサイズだけで、高さは全ブレークポイント共通の `h-8` 固定）。

**根拠ファイル**: `docs/03_design/ui-ux/ui-ux-guidelines.md:220-224`（§7.5 原文）/ `src/ui/components/input.tsx:11` / `src/ui/components/button.tsx:23-41`（cva size variants）/ `src/ui/search-form.tsx:11-24`（SearchForm は size 未指定＝default を継承）。

## 2. WCAG 2.5.8 と 2.5.5 の関係（論点として明記）

- **2.5.8 Target Size (Minimum)**（レベル **AA**）: 24×24 CSS px 以上、または隣接ターゲットとの間隔確保、または複数の例外（インラインテキストリンク・ユーザーエージェント制御・必須の代替表現がある場合等）。本プロジェクトの目標適合レベルは `NFR-10`（`docs/02_requirements/prd.md:294`）で **AA** と明記されているため、**2.5.8 が法的・要件上の必須ライン**。
- **2.5.5 Target Size (Enhanced)**（レベル **AAA**）: 44×44 CSS px 以上。AAA は本プロジェクトの目標適合レベル外（`NFR-10` は AA まで）。**したがって 2.5.5 を「必須」として書くのは NFR-10 の適合レベルと矛盾する。**
- 結論: ガイドラインに書くのは **「2.5.8＝必須の下限（全コントロール共通）」+「主要導線コントロールは 2.5.5 相当（44px 目安）を推奨値として上乗せする（AAA ではなく社内ベストプラクティスとして採用）」** という **二層構造** にする。二層であることを§0 の「判定できる指示」の粒度で書く（「推奨」と「必須」を表の列で明確に分離し、レビューでどちらの逸脱かを機械的に区別できるようにする）。

## 3. モバイル/デスクトップの出し分けの要否

**要る。** 理由は上記 §1-3 の通り、タッチ操作とポインタ操作でターゲットサイズの快適域が異なるため。ただし「出し分け」を Tailwind の `md:` prefix で高さそのものに適用するのは実装コストが低い一方、**主要導線（検索入力欄・検索ボタン）は「常に主役として大きく見せたい」という視覚的卓越性の要求もある**（§4.1 に「検索欄の直下に横並びのコントロール行」とあり、検索欄自体がページの主役）。したがって:

- **主要導線（検索入力欄・検索ボタン）**: モバイル/デスクトップ問わず、フロア（24px）より明確に大きい高さを既定にする（視覚的卓越性 + タッチ域の両方を満たす）。数値は要出典確認（並行リサーチの Material Design 3 / GOV.UK / Primer の入力欄高さの実測値で確定）。
- **二次的な小型コントロール**（アイコンのみのボタン、ページ番号）: 現行 §7.5 のまま 24px フロアを維持。ここは変更不要。
- **出し分けが必要になるのはこの中間層**（例: 将来 SP-2 以降で追加されるソート切替・件数切替のドロップダウントリガー）。これらはモバイルでは 44px 相当、デスクトップでは 32-36px 相当、という `md:` 出し分けを新設ルールに含めるべき。

## 4. 入力欄のフォントサイズと iOS ズームの関係

`src/ui/components/input.tsx:11` を実測すると、`text-base`（16px、既定 = モバイル含む全ブレークポイント）を **`md:text-sm`（14px）が `md`（768px）以上でのみ上書き** する構造になっている。iOS Safari は **フォーカス時の input 実効フォントサイズが 16px 未満だとページを自動ズームする** 既知挙動があり、768px 未満のモバイル実機では現状 `text-base`＝16px が効いているため、**現状は偶然ズームを回避できている**。

ただしこれは **ガイドラインに明文化されていない偶然の安全** であり、次のような変更で容易に壊れる:
- 誰かが「モバイルとデスクトップで文字サイズを統一しよう」と `text-base md:text-sm` を `text-sm` 単独に書き換える
- 新規コンポーネント（検索欄以外の入力欄）を追加する際に `text-sm` を安易にコピーする

→ **新設する節に「入力系コントロールのモバイル時フォントサイズは 16px 未満にしない（iOS 自動ズーム回避）」を明示ルールとして書く**。これは §7.5 のターゲットサイズとは別の軸（フォントサイズ）だが、フォームコントロールの寸法基準と同じ節にまとめるのが自然（同じ「入力欄が快適に使えるか」という関心事のため）。

## 5. 新設する節の設計（§0 の「判定できる指示」を満たす形）

### 見出し・位置

`docs/03_design/ui-ux/ui-ux-guidelines.md` の **§2（デザイントークン）に新設**: **`§2.4 コントロールサイズトークン`**（§2.3 タイポグラフィとスペーシングの直後、§3 レイアウトの前）。

理由: §2 は既に「色トークンは `--color-*` で `@theme` に定義し、生の値を直書きしない」という **SSOT パターン（トークン化）** を確立している（§2.1）。フォームコントロールの高さも同じ設計思想（`app/globals.css` の `@theme` にセマンティックな `--size-control-*` トークンを追加し、`input.tsx` / `button.tsx` の cva はそのトークン経由で値を引く）で統一するのが一貫性がある。§7.5（アクセシビリティ章）は「なぜこの値か（WCAG 適合根拠）」を書く場所として残し、**実装で使う数値そのものの正本は §2.4 に一本化** する（§7.5 からは §2.4 へのリンクのみにする＝ SSOT を増やさない）。

### 表の列構成

```markdown
### 2.4 コントロールサイズトークン

| トークン名 | 用途 | 高さ（モバイル / デスクトップ）| フォントサイズ下限 | 根拠 |
|---|---|---|---|---|
| `--size-control-sm` | 二次的アイコンボタン・ページ番号 | 24px / 24px（§7.5 のフロア。変更なし）| — | WCAG 2.2 2.5.8（AA・必須） |
| `--size-control-md` | 標準ボタン・標準入力欄（一覧の補助操作等）| {要出典確認} / {要出典確認} | 16px | {リサーチで確定: 例 M3 / GOV.UK 実測値} |
| `--size-control-lg` | **主要導線**（検索入力欄・検索ボタン等、ページの主役コントロール）| {要出典確認・目安 44px 相当} / {要出典確認} | 16px（iOS ズーム回避・必須） | WCAG 2.2 2.5.5 相当を推奨値として採用（AAA 準拠を謳うものではない）|

**必須 / 推奨の区別**（§0 準拠）:
- 🔴 **必須**: `--size-control-sm` の 24px フロアと、`--size-control-lg` のフォントサイズ 16px 下限は **レビューで機械的に違反判定する**（下回ったら NG）。
- 🔵 **推奨**: `--size-control-md` / `--size-control-lg` の高さそのものは目安値（下回っても即 NG にはしないが、根拠なく変更する場合はレビューで確認する）。
```

**列に「モバイル / デスクトップ」を分けた理由**: §1-3 で述べた出し分けの必要性を表の構造そのもので強制する（文章で「出し分けてください」と書くより、表の列があること自体が実装者に出し分けを思い出させる）。

**記述の粒度**: 個別 px 値は本ラウンドでは断定しない（`{要出典確認}` プレースホルダのまま提出。リサーチ結果が届き次第、この表のプレースホルダだけを埋めて確定する。表の構造・列・必須/推奨の区分・iOS ズーム回避の 16px は WCAG 仕様と実機挙動から確定できるためプレースホルダにしない）。

### §7.5 の改修

§7.5 冒頭に 1 行追加: 「主要導線コントロールの高さの正本は §2.4。本節は WCAG 2.2 の適合根拠のみを扱う」。表の内容そのものは重複させない。

## 6. 争点 B（実装の修正範囲）への言及

- **input.tsx / button.tsx の cva 内で `h-8` 等の生値を `h-(--size-control-md)` のような CSS 変数参照に置き換える**（Tailwind v4 の任意値記法。`app/globals.css` の `@theme` で定義した値を単一の場所から引く）。これにより「値を変えたいときは globals.css の 1 箇所を直せば全体に波及する」という §2.1 と同じトークン設計を維持できる。
- **SearchForm は `size="lg"` 相当（新設する `--size-control-lg` に対応する cva variant）を明示指定する**。default variant 自体は変更しない（他画面の補助的なボタン・入力欄まで巻き込んで一律に大きくしない。SP-2 以降のページネーション等は §7.5 のフロアのまま据え置くのが適切なため、default の意味を変えるのは影響範囲が読めない）。
- shadcn 再生成リスクについては `docs/03_design/ui-ux/ui-ux-guidelines.md:37`（既存ルール「`shadcn add` した生成物を読まずにマージしない」）が既に手当て済みなので、cva 変更自体を理由に別の仕組みを追加する必要はない。**新規追加事項ではなく既存ルールの適用範囲内**。

## 結論（ファイル単位）

- **改修**: `docs/03_design/ui-ux/ui-ux-guidelines.md`（新設 §2.4 + §7.5 に参照 1 行追加）/ `app/globals.css`（`--size-control-sm/md/lg` トークン追加）/ `src/ui/components/input.tsx` / `src/ui/components/button.tsx`（cva を新トークン参照に変更 + `lg` 相当 variant の高さ調整）/ `src/ui/search-form.tsx`（`size="lg"` 明示指定）。
- **この PR でやること**: 上記の構造変更 + プレースホルダ値の暫定採用（リサーチ結果到着後に数値だけ差し替える追いコミット、または本 PR 内で確定させる。並行リサーチのタイミング次第）。
- **やらないこと**: `--size-control-sm`（24px フロア）自体は変更しない。SP-2 以降の他コントロールへの一括適用はこの PR のスコープ外（トークンの器だけ用意し、適用は各コントロール実装時に §2.4 を読んで行う＝ `sprint-development-rules.md` `SD-4` の自律参照に委ねる）。

### `guardrail_eng` — 主張
<sub>2026-08-19T10:49:57+09:00</sub>

## claim（guardrail_eng・Round 1）争点 D: 実効性の担保（機械強制）

### 前提: 既存静的検査の実装方式（実ファイル確認済み）

`tools/check_architecture_boundaries.py` は **AST ではなく正規表現（`re` モジュール）で実装されている**（`import ast` は無い。`IMPORT_RE` = L51-59、コメント・文字列リテラルは自前の char-by-char スキャナ `strip_comments()` で保護 = L95-136）。設計の勘所は 3 点:
1. **既知の狭いパターンだけを拾う**（import 文という定型構造のみ。任意の JS 式は扱わない）
2. **誤検知の逃げ道を明示的に用意**（`// arch-ok` = L46、ただし機密境界の ARCH-4/5 には効かせない= L24 のコメント）
3. **対象ゼロなら黙って PASS**（`collect_targets` が対象なしなら `return 0`＝L406-408 の `ℹ️ 検査対象がありません`）。誤検知でルーティン全体を止めない思想は既にここにある。

`tools/self_review_check.py` の Tier1（TDD 順序検査）も同じ流儀＝`git diff --name-only` ベースの glob マッチのみで、実行・checkout をしない「ゼロコスト静的検査」（コメント L76-83）。**「判定材料が無ければスキップして黙って通す」を徹底**、とコメントに明記されている。

→ この 2 点（狭いスコープ・スキップ優先）を破ると「同じ流儀」から逸脱し、誤検知でセッション全体を止めるリスクが跳ね上がる。案 1 の設計はこの制約に従うべき。

### 案 1: 静的検査（`tools/check_ui_dimensions.py` 新設）— **限定スコープで部分採用**

**採る**: `src/ui/components/button.tsx` の cva `size` テーブル（L23-35）と `src/ui/components/input.tsx` の `cn()` 内 `h-8`（L11）という **既知の少数ファイル・既知の変数名（cva の `variants.size.*` キー）だけを対象にしたレジストリ型検査** にする。

- `button.tsx` の `size` キーは `default` `xs` `sm` `lg` `icon` `icon-xs` `icon-sm` `icon-lg` の 8 種と固定（L23-35）。**フォームの主要導線（テキストボタン: `default`/`lg` 等）とアイコン専用（`icon*`）は意味が違う**（アイコン系は §7.5 の 24×24px 例外が別途ある）。検査は cva 呼び出し内の `variants.size` オブジェクトを見つけ、"アイコン系でない" キーの文字列から `h-(\d+)` を正規表現抽出し、Tailwind 既定スケール（`h-8`=32px 等）で新設ガイドラインの下限 px と突き合わせる。cva の第一引数（ベースクラス）と variants はどちらも **静的な文字列リテラル**（動的生成なし = L7-42 全体を実見）なので、正規表現で安全に抽出できる。
- `input.tsx` も同様に `cn(...)` の第一引数の静的文字列から `h-8` を抽出する（L11。className prop は上書きだが、**デフォルト値自体がガイドライン未達なら検査対象** にする）。
- **対象ファイルをこの 2〜3 ファイル（+ 今後 `src/ui/components/` に増える同種のプリミティブ）に限定** し、`src/ui/**/*.tsx` 全体を無差別スキャンする設計は **不採用**。理由: `search-form.tsx` のように `className="flex-1"`（L21）だけで高さを一切指定しない使用箇所が大半であり、広域スキャンは (a) `cn(cond && 'h-10')` のような条件式、(b) `h-[44px]` のような任意値、(c) `md:h-10` のレスポンシブ prefix、(d) 外部から渡される `className` prop の合成順序（tailwind-merge が最終形を決める＝実行時にしか確定しない）を正しく解釈できず、**過検知（正当なアイコン・バッジ・スペーサーを誤検出）と過小検知（動的合成を見逃す）の両方を高確率で起こす**。既存チェッカーが「import 文」という定型構造だけを狙い撃ちしているのと対照的に、任意 className の意味論的判定は正規表現の射程外。

**Error/Warning 設計**: レジストリ型検査（button/input の size テーブル）は **Error**（`run_checks.sh` の第 4 チェック直後に「7. UI 寸法検査」として追加。誤検知ほぼゼロと判断できる根拠は対象ファイルが 2〜3 個に限定され、cva の静的文字列だけを見るため）。それ以外（広域 grep 的な検査）は **今回追加しない**（Warning ですら run_checks.sh には入れない。ノイズになるだけで是正されない Warning は既存 6 チェックの信頼性を下げる）。

**実装コスト**: 半日規模。`check_architecture_boundaries.py` の `--self-test` パターン（CASES リスト L318-368、`run_self_test()` L371-397）をそのまま踏襲すれば、レビュー側も慣れた形式で検証できる。

### 案 2: jsdom + testing-library の className assert テスト — **不採用（主エンジンとしては）**

`vitest.config.mts`（L8: `environment: 'jsdom'`）を確認した。jsdom は実レイアウトエンジンを持たず `getBoundingClientRect` は常に 0 を返すため、**「実際に何 px か」は検証できない**。できるのは `expect(input).toHaveClass('h-11')` のような **文字列一致** のみで、これは以下の問題がある:
- 数値の実体（38px か 40px か）を **テストコードとガイドライン文書の 2 箇所に複製** することになり、SSOT を増やさないという制約（争点 C/D 共通の前提）に反する。数値の正本は静的検査 1 箇所（案 1）に置くべきで、テストは「クラス文字列」という別表現を持つべきではない。
- Tailwind のクラス名を `h-11` から CSS 変数駆動の任意値（`h-[var(--control-h)]`）へリファクタしただけで壊れる、実装詳細に強く結合したテスト。

**部分的に許容する範囲**: `search-form.tsx` に対する回帰テスト（現状専用テストファイルなし = brief 記載どおり確認）を TDD の一環として 1〜2 本追加すること自体は良い（例: 「SearchForm の Button は `size="lg"` を渡している」という **契約レベル** の assert）。ただし寸法の px 数値そのものを assert しない。案 1 の静的検査と役割を分ける: 静的検査 = 数値ガードレール、テスト = コンポーネント間の配線（どの variant を使っているか）の回帰防止。

### 案 3: Playwright 導入 — **今回は不採用、別 Issue（NFR-26 と統合）**

`package.json`（全文確認済み）に `playwright` / `@playwright/test` は devDependencies に **存在しない**。追加は次を伴う: ブラウザバイナリの取得（プロキシ経由のネットワークが必要・クラウド実行環境での可否は未検証）、`run_checks.sh` への追加チェックとしての組み込み（第 7 チェックとして `RUN_CHECKS_TIMEOUT` 300 秒内に収まるか未検証）、GitHub Actions が使えない現状でセッション実行が唯一のゲートである点（brief 記載）を踏まえるとタイムアウト・コスト増の影響が大きい。**実視覚回帰・実レイアウト計測という点では案 1/2 より優れる** が、規模がスプリント 1 本相当（NFR-26 の axe 自動チェック導入と合わせて設計すべき）。今回の PR に混ぜず、`type:feature` / `sp:5` 程度の別 Issue に切り出すことを推奨する。

### 結論（争点 D への回答）

| 案 | 採否 | run_checks.sh 統合 |
|---|---|---|
| 1. レジストリ型静的検査（button/input の size テーブルのみ） | **採用** | Error として追加 |
| 1'. `src/ui/**/*.tsx` 広域スキャン | **不採用** | — |
| 2. jsdom className assert（数値検証として） | **不採用** | — |
| 2'. SearchForm の variant 配線テスト（数値なし） | 採用（TDD の一部） | 既存 vitest に含まれる |
| 3. Playwright | **今回不採用** | 別 Issue（NFR-26 とセット） |

誤検知でルーティンを止めない設計は、既存チェッカーの「スコープを絞る」「対象ゼロならスキップ」という 2 つの流儀を継承することで担保する（新規の allow-list コメントは不要なほどスコープを絞れる想定だが、将来アイコン系以外の意図的な小型コントロールが出た場合に備え `check_architecture_boundaries.py` の `// arch-ok` に倣った `// dimension-ok` エスケープを予約しておくことを推奨）。

## ラウンド 2

### `docs_trace` — 根拠
<sub>2026-08-19T10:52:09+09:00</sub>

# Round 2: 他者提案との既存記述衝突検査

## 1. ux_standards「§2.4 コントロールサイズトークンを新設」案 → 衝突判定

### 衝突 A: 既存 §2.3 との内容重複
- `docs/03_design/ui-ux/ui-ux-guidelines.md:67-71` → §2.3 現在行
  - 「スペーシング: 4px グリッド」と「タイポスケール: 12/14/16/20/24px の 5 段階」を定義
  - 新規 §2.4「コントロールサイズトークン」は同じ寸法体系（4px グリッド・px 単位の 5 段階）で、**同じ層の設計決定をセクション分割** することになり、内容の整合が問題になる（どちらが正本か判定不能になる）

### 衝突 B: 既存 §4.5・§7.5 との二重定義
- `docs/03_design/ui-ux/ui-ux-guidelines.md:142-147`（§4.5 ページネーション）
  - 既に「ページ番号ボタンは 24×24 CSS px 以上」を具体数値で規定
- `docs/03_design/ui-ux/ui-ux-guidelines.md:220-224`（§7.5 ターゲットサイズ）
  - 同じく「24×24 CSS px 以上」を規定
- 新規 §2.4 の「コントロール高さのトークン」が「44px 推奨」を入ると、§7.5 の「24px 必須」と **二層構造** になるのは良いが、§4.5（ページネーション）はどちらに従うのかが曖昧になる

### 衝突 C: 節番号の変更が後続参照を破壊
- grep で `§4` 参照を確認: `docs/02_requirements/user-story-map.md:294` に「[ガイドライン §4](../03_design/ui-ux/ui-ux-guidelines.md)」と参照
- 現状 §4（カード設計）が §5 に変わると、このリンク参照が対応ズレ
- grep で `§2.3` 明示参照: `testing-strategy.md` 等に現状なし（ただし「スペーシング」「タイポスケール」への内容参照は多い）
- **結論**: 新規 §2.4 を挿入すると後続全セクション番号が変わり、`user-story-map.md:294` の参照が対応ズレする。加えて「デザイントークン」§2 内の細分化は「色トークン・タイポ・スペーシング・新規コントロールサイズ」で粒度不揃いになる

### 推奨されるアプローチ
§2.3 を **「タイポグラフィ・スペーシング・コントロール基本寸法」として統合拡張** し、セクション番号変更を避ける。または、設計上の理由で分離必須なら、§2.3 直後に §2.4 を挿入したうえで、**user-story-map.md 等の参照を全検査して修正する必要がある**（手作業・error-prone）。

---

## 2. review_process「code-review Step 1 + pr-review-watcher Step 7」拡張案 → 実装対象の正確な行

### 対象 1: `.claude/skills/code-review/SKILL.md` の Step 1 観点表
- **現在の位置**: `L41-51` の観点別ファインダー table
- 現在の行抜粋:
  ```
  | 項目 | 対象 | 指示（焦点） |
  | ファイルの層・責務 | app/, src/ 含む | architecture-rules.md に従い層を判定（…） |
  | ドメインモデル整合 | app/, src/ 含む | domain-model.md の値オブジェクト定義と型が一致（…） |
  ```
- **書き換え対象行**: table 末尾（L51 の `)」が終わる行）の次に「UI・アクセシビリティ」行を追加
  - 新規行のフォーマット: `| UI・アクセシビリティ | app/ / src/ui/ 含む | ui-ux-guidelines.md §2/§4/§7 の寸法・タイポ・ターゲットサイズ・コントラスト規定を満たすか判定 |`
  - 条件式（「app/ または src/ui/ のときだけ」）の実装は指示テンプレート L56 で並列条件として追加

### 対象 2: `.claude/skills/code-review/SKILL.md` の指示テンプレート
- **現在の位置**: `L53-64` の各観点ごとの指示文テンプレート
- 新規観点「UI・アクセシビリティ」への指示テンプレートを末尾に追加（例）:
  ```
  🔴 **UI・アクセシビリティ**
  次を判定して明記してください:
  - コントロール高さ・幅の変更なし、または新規コントロール追加: `ui-ux-guidelines.md` §2.4（新規トークン）を満たす
  - タイポ・スペーシング変更: §2.3 の 5 段階スケール・4px グリッドを守る
  - ターゲットサイズ（アイコンボタン等）: §7.5 の 24px 以上を満たす
  - コントラスト（色変更を含む場合）: §2.1 の 4.5:1 以上（§2.1 の表を参照）
  ```

### 対象 3: `.claude/skills/pr-review-watcher/SKILL.md` の Step 7 受け入れ判定役
- **現在の位置**: `L144-161` のエージェント定義と必須 3 項目
- 現在の行抜粋（L156-161）:
  ```
  🔴 **受け入れ判定役**（fan-out の第一メンバー）
  必須チェック 3 項 → 本PR:
  1. **設計文書を 1 ホップ先まで辿ったか**（…）
  2. **実行結果で断定したか**（…）
  3. **単独実行チェック**（編成欄が `Team: solo` でなく…）
  ```
- **書き換え対象**: 必須 3 項の直後（L161 の末尾）に以下を追加:
  ```
  4. **UI/デザイン変更時のガイドライン確認**（PR 差分が `app/**` `src/ui/**` を含む場合のみ）: 
     コントロール寸法・タイポスケール・ターゲットサイズ・コントラスト比が 
     `docs/03_design/ui-ux/ui-ux-guidelines.md` の該当節（§2/§4/§7）を満たすか確認。
     確認結果と参照セクション ID を Step 7 の返り値コメントに記載する。
  ```
- 役割数は 2 のまま変更なし（既存「受け入れ判定役」の責務を4項に拡張するだけで、3つ目の役割は増やさない）

### 参照ファイルの確認
- grep で `code-review/SKILL.md` 参照: `pr-review-flow-summary.md:17`（「Layer 1 の標準実行手段は `Skill(code-review)`」と記載 → 該当で参照が生きている）
- grep で `pr-review-watcher/SKILL.md` 参照: `pr-review-flow-summary.md:11` 等で明示（参照は生存）

---

## 3. guardrail_eng「`tools/check_ui_dimensions.py` 新設 + `run_checks.sh` 統合」案 → 実装箇所と書式

### 対象ファイル: `tools/run_checks.sh`
- **現在の構成**: L1-8（前文）、L10-60（6 個のチェック関数 check_lint/format/test/a11y など）
- **新規追加位置**: L60 の最終チェック（現在は a11y `axe`）直後に「7. UI 寸法検査」を追加
- **挿入予定行**: L62 相当（確認: `tail -20 tools/run_checks.sh` で最終行確認後に決定）

### 書式（既存チェックに倣った追加）
```bash
# 7. UI 寸法検査（コントロール高さ・幅がガイドライン準拠）
echo "🔍 UI 寸法検査..."
python3 tools/check_ui_dimensions.py --self-test > /dev/null 2>&1 && \
    python3 tools/check_ui_dimensions.py src/ui/components/button.tsx src/ui/components/input.tsx || {
    echo "❌ UI 寸法違反（詳細は上記参照）"
    return 1
}
```
- 既存チェック（check_lint など）と同じ「成功時は沈黙、失敗時は Error として停止」の流儀を継承
- **対象ファイル固定**: `src/ui/components/button.tsx` / `src/ui/components/input.tsx`（過検知を避けるため）
- `--self-test` フラグ（既存 `check_architecture_boundaries.py` に倣う）で統一テストを実行可能

### 既存チェックの確認（責務重複なし）
- grep で `tools/` 配下の existing checkers:
  - `check_architecture_boundaries.py`: import 境界（責務なし重複）
  - `check_cjk_markdown.py`: Markdown CJK スペーシング（責務なし重複）
  - `check_datetime_tz.py`: datetime TZ（責務なし重複）
  - `self_review_check.py`: TDD 順序・アーキテクチャ静的チェック（責務なし重複）
  - 新規 `check_ui_dimensions.py`: CSS クラス静的抽出 → px 数値マッピング（独立領域）

**衝突なし。** 各チェッカーは独立した正規表現パターン対象で、同じファイルを複数回スキャンしても結果の矛盾は起きない。

---

## まとめ

| 提案者 | 提案 | 衝突 | 根拠ファイル |
|--------|------|------|--------------|
| ux_standards | §2.4 新設 | **あり**（§2.3/§4.5/§7.5 との内容重複・後続参照ズレ） | user-story-map.md:294 の参照が変わる |
| review_process | code-review/pr-review-watcher 拡張 | **なし**（既存テーブル末尾への追加） | SKILL.md:41-51/L156-161 |
| guardrail_eng | check_ui_dimensions.py + run_checks.sh | **なし**（独立した新規検査エンジン） | run_checks.sh:60 の次行に追加 |

### `ux_standards` — 反論
<sub>2026-08-19T10:52:58+09:00</sub>

### ux_standards / Round 2 rebuttal

## 1. guardrail_eng への応答（レジストリ型静的検査の対象範囲）

**同意する部分**: `src/ui/**/*.tsx` の広域スキャンを不採用にした判断（round1 の guardrail_eng claim・案1' 不採用）は正しい。`cn(cond && 'h-10')` や `className` の合成順序は静的に解けないという指摘はその通りで、私の §2.4 案もそこまでの機械保証を前提にしていない。

**しかし、guardrail_eng の指摘（「SearchForm 側でサイズ指定するケースをどう扱うか」）は自分の Round1 案の穴を正確に突いている。これは concede する。**

Round1 で私は「SearchForm は `size="lg"` 相当を明示指定する」と書いたが、`src/ui/components/input.tsx:5-17` を実測すると **Input には `size` variant が存在しない**（`button.tsx` の cva `variants.size` のような選択式テーブルではなく、`cn()` 内に `h-8` が単一の生値として直書きされているだけ）。つまり Round1 の私の提案は「選べる仕組みが無いものを選ぶ」という前提の欠落があった。このままだと SearchForm 側が寸法を変えたくなったとき、開発者が取りうる手段は **`className="h-11"` のような呼び出し側での生の上書き** しかなく、これはまさに guardrail_eng がレジストリ型検査の射程外（=検出不能）と明言した経路そのものに落ちる。

**修正案**: 争点 B のファイル変更に以下を追加する。

- `src/ui/components/input.tsx` を `button.tsx` と同じ **cva パターンに変更し、`size` variant テーブルを新設** する（`default` / `lg` の最低 2 段階。`lg` が §2.4 の `--size-control-lg` に対応）。
- **SearchForm は `className` で高さ・フォントサイズを一切指定せず、`size="lg"` という named prop でのみ選ぶ**（呼び出し側での生の `h-*`/`text-*` 上書きを禁止する、という一文を §2.4 に明記する）。
- これにより guardrail_eng の案1（レジストリ型検査）の対象ファイルが「`button.tsx` の `variants.size` + `input.tsx` の新設 `variants.size`」の **2ファイル・2テーブルに揃う**（guardrail_eng が Round1 で書いた「今後 `src/ui/components/` に増える同種のプリミティブ」に含まれる範囲内であり、案1の設計方針を破らない）。

**「cva の外で className に直接 h-* を書かれたケース」への残余リスクへの回答**: これは guardrail_eng 自身が「広域スキャンは不採用」と判定した以上、**静的検査では原理的に閉じられない**。ここは役割分担で埋める。§2.4 に「フォームコントロールの高さ・フォントサイズは必ず `size` variant 経由で指定し、呼び出し側で `h-*`/`text-*` を直接書かない」という **判定可能な禁止規定** を明文化した上で、review_process が提案する code-review Step1 の UI/a11y ファインダー（フレッシュ文脈の意味論的レビュー）にこの規定を判定させる。数値の正本は増やさず（§2.4 のまま）、「呼び出し側で生クラスを書いていないか」という **パターンの逸脱判定** だけをファインダーに委ねる、という二層構成にする。

## 2. review_process への応答（新設エージェントなし・観点表拡張で足りるか）

**①（code-review Step1 拡張）には同意する。** 私が §1 で修正した「`size` variant 経由を強制する」規定は、まさに review_process の言う「基準の実体は書かず参照させるだけ」の型に合う。フレッシュ文脈のファインダーは実画面を見られなくても、**ソースコードから「default のまま検索欄に使っていないか」「呼び出し側で生の `h-*` を書いていないか」を判定できる** ——これは視覚判断ではなくパターン判断であり、§2.4 を named-token 化したことで初めて機械可読になる（§2.4 案がなければ①は「なんとなく小さい」を判定できず機能しない。逆に言えば①の実効性は §2.4 の設計に依存している）。

**③（Step7 受け入れ判定役 3点→4点）は必要条件だが、粒度に懸念が残る。ここは一部反論する。**

Step7 の受け入れ判定役はサブエージェントであり、**実際のプレビュー URL を視覚的に開いて「縦幅が小さい」と判断する能力を持たない**（今回の欠陥はまさに人間がスクリーンショットを見て気づいたもので、コードの数値だけでは「WCAG 2.2 の 24px は満たしている」ため検出できなかった＝Round1 §1 で述べた通り）。4点目の文言が「`ui-ux-guidelines.md` の該当節を参照し寸法・タイポ・ターゲットサイズ規定を満たすか判定する」というコード上の数値照合だけだと、**①の code-review ファインダーと同じ判定を Step7 でもう一度やるだけ** になり、視覚的な「操作性」という飼い主の本来の指摘軸をどちらのレイヤーも拾えない。

**追加提案**: 4点目の文言に「`sprint-development-rules.md` `SD-1` のプレビュー URL を実際に開き（人間側の開封を前提とする、または開けるサブエージェントがいればそれを使う）、§2.4 の主要導線コントロールが視覚的に主役として十分な大きさに見えるかを目視確認した旨を PR コメントに記録する」を加える。これは新しい判定基準の実体を増やすのではなく（§2.4 の「主要導線は推奨値を満たす」という既存規定の **目視確認手段** を明記するだけ）、SSOT は増えない。

**④（self-reviewer 不採用）に同意**。理由も round1 の指摘（自己修正盲点）で足りている。

## 3. docs_trace への応答（§2.4 案が生む新たな矛盾・§4.5 / §7.5 との整理）

**docs_trace の証拠（行147・行222・行242）を受けて、Round1 の自分の提案に不足があったことを認める。**

Round1 で私は「§7.5 冒頭に §2.4 への参照 1 行を追加する」とだけ書き、**`docs/03_design/ui-ux/ui-ux-guidelines.md:147`（§4.5 ページネーション「ページ番号ボタンは 24×24 CSS px 以上」）と `:242`（§7.7 手動チェックリスト「アイコンボタンの実測サイズが 24×24px 以上」）を見落としていた**。§2.4 を新設して「コントロールサイズの数値の正本は §2.4」と宣言した以上、この2箇所も同じ数値（24px）を独自に保持したままにすると、**§2.4 を作ったこと自体が新しい重複源になる**（docs_trace が Round1 の「結論4」で指摘した「SSOT が複数に分散」を、直そうとして別の形で再生産することになる）。

**修正案（整理表）**:

| 箇所 | 現状 | 修正後 |
|---|---|---|
| `:147`（§4.5） | 「ページ番号ボタンは 24×24 CSS px 以上（WCAG 2.2 の 2.5.8）」 | 「ページ番号ボタンは `--size-control-sm`（§2.4）以上（WCAG 2.2 の 2.5.8）」に置換。数値は書かない |
| `:220-224`（§7.5） | 24×24px の値を直接規定 | Round1提案通り、値は §2.4 参照に一本化。本節は WCAG 適合根拠（2.5.8 と 2.5.5 の関係・§0 で論じた必須/推奨の二層構造）の解説に専念 |
| `:242`（§7.7 チェックリスト） | 「アイコンボタンの実測サイズが 24×24px 以上」 | 「アイコンボタンの実測サイズが `--size-control-sm`（§2.4）以上」に置換。**チェックリストは手動確認の実行手順であり、確認对象の名称を持つのは正しいが、数値そのものを持つ必要はない** |

これで §2.4 が **唯一の数値の正本** になり、§4.5・§7.5・§7.7 はいずれも「参照 + その文脈での使い方の説明」だけを持つ（§2.1 のカラートークンが「用途」列を持ちながら値そのものは `@theme` にしか書かないのと同じ構造に揃う）。

**§4.5 との論理的な矛盾の有無**: 数値自体（24px フロア = `--size-control-sm`）は変更しないため、§4.5 の実体規定と §2.4 は矛盾しない。矛盾していたのは「同じ規範を2箇所に別々の文で書いていた」という **表現の重複** であり、docs_trace の指摘どおり Round1 の自分の提案はこの重複を §7.5 でしか解消していなかった。

**docs_trace の SP 波及指摘（結論1「寸法基準が未決定 → ページネーション・ソート・件数切替の設計が曖昧」）への回答**: §2.4 を §2（デザイントークン、文書全体に適用される章）に置いた設計判断は、まさにこの波及範囲（SP-2 以降の新規コントロール）を見越したものである。`--size-control-sm`（二次的コントロール）と将来追加されるソート・件数切替は §2.4 の中間層（Round1 §3 で述べた「二次的な小型コントロール」の枠）に自然に収まり、新設コントロールごとにガイドラインへ新しい数値規定を追記する必要がない。この点は docs_trace の懸念への直接の回答になっている。

**docs_trace の結論2（SD-1/SD-2 と Playwright 未導入の乖離）については**、§2.4 は E2E・Playwright の有無に依存しない設計（静的トークン + cva variant + code-review の意味論的判定）なので、この乖離を悪化させることも解消することもない。争点 E（今回のスコープ線引き）の中で「Playwright 導入は別 Issue」という guardrail_eng の結論と矛盾しない。

## 結論（Round2 での修正点まとめ）

1. `input.tsx` に `size` variant（cva）を新設し、SearchForm は `size="lg"` のみで指定・生の `h-*`/`text-*` 上書きを禁止する規定を §2.4 に追加（guardrail_eng の検査対象と整合させる）。
2. Step7 受け入れ判定役の4点目に「プレビュー URL を実際に開いて主要導線の視覚的卓越性を目視確認した旨を記録する」を追加（review_process 案への補強）。
3. `:147`（§4.5）と `:242`（§7.7）の 24px 直書きを `--size-control-sm`（§2.4）参照に置換し、数値の正本を §2.4 に完全一本化する（docs_trace の指摘を受けた Round1 案の修正）。

### `review_process` — 反論
<sub>2026-08-19T10:52:58+09:00</sub>

## review_process — Round 2 rebuttal / concession

### 1. ux_standards §2.4 への「参照の書式」を具体化する（rebuttal 兼・自分の提案の精緻化）

ux_standards の §2.4 案（🔴必須／🔵推奨の二層構造・トークン表）を前提に、code-review Step1 の UI 行と Step7 4点目の文言を以下に確定する。**基準の実体（px 値・トークン名）は書かず、「どの区分（必須/推奨）に当てはめて severity を決めるか」という判定ルールだけを書く** ことで参照を保つ。

- **code-review Step1「UI・アクセシビリティ」行の焦点列**（確定文言）:
  「`docs/03_design/ui-ux/ui-ux-guidelines.md` §2.4 を実際に Read してから判定する（本文に埋め込まれた値を訓練データから推測しない・L-113）。🔴必須行の逸脱＝`severity: CRITICAL`、🔵推奨行の逸脱＝`severity: WARNING` 以下。§2.4 に該当行がない新規コントロールへの言及は `severity: NIT`（規定不在の指摘であって違反ではない）」
- **Step7 受け入れ判定役 4点目**（確定文言）:
  「UI/デザイン変更を含む場合、`ui-ux-guidelines.md` §2.4 を実際に開いて 🔴必須行を満たすか判定する（『たぶん満たしている』を書かない＝既存2点目と同型の実行原則）。🔵推奨行の逸脱は Problem として記録し次スプリントへ送る（ブロックしない）」

これにより「severity/ブロック要否の決定ロジック」だけがスキル側にあり、**数値の実体は §2.4 の 1 箇所のみ**（ux_standards claim: L52-70）。

### 2. guardrail_eng の機械検査との境界（rebuttal・自分の Round1 案を修正）

**concession**: Round1 の私の提案は焦点列を「寸法・タイポスケール・ターゲットサイズ等に照らして判定」と書いたが、これは guardrail_eng の `check_ui_dimensions.py`（button.tsx/input.tsx の cva size テーブルの🔴必須フロア値検査・Error・run_checks.sh 統合。guardrail_eng claim: L21-31）と **完全に重複する**。同じ違反を機械が Error で止め、かつレビューが指摘するのは二重ゲートであり無駄なノイズになる。**Round1 の文言を撤回し、以下に絞る**。

**境界の確定**:
| 主体 | 対象 | 根拠 |
|---|---|---|
| 機械検査（`check_ui_dimensions.py`） | `button.tsx`/`input.tsx` の cva `size` テーブルの静的文字列に現れる🔴必須フロア値のみ | guardrail_eng claim L25-29（対象2〜3ファイル・静的文字列限定） |
| レビュー（code-review Step1 / Step7 4点目） | 機械検査の構造的な射程外のみ: ① §2.4 に未登録の新規コンポーネント（レジストリに無いためチェッカーが黙って PASS する対象＝guardrail_eng claim L15「対象ゼロなら黙ってPASS」の裏返し）、② 動的 className 合成・`md:` 出し分け・任意値（guardrail_eng claim L27 (a)-(d) で明示的にスキャン対象外とされたもの）、③ 🔵推奨行の逸脱（機械検査は🔴必須のみ Error 化・guardrail_eng claim L29「それ以外は今回追加しない」）、④ 選んだ variant/トークンが情報設計上妥当か（例: この操作が本当に主役として `--size-control-lg` を使うべき導線か。これは px 比較でなく設計判断） | 本節の切り分け |

**焦点列の再確定文言**: 「`check_ui_dimensions.py`（run_checks.sh の Error 検査）が既に検査済みの `button.tsx`/`input.tsx` の🔴必須フロア値は **指摘しない**（二重指摘禁止）。レビューが見るのは、機械検査の対象外（未登録の新規コンポーネント・動的 className 合成・🔵推奨行の逸脱・トークン選択の設計妥当性）に限る」

### 3. Layer 1 前倒しの価値は残るか（Round1 結論の維持・ただしスコープを再定義）

**Round1 結論「①を採る」は維持する。ただし価値の中身は差し替える。** guardrail_eng の静的検査が入ることで「既存の登録済みコンポーネントの数値フロア割れ」は機械が完全にカバーするため、Layer1 レビューがそこを担う必要は消えた（これが上記2の concession）。しかし残る領域がある:

1. **未登録コンポーネントの穴**（今回の欠陥そのものの再演リスク）: 今回の欠陥は「§2.4 が存在しない時点で input.tsx/button.tsx が作られた」ために起きた。同じことは SP-2 以降で新規プリミティブが追加されるたびに起こりうる。静的検査は `check_ui_dimensions.py` のレジストリに手動で追加されるまで対象外（guardrail_eng claim L15 のスキップ思想どおり）。**Layer1 レビューの役目は「この PR が新規の UI プリミティブを追加したなら、`check_ui_dimensions.py` のレジストリ対象に追加されているか」を確認するメタチェックに絞る**。これは静的検査自体では検出不能（自分のスコープ外を自分では気づけない）。
2. **🔵推奨行・設計判断**: ux_standards claim L67-69 の「推奨（`--size-control-md`/`lg` の高さ目安）」は機械の Error 対象外。ここは人間相当の判断が要る。
3. **動的 className・`md:` 出し分け**: guardrail_eng claim L27 が明示的にスキャン対象外とした領域（`search-form.tsx` のような合成箇所）は依然としてレビューでしか拾えない。

→ **前倒しの価値は「機械が既に守っている数値フロア」ではなく「機械が構造的に守れない3領域」に残る。** guardrail_eng の静的検査導入は Layer1 UI 観点を不要にするのではなく、**スコープを重複部分から外して縮小する**（コストはむしろ下がる: 焦点が絞られファインダーの誤検知余地も減る）。

### 4. docs_trace の指摘との突合・書き換え要否の確定

- docs_trace「`.claude/skills/` 配下: 該当ファイルなし（UI/a11y 専門スキル不在）」（docs_trace evidence L74-75）→ 本提案が着地すると **この行は陳腐化する**（`code-review/SKILL.md` と `pr-review-watcher/SKILL.md` の2ファイルに UI/a11y 言及が新設されるため）。ただし docs_trace の Round1 評価そのものは正しい（提案前時点の事実）。**書き換えが必要なのはこの2ファイルのみ** で、docs_trace が挙げた他の言及箇所（`sprint-development-rules.md` / `testing-strategy.md` / `user-story-map.md`）には触れない。
- docs_trace「SD-2 と E2E 基盤の乖離（SP-1〜SP-3 は Playwright 無し）」（docs_trace evidence L88-115）→ **本提案のスコープ外**。guardrail_eng が Playwright 導入を別 Issue に切り出す判断（guardrail_eng claim L41-43）と整合し、`sprint-development-rules.md` 自身が SP-1〜SP-3 を例外として明記済み（同ファイル内で自己無矛盾）なので書き換え不要。
- docs_trace「SSOT が複数に分散（ガイドライン・テスト戦略・ユーザーストーリーマップの3所）」（docs_trace evidence L124）→ これは **E2E 導入タイミングの分散** であり、デザイン判定基準の分散ではない。デザイン判定基準は ux_standards 案どおり `ui-ux-guidelines.md` §2.4 に一本化されるため、本提案が新たな分散を生むことはない。`testing-strategy.md` / `user-story-map.md` の書き換えは本提案の射程外（テスト基盤タイミングの決定は別レーン）。

### 結論（Round2 で確定した変更点）

- 書き換えファイルは Round1 と同じ2つ（`.claude/skills/code-review/SKILL.md` Step1 表 / `.claude/skills/pr-review-watcher/SKILL.md` Step7 受け入れ判定役4点目）だが、**両方とも「§2.4 を Read してから必須/推奨で severity を分ける」形に文言を確定** し、**guardrail_eng の機械検査と重複する数値フロア判定は明示的に除外** する。
- 新規ファイル・新規 SSOT は増えない（Round1 の結論を維持）。

### `guardrail_eng` — 反論
<sub>2026-08-19T10:54:18+09:00</sub>

## rebuttal（guardrail_eng・Round 2）

### 0. ux_standards の §2.4 案への回答: CSS 変数トークン化は round1 案より優れている（譲歩）

ux_standards が提案する `--size-control-sm/md/lg` トークン化（`app/globals.css` の `@theme` ブロックに追加。既存パターンは `app/globals.css:7-29` で確認済み＝ `--color-*` を `@theme inline` に集約する SSOT 設計）に **同意し、round1 の検査設計を修正する**。

round1 では「Tailwind 既定スケールの `h-8`=32px のような **生の数値** を Python 側にハードコードして下限と突き合わせる」という設計だった。これだと WCAG フロア値そのものは `ui-ux-guidelines.md` と検査スクリプトの **2 箇所** に実質重複する（ドキュメントの数値を変えてもスクリプトが追従しない = SSOT を増やさないという制約に抵触しかける）。

**修正案**: 検査対象を「生の `h-\d+` / `text-\w+` リテラルの禁止」に変える。
- `button.tsx` / `input.tsx` の cva `size` テーブルが `h-(--size-control-sm)` のような **CSS 変数参照**（Tailwind v4 の任意値記法）を使っているかを検査し、**生のユーティリティ値（`h-8` 等）が直書きされていたら Error**（トークン経由を強制）。これは `check_architecture_boundaries.py:62-64` の `VENDOR_BINDING_RE`（「事業者固有バインディングへの直アクセスを禁止し、必ず `src/infrastructure/platform/` の抽象を経由させる」= L205-209）と **全く同型の設計パターン**。「素の実装詳細に触るな、抽象（トークン）を経由しろ」という既存の流儀をそのまま転用できる。
- フロア値（24px・16px）自体は `app/globals.css` の `--size-control-sm` 等の **実際の宣言値を正規表現で読み取って** 動的に取得する（`--size-control-(sm|md|lg):\s*([\d.]+)px` 相当）。**Python にハードコードするのは「どの tier が WCAG 必須か」という tier ↔ ポリシーの対応表だけ**（= `check_architecture_boundaries.py:166-173` の `FORBIDDEN_TARGETS` dict と同じ「層 → ルール」のマッピング方式）。数値そのものの正本は `globals.css` 1 箇所に一本化される。

→ **round1 の「実装コスト半日」は撤回し、「1 日程度」に修正する**（2 ファイル間のクロス参照ロジックが増えるため）。ただし複雑さの質は既存チェッカーの流儀の範囲内に収まる。

### 1. §2.4 の各規定は機械検査で守れるか（名指しで回答）

| §2.4 の規定 | 機械検査で判定可能か |
|---|---|
| `--size-control-sm` 24px フロア（必須） | **できる**（上記トークン強制 + globals.css の値チェック） |
| `--size-control-lg` 主要導線のフォントサイズ 16px 未満禁止（必須・iOS ズーム回避） | **できる**。`text-base`/`text-sm` 等は Tailwind 固定スケール（12/14/16/18/20px）なので、cva 文字列中の **無プレフィックス（＝モバイル既定）** の `text-*` トークンを抽出し 16px 未満なら Error にできる |
| `--size-control-md` / `lg` の高さそのもの（推奨値） | **できない（明記する）**。ux_standards 自身が「推奨」と位置づけており（表の🔵注記）、根拠のない変更を許容する余地がある値は機械的な合否基準にできない。**レビュー観点に回す**（§4 で回答） |
| モバイル/デスクトップの出し分け設計の妥当性（「なぜこの層は出し分け、この層は固定か」という設計判断） | **できない**。「主要導線は出し分けない」「中間層は出し分ける」は意味的な設計判断であり、静的検査はパターン一致しかできない。**Error にはしない**（下記2で「構造だけ検出できる」と回答） |

### 2. レスポンシブな出し分け（`h-11 md:h-9` 相当）への具体的な振る舞い

トークン化後は `h-(--size-control-lg) md:h-(--size-control-md)` のような形になる想定。検査の具体案:

1. cva の各 variant 文字列から `(?:^|\s)(?:([a-z0-9]+):)?h-\(--([\w-]+)\)` でプレフィックス（無ければ base=モバイル）とトークン名のペアを **全て** 抽出する
2. `globals.css` から各トークンの px 値を解決する
3. Tailwind のモバイルファースト・カスケード規則（無プレフィックス値が既定、`md:` 以上は該当ブレークポイント以上でのみ上書き）に従い、**{ブレークポイント: 実効トークン}** の表を組み立てる（sm/md/lg/xl の並び順に「その prefix 以下で最後に登場した値」を採用する単純な畳み込みで実装できる。既存チェッカーの正規表現ベースの流儀を破らない）
4. その実効値が、**§2.4 の config テーブル（`{variant key: {breakpoint: 許容 tier}}`）に照らして下回っていないか** を判定する。この config は Python 側に明示的に持つ（`FORBIDDEN_TARGETS` 方式）。例: `default`（検索ボタン相当）は「全ブレークポイントで `--size-control-lg` 以上」と固定し、`md:` で `--size-control-sm` に落ちていたら Error
5. **「出し分けの設計判断が正しいか」（ux_standards の言う『主要導線は出し分けない』という方針自体の妥当性）は検査しない** — config テーブルへの登録はレビュー時に人（または Layer 1 の UI 観点ファインダー）が承認して初めて検査対象になる、という運用にする。**新規コントロール追加時は config テーブルへの追記が要る** という制約は、`check_architecture_boundaries.py` が新しい層を `LAYER_PREFIXES` に追記しないと検査対象にならないのと同じ既存の限界であり、目新しい弱点ではない。

**結論**: プレフィックス付きクラスがトークン参照の形で書かれている限り、ブレークポイントごとの下限判定は **できる**。ただし「その config テーブルの中身（どの variant がどの tier に属すべきか）」自体は人間が決めて登録するものであり、検査は「登録された規約からの逸脱」だけを機械的に守る、という役割分担になる。

### 3. `className` 経由の上書き（例: `SearchForm` が `<Input className="h-8" />` のように直接渡す）は防げるか

**部分的にしか防げない。穴を名指しし、埋め方を 2 段で示す。**

- **防げる範囲**: `search-form.tsx` のような **既知の呼び出しサイトに限定して登録** し、そこに渡される **リテラル文字列の** `className` prop（`cn()` や三項演算子を挟まない、JSX 属性値がそのまま文字列リテラルであるケース）から `h-*`/`text-*`/トークン以外の生値を抽出して同じ Error 判定にかける。実際 `search-form.tsx:21`（`className="flex-1"`）はまさにこの形で、`flex-1` には高さ指定が無いことも正規表現で確認できる（＝現状は違反なしと判定できる）。今回のスプリントでは `search-form.tsx` を検査対象ファイルとして明示登録することを推奨する。
- **防げない範囲（明言する）**: `cn(condition && 'h-6', className)` のような **動的合成**、変数展開、`className={smallVariant ? 'h-6' : 'h-9'}` のような条件式は、静的にどの文字列が最終的に採用されるか確定できないため **検出できない**。これは round1 で不採用と判断した「広域無差別スキャン」が抱えていたのと同じ限界で、対象ファイルを絞っても消えない。
- **埋め方**:
  1. （機械側で軽減）**未登録の呼び出しサイト** で `<Input>`/`<Button>` に literal な `h-*`/`text-*` を含む `className` が渡されているのを見つけたら **Warning**（Error にはしない＝未登録であること自体は設計判断の余地があるため）で「サイズ影響 className が検出されました。意図的なら config に登録してください」と促す。これは低コストで実装できる「気づきの網」であり、run_checks.sh を止めない。
  2. （残りはレビュー観点に回す）動的合成によるサイズ上書きは機械検査の対象外と **明言し**、review_process の「受け入れ判定役」4 点目（UI/デザイン判定）に **1 行だけ** 追加してもらうことを提案する: 「className の動的合成でコントロールサイズが縮んでいないか（機械検査の対象外）」。基準の実体（tier・px）はここに書かず、「機械検査の対象外である」という **境界の事実だけ** を書く形なので SSOT 増殖にはならない。

### 4. review_process への回答: 検査が保証する範囲（具体項目リスト）

「機械が見る部分をレビュー観点に重複して書かない」という境界要求に対し、**私の静的検査が保証する範囲** を確定させる。これ以外はレビュー観点（`code-review` の UI ファインダー / `pr-review-watcher` Step 7 の受け入れ判定役）に委ねてよい。

**機械検査が保証する（Error・run_checks.sh に統合）**:
1. `button.tsx` / `input.tsx` の cva size テーブルに、CSS 変数トークン（`h-(--size-control-*)` 相当）ではない生の `h-*`/`size-*` リテラルが直書きされていないこと
2. 上記ファイルの無プレフィックス（モバイル既定）`text-*` が 16px 未満でないこと
3. `app/globals.css` の `--size-control-sm` 宣言値が 24px 未満でないこと（WCAG 2.5.8 フロアの実体保証）
4. レスポンシブ prefix 付きトークン参照について、config テーブルに登録された tier の実効値がブレークポイントごとに下回っていないこと（登録済み variant のみ）
5. 登録済み呼び出しサイト（`search-form.tsx` 等）でリテラル className により上記フロアを下回っていないこと

**機械検査が保証しない（Warning に留めるか、レビュー観点に明示的に委ねる）**:
a. `--size-control-md`/`lg` の推奨高さ値そのものの妥当性（根拠のない変更か否かの判断）
b. 動的・条件付き className によるサイズ上書き（Warning で気づきのみ、確定判定はレビュー）
c. 未登録の新規コントロール（SP-2 以降）が config テーブルに追記されているか（Warning で促すのみ）
d. 実際のブラウザでの体感操作性（今回の発端そのもの）— Playwright 不採用（round1 結論を維持）に伴い、Step 7 のプレビュー URL 目視が引き続き唯一の担保

review_process の「①code-review Step 1 に条件付き追加」「③ pr-review-watcher Step 7 の受け入れ判定役に4点目追加」という結論に **異議はない**。ただし4点目の文言には上記 a/b/c/d の **「機械検査の対象外である」という境界の事実** を含めることを提案する（基準の実体は書かない・SSOT は増やさない）。

### 5. 結論のアップデート（争点 D）

round1 の「レジストリ型静的検査を Error として採用」という結論は維持しつつ、実装設計を **トークン強制方式**（VENDOR_BINDING_RE と同型）に修正する。これにより ux_standards のトークン化案（争点 A/B）と技術的に完全に整合し、数値の正本を `globals.css` に一本化できる（Python 側は「tier ↔ ポリシー」の対応表と「トークン経由を強制する」ロジックだけを持つ）。

## ラウンド 3

### `lead` — 合意
<sub>2026-08-19T10:55:45+09:00</sub>

## lead — 合意点と対立の解消

### 合意（4 レンズが一致）

1. **数値の正本は 1 箇所**: コントロール寸法の実値は `app/globals.css` の `@theme` に置くトークン（`--size-control-*`）を唯一の正本とし、`ui-ux-guidelines.md` §2.4 は「トークンの意味・必須/推奨の区分・使い方」を、スキル側は「参照と severity 判定ルール」だけを持つ。ドキュメント・テスト・Python スクリプトのいずれにも px 数値を複製しない。
2. **新規エージェント定義は作らない**（`design-reviewer` は不採用）。既存の `code-review` Step 1 観点表と `pr-review-watcher` Step 7 受け入れ判定役の拡張で足りる。
3. **機械検査とレビューの二重ゲートを作らない**。機械が保証する範囲（トークン経由の強制・モバイル既定フォントサイズ・フロア値の実体・登録済み variant のブレークポイント解決・登録済み呼び出しサイトのリテラル className）はレビュー観点から明示的に除外する。
4. **Playwright / axe は今回のスコープ外**（別 Issue）。E2E 基盤が無い制約は本 PR で解消しない。

### 対立の解消（lead 判定）

- **docs_trace「§2.4 を新設すると後続の全セクション番号が変わり `user-story-map.md:294` の §4 参照が壊れる」→ 棄却**。§2.3 の直後に §2.4 を足すのは §2 の内部追加であり、§3 以降の番号は変わらない（実ファイルで確認済み: §3 は行 75、§4 は行 99 で、いずれも §2 配下の小節追加の影響を受けない）。ただし docs_trace が指摘した **§4.5:147 と §7.7:242 の 24px 直書き** は実在する重複であり、ux_standards の Round 2 修正案（トークン参照への置換）を採用する。
- **ux_standards「Step 7 の 4 点目にプレビュー URL の目視確認を含める」→ 採用（ただし文言を限定）**。サブエージェントは実画面を開けないため「目視確認した旨を記録する」を義務化すると虚偽記載を誘発する（L-113）。**「プレビュー URL を PR 本文から引用し、主要導線コントロールが §2.4 の推奨 tier を使っているかをコードで確認したうえで、視覚的な最終判断は人間の操作レビューに委ねる」** という形にし、エージェントに「見た」と書かせない。
- **guardrail_eng「生の `h-*` 直書き禁止（トークン経由の強制）」→ 採用**。`check_architecture_boundaries.py` の `VENDOR_BINDING_RE` と同型で、既存の流儀を踏襲できる。ただし config テーブル（variant → 必要 tier）は Python 側に持つため、**未登録コンポーネントは検査対象外** という限界が残る。この穴は code-review Step 1 のメタチェック（「新規 UI プリミティブを追加したなら config に登録したか」）で埋める。
- **`input.tsx` の cva 化（size variant 新設）→ 採用**。現状 Input には size の選択肢が無く、呼び出し側が `className` で生の高さを上書きするしか手段がない＝機械検査の射程外に落ちる経路を構造的に作っている。

### 残る限界（明記して受け入れる）

- 動的 className 合成によるサイズ上書きは静的検査で検出できない（Warning 止まり）。
- 「実際のブラウザでの体感操作性」は Playwright 不採用のため機械では担保されない。人間の操作レビュー（`SD-1` のプレビュー URL）が唯一の担保である状態は本 PR では変わらない。

### `lead` — 判定
<sub>2026-08-19T10:57:11+09:00</sub>

{
  "decisions": [
    {
      "issue": "A: 寸法基準をガイドラインにどう置くか",
      "decision": "docs/03_design/ui-ux/ui-ux-guidelines.md の §2.3 直後に §2.4『コントロールサイズトークン』を新設し、コントロール寸法の判定基準（必須/推奨の二層）をここに一本化する。実値は app/globals.css の @theme のトークンが正本で、§2.4 は用途・必須/推奨の区分・禁止事項（呼び出し側での生クラス上書き）を持つ。§4.5 と §7.5 と §7.7 の 24px 直書きはトークン参照に置換する。§7.5 は WCAG 2.5.8（AA・必須）と 2.5.5（AAA・推奨として採用）の適合根拠の解説に専念する",
      "files": [
        "docs/03_design/ui-ux/ui-ux-guidelines.md: §2.4 新設 / §4.5・§7.5・§7.7 の数値をトークン参照へ置換",
        "app/globals.css: @theme に --size-control-sm/md/lg と最小フォントサイズのトークンを追加（実値はリサーチの一次情報で確定）"
      ],
      "rejected": [
        "§2.3 に統合拡張する（docs_trace 案）: タイポ・スペーシングとコントロール寸法は関心事が異なり、必須/推奨の二層表を混ぜると判定粒度が落ちる",
        "節番号の変更を理由に新設を見送る: §2 内部への追加であり §3 以降の番号は変わらない（実ファイルで確認済み）"
      ]
    },
    {
      "issue": "B: 実装の修正範囲",
      "decision": "input.tsx を cva 化して size variant（default / lg）を新設し、button.tsx と合わせて cva の size テーブルからトークンを参照する形にする。SearchForm は size を named prop で選ぶだけにし、className で高さ・フォントサイズを上書きしない。shadcn の default variant の意味は変えない（他画面への一括波及を避ける）",
      "files": [
        "src/ui/components/input.tsx: cva 化 + size variant 新設",
        "src/ui/components/button.tsx: size テーブルをトークン参照へ",
        "src/ui/search-form.tsx: size を明示指定（生クラス上書きをやめる）",
        "src/ui/search-form.test.tsx: 新規（variant 配線の回帰テスト。px 数値は assert しない）"
      ],
      "rejected": [
        "SearchForm 側で className に h-* を直接書く: 静的検査の射程外に落ちる経路を作るため禁止（§2.4 に明文化）",
        "shadcn の default size 自体を大きくする: SP-2 以降の二次コントロールまで巻き込み影響範囲が読めない"
      ]
    },
    {
      "issue": "C: デザイン観点レビューをどこに差し込むか",
      "decision": "① code-review スキル Step 1 の観点表に『UI・アクセシビリティ』行を追加（差分が app/ か src/ui/ を含むときだけ発火）し、③ pr-review-watcher Step 7 の受け入れ判定役の必須項目を 3 点→4 点に拡張する。両方とも判定基準の実体は書かず『§2.4 を Read し、必須行の逸脱は CRITICAL / 推奨行の逸脱は WARNING』という severity 判定ルールと、機械検査済み範囲の除外だけを持つ",
      "files": [
        ".claude/skills/code-review/SKILL.md: Step 1 観点表に 1 行追加 + ファインダー指示テンプレートに UI・アクセシビリティ節を追加",
        ".claude/skills/pr-review-watcher/SKILL.md: Step 7 受け入れ判定役の必須項目を 4 点に拡張"
      ],
      "rejected": [
        "② .claude/agents/design-reviewer.md の新設: 呼び出し元が結局 ①③ になり、frontmatter に判断基準を書き込む構造的誘引で ui-ux-guidelines.md と二重管理になる",
        "④ self-reviewer の観点表に追加: 実装者自身のセルフチェックは自己修正盲点の対象で、観点一覧の実体が 2 箇所に増える",
        "Step 7 の役割を 3 つに増やす: 既存 2 役割の拡張で足り、fan-out コストを増やさない"
      ]
    },
    {
      "issue": "D: 実効性の担保（機械強制）",
      "decision": "tools/check_ui_dimensions.py を新設し、run_checks.sh に Error チェックとして追加する。方式は『生の h-*/text-* 直書きを禁止しトークン経由を強制する』（check_architecture_boundaries.py の VENDOR_BINDING_RE と同型）+『globals.css の宣言値からフロアを動的に解決する』。Python 側が持つのは variant → 必要 tier の config テーブルのみで px 数値は持たない。--self-test を備える",
      "files": [
        "tools/check_ui_dimensions.py: 新規",
        "tools/run_checks.sh: チェック追加"
      ],
      "rejected": [
        "src/ui/**/*.tsx の広域スキャン: 動的 className・任意値・レスポンシブ prefix を解釈できず過検知と過小検知を同時に起こす",
        "jsdom + testing-library で px 数値を assert する: 実レイアウトを持たずクラス文字列一致にしかならないうえ数値の複製になる",
        "Playwright 導入: 今回のスコープ外（別 Issue）"
      ]
    },
    {
      "issue": "E: スコープの線引き",
      "decision": "この PR では ガイドライン §2.4 + トークン + Input/Button/SearchForm の実装 + 静的検査 + レビュー仕組みの 5 点を完遂する。Playwright + axe による E2E・a11y 自動検証（NFR-26 / SD-2 の E2E 要件）と、SP-2 以降のコントロール（ページネーション・ソート・件数切替）への適用は別 Issue に送る",
      "files": [],
      "rejected": [
        "SP-2 以降のコントロールを先回りで実装する: YAGNI。§2.4 のトークンだけ用意し適用は各スプリントの SD-4 自律参照に委ねる"
      ]
    }
  ],
  "guideline_section": {
    "section_title": "§2.4 コントロールサイズトークン（§2.3 の直後）",
    "rules": [
      "必須: 全コントロールは --size-control-sm（WCAG 2.2 の 2.5.8 フロア）を下回らない",
      "必須: 入力系コントロールのモバイル既定フォントサイズは 16px を下回らない（iOS Safari の自動ズーム回避。maximum-scale での抑止は禁止）",
      "推奨: 主要導線（検索入力欄・検索ボタン）は --size-control-lg を使う（WCAG 2.5.5 相当を推奨として採用。AAA 適合を謳うものではない）",
      "必須: コントロールの高さ・フォントサイズは cva の size variant 経由でのみ指定し、呼び出し側の className に生の h-* / text-* を書かない",
      "モバイル / デスクトップの出し分けはトークン参照 + レスポンシブ prefix で表現する（数値の直書きをしない）",
      "実値（px）はリサーチの一次情報（Material Design 3 / Apple HIG / GOV.UK / MOJ / Primer / shadcn）で確定し app/globals.css の @theme に置く"
    ]
  },
  "review_mechanism": {
    "where": [
      "マージ前: code-review スキル Step 1 の観点表『UI・アクセシビリティ』（app/ か src/ui/ を含む差分のみ）",
      "マージ後: pr-review-watcher Step 7 受け入れ判定役の必須 4 点目（スプリントレビュー）"
    ],
    "new_files": ["tools/check_ui_dimensions.py", "src/ui/search-form.test.tsx"],
    "modified_files": [
      "docs/03_design/ui-ux/ui-ux-guidelines.md",
      "app/globals.css",
      "src/ui/components/input.tsx",
      "src/ui/components/button.tsx",
      "src/ui/search-form.tsx",
      ".claude/skills/code-review/SKILL.md",
      ".claude/skills/pr-review-watcher/SKILL.md",
      "tools/run_checks.sh"
    ]
  },
  "machine_checks": {
    "adopt": [
      "cva size テーブルでの生の h-*/size-* 直書き禁止（トークン経由の強制）→ Error",
      "無プレフィックス（モバイル既定）の text-* が 16px 未満なら Error",
      "globals.css の --size-control-sm 宣言値が WCAG フロア未満なら Error",
      "登録済み variant のレスポンシブ実効値が config テーブルの tier を下回るなら Error",
      "登録済み呼び出しサイト（search-form.tsx）のリテラル className によるサイズ上書き → Error",
      "未登録コンポーネント・動的 className によるサイズ影響 → Warning（run_checks.sh を止めない）"
    ],
    "reject": [
      "src/ui 全体の無差別クラススキャン（過検知）",
      "jsdom での px 数値 assert（数値の複製・実装詳細への結合）",
      "Playwright / 視覚回帰（今回のスコープ外）"
    ]
  },
  "scope": {
    "this_pr": [
      "ui-ux-guidelines.md §2.4 新設と既存節のトークン参照化",
      "app/globals.css へのコントロールサイズトークン追加（実値はリサーチで確定）",
      "input.tsx の cva 化 / button.tsx のトークン参照化 / search-form.tsx の size 明示",
      "search-form.test.tsx（variant 配線の回帰テスト）",
      "tools/check_ui_dimensions.py + run_checks.sh 統合",
      "code-review / pr-review-watcher のレビュー観点拡張"
    ],
    "follow_up_issues": [
      {
        "title": "feat: Playwright + axe による E2E・アクセシビリティ自動検証を導入する（NFR-26 / SD-2）",
        "labels": ["type:feature", "sp:5"],
        "why": "実ブラウザでの体感操作性・視覚回帰・a11y 自動検証は静的検査では担保できず、SP-4 の E2E 基盤整備と一体で設計すべき"
      },
      {
        "title": "improvement: SP-2 以降の新規コントロール（ページネーション・ソート・件数切替）に §2.4 のサイズトークンを適用する",
        "labels": ["type:improvement", "sp:2"],
        "why": "今回はトークンの器と主要導線のみ。他コントロールへの適用は各スプリントで行うが、抜けを防ぐため追跡する"
      }
    ]
  },
  "open_questions": [
    "実ブラウザでの体感操作性は Playwright 不採用のため人間の操作レビューが唯一の担保である（今回の欠陥もそこで見つかった）。この状態を許容し続けるか、別 Issue の優先度を上げるかは飼い主の判断事項"
  ]
}
