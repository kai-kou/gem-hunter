<!--entry
author: lead
round: 3
kind: verdict
ts: 2026-08-22T12:46:34+09:00
-->

{
  "decisions": [
    {
      "issue": "A: 検索への Gem Index 再導入をやるか",
      "conclusion": "主ソート軸（sort=gem-index）としては再導入しない。キーワード起点は維持したまま『検索結果のバッジ + 検索語を引き継ぐ Gem 絞り込み一覧への導線』として再導入する",
      "rationale": "被覆率は実測 react 32% / test framework 36% / image processing 19% で、品質フィルタを掛けても 0.001〜0.086% しか動かない（data 実測）。それでも主ソートを却下するのは『一部だけ並べ替わり残りは無意味順』という体験破綻が母集団拡大では解消しないため。一方 D-33 が挙げた 2 つの障害（npm 限定・バンドル 3MB）はいずれも解消済み",
      "rejected": "sort=gem-index の全面復活（体験破綻・D-33 の rejected 理由が形を変えて再発）／現状維持（10.9 万リポジトリの母集団を活かさない機会損失）"
    },
    {
      "issue": "B: 母集団設計",
      "conclusion": "Ecosyste.ms の一覧 API を 12 レジストリ × 被依存数降順で巡回し、(1) レジストリ別成層化（レジストリごとに固定枠 or 母数比例枠で選出してから混ぜる） (2) stars=0 汚染フィルタ（dependents 上位帯 × stars=0 は除外／repo_metadata の有無で欠損と真の 0 を区別） (3) repo 単位 dedupe・代表は dependents 最大の flagship パッケージ、の 3 点を必須とする。Paid 前提なので npm / PyPI 等の大規模レジストリはさらに深く（3〜5 万件）取ってよい",
      "rationale": "レジストリ内 rank を素で混ぜると Maven WebJars ミラーが上位 100 件中 87 件を占め、素の絶対値でグローバル再計算すると npm が 90 件を占める（両方 data が実装して確認）。成層化は両極の欠陥を同時に潰す。モノレポの min / max 代表は arkworks-rs/algebra の実例で false positive / false negative を起こす",
      "rejected": "レジストリ内 rank の単純結合／素の絶対値でのグローバルパーセンタイル再計算／sum 集約（同一レジストリ内 workspace 分割が 95% を占め水増しになる）／min・max による代表選定／deps.dev BigQuery への乗り換え（無料枠 1TB 事故リスク・運用複雑化）／63.7GB バルクダンプ（API 巡回のほうが速く新しい）"
    },
    {
      "issue": "C: 配信方式",
      "conclusion": "検索結果にバッジを出す場合のみ runtime join 基盤が要る。その形は『レジストリ別 12 シャードの静的 JSON を Static Assets に置き、isolate の cold start で Promise.all 並列 fetch → isolate スコープの単一 Map にマージ、以降は Map.get() で join』。limits.cpu_ms は暫定 50ms を設定し、wrangler tail の p95 実測後に 20〜30ms へ締める。Gem 一覧だけを作る場合は現行の静的 JSON 配信の延長で足り、この基盤は不要",
      "rationale": "ハッシュ分割は『シャードを増やすほど転送量が減り subrequest が増える』二律背反で Free の 50/invocation に収まる N が存在しない（infra のシミュレーション: N=64 で期待 51 シャード）。レジストリ別分割なら 12 本固定で並列 fetch でき、レジストリ単位の個別再生成という運用上の利点も得られる。cold start は isolate ごとに継続的に発生するため実測が必須",
      "rejected": "D1 の IN クエリ（D-18 が D1 を明示列挙して不採用と確定・D-27 の境界注記は静的アセットファイルの判定基準であり D1 を救済しない）／ハッシュ分割シャード／Static Assets の Range 二分探索（Range 未対応報告があり未検証）／バンドル焼き込みの継続（データ更新のたびに JS 再ビルドが要り D-28 の設計意図と逆行）"
    },
    {
      "issue": "D: UX",
      "conclusion": "(1) 検索結果カードには Gem Index が付くものにだけバッジを出す（ソート順は変えない・付かないことが低評価を意味しない旨の注記を添える） (2) 検索語をそのまま引き継ぐ『この検索語の Gem を見る』導線を置き、遷移先で Gem 絞り込み一覧を Gem Index 順に見せる。0 件のときは対象母集団を明示して空を見せる",
      "rationale": "空状態を主体験（検索結果）から隔離しつつ、キーワード起点を失わない。ブラウズ専用の発見面はミッション『個人開発者が見つけられる』に届かず、かつ汚染の露出濃度が最大になる（product が round 2 で撤回）",
      "rejected": "Gem Index を検索の既定ソートにする／ブラウズ専用の発見面を主軸にする／検索対象そのものを Gem コーパスに置き換える（FR-1『GitHub 公開リポジトリ検索』と矛盾）"
    },
    {
      "issue": "E: 運用",
      "conclusion": "収集は Ecosyste.ms REST の巡回（匿名 5,000 req/時の枠内・約 10 分）を Cloudflare の外（セッション or Routine の cron）で実行し、生成物を commit → デプロイで Static Assets ごと差し替える。更新は日次〜週次。配信 JSON には repoFullName / gemIndex に加えて registry を持たせる。CC BY-SA 4.0 の帰属・改変明示・継承は現行の AttributionNotice を踏襲し、Ecosyste.ms の生テキスト（description 等）は再配信しない",
      "rationale": "D-28 の『Worker が実行時に書き込まない読み取り専用アセットの差し替え』という枠組みをそのまま維持できる。registry を持たせておくと、あるレジストリだけ汚染が発覚したときに再収集なしで緊急除外できる",
      "rejected": "GitHub Actions での自動収集（D-23 により起動不可）／Ecosyste.ms の description 等を配信物に載せる（GR-6 が禁止）"
    }
  ],
  "recommendation": {
    "name": "バッジ + 検索語連動 Gem 一覧（キーワード起点を保った再導入）",
    "what": "① 12 レジストリ・成層化・汚染フィルタ済みの候補プール生成 CLI（現行 generate_gem_digest.mjs の拡張）② レジストリ別 12 シャードの静的配信 + isolate キャッシュによる join ③ 検索結果カードの Gem バッジ（ソート順は変えない）④ 検索語を引き継ぐ Gem 絞り込み一覧ページ",
    "sprints": "3 本（① データ生成 = sp:5 / ② 配信基盤 + バッジ = sp:5 / ③ Gem 一覧ページ = sp:3）",
    "drops": "sort=gem-index の復活（D-33 の撤去を維持）。1,000 件取得ループ（D-30 ②）も復活させない",
    "fail_criteria": "汚染フィルタ後の Gem 一覧の上位 20 件を目視して、明らかな自動生成ミラー・repo 誤紐付けが 3 件以上混ざる／wrangler tail の p95 CPU が limits.cpu_ms=50ms を超える／バッジ付与率が主要 5 キーワードの上位 20 件で平均 20% を下回る"
  },
  "alternative": {
    "name": "Gem 一覧だけ（検索結果には手を入れない）",
    "what": "① と ④ のみ。事前計算した上位 N 件（成層化・フィルタ済み）を静的 JSON で配信し、Gem 一覧ページとして見せる。検索結果カードは現状のまま",
    "sprints": "1〜2 本（sp:5 + sp:3）",
    "drops": "検索結果へのバッジ（＝runtime join 基盤 ②）。キーワードからの導線は残すが、絞り込みは Gem 一覧側の入力欄で行う",
    "fail_criteria": "Gem 一覧が『今日の Gem』の件数違いにしか見えず、キーワードから来たユーザーの 0 件率が高い（主要 5 キーワードで 3 件未満が過半）"
  },
  "open_questions": [
    {
      "question": "検索結果カードに Gem バッジを出すか（＝runtime join 基盤を作るか）",
      "options": ["出す（推奨案・スプリント 3 本）", "出さない（対案・スプリント 1〜2 本）"],
      "recommended": "出す（推奨案・スプリント 3 本）"
    },
    {
      "question": "Gem 一覧のキーワード絞り込みに何を使うか（配信データに何を載せるか）",
      "options": ["repo 名 + topics（GitHub API から自前取得・サイズ増を許容）", "repo 名とパッケージ名のみ（配信データを最小に保つ）"],
      "recommended": "repo 名とパッケージ名のみ（配信データを最小に保つ）"
    }
  ],
  "critical": [
    "汚染フィルタ（stars=0 × 高被依存の除外・repo_metadata による欠損判定）を実装しないまま Gem を上位表示する面を作ると、Maven WebJars ミラーや repo 誤紐付けがトップに並び、ミッションの主張そのものを裏切る",
    "レジストリ横断の順位付けは成層化なしには成立しない（レジストリ内 rank の素の結合も、絶対値のグローバル再計算も、どちらか 1 レジストリが上位を独占する）",
    "cold start は isolate ごとに継続発生し、その割合は Cloudflare 非公開。limits.cpu_ms の妥当性は wrangler tail の実測でしか確かめられない"
  ]
}
