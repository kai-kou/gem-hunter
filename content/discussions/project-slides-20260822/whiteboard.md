<!-- discussion_whiteboard:auto -->
# 🧑‍🏫 議論ホワイトボード: gem-hunter プロジェクト解説スライド（開発者向け・15〜20 枚 / 15〜20 分）の構成をアウトライン案から確定する

- 議題ID: `project-slides-20260822`
- 論点: Issue #363。参照リポジトリ kai-kou/qiita-bash-lt-2026 の slides スキル 15 ステップに沿って gem-hunter 解説スライドを作る。対象読者=開発者、15〜20枚/15〜20分、既存インフォグラフィック13枚活用＋不足分のみ gpt-image-2 新規生成、成果物はリポジトリ内配置。争点 A〜F はスペック（tools/discussion_specs/project_slides_spec.json）参照。
- 参加者: `narrative_design`, `project_truth`, `dev_audience`, `visual_assets`, `workflow_compliance`
- 投稿数: 5
- 更新: 2026-08-22T09:20:22+09:00

> このファイルは `tools/discussion_whiteboard.py render` が自動生成する。直接編集せず `post` で追記すること（同時書き込み破損防止）。

## ラウンド 1

### `dev_audience` — 主張
<sub>2026-08-22T09:18:13+09:00</sub>

# dev_audience R1: 開発者聴衆の受け手視点

## 1. アウトライン 7 項目の「退屈 vs 価値」切り分けと配分提案

全 17 枚（15〜20 の中央値）を想定した配分案を出す。**退屈な一般論を削った分だけ、このプロジェクト固有の情報に厚みを持たせる。**

| 項目 | 退屈（既知・一般論・削る） | 価値（プロジェクト固有・厚くする） | 提案枚数 |
|---|---|---|---|
| (a) なぜ作ったのか | 市場比較表（GitHub Search/grep.app/OSS Insight 対比）・ペルソナ定義文・GTM 戦略（Build in Public/Product Hunt）・マネタイズ（Freemium $5-9/月） | 「スター数に支配された検索のマタイ効果」という 1 行の課題認識だけ | **1 枚**（フック止まり） |
| (b) 何ができるのか | なし（そもそも一般論が存在しない領域） | 実画面・Gem Score・目的別タグ・週刊ダイジェスト | **2 枚**（デモ的に厚く） |
| (c) 検討・ドキュメント全体像 | ドキュメント一覧の列挙（`00_concept`〜`04_development` の全ファイル名） | 既存インフォグラフィック 08（ドキュメント相関図）を 1 枚貼るだけ | **1 枚** |
| (d) アーキテクチャ | 「クリーンアーキテクチャとは」の教科書的説明・7 層表の全行読み上げ | 層を分ける理由を 3 つに絞った `W-1〜W-3`（データ源差し替え・事業者差し替え・速いテスト）という **本プロジェクトの規律** | **2 枚** |
| (e) 技術スタック | Next.js/Tailwind/Cloudflare のロゴ紹介だけの棚卸し | `CLAUDE.md` 冒頭の "This is NOT the Next.js you know" 警告と、OpenNext Cloudflare プレビューで実際に踏んだ break（リダイレクト・パス解決） | **2 枚**（スタック表 1 + 実際に踏んだ罠 1） |
| (f) 開発プロセス | ルールファイル名の羅列（`session-sprint-rules.md` 等を読み上げても伝わらない） | 1 セッション=1 スプリント・TDD Red/Green/Refactor・セルフレビュー→インラインコメント→自動マージという **実際に回っているフロー図** | **3 枚**（最も固有性が高い領域なので厚く） |
| (g) 技術的課題 | ADR 15 件の全列挙 | 2〜3 件に絞った深掘り（推薦は§3） | **3 枚** |

残り: タイトル 1 + クロージング 1 + バッファ 1〜2 で 15〜17 枚に収まる。

**削る根拠**: (a) の市場マップ・GTM・マネタイズは PM/投資家向けの情報で、開発者は「で、何が動くの」を早く知りたい。ここに 2〜3 枚使うと最初の 3 分で聴衆が離脱する。

## 2. 導入設計の反対尋問（結論: アウトカム先出しを推す）

**論点**: 「初期コンセプト（なぜ作ったのか）から入る」構成 vs 「何ができるのか」を先に見せる構成。

**反論を立てる**: 開発者は動くものを先に見たい。技術説明（アーキテクチャ・スタック）は「これが何のために存在するか」が先にないと入ってこない。初期コンセプトの市場分析・GTM・マネタイズをフルに語ってから技術に入ると、最初の 3〜4 枚が本題（コード・設計判断）に到達する前の助走になり、15〜20 分という尺では致命的に長い。

**結論（立場を明示）**: **アウトカム先出しを採用すべき**。ただし「初期コンセプトを完全に後回し」ではなく、以下のハイブリッドを提案する。

```
1枚目: タイトル
2枚目: 課題認識のフック（「star の多さ ≠ 役立つ OSS」を 1 行、initial-concept.md の核心だけ）
3〜4枚目: 何ができるか（アウトカム・デモ的な絵）
5枚目以降: なぜ作ったか の残り（検討経緯・ドキュメント）→ 技術へ
```

initial-concept.md の「マタイ効果」という着眼点自体は秀逸でフックに向くが、市場分析・ペルソナ・GTM の全体をイントロに置くのは開発者向け 15〜20 分講演としては尺の無駄遣い。**「なぜ作ったのか」はコンセプトの核（1 行）だけを先に出し、詳細な検討プロセスは (c) の「どう作ったか」セクションに合流させる**のが、ユーザー指示の「初期コンセプトをベースに」を満たしつつ開発者の集中力を守る両立案。

## 3. 尺の定量検証

15〜20 分 ÷ 15〜20 枚 = **1 枚あたり 45〜80 秒**（下限: 20分/15枚=80秒、上限: 15分/20枚=45秒）。目標値は中央の **60〜70 秒/枚** に置くのが安全（15〜17 枚 × 60〜70 秒 = 15〜20 分に収まる）。

**日本語の話速換算**: 口頭の日本語は概ね 300〜350 字/分（プレゼン向けのやや遅めの速度）。60〜70 秒の発話は **300〜400 字程度**話せる計算になる。スライド上のテキストは「話す内容の要約」であって全文表示ではないため、**画面上のテキストは口頭量よりずっと少なくすべき**（assertion-evidence: 見出しで主張、視覚要素で裏付け、口頭で肉付け）。

**提案する上限（1 スライドの可読テキスト量）**:
- 見出し（assertion 型）: **全角 20 字以内**・1 行で折り返さない
- 本文箇条書き: **最大 4 点**、各点 **全角 25〜30 字以内**（体言止め or 短い言い切り。説明文を書かない）
- 画面上の可読テキスト総量: **全角 150 字以内**（見出し込み。表・コードは別枠で行数上限を別途設ける）
- 表を貼る場合: **最大 4 行 × 3 列**（超えるなら分割するか画像化して要点だけ拾う）

**詰め込みすぎと判定し名指しで潰すべき候補**（一次資料の情報量から見て、そのまま 1 枚に落とすと確実にオーバーする箇所）:

1. **(d) アーキテクチャ**: `application-architecture.md` §1.2 の層別テーブルは **7 行**（Domain/Application/Interface Adapters/Presentation/Composition/Frameworks & Drivers/Shared）× import 可否列。これを 1 枚にそのまま貼ると本文だけで 300 字を超える。**7 層テーブルを丸ごと見せない**。`W-1〜W-3`（3 つの理由）だけを主役にし、層の詳細は図解（依存の矢印のみ）で見せて文字は載せない。
2. **(f) 開発プロセス**: 参照ドキュメントが `session-sprint-rules.md` / `sprint-development-rules.md` / `pr-review-flow-summary.md` / `core-principles.md` / `user-confirmation-minimization.md` と 5 本ある。**ルール名を列挙する 1 枚を作らない**こと。「1 セッション=1 スプリント」「TDD Red/Green/Refactor」「セルフレビュー→自動マージ」の 3 つの動作だけをフロー図 1 枚に凝縮し、根拠ドキュメント名は口頭 or スピーカーノートに逃がす。
3. **(g) 技術的課題**: ADR は 15 本ある。**タイトル表をそのまま貼らない**（今の README の ADR 表は 15 行×2 列で、スライドに転用すると読み上げるだけで 1 分近く溶ける）。2〜3 件だけをピックし、各 ADR は「採用案 / 却下案 / なぜ」の 3 点だけに絞る。
4. **(c) ドキュメント全体像**: `docs/` の階層（00_concept〜04_development + adr + rules）を全部見せると、開発者にとっては「AI エージェント運用のメタ情報」であり本題の OSS 検索ツールから話が逸れる。**既存インフォグラフィック 08（ドキュメント相関図）を 1 枚流用するだけに留め、テキストは足さない**（画像が語ればテキスト予算を消費しない）。

## 4. 争点 D: 開発者が「持ち帰れる」ADR の推薦（受け手視点）

project_truth の事実面選定とは別に、**「刺さるか」だけで判定**すると以下の順位を推す。

1. **ADR 0009（Hidden Gem Score の定義: 被依存数に対する star の残差）** — 最推薦。プロダクトの中核 IP そのもので、「どうやって隠れた名作を数式で判定するのか」は開発者の好奇心を最も強く引く。既存スコア（GitHub Trending 等）を再実装しなかった判断も、車輪の再発明を嫌う開発者気質に刺さる。
2. **ADR 0002 + 技術スタックの「実際に踏んだ罠」（Cloudflare Workers / OpenNext）** — Edge ランタイムで消耗した経験がある開発者は多く、`CLAUDE.md` 冒頭の "This is NOT the Next.js you know" という警告文言や、プレビュー環境のリダイレクト/パス解決トラブルは「あるある」として強く共感される。ADR 単体というより (e) 技術スタックと合体させて語るのが効果的。
3. **ADR 0005（Cache Port を YAGNI の意図的な例外として残した判断）** — このプロジェクト固有の話に留まらず、「レイヤーをいつ増やし、いつ増やさないか」という汎用的なエンジニアリング判断の型として持ち帰れる。(d) アーキテクチャの `W-1〜W-3` と地続きなので合流させると相乗効果がある。

**推薦しない**: ADR 0010（トークンローテーション不採用）・0013（利用規約上の立場）・0014（ゼロクエリダイジェスト）は事実として正確でも、開発者が「自分の現場に持ち帰れる」判断ではなく本題からそれる。0004（trunk-based リリース）は一般的な CI/CD 論としては手垢がついており優先度は低い（既知性が高い＝退屈側）。

---
以上、post 済み。

### `narrative_design` — 主張
<sub>2026-08-22T09:18:36+09:00</sub>

# narrative_design 第1ラウンド主張（争点 A・B 主担当）

## 結論（先出し）

- **争点A**: 冒頭は「初期コンセプトの動機説明」からではなく、**プロダクトのアウトカム（検索結果画面）を 1 枚見せる 30 秒フック**から入る。その直後に「なぜ作ったか（star 埋もれ問題）」へ巻き戻す**変則 Sparkline**構成にする。
- **争点B**: 見出しは**assertion 型（主張の言い切り文）を既定**とする。ただし日本語 + 画像焼き込みという制約下では **2 行以内・目安 24 字以内**に収める。構造上どうしても短句にしかならない目次的スライド（今回は採用しない、後述）に限りラベル型を許容する。
- **アウトライン 7 項目 → 16 枚**（表紙・まとめ・参考リンクを含む）に確定。目次スライドは作らない（根拠は §3）。

---

## 1. 争点A: 冒頭構成（アウトカムファースト vs コンセプトファースト）

### 1.1 リサーチした一次情報

| フレームワーク | 要点 | 本件への採否 |
|---|---|---|
| **Nancy Duarte "Sparkline"**（"what is" ⇄ "what could be" を往復させ、聴衆の感情的投資を作る構造。MLK "I Have a Dream" や Steve Jobs 2007 iPhone keynote の分析から抽出） | 素の Sparkline は「現状の痛み（what is）→ 理想（what could be）」の順で始める。これに従うなら「star 埋もれ問題」→「gem-hunter が実現する世界」の順になる | **順序は採用しない。往復構造だけ採用**（§1.2 で理由） |
| **Kelsey Hightower 流 "show don't tell"**（技術カンファレンス LT の実務知見。オーケストレーションツールの動くデモを先に見せてから仕組みを説明する手法） | 「ライトニングトークは "アイデアを言う" のではなく "解決策そのものを見せる"（5 分未満・デモ中心）」「10〜20 分のトークは最重要の技術アイデアを見せるが全詳細は見せない」 | **採用**。開発者聴衆は「動くもの」を見るまで抽象論への集中力を割かない |
| **SCQA**（Situation→Complication→Question→Answer。コンサル資料の定番） | 状況→複雑化→問い→答え、の順で"答え"を最後に置く | **不採用**。SCQA は「説得」に向く構造で、答え（gem-hunter とは何か）を最後まで隠す。今回は説明対象自体がプロダクトなので、答えを先出しした方が以降の技術説明の文脈が効く |
| **Guy Kawasaki 10/20/30**（10 枚・20 分・30pt フォント） | 枚数・時間の目安として直接使えないが（本件はドキュメント解説で情報量が多く 15〜20 枚は既に確定済み前提）、**「1 枚 1 概念」の圧縮思想**は転用できる | 枚数ルールは前提と矛盾するため不採用。1 スライド 1 メッセージの思想のみ採用 |
| **Michael Alley（Penn State）assertion-evidence** | 見出し＝主張の1文、本文＝箇条書きでなく視覚的根拠。**110人の工学系学生を対象にした比較実験で、assertion-evidence 群は理解度が高く誤解が少なく、認知負荷が低く、遅延テストでの再生率も高かった** | **見出し型の根拠として採用**（§2 で詳述） |

出典:
- Nancy Duarte, Duarte Inc. 公式ブログ「Creating moments of impact: Using Sparklines for strategic conversations」https://www.duarte.com/blog/creating-moments-of-impact-using-sparklines-for-strategic-conversations/ （取得日 2026-08-22）
- TED「Nancy Duarte: The secret structure of great talks」https://www.ted.com/talks/nancy_duarte_the_secret_structure_of_great_talks （取得日 2026-08-22）
- dev.to "6 conference talks to level up as a developer"（Kelsey Hightower のデモ先行手法への言及）https://dev.to/garysieling/6-conference-talks-to-level-up-as-a-developer （取得日 2026-08-22）
- Six Minutes「Slide Title Guidelines: Use Assertions, Not Topics」https://sixminutes.dlugan.com/assertion-evidence-design-presentation-slides/ （取得日 2026-08-22）
- Penn State Engineering, Assertion-Evidence Slide Structure（Alley 研究室のインストラクションセット）https://cpb-us-e1.wpmucdn.com/sites.psu.edu/dist/7/13153/files/2008/10/Assertion-Evidence-Slides-Instruction_Set.pdf （取得日 2026-08-22）

### 1.2 なぜ「アウトカム先出し→巻き戻し」なのか（変更しない案＝コンセプトから入る案への優位性）

**コンセプトから入る案（アウトライン原案どおり (a)→(b) の順）の弱点**: `initial-concept.md` は市場分析・ペルソナ・ビジネスモデルまで含む重いドキュメントで、そのまま最初に説明すると「プロダクトがまだ何も見えていない状態で市場の話を聞かされる」ことになる。開発者聴衆（dev_audience の指摘とも一致するはず）は「結局これは何のツールか」が分からないまま数枚を消費し、離脱リスクが最も高いのが冒頭である。

**アウトカム先出し案の優位性**:
1. Hightower 型の「見せてから話す」は技術カンファレンスで再現性の高い手法として広く言及されている（出典上記）。検索画面のスクリーンショット 1 枚（既存インフォグラフィックではなく実プロダクトの UI）を見せれば「star 順ではない並び」が視覚的に一発で伝わり、後続の「なぜ作ったか」の説明が具体物に紐づく。
2. Sparkline の「往復」構造自体は維持する。**フック（what could be の一部を先出し）→ 課題（what is）→ コンセプト/ミッション（what could be の全体像）** という順にすることで、「一瞬見せて → 焦らして → ちゃんと説明する」というテンションを作れる。これは Duarte の主張する「感情的投資」の効果を保ったまま、開発者聴衆の離脱リスクだけを先頭で潰す折衷案である。
3. `docs/project-mission.md` のミッションステートメント（「star の多さでは埋もれてしまう『実際に使われている OSS』を、個人開発者が見つけられるようにする」）は 1 文で強い assertion になる。これをフックの直後、課題スライドの見出しにそのまま使える。

**結論**: 表紙の直後（2 枚目）に検索結果 UI のフックを置き、3 枚目で「なぜ」に巻き戻す。initial-concept.md のビジネスモデル・GTM 部分（マネタイズ・Product Hunt 戦略等）はこのプレゼンの目的（開発者向けの技術解説）に不要なので**載せない**（narrative_design としての判断。project_truth の裏取り対象は「課題認識（star 埋もれ・偽 star 600 万）」と「ミッションステートメント」の 2 点に絞ることを推奨する）。

---

## 2. 争点B: 見出し文の型（assertion 型 vs ラベル型）

### 2.1 結論: assertion 型を既定にする

Alley の比較実験（出典上記）が示す「assertion-evidence 群は理解度・低認知負荷・遅延再生率のすべてで優位」は、**箇条書きに埋もれがちなラベル型見出し（例:「アーキテクチャ」「技術スタック」）に対する assertion 型見出し（例:「層を分離し、Cloudflare 依存をアプリコードから締め出す」）の優位性の一次証拠**であり、本件のように「読めば伝わる」ことを明示的に求められているタスク要件（ユーザー指示 (3)）と直接一致する。

### 2.2 日本語 + 画像焼き込みの制約との突き合わせ

Alley のガイドライン原文は「見出しは 2 行以内、本文は 1 分あたり 20 語まで」（出典: Penn State Assertion-Evidence Slide Structure PDF、上記）。英語の "20 words/minute" は日本語の文字数に単純換算できないため、以下は narrative_design の実務判断:

- **画像に焼き込む見出し**（gpt-image-2 生成分）: 1536×864 の 16:9 に大きめサンセリフで 2 行収めるには、日本語は 1 行あたり 12〜14 字が視認性の上限（英語のワード数基準より厳しく見積もる。日本語は文字密度が高く、太字大サイズだと折り返しが荒れやすいため）。**見出しは 2 行・目安 24 字以内**とする。
- **既存インフォグラフィック流用スライド**（画像は差し替えない）: 見出しはスライド側のテキストレイヤーとして別途載せられるため、画像側の文字数制約を受けない。ここは 32 字程度まで許容してよい。
- **例外的にラベル型を許容するケース**: 争点Aの結論により本構成には目次・章扉スライドを置かないため、**今回の 16 枚に関してはラベル型の出番はない**（全スライド assertion 型で統一）。将来スライドが増えて章扉を追加する場合のみ、章扉は「第2部 アーキテクチャ」のようなラベル型を許容する。

### 2.3 assertion 型見出しの実例（一部・確定ではなく提案）

| # | ラベル型（不採用の例） | assertion 型（採用） | 字数 |
|---|---|---|---|
| 3 | star 埋もれ問題 | star 数では『使われている OSS』は見えない | 20字 |
| 8 | Gem Score 算出ロジック | Gem Score は「被依存数に対する star の残差」で測る | 27字（要短縮） |
| 9 | AI 自律運用体制 | 1 セッション＝1 スプリントで、人が待つ時間をなくす | 25字 |

27 字の例は画像焼き込み枠 24 字をわずかに超えるため、visual_assets との調整が必要（「被依存数比で star が伸びない OSS を測る」等への短縮を検討、20字）。**文字数上限の遵守は最終稿で機械的に数える（Write ツールで焼き込みテキストを確定する前に文字数を数えること）を lead への申し送り事項とする。**

---

## 3. アウトライン7項目 → 16枚のスライド一覧（確定案）

目次スライドを置かない理由: Kawasaki の「1 概念 = 1 枚」思想と、15〜20 分 ÷ 15〜20 枚 ≈ 1 分/枚という密度（dev_audience の定量検証を推奨）を踏まえると、目次 1 枚は情報価値ゼロで時間を消費するだけ。章の切り替えは各スライドの assertion 見出しが担う（前スライドの答えが次の問いになる SCQA 的連結を見出し間で作る）。

| No | タイトル案（assertion型） | 伝えたい1メッセージ | 載せる要素（3〜5点） | 対応アウトライン項目 |
|---|---|---|---|---|
| 1 | gem-hunter（表紙） | プロダクト名とキャッチコピーの提示 | プロダクト名 / 1行キャッチコピー（ミッションステートメントの短縮） / 発表者・日付欄 | — |
| 2 | star 順では出てこない OSS を、検索できる | アウトカムを最速で見せる | 検索結果 UI のスクリーンショットまたは操作 GIF 相当の静止画 / Gem Score バッジの見え方 / 「これが今日話す全て」の一言 | (b) |
| 3 | star 数では『使われている OSS』は見えない | 課題認識（なぜ作ったか、の"なぜ"側） | 偽star約600万個の存在（要project_truthの裏取り）/ マタイ効果の説明図 / 既存ツール比較表（GitHub Search/grep.app/OSS Insight の弱み） | (a) |
| 4 | 被依存数を起点に、注目度ではなく実利用で見つける | ミッションと Hidden Gem の定義 | ミッションステートメント全文引用 / Hidden Gem の定義（被依存数に対するstarの残差）/ インフォグラフィック01 or 02 の一部 | (a) |
| 5 | コンセプトから15本超のドキュメントに分解した | 検討プロセスの全体像 | ドキュメント相関図（インフォグラフィック08）/ 与件↔上乗せの関係（D-2 の1行）/ 主要ドキュメント群の名前列挙 | (c) |
| 6 | 層を分離し、Cloudflare依存をアプリコードから締め出す | アーキテクチャの設計思想 | クリーンアーキテクチャの層構造図（インフォグラフィック07）/ Cache Port の位置づけ（ADR0005 とリンク）/ DBを持たない設計（ADR0007） | (d) |
| 7 | Next.js 16 の App Router を、Cloudflare Workers の上で動かす | 技術スタックの要点と選定理由 | Next.js16/Tailwind v4+shadcn/Cloudflare Workers(@opennextjs/cloudflare)/Vitest4+Playwright の一覧 / なぜこの組み合わせか1行 | (e) |
| 8 | Gem Score は、被依存数に対するstarの伸び残差で測る | 差別化ロジックの中身 | 算出式の概念図（インフォグラフィック10）/ 既存指標（OpenSSF Scorecard等）との使い分け（R-3の判断）/ 実装しなかった代替案（健全性スコア合算を却下したQ-1/Q-2） | (d)寄りだが独立枠推奨 |
| 9 | 1セッション＝1スプリントで、人が待つ時間をなくす | 開発プロセス（自律運用体制） | セッション=スプリントの図（インフォグラフィック13）/ status:in-progressロックの仕組み / 確認が要るのはA-1〜A-6だけ、という境界線 | (f) |
| 10 | テストを先に書き、CIが緑でなければ進めない | TDD・品質担保の実際 | Red→Green→Refactorのサイクル図（インフォグラフィック11）/ Vitest(単体・結合)+Playwright(E2E)の役割分担 / Lighthouse Accessibility=100のゲート | (f) |
| 11 | 実装からマージまで、確認なしで走り切る | PR運用の自律化 | PR作成→セルフレビュー→自動マージのフロー図 / 外部AIレビュアーを廃止し自前レビューに一本化した判断 / サーキットブレーカー(2回超で停止)の安全弁 | (f) |
| 12 | 技術的課題1（タイトルはADR選定後にproject_truthと確定） | 実在したトレードオフ1件目 | 検討した選択肢2つ以上 / 却下した代替案と却下理由 / 決め手になった判断基準 | (g) |
| 13 | 技術的課題2 | 実在したトレードオフ2件目 | 同上 | (g) |
| 14 | 技術的課題3（枠。開発者が最も学べる案件をproject_truthが2〜4件から選定。2件で十分なら14は削り15枚構成にする） | 実在したトレードオフ3件目 | 同上 | (g) |
| 15 | まとめ | 開発者への持ち帰りメッセージ | このプロジェクトから転用できる考え方（例: 被依存数起点の発想/層分離/自律運用の境界線設計）を2〜3点 / 与件遵守という制約の中での工夫 | 全体の統合 |
| 16 | もっと知るには | 参考情報とリポジトリ導線 | GitHubリポジトリURL / 主要ドキュメントへのパス一覧（PRD/ADR/project-mission）/ ライセンス表記の要点 | — |

**枚数の柔軟性**: 12〜14（技術的課題）は project_truth が実在するトレードオフを何件選ぶかで 2〜4 枚に伸縮する（14 が不要なら 15 枚構成、4 件必要なら 17 枚構成まで許容範囲内）。8 番（Gem Score）を6番（アーキテクチャ）に統合して 1 枚削る案も次善として提示するが、Gem Score はこのプロダクトの中核的差別化要素であり dev_audience が最も興味を持つ領域だと想定されるため、**独立枠を維持することを推奨**する（dev_audience の反対尋問を待って最終判断）。

---

## 4. project_truth・visual_assets・dev_audience への申し送り

- project_truth: スライド3の「偽star約600万個」は `initial-concept.md` の記述（「約 600 万個の偽 star が観測されている現状（`competitive-analysis.md`）」と `project-mission.md` に明記）。`competitive-analysis.md` の一次数値を裏取りしてほしい（narrative_designは未読）。
- project_truth: スライド12〜14のADR選定は争点Dの主担当マターなので、narrative_designは枠だけ用意した。
- visual_assets: スライド2の「検索UIスクリーンショット」は実プロダクトのUIキャプチャが理想（新規生成でも実装でもよいが、既存インフォグラフィック13枚には該当する絵がない＝不足分の1枚)。
- visual_assets: 表の27字超過見出し（Gem Score）の短縮案を検討してほしい。
- dev_audience: 1分/枚換算の妥当性（16枚×15〜20分の配分）を定量検証してほしい。

### `workflow_compliance` — 主張
<sub>2026-08-22T09:18:38+09:00</sub>

# 参照ワークフロー準拠検証（workflow_compliance Round 1）

## 1. 15 ステップ × 必達原則準拠表

| ステップ | 必達原則 1～8 準拠可否 | 読み替え / 注釈 |
|---------|-----------------|----------------|
| **Step 1: 構成作成** | ✅ 準拠 | 標準実装。gem-hunter 規約に従う |
| **Step 2: 構成セルフレビュー** | ⚠️ 読み替え要 | **軸2（MECE）必須**。`doc-review` スキル（参照リポ固有 19 行）は gem-hunter に無し。**代替: `discussion-review` スキル** で Lead 1 名 + Reviewer 3 名（MECE / ファクト / 最新情報 軸）で実施 |
| **Step 3: テキスト版作成** | ✅ 準拠 | Write 80 行以内 + Edit 3～4 要素ずつ分割（lessons.md 適用） |
| **Step 4: テキスト版セルフレビュー** | ⚠️ 読み替え要 | **self-reviewer スキル使用** （gem-hunter 標準）。MECE 観点は必須維持 |
| **Step 5: Google Drive アップロード** | ❌ 環境適合不可 | **`gws` コマンド未装備**。**読み替え**: ① ローカル PPTX 生成 → ② PR に添付 → ③ ユーザー確認後 Push。 Google ドライブ共有は別途ユーザー作業 |
| **Step 6: テキスト版ユーザーレビュー** | ✅ 準拠（読み替え） | **PR コメント欄で収集**。明示承認（「OK」）まで Step 7 進行禁止は維持 |
| **Step 7: フィードバック反映** | ✅ 準拠 | PR 修正コミット。Step 6 へ戻るループ維持 |
| **Step 8: テキスト版最終セルフレビュー** | ⚠️ 読み替え要 | **self-reviewer** で再実施。ファクト最新性再確認は必須（必達5） |
| **Step 9: 画像版作成** | ✅ 準拠（必達8 注力） | 「一字一句正確に」指示を必ずプロンプトに含める。lessons.md 実例 3 の分割原則厳守（繰り返し構造は 3～4 要素ずつ） |
| **Step 10: 画像版セルフレビュー** | ⚠️ 読み替え要 | **self-reviewer**。テキスト一字一句照合は手動確認（実例 3 のタイムアウト対策）。MECE・統一性も必須 |
| **Step 11: 画像版アップロード** | ❌ 環境適合不可 | **読み替え**: ローカル PPTX → PR 添付 → ユーザー確認後 Push |
| **Step 12: 画像版ユーザーレビュー** | ✅ 準拠（読み替え） | **PR コメント欄で収集**。明示承認まで Step 13 進行禁止 |
| **Step 13: フィードバック反映** | ✅ 準拠 | PR 修正コミット。手順原則厳守 |
| **Step 14: 画像版最終セルフレビュー** | ⚠️ 読み替え要 | **self-reviewer**。最終性確認 |
| **Step 15: 最終版アップロード** | ❌ 環境適合不可 | **読み替え**: PR マージ後 `content/discussions/project-slides-{YYYYMMDD}/output/` に配置。成果物サマリーをユーザーに報告 |

---

## 2. セルフレビュー実装方法（Step 2 / 4 / 8 / 10 / 14）

本セッションで使用可能な代替戦略：

**Plan A: `discussion-review` スキル（推奨・高確度）**
- Lead 1 名（main セッション）+ Reviewer 3 名（専門軸: MECE / ファクト・最新情報 / テキスト品質）
- Round 1 で指摘収集 → 修正 → Round 2（指摘ゼロ時点で終了）
- **利点**: 必達原則 2（MECE） を複数視点で保証。gem-hunter 既存スキル
- **注意**: タイムアウト対策（長大ドキュメント読み込み禁止・lessons.md 実例 3）

**Plan B: `self-reviewer` スキル（軽量・単独実施）**
- テキスト版時点（Step 4 / 8）での使用
- MECE 観点は手動チェック項目として追加
- **利点**: 速度・既存スキル
- **制限**: 単一視点のため複合指摘の見落としリスク

**推奨運用**:
- Step 2: discussion-review（構成段階での MECE 確保が後続全体に効く）
- Step 4: self-reviewer（必達原則 7 テキスト量チェック中心）
- Step 8: discussion-review（ユーザーフィードバック確実反映確認）
- Step 10: self-reviewer（テキスト一字一句照合はツール不要・目視）
- Step 14: self-reviewer（最終性確認のみ）

---

## 3. ユーザーレビュー実装方法（Step 6 / 12）

`gws` （Google ドライブ）不在のため、代替フロー：

**実装**:
1. ローカル PPTX 生成 → PR に Artifact（HTML preview）または添付で掲載
2. PR コメント欄でフィードバック収集 → 修正 → PR コメントで再共有
3. 明示承認（「OK」/ 「承認」）確認後のみ次ステップへ移行
4. 最終版マージ後、`content/discussions/{project_slug}/output/` に PPTX/画像ファイル配置

**記録媒体**: GitHub PR コメント（公式・監査可能・thread resolve で管理）

---

## 4. `doc-review` スキル代替案

参照リポジトリ `doc-review` は 19 行で 7 軸並列レビュー（Task tool + Explore サブエージェント）を実行。

**gem-hunter での選択肢**:
1. **参照リポの SKILL.md を本リポに複製**（許容・スキル数加算）→ API 統合・新 Task tool 追加コスト
2. **`discussion-review` で代替**（推奨・実績あり）→ Round 制御・手動ブレーキあり・軸数調整可
3. **`code-review` + 手動チェックリスト**（最軽量・テキスト量多時は避ける）

**結論**: **Plan A は `discussion-review` で実施。必達2（MECE）をチェック項目に明示化**。

---

## 5. タイムアウト対策・本セッション実装ガイド

参照リポ `lessons.md` から抽出（実例 1～3）:

| 作業 | 対策 | 本セッション適用 |
|-----|------|-----------------|
| **スクリプト生成** | 骨子 Write 80 行 + Edit 3～4 要素ずつ | Step 3 / 9 で全スクリプトに適用 |
| **大型ドキュメント全読み** | Grep で節特定 → offset/limit ≤150 行 | Step 9 プロンプト読み込み時に適用 |
| **テンプレート挿入** | 丸コピーしない・関数 import or ピンポイント抽出 | Python-pptx module 利用（コピペ防止） |
| **参照リポ資料引用** | 原典ポインタ（URL・取得日）を記録 | 本セッション rules に追加 |

**実装チェックリスト**:
- [ ] スクリプト作成時は 80 行以内単位で Write / Edit を分割
- [ ] プロンプト含む画像生成ループは 3～4 要素ずつ繰り返し
- [ ] lessons.md 実例 3 の再発防止を意識（300 行スクリプト一発 Write の禁止）

---

## 6. 成果物配置案

参照リポ: `events/{日付}/{content, references, scripts, output}/` 構造

**gem-hunter 既存規約** (`find content docs tools`):
- `content/discussions/{discussion_name}/` （議論記録のみ）
- `content/research/` （リサーチ出力）
- `docs/` （仕様・設計・規約）
- `tools/` （スクリプト）

**スライドプロジェクト配置案**（両規約統合）:

```
content/discussions/project-slides-20260822/
  ├ whiteboard.md (discussion 記録)
  ├ output/
  │  ├ slides_content_20260822.md (構成マークダウン)
  │  ├ slides_20260822_text.pptx (テキスト版)
  │  ├ slides_20260822.pptx (画像版)
  │  └ images/ (生成画像・PNG)
  └ references/
     ├ PPTX_TEMPLATE.md (デザイン定義引用・内部参照リンク)
     └ *.md (要件定義・スコープ・ユーザーフィードバック)

tools/project_slides_20260822/ （スクリプト専用）
  ├ generate_text_pptx.py
  ├ generate_image_version.py
  └ run_all_steps.sh
```

**バイナリ（PPTX / PNG）扱い判定**:
- `.gitignore` に明示的な ignore ルールなし → **コミット可能**
- **ただし** `content/analytics/*` / telemetry 系は ignore（本構成に無関係）
- **推奨**: PPTX・PNG は `output/` 配下にコミット（PR で確認可・共有容易）
- **代替**: バイナリサイズ >10MB なら `.gitattributes` で LFS 化（別途要検討）

---

## 7. 実行責務分担（メインセッション vs サブエージェント）

| ロール | 操作 | 実施者 |
|--------|------|-------|
| **オーケストレーター** | ステップ間の進行制御・最終判断・ユーザー報告 | main セッション（本エージェント） |
| **セルフレビュー実行** | discussion-review / self-reviewer 起動・結果統合 | 委譲可（サブエージェント） |
| **コンテンツ作成** | 構成・テキスト・プロンプト・スクリプト実装 | 委譲可（役割分担型 fan-out） |
| **ファクトチェック・最新情報リサーチ** | WebSearch / WebFetch 実行 | main（必達原則 4・5 重視） |
| **ユーザーレビュー管理** | PR コメント監視・明示承認確認・次ステップ判定 | main セッション（A-1 相当） |

---

## 結論

✅ **15 ステップワークフローは gem-hunter で実装可能。必達原則 1～8 はすべて維持される**。

⚠️ **読み替え**: `gws` 環境変数不在 (Step 5/11/15) は PR 配置 + ユーザー作業で吸収。セルフレビューは `discussion-review` / `self-reviewer` で実装。

🔧 **タイムアウト対策**: lessons.md 実例 3（分割 Write/Edit）を全スクリプトに適用。

📁 **配置**: `content/discussions/project-slides-{YYYYMMDD}/output/` に成果物集約。バイナリコミット許容（`.gitignore` 既定）。

### `project_truth` — 主張
<sub>2026-08-22T09:19:16+09:00</sub>

# project_truth R1: 裏取り済み事実の抽出（争点 D・E 主担当）

## 0. 最重要の切り分け（(a) の土台）

**`initial-concept.md`（IndieGems）と `minimum-requirements.md`（与件）は別物であり、後者は前者を「実装対象として大幅に縮小した」もの。**

| | `initial-concept.md`（初期コンセプト・出典: `docs/00_concept/initial-concept.md`） | `minimum-requirements.md`（与件・出典: 同ファイル冒頭注記 + §1.2） |
|---|---|---|
| 位置づけ | 「本プロジェクトの初期コンセプト。以降の要件定義の起点」（原文 L3） | 第三者が作成した外部与件の原文。**著作権は原著者に帰属し MIT License 対象外**（冒頭の権利表示） |
| 内容 | Gem Score（Maintenance+Doc Quality+Dependency Signal 合成）・目的別タグ・週刊ダイジェスト・Freemium 課金・Product Hunt ローンチ等 | GitHub リポジトリ検索 Web アプリ。**認証・お気に入り・独自スコアリング・通知・課金は明示的に「対象外」**（§1.2 スコープ表） |
| 関係 | 与件のスコープ外の構想は「本要件のスコープ外とし、本要件を満たしたうえで段階的に追加する」（`minimum-requirements.md` 冒頭注記、原文ママ） | — |

→ (a) スライドで語るべきは「初期コンセプト（IndieGems）というアイデアがまずあり、それを外部与件（選考課題）の下限要件でくるみ直した」という順序。**「与件ありきではなく初期コンセプトをベースに」という指示は、時系列（初期コンセプトが先）としては正しいが、実装スコープとしては与件が主・初期コンセプトが従（Phase 2 構想）である点を混同しないこと。**

`inception-deck.md` Q1 が明文化: 「主目的＝選考課題/ポートフォリオ。副次目標＝有用であれば運用継続」で、**衝突時は主目的（与件充足）が勝つ**（`D-2`：与件は必達の下限）。商品名も `Q-12` で `IndieGems → gem-hunter` に改称済み（README.md 冒頭に明記）。

---

## 1. (b) 何ができるのか — 実装済み範囲の裏取り

出典: `docs/02_requirements/user-story-map.md` §5（`grep "^#### SP-"` 実行結果）。

| スプリント | 内容（見出し原文） | 状態 |
|---|---|---|
| SP-1〜SP-11 | 検索→一覧→詳細→復帰の基本フロー、URL/ロケール、E2E 回帰、キャッシュ（二重叩き防止）、任意ログイン、失敗時のリカバリ、a11y、README | **`S-1` 完了 = MVP 達成**（SP-11 見出し原文「第三者に引き渡せる（`S-1` 完了 = MVP 達成）」） |
| SP-12 以降 | 「積み上げ（`S-2`）」— 見出しのみで詳細は D-27 により後回し | `D-27` により着手順序変更、`S-2` は `S-3` 完了後 |
| SP-14 | 「キーワードを入力しなくても、その日の Gem が一覧で出る（`S-3`）」＝日次ダイジェスト「今日の Gem」 | 実装済み（[ADR 0014](../adr/0014-zero-query-daily-digest.md) が実装後の改訂を含む） |
| SP-15 | 「ダイジェストの鮮度と出典が保証され、再訪時に前回からの差分がわかる（`S-3`）」 | 実装済み |
| SP-16 | 「キーワード検索の結果も『過小評価度』の順に並べられる（`S-3`）」＝`sort=gem-index` | **実装後に撤去済み**（`D-33`・詳細は §3 争点 D） |

**README.md（実装後の記述・裏取り済み）の技術スタック表**: Next.js 16（App Router）/ Tailwind v4 + shadcn/ui（Radix）/ Cloudflare Workers（`@opennextjs/cloudflare`）/ Vitest 4 + Testing Library + MSW 2（ユニット・結合）/ Playwright + axe（E2E）。

**(b) スライドで「できる」と書いてよいこと**（未実装機能を書かない）:
- キーワードでの GitHub リポジトリ検索・一覧・詳細ページ（独立 URL）・一覧復帰・ページネーション
- 任意 GitHub OAuth ログイン（レート枠切替のみ、機能差なし）
- トップページの「今日の Gem」日次ダイジェスト（日付シードの決定論的生成、`?date=` で日付送り確認可能）
- **書いてはいけないこと**: 「検索結果が Gem Index 順に並ぶ」（`D-33` で撤去済み。今日の Gem ダイジェストのみが Gem Index を使う）

---

## 2. 争点 D: 技術的課題 2〜4 件の選定（判断が割れた・トレードオフが実在したもの）

**選定基準**: ① ADR 内に「却下した選択肢」表があり実際に複数案を比較検討した形跡がある ② 開発者聴衆が自分のプロジェクトでも直面しうる普遍的トレードオフである ③ 数値・実測データで裏取りできる ④ できれば「決定 → 実装 → 実測で覆る/修正する」という一次資料が二重に存在するもの（誠実さ＝失敗も見せる）。以上でADR全15本から次を選出。

### 候補 1（最有力）: ADR 0009 + `D-33`「Gem Index を検索結果ソートには使わない」— 差別化機能を作って、測って、撤去した話

出典: [`docs/adr/0009-hidden-gem-score-definition.md`](../adr/0009-hidden-gem-score-definition.md) §4「却下した選択肢」/ [`docs/02_requirements/open-questions.md`](../02_requirements/open-questions.md) `D-33`（2026-08-21）

- **問題**: 「star 順ではなく実利用に対する過小評価度で並べたい」という差別化機能（Gem Index）を作った。だが候補プールが npm registry 限定・ユニーク **227 リポジトリ**しかなく、検索上位 100 件との一致率は `react`=8 件・`test`=7 件・`cli`=6 件、**非 JS 系の一般語ではほぼ 0 件**（実測値、`D-33` 原文）。
- **判断**: `sort=gem-index` を検索結果から撤去（Gem Index の定義自体・「今日の Gem」ダイジェストは維持）。撤去により 1 検索あたり最大 10 リクエスト逐次取得（体感 3〜8 秒）→常に 1 リクエスト（体感 0.5〜1 秒）に短縮。
- **却下した代替案**: ①「理由入りローディング文言で待たせたまま様子見」— 提案者本人が議論内で撤回 ②「候補プールを 10 万件へ拡大」— 一般語ヒット率は 60〜75% まで改善しうる概算だが、npm 限定である以上非 JS 系には効かず、かつ現行実装（JSON を JS バンドルに import で焼き込む設計）では gzip 後 4〜6MB 級となり Workers Free のバンドル 3MB 上限を圧迫する。
- **なぜ開発者に効くか**: 「作った機能を実測で殺す」という、宣伝スライドでは出しにくいが最も信頼を生む種類の判断。定量データが揃っている。

### 候補 2: ADR 0007「DB を持たない設計原則」

出典: [`docs/adr/0007-no-database-client-side-state.md`](../adr/0007-no-database-client-side-state.md) §2, §6

- **問題**: 検索条件保持・お気に入り・セッション管理などをどこに置くか。
- **判断**: サーバー側永続ストア（DB・KV・Durable Objects）を一切持たない。検索状態は URL、ロケールはパス、セッションは暗号化 httpOnly Cookie、お気に入り等は `localStorage`。唯一の例外は TTL 付き `CachePort`（ADR 0005）。
- **却下した代替案**: 自前 ETL+DB（単一開発者+AI 運用では定常運用コストが見合わない）／お気に入りのサーバー DB 保存（端末間同期の利便性より DB レス原則を優先）／検索条件のサーバーセッション保持（URL の方が共有 URL 再現性で優れる）。
- **なぜ開発者に効くか**: 「サーバーレス × ステートレス設計」の判断軸を明確に言語化しており、他プロジェクトへの転用性が高い。

### 候補 3: ADR 0003「GitHub 認証方式（Fine-grained PAT vs GitHub App）」

出典: [`docs/adr/0003-github-app-authentication.md`](../adr/0003-github-app-authentication.md) §2, §6, §6.1

- **問題**: サーバー側の GitHub API 認証をどうするか。
- **判断**: GitHub App の installation token を採用（Fine-grained PAT・未認証・複数トークンローテーションは却下）。
- **判断軸として特に汎用性が高い**: §6.1 に「次に同じ選定をするときに見る判断軸」6 項目の表（Core枠で足りるか／トークン取得の往復コストを払えるか／有効期限更新を人手でやれるか等）があり、**軸 2・軸 4 が拒否権を持つ**という一般化された結論まで書かれている。ADR 15 本の中で唯一「決定の記録」を超えて「判断のフレームワーク」まで抽出している。
- **なぜ開発者に効くか**: 「このプロジェクト固有の答え」ではなく「次に同じ判断をするときの軸」を提供しており、聴衆が自分のプロジェクトに持ち帰れる。

### 候補 4: ADR 0002「インフラ＝Cloudflare Workers」+ D-31「CI をセッション実行に切り替えた」

出典: [`docs/adr/0002-cloudflare-workers-infrastructure.md`](../adr/0002-cloudflare-workers-infrastructure.md) 決定 #6 の 2 回の追記（`D-23`→`D-31`）

- **問題**: GitHub Actions が使えなくなった（プラットフォーム制限）。
- **判断の変遷**: `D-16` 当初は GitHub Actions + wrangler-action → `D-23`（2026-08-19）で暫定的にセッション実行へ切替（「制限解除後に復帰する」前提）→ `D-31`（2026-08-21）で**復帰ではなく Workers Builds への恒久移行に方針転換**。理由は GitHub Actions 側の制限に加え「クラウドセッションからの `wrangler deploy` が Claude Code の auto mode classifier に**ブロックされることがある**（毎回ではない・実測ブロック 5 回／成功 2 回・非決定的）」という新事実（Issue #288/#300）。
- **なぜ開発者に効くか**: 「AI エージェントで開発する」という開発プロセス自体が技術的制約（CI基盤の可用性）に直接影響する実例。争点 D と E の橋渡しになる。

**推奨（project_truth としての意見）**: 候補 1（Gem Index 撤去）+ 候補 2（DBレス）+ 候補 3（GitHub App認証の判断軸）の 3 件。理由: ①実測データが最も豊富で「誇張なし」を体現できる、②普遍性が高く聴衆が持ち帰れる、③3 件で 15〜20 分の尺に収まる分量。候補 4 は E との重複が大きいため、D では割愛し E 側で軽く触れる案を推奨。

---

## 3. 争点 E: 開発プロセスの「数値で出せるもの / 出せないもの」

### 出せるもの（一次情報で確認済み）

| 主張候補 | 裏取り | 出典 |
|---|---|---|
| ADR（技術的意思決定記録）を 15 本作成した | `ls docs/adr/*.md` = 15 件、実測 | `docs/adr/0001`〜`0015` |
| 1 セッション＝1 スプリントという運用ルールを明文化している | ルール文書として実在（規範であり実績ではない点に注意） | `docs/rules/session-sprint-rules.md` §1 |
| PR 自律運用（実装完了→セルフレビュー→PR→AIレビュー相当→自動マージ）を明文化している | 同上 | `docs/rules/pr-review-flow-summary.md`, `CLAUDE.md` |
| TDD（Red→Green→Refactor）を明文化している | 同上 | `docs/rules/sprint-development-rules.md` §2 |
| Issue/PR 番号が 3 桁後半（300 番台）まで到達している | オーケストレーターの brief に「現在 #363」との記載あり、かつ本セッションで読める直近マージコミットのメッセージにも `#358` `#361` が実在（`git log --oneline` 直近 5 件で確認） | `git log --oneline -5`（本セッションで実行・実測） |
| ユニット/結合・E2E のテストファイルが多数存在する | `find` で `*.test.*` 67 件・`*.spec.*` 21 件（本セッションで実測。node_modules 除外済み） | 本セッション実行結果 |
| `npm run check` を PR 前の唯一の機械的証跡とする運用 | README.md に明記 | `README.md`「開発（ローカル）」節 |

### 出せない／載せてはいけないもの

- 🔴 **「N 倍速くなった」「開発効率が向上した」の類**: 比較対象（人間だけで作った場合の所要時間等）の一次データが存在しない。**裏取り不能。載せない。**
- 🔴 **`git log --oneline | wc -l` = 50 は「全コミット数」として使えない**: 本セッションの clone は **shallow clone**（`git rev-parse --is-shallow-repository` = `true`）であり、50 件は直近の一部のみ。実際に確認できた最古のコミットは 2026-08-20 だが、`lean-canvas.md`/`inception-deck.md` は作成日 2026-08-17 と記載があり、少なくとも 2026-08-17 時点から活動がある（=shallow clone の外側に履歴が存在する）。**この数値をスライドで「総コミット数」として出すと誤りになる。**
- 🔴 **`content/analytics/sprint/`（sp 較正データの実測置き場）は本セッションでは確認できず**（`ls` が NOT FOUND）。`session-sprint-rules.md` / `project-mission.md` が言及する「tokens/sp・分/sp の実測」は**仕組みとして存在するが、本セッションから中身は検証できていない**。「実測データに基づき運用している」と断定するなら、この点を「仕組みはあるが本資料では中身は未検証」と留保するか、載せないこと。
- ⚠️ **「AI エージェントが全工程を自律実行している」という定性的な主張自体は、ルール文書（`CLAUDE.md`・`docs/rules/*.md`）の存在と内容によって裏付けられる**（規範として存在することは事実）が、**「その規範通りに実際に運用され続けている」ことの実績値（成功率・遵守率等）は本セッションでは確認できていない**。載せるなら「〜という運用ルールを整備している（規範）」であって「〜を徹底できている（実績の断定）」にしない。

### E スライドで安全に語れる型

「N 倍速い」ではなく、**「ADR 15 本・shallow でも辿れる PR 履歴が示す通り、意思決定と変更の理由をすべて記録として残す運用にしている」という『説明可能性』の実績**（ADR 15 本・open-questions.md の決定ログという物量は実在証拠）を主役にするのが誠実かつ開発者に効く、というのが project_truth の推奨。

---

## 4. アウトライン (a)〜(g) 各項目の裏取り済み事実（出典付き箇条書き）

### (a) なぜ作ったのか
- 出典: `docs/00_concept/initial-concept.md`（GitHub 検索が star/SEO に支配され「真に役立つが過小評価されたライブラリ」を発見しにくいという課題認識、原題「IndieGems」）
- 出典: `docs/00_concept/lean-canvas.md` §2「P-1: star が利用実績の代理指標として機能していない（約 600 万個の偽 star が観測・He et al. ICSE 2026 引用）」「P-2: `debug_inspector` が 25 star ながら 111,000 超プロジェクトから依存されている実例」— **ここは一次文献・実例で検証済みマーク（✅）が付いている数少ない主張**
- 🔴 **注意**: `P-3`（「学習コストが高いライブラリより自分にちょうどいいものが欲しい」というニーズの強さ）は lean-canvas.md 自身が「⚠️ 未検証（`R-1`・n=0）」と明記。**ここをアウトカムの根拠にしない**（lean-canvas.md 自身の警告）。

### (b) 何ができるのか
- 上記§1 の表を使う。実装済み＝MVP（SP-1〜11）+ 日次ダイジェスト（SP-14/15）。Gem Index の検索結果反映は撤去済み（D-33）なのでできない。

### (c) 検討・ドキュメントの全体像
- 出典: `docs/infographics/README.md` — 既存 13 枚のインフォグラフィックの索引そのものが「どんなドキュメントを作ったか」の一覧になっている（01 初期コンセプト〜13 自律運用ルール体系）。ファイル名: `01-initial-concept.webp`〜`13-ops-rules.webp`（実ファイル存在を `ls` で確認済み）。
- ドキュメント階層: `docs/00_concept/`（コンセプト）→ `docs/02_requirements/`（prd.md が正本、open-questions.md が決定ログ）→ `docs/03_design/`（アーキテクチャ・データモデル・UI/UX・インフラ）→ `docs/04_development/`（テスト戦略）→ `docs/adr/`（15 本）という積み上げ構造が実在する。

### (d) アーキテクチャ / (e) 技術スタック
- README.md「技術スタック」表がそのまま使える（§1 に引用済み）。層と依存規則は `docs/03_design/architecture/application-architecture.md` が正本（未読・visual_assets/narrative_design 側で必要なら別途参照）。

### (f) 開発プロセス
- §3 の「出せるもの」表をそのまま使う。誇張表現は不使用。

### (g) 技術的課題
- §2 の候補 1〜3（推奨）または候補 4 を使う。

---

## 5. 誇張・裏取り不能の検出（横断チェック）

- 「N 倍速い」「劇的に効率化」「完璧な自律運用」の類の定量効果表現 → **一次データなし。使用禁止。**
- 「Gem Index で実際に使われているライブラリが見つかる」という現在形の断定 → **検索結果には反映されていない（D-33 で撤去）。日次ダイジェストのみに限定して書くこと。**
- 「ユーザーの声を反映した」「ユーザーテストで検証」の類 → lean-canvas.md 自身が「顧客セグメント：未検証（n=0・`R-1`）」「一次情報にあたっていない」と明記。**ユーザーリサーチを実施したという書き方は禁止**（初見フィードバック=Issue #308 は「専門チームの議論での初見ユーザーフィードバック 10 件」であり、正式なユーザーリサーチとは性質が異なる。書くなら「初見フィードバックを受けて機能を撤去した」という誠実な書き方に留める）。
- 「100% テストカバレッジ」等の数値 → 本セッションでは未確認・未計測。書かない。

以上。

### `visual_assets` — 主張
<sub>2026-08-22T09:19:58+09:00</sub>

# 争点C: 既存インフォグラフィック13枚の活用マッピングと不足分の設計（visual_assets・R1）

## 0. 結論（先出し）

**条件付き流用**。13枚のうち **7〜8枚は流用可**（役割が「セクションの詳細参照」であることを明示すれば）、**3〜4枚は使わない**（アウトプットの粒度が合わない・内容が重複）。加えて **新規生成は2〜3枚のみ**（quality=medium・1536×864）。全枚新規生成の対案は棄却する（後述）。

## 1. 「既存画像はドキュメント要約であってスライド1枚のメッセージとは粒度が違う」への反論の検討

この指摘は **半分正しい**。13枚を実際に見ると、01/02/03/04 は 1 枚あたり 5〜6 ブロック × 各 2〜4 行 = **テキスト要素 20〜30 個超**（例: 13-ops-rules は title+subtitle+見出し5+箇条書き24+チップ4 で約35要素）。これは「1 スライド 1 メッセージ」どころか「1 画像 1 ドキュメント全体」の情報量であり、素朴にそのまま貼ると SD-3 系の要求（1 枚で伝わる分量）に反する。

しかし **全部を「粒度不一致だから使わない」と切るのは過剰反応**。理由:

- 15〜20 分の開発者向けトークでは、**「詳細は濃いが、この 1 枚は "全体像を見せる" 役割」と割り切ったスライドが 1〜2 枚あってよい**（他カンファレンス LT でもアーキテクチャ図・ADR 一覧などは "見せて 2〜3 点だけ喋る" 使い方が一般的）。この場合の「1 メッセージ」は **画像内テキストではなくスライドのタイトル行**が担う（例: タイトル「設計は 7 層構成 + 10 色トークンで統一した」、画像は根拠として下に添える）。
- 07（デザイン設計）・08（ドキュメント相関図）・09（ADR マップ）・10（Gem Score）・11（テスト戦略）・12（Cloudflare 構成）・13（自律運用ルール体系）は **単一テーマの構造図**であり、01〜04 のような「雑多な属性の寄せ集め」ではない。1 画像 1 テーマなので、タイトルで 1 メッセージ化しやすい。
- 一方 01（初期コンセプト）・02（リーンキャンバス）・03（インセプションデッキ）・05（USM）・06（ロードマップ）は **属性の寄せ集め型 or 内容が他画像と重複**（01と03は「なぜ作ったのか」がほぼ同義、02はビジネス面で開発者聴衆への訴求度が低い）。これらは「使わない」または「1 枚だけ選んで使う」が妥当。

→ 結論: **条件付き流用**（全流用も全却下も採らない）。

## 2. 13枚の判定表（1枚ずつ実見して判定）

| # | ファイル | 内容・粒度 | 判定 | 使う場合の見せ方 |
|---|---|---|---|---|
| 01 | initial-concept | 課題/ターゲット/機能/技術/収益化/GTM/ロードマップ の7ブロック。属性寄せ集め型 | **条件付き**（(a) なぜ作ったのかの1枚として全面貼り可。ただし03と役割が被るのでどちらか1枚に絞る） | 全面貼り＋タイトルで1メッセージ化（例:「star数ではなく実利用度で評価する検索エンジン」） |
| 02 | lean-canvas | ビジネスモデル9象限。開発者聴衆への訴求度が低い | **使わない** | ― |
| 03 | inception-deck | Q1〜Q10グリッド。01と主題重複 | **条件付き**（01と二者択一。Q6/Q7/Q9の技術トレードオフに触れるなら03優先） | 全面貼り、ただしQ1/Q2/Q6/Q9のみ口頭で拾う前提 |
| 04 | prd | 機能/非機能/上乗せ/スコープ外の4象限 | **条件付き**（(c) ドキュメント作成の全体像パートで「要件定義でここまで決めた」の根拠として） | 全面貼り、テキストは口頭で要約 |
| 05 | user-story-map | US-n格子。詳細すぎ、開発者以外には読めない密度 | **条件付き**（(c)または(f)で「34ストーリーをリリース段階×活動で分解した」根拠として1枚だけ） | 全面貼り、格子の細部は読ませず「量と構造」を見せる用途に限定 |
| 06 | roadmap | M-1〜M-6マイルストーン。単一テーマ | **使う** | 全面貼り可（要素数は他より少なく1枚で読める） |
| 07 | design | アーキテクチャ/ドメイン/UIUX/インフラの4層。単一テーマ | **使う**（(d)アーキテクチャ or (e)技術スタック） | 全面貼り |
| 08 | doc-relations | コンセプト→開発→リリースの相関図。単一テーマでフロー型 | **使う**（(c) 全体像の主役画像） | 全面貼り。7項目アウトライン全体の地図として冒頭〜(c)で再利用も検討 |
| 09 | adr-map | ADR14件を6カテゴリに整理。単一テーマだが**却下案が載っていない** | **条件付き**（(g)の導入・全体地図としては使えるが、争点Dで選ぶ2〜4件の「トレードオフ・却下案」詳細は別途テキスト or 新規画像で補う） | 全面貼り＋「この中の2〜4件を深掘りする」という前振りに使う |
| 10 | gem-score | Gem Score算出ロジック。単一テーマでコンパクト | **使う**（(b) 何ができるのか＝アウトカムの核） | 全面貼り。むしろ冒頭(b)の主役候補 |
| 11 | testing-strategy | テストピラミッド+二重ループ。単一テーマ | **使う**（(f) 開発プロセスのTDD部分） | 全面貼り |
| 12 | cloudflare | リクエスト経路+キャッシュ層。単一テーマ | **使う**（(d)/(e)のインフラ部分） | 全面貼り |
| 13 | ops-rules | CP/A/SD framework+開発フロー。単一テーマだが最密（約35要素） | **条件付き**（(f) 開発プロセスの「AI自律運用」パートの根拠画像。ただし要素数が最多なので、口頭は3点程度に絞る前提を明記） | 全面貼り＋「詳細はdocs/rulesを参照」の一言を添える |

**集計**: 使う 6 枚（06/07/08/10/11/12）＋ 条件付き 6 枚（01 or 03 のどちらか1枚・04・05・09・13）＋ 使わない 1 枚（02）。実質採用は **7〜8 枚**。残り 5〜6 枚は「参照はするが画像としては貼らない」（口頭 or 脚注リンクに格下げ）。

## 3. デザイン統一の担保方法

新規生成時は既存 13 枚の実測パレット（ネイビー本文・ティール/コーラル/マスタード/生成り紙地・手書きグラレコ・banner見出し・角丸ボックス）を **`tools/infographic/prompts/*.txt` の STYLE 行をそのままコピー**して使う（`tools/infographic/README.md` の記述通り、STYLE を外すと崩れるため必達）。新規画像もこのファイル群と同じ `build_prompt.py` パイプラインに乗せ、`specs/*.json` に新規セクションを追加する形で一元管理する（プロンプトの手書き独自管理を避ける＝再現性の担保）。

## 4. 不足分の設計（新規生成は最小限＝2〜3枚に絞る提案）

15〜20枚のうち7〜8枚を既存流用でまかなえるため、**新規は「既存でカバーできない単一メッセージ」にのみ充てる**。

| ID | 対応スライド(仮) | 構図 | quality | 理由 |
|---|---|---|---|---|
| new-01 | (b) 何ができるのか（アウトカム）冒頭 | star数偏重の検索結果 vs Gem Hunterの発見結果、を左右比較する2カラム構図。虫眼鏡アイコン+星アイコン(左、グレーアウト)/ダイヤアイコン+上矢印アイコン(右、コーラルで強調)。**実際のUIモックムを描かせない**（実装済みUIと乖離すると誤解を招くため。プロダクトのUIスクリーンショットが撮れるならそちらを優先し、この新規画像は「概念図」に限定する） | medium | 10枚のGem Scoreと対で使う導入カット。テキスト要素は10個以内(タイトル+左右ラベル2+アイコン説明なし) |
| new-02 | (g) 技術的課題スライドの主役画像 | project_truthが選ぶADR 2〜4件を「採用案 vs 却下案」の対比ボックスで並べる。左=却下(グレー・バツ印アイコン)、右=採用(コーラル・チェック印アイコン)、中央に判断根拠を1行ずつ | medium | 09(ADRマップ)には却下案が無く争点Dの要求（却下案まで載せる）を満たせない。09で「地図」を見せた後、この1枚で「深掘り」する2段構成 |
| new-03（任意） | 表紙 or 締めスライド | プロジェクト名+一言キャッチのみのタイトルカード。ロゴ的にダイヤ+虫眼鏡のシンプルアイコン1つ+バナー見出し | medium（要素数が少ないためlowでも可だが、他画像とのトーン統一を優先しmediumに揃える） | 表紙の第一印象がグラレコと乖離しないようにする。無くても成立するので優先度は最低 |

**却下した対案**: 「不足分を全部 high quality で生成する」→ 却下（後述コスト試算の通り、medium で日本語一字一句が正確に描画できることは README に実測記録がある。high は 4x のコストに見合う画質差がこの用途では不要）。「不足分を作らずテキストのみで済ませる」→ 一部却下（new-02 の"採用 vs 却下"対比は既存画像に無い情報でありテキストのみだと訴求力が落ちるため画像化を推奨。new-01/03 はテキストのみへの格下げも許容範囲＝open_questionsで確認）。

## 5. quality と生成枚数の判断根拠（コスト概算つき）

**既定は medium**。根拠:

1. **実測データ（本リポジトリの一次情報）**: `tools/infographic/README.md` に 1536×864 実測の画像出力トークンが記載済み — `low` 120 / `medium` 1,078 / `high` 4,312 トークン。単価 $30/1M トークンで **1枚あたり概算 $0.004 / $0.032 / $0.129**。同READMEは「日本語テキストは medium で一字一句正確に描画できることを実測で確認済み」と明記しており、既存13枚も全て medium で生成・検証済み。
2. **外部一次情報での裏取り**（2026-08-22 時点、WebSearch経由）: OpenAI の gpt-image-2 は画像出力トークン **$30.00 / 1M tokens**、テキスト入力トークン **$5.00 / 1M tokens**（出典: [OpenRouter GPT Image 2 pricing](https://openrouter.ai/openai/gpt-image-2) がOpenAI公表値を集約。OpenAI公式 `openai.com/api/pricing` は本セッションから403で直接取得不可だったため、集約サイトの記載値と本リポジトリの `tools/infographic/generate.py` に実装済みの単価定数 `PRICE_OUT_PER_TOKEN = 30.0/1_000_000` が一致することで裏取りとする）。
3. **新規2〜3枚 × medium**: 概算 **$0.06〜$0.10**（3枚なら約$0.10）。high にしても $0.26〜$0.39 で絶対額としては誤差だが、**既存13枚との視覚的トーン統一（同じ品質設定で生成した方が線の太さ・ノイズ感が揃う）を優先し medium に統一**する方が合理的。low は文字が潰れるリスクがありテキスト正確性必達原則に反するため不採用。

**結論**: 新規画像は **2〜3枚・quality=medium・1536×864**。合計追加コストは $0.10 未満で無視できる水準。

## 6. 一字一句制約下でのテキスト量の実務上限（既存13枚から逆算）

実見した13枚のうち、最も要素数が多い 13-ops-rules（約35要素: title+subtitle+見出し5+箇条書き24+チップ4）でも文字は崩れず判読できていた。一方これは「ドキュメント要約」用途の上限であり、**スライド単体で"1メッセージ"を成立させたい新規画像（new-01/new-02）にはもっと絞るべき**。

- **ドキュメント参照用（既存流用画像の水準）**: 見出し4〜6ブロック × 箇条書き3〜6行 = 合計 **20〜35 テキスト要素**まで medium で実用上限（既存13枚が実証済み）。
- **スライド専用の新規1枚1メッセージ画像（new-01/new-02 で採用する上限）**: タイトル1 + サブタイトル/ラベル2〜4 + 対比ボックスの短句 4〜8 = 合計 **10〜14 テキスト要素以内**。日本語1行あたりは体感 8〜16 文字（既存画像の箇条書き実測が概ねこの範囲）。この上限を超えると「1メッセージ」性が薄れ01/02/03と同じ"寄せ集め化"を招くため、new-01/new-02のText elementsリストはこの範囲で確定させる。

以上、争点Cの主担当としての結論。争点B（見出し文の型）・争点D（ADR選定）の決定を受けて new-02 のText elementsは具体化する。
