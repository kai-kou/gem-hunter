<!--entry
author: fact_check
round: 1
kind: claim
ts: 2026-08-23T14:22:59+09:00
-->

## fact_check（事実整合レンズ）

| 項目 | README の記述 | 実態 | 判定 | 根拠 |
|---|---|---|---|---|
| 機械検査本数 | 「機械検査 30 本超」 | `run_check`/`run_check_timeout` の呼び出しは 38 件（Lint/型/vitest/E2E/Lighthouse を含む） | ✅ | `tools/run_checks.sh:92,99,106,130,147,152〜381`（計 38 呼び出し） |
| Lighthouse gate | 「Lighthouse の Accessibility スコア 100 を満たさなければ落ちるゲート」 | コメントで明記「Accessibility = 100 は blocking、Performance は記録のみ」 | ✅ | `tools/run_checks.sh:134` |
| 本番デプロイ一次経路 | 「Workers Builds が main push を拾ってビルド・デプロイ、ゲートが閉じていた分は再試行されないため trigger_workers_build.py で明示再ビルド」 | docstring に同内容: 「Workers Builds は push のたびに正常に発火」「ゲートが後で開いても再ビルドする経路が存在しない」ため本 CLI を用意 | ✅ | `tools/trigger_workers_build.py:5-15` |
| §8.2 の存在 | 「手順とゲート判定の正本は cloudflare-infrastructure.md §8.2」 | §8.2「セッション実行の手順」が実在（§8.2.1〜8.2.4 も存在） | ✅ | `docs/03_design/infrastructure/cloudflare-infrastructure.md:555` |
| ErrorKind 7 種 | 「network / rateLimitPrimary / rateLimitSecondary / auth / validation / notFound / upstream の 7 種」 | 型定義が完全一致（7 種） | ✅ | `src/domain/errors.ts:6-20` |
| 秘匿情報の読み手 | 「秘匿情報は src/infrastructure/ 配下だけが読む」 | `session-cookie.ts`（SESSION_ENCRYPTION_KEY）/ `oauth.ts`（GITHUB_OAUTH_*）/ `installation-token.ts`（GITHUB_APP_*）がいずれも infrastructure 配下 | ✅ | `src/infrastructure/platform/session-cookie.ts:41` / `src/infrastructure/github/oauth.ts:24-26` / `src/infrastructure/github/installation-token.ts:35-37` |
| 非秘匿 2 変数の読み手 | 「RATE_LIMIT_SALT / SITE_URL は src/composition が読む」 | `rate-limit.ts:59` が `RATE_LIMIT_SALT`、`site-url.ts:28` が `SITE_URL` を読む | ✅ | `src/composition/rate-limit.ts:59` / `src/composition/site-url.ts:28` |
| SITE_URL 空文字で起動失敗 | 「空文字（SITE_URL=）を設定すると起動に失敗する（`??` は空文字に効かず `new URL('')` が TypeError）」 | `getSiteUrl()` は `process.env.SITE_URL ?? DEFAULT` で null/undefined のみ捕捉。空文字はそのまま通り `app/[locale]/layout.tsx:23` の `new URL(getSiteUrl())` が空文字を渡されて例外になる | ✅ | `src/composition/site-url.ts:28` / `app/[locale]/layout.tsx:23` |
| RATE_LIMIT_SALT 未設定時の挙動差 | 「ローカルは binding 無しで完全無音、Workers 上で binding があり salt だけ無いと警告ログ 1 行」 | binding 無し（ローカル）は 2. で早期 return（無音）。binding ありで salt 無しのときだけ `warn(...)` を 1 回呼ぶ | ✅ | `src/composition/rate-limit.ts:46-64` |
| npm スクリプト追加分 | build / start / test:watch / format:check / preview:build / preview:upload / deploy / cf-typegen | package.json に全て存在し、`deploy` は `--tag "$SHA"` でコミット SHA を付与 | ✅ | `package.json:12,13,16,18,21-25` |
| デプロイ経路表（LP=gh-pages） | 「site/ を gh-pages ブランチのルートへ同期して GitHub Pages で配信」 | D-35 に完全一致する記述あり | ✅ | `docs/02_requirements/open-questions.md:346`（D-35） |
| next/image 不使用・sサイズ | 「一覧は `?s=80`、詳細は `?s=128`」 | `repository-list.tsx:117` が `?s=80`、`repository-detail.tsx:67` が `?s=128`。`loading="lazy"` も一覧側に実在 | ✅ | `src/ui/repository-list.tsx:117,72,124` / `src/ui/repository-detail.tsx:67` |
| Hidden Gem 数値の書き分け | 「62,483 は D-37 適用後の配信プール実数。D-36 の実測母集団は約 11 万リポジトリ」 | D-36 実測「ユニーク 109,469 リポジトリ」（≒ 約 11 万）。D-37 の `minStars=5`・全帯フィルタ後の最終プールが 62,483 件（本文中に明記） | ✅ | `docs/02_requirements/open-questions.md:347`（D-36）/ `:348`（D-37） |
| index.json の totalCount | 62,483 / 12 レジストリ | `public/data/gem-index/index.json` の `totalCount=62483`、`shards` 配列は 12 要素 | ✅ | `public/data/gem-index/index.json`（`totalCount`, `shards`） |
| レジストリ別 15,000 件固定枠 | 「各レジストリから被依存数降順で同数（既定 15,000 件）を取る固定枠」 | D-37 本文に一言一句近い記述（「各レジストリから被依存数降順で同数（既定 15,000 件）を取る固定枠」） | ✅ | `docs/02_requirements/open-questions.md:348`（D-37） |
| ADR 0007 / DB を持たない | サーバー側永続ストアなし・状態は URL/Cookie/localStorage | ADR 0007 が「サーバー側ストアを持たない」「状態を要素ごとに振り分け」を決定として明記 | ✅ | `docs/adr/0007-no-database-client-side-state.md:1-13` |
| ADR 0011 / next-intl 不採用・middleware 不使用 | 「i18n は next.config.ts の redirects() で既定ロケールへ送る（middleware を使わない）」 | `next.config.ts:24` に `redirects()` あり、`middleware.ts` はリポジトリに存在しない。ADR 0011 が proxy.ts 不採用の経緯を記録 | ✅ | `next.config.ts:24` / `docs/adr/0011-i18n-routing-and-default-locale.md:1-17` |
| ADR 0004 の 2026-08-23 追記 | 「GitHub Actions が起動できない（D-23）ためテストゲートはセッションが main HEAD で npm run check を再実行する形で運用。§8.2 が正本」 | D-23 が GitHub Actions 制限とセッション実行切替の根拠と一致。§8.2 が「セッション実行の手順」として実在 | ✅ | `docs/02_requirements/open-questions.md:333`（D-23）/ `docs/03_design/infrastructure/cloudflare-infrastructure.md:555` |

検証項目はすべて実ファイル・実データと一致した（❌ 0 件）。今回の改稿で新しく書かれた数値・引用ファイルパス・コード参照に事実誤りは見つからなかった。
