<!-- discussion_whiteboard:auto -->
# 🧑‍🏫 議論ホワイトボード: README 改稿（最新性の事実確認・与件 §6 充足・スマホ実画面の掲載）をマージ前に敵対的相互レビューで検証する

- 議題ID: `readme_refresh_20260823`
- 論点: Issue #513 / ブランチ claude/readme-documentation-update-hrjq14 の差分に対するマージ前ゲート。争点 A 与件 §6 充足 / B 事実整合 / C 認証の再混入 / D 画面キャプチャの採否と量 / E 構成と分量 / F スコープ。
- 参加者: `req_gate`, `fact_check`, `scope_guard`, `reader_flow`
- 投稿数: 10
- 更新: 2026-08-23T14:30:24+09:00

> このファイルは `tools/discussion_whiteboard.py render` が自動生成する。直接編集せず `post` で追記すること（同時書き込み破損防止）。

## ラウンド 1

### `req_gate` — 主張
<sub>2026-08-23T14:22:39+09:00</sub>

## req_gate（与件・要件充足レンズ）

### 充足していると判定した項目

- **NFR-29（セットアップ手順）**: 充足。README.md:26-50「開発（ローカル）」に依存インストール（`npm ci`）・起動（`npm run dev`/`build`/`start`）・テスト（`npm test`/`test:watch`/`test:e2e`）・環境変数節（README.md:68-86）が揃っている。今回追加された `preview:*` / `deploy` / `cf-typegen` はアプリ本体のセットアップに必須ではないが「別系統」と明示区分されており（README.md:43）、必須手順との混同はない。
- **NFR-30（設計上の判断・工夫した点）**: 充足。8 項目すべてに ADR または `D-n`（決定ログ）へのリンクがあり、採用理由だけでなく却下した代替案も明記されている（例: README.md:117「作らないことにした」、README.md:168-169 の `next-intl` 不採用の経緯）。PRD `NFR-30`（「採った選択と捨てた代替案を、根拠の ADR へのリンクとあわせて書く」）の書式要件を満たす。
- **NFR-31（AI 利用範囲）**: 充足。今回の差分では変更していないが、既存の「AI を利用した範囲と方法」節（README.md:184-193）は独立節としてスプリント自走・TDD・セルフレビュー・人間判断領域・定期運用の 5 点をカバーしており与件 §6 の 3 点目を満たす。
- **NFR-32（ADR 記録）/ 与件 §7 チェックリスト最終項目**: 充足。ADR 索引表（README.md:199-215）は 15 本を機械検証済み（`check_adr_coverage.py` PASS、brief 記載の事前確認済み事実）。

### 指摘

1. **[major]** README「設計上の判断」節（README.md:142-148「Gem Index の母集団と配信の作り方」）と、直前の「Hidden Gem を...と定義した」節（README.md:115-122）が、母集団拡大の同一事実（約 11 万リポジトリ → 汚染フィルタ + repo 単位 dedupe 後 62,483）をほぼ同じ言葉で二重に説明している。1 箇所（Hidden Gem 節）に統合し、レジストリ別成層化・シャード配信という実装寄りの内容は ADR 0009 §2.3 か新設 ADR への参照 1 行に格下げすることを要求する。二重記述はどちらか一方だけが将来更新され食い違う desync リスクを生む。
2. **[major]** ADR 0014（キーワード非依存の発見面＝「今日の Gem」を日次の有限ダイジェストとして実装する判断）が「設計上の判断」節に一切現れない。「今日の Gem」は README 冒頭（README.md:5）で最初に紹介される目玉機能であり、`AR-9`/`AR-10`/`AC-9` に対応する PRD §12 記録済み ADR でもあるのに、ADR 索引表（README.md:214）に埋もれたままで「なぜ有限件数か・なぜ無限フィードや通知を採らなかったか」という工夫が本文で語られていない。他 6 項目（i18n 自前実装・a11y ゲート限定・next/image 不使用等）と同格の 1 節として昇格を要求する。
3. **[minor]** ADR 0008（`FR-7` でページネーションを選び無限スクロールを採らない判断）は与件 §3 が明示的に二者択一を課した MVP 必須要件であり、`AC-7` 対応の ADR でもあるが、本文には現れず ADR 索引のみ。i18n・エラー分類と同格の「こだわり」候補であり、余裕があれば 1 段落の昇格を提案する（必須ではない）。
4. **[minor]** 「Gem Index の母集団と配信の作り方」節（README.md:148「レジストリごとに分割した静的 JSON を isolate の cold start で並列取得...」）はエラー分類節（README.md:136 `src/domain/errors.ts` へのリンクあり）と異なり、実装ファイルへのリンクが無く「書いてあるが第三者に追えない」。`src/infrastructure/platform/static-gem-index.ts`（存在確認済み）等への参照を足すべき。指摘 1 で節を圧縮する際にあわせて解消できる。

### 補足（本 PR のスコープ外・参考情報）

- `docs/02_requirements/minimum-requirements-checklist.md:137`（D-2 行）が「設計上の判断」節を旧版の 2 項目（Hidden Gem / D-21）のまま記述しており、今回の 8 項目化に追従していない。README 自体の充足性には影響しないが、checklist 側の desync として別 Issue 候補。同 checklist:136（D-1 行）の「8 変数」という記載も、環境変数表が 9 行になった現状と食い違っている（README 側は正しい）。

指摘件数: critical 0 / major 2 / minor 2

### `fact_check` — 主張
<sub>2026-08-23T14:22:59+09:00</sub>

## fact_check（事実整合レンズ）

| 項目 | README の記述 | 実態 | 判定 | 根拠 |
|---|---|---|---|---|
| 機械検査本数 | 「機械検査 30 本超」 | `run_check`/`run_check_timeout` の呼び出しは 38 件（Lint/型/vitest/E2E/Lighthouse を含む） | ✅ | `tools/run_checks.sh:92,99,106,130,147,152〜381`（計 38 呼び出し） |
| Lighthouse gate | 「Lighthouse の Accessibility スコア 100 を満たさなければ落ちるゲート」 | コメントで明記「Accessibility = 100 は blocking、Performance は記録のみ」 | ✅ | `tools/run_checks.sh:134` |
| 本番デプロイ一次経路 | 「Workers Builds が main push を拾ってビルド・デプロイ、ゲートが閉じていた分は再試行されないため trigger_workers_build.py で明示再ビルド」 | docstring に同内容: 「Workers Builds は push のたびに正常に発火」「ゲートが後で開いても再ビルドする経路が存在しない」ため本 CLI を用意 | ✅ | `tools/trigger_workers_build.py:5-15` |
| §8.2 の存在 | 「手順とゲート判定の正本は cloudflare-infrastructure.md §8.2」 | §8.2「セッション実行の手順」が実在（§8.2.1〜8.2.4 も存在） | ✅ | `docs/03_design/infrastructure/cloudflare-infrastructure.md:555` |
| ErrorKind 7 種 | 「network / rateLimitPrimary / rateLimitSecondary / auth / validation / notFound / upstream の 7 種」 | 型定義が完全一致（7 種） | ✅ | `src/domain/errors.ts:6-20` |
| 秘匿情報の読み手 | 「秘匿情報は src/infrastructure/ 配下だけが読む」 | `session-cookie.ts`（SESSION_ENCRYPTION_KEY）/ `oauth.ts`（GITHUB_OAUTH_*）/ `installation-token.ts`（GITHUB_APP_*）がいずれも infrastructure 配下 | ✅ | `src/infrastructure/platform/session-cookie.ts:41` / `src/infrastructure/github/oauth.ts:24-26` / `src/infrastructure/github/installation-token.ts:35-37` |
| 非秘匿 2 変数の読み手 | 「RATE_LIMIT_SALT / SITE_URL は src/composition が読む」 | `rate-limit.ts:59` が `RATE_LIMIT_SALT`、`site-url.ts:28` が `SITE_URL` を読む | ✅ | `src/composition/rate-limit.ts:59` / `src/composition/site-url.ts:28` |
| SITE_URL 空文字で起動失敗 | 「空文字（SITE_URL=）を設定すると起動に失敗する（`??` は空文字に効かず `new URL('')` が TypeError）」 | `getSiteUrl()` は `process.env.SITE_URL ?? DEFAULT` で null/undefined のみ捕捉。空文字はそのまま通り `app/[locale]/layout.tsx:23` の `new URL(getSiteUrl())` が空文字を渡されて例外になる | ✅ | `src/composition/site-url.ts:28` / `app/[locale]/layout.tsx:23` |
| RATE_LIMIT_SALT 未設定時の挙動差 | 「ローカルは binding 無しで完全無音、Workers 上で binding があり salt だけ無いと警告ログ 1 行」 | binding 無し（ローカル）は 2. で早期 return（無音）。binding ありで salt 無しのときだけ `warn(...)` を 1 回呼ぶ | ✅ | `src/composition/rate-limit.ts:46-64` |
| npm スクリプト追加分 | build / start / test:watch / format:check / preview:build / preview:upload / deploy / cf-typegen | package.json に全て存在し、`deploy` は `--tag "$SHA"` でコミット SHA を付与 | ✅ | `package.json:12,13,16,18,21-25` |
| デプロイ経路表（LP=gh-pages） | 「site/ を gh-pages ブランチのルートへ同期して GitHub Pages で配信」 | D-35 に完全一致する記述あり | ✅ | `docs/02_requirements/open-questions.md:346`（D-35） |
| next/image 不使用・sサイズ | 「一覧は `?s=80`、詳細は `?s=128`」 | `repository-list.tsx:117` が `?s=80`、`repository-detail.tsx:67` が `?s=128`。`loading="lazy"` も一覧側に実在 | ✅ | `src/ui/repository-list.tsx:117,72,124` / `src/ui/repository-detail.tsx:67` |
| Hidden Gem 数値の書き分け | 「62,483 は D-37 適用後の配信プール実数。D-36 の実測母集団は約 11 万リポジトリ」 | D-36 実測「ユニーク 109,469 リポジトリ」（≒ 約 11 万）。D-37 の `minStars=5`・全帯フィルタ後の最終プールが 62,483 件（本文中に明記） | ✅ | `docs/02_requirements/open-questions.md:347`（D-36）/ `:348`（D-37） |
| index.json の totalCount | 62,483 / 12 レジストリ | `public/data/gem-index/index.json` の `totalCount=62483`、`shards` 配列は 12 要素 | ✅ | `public/data/gem-index/index.json`（`totalCount`, `shards`） |
| レジストリ別 15,000 件固定枠 | 「各レジストリから被依存数降順で同数（既定 15,000 件）を取る固定枠」 | D-37 本文に一言一句近い記述（「各レジストリから被依存数降順で同数（既定 15,000 件）を取る固定枠」） | ✅ | `docs/02_requirements/open-questions.md:348`（D-37） |
| ADR 0007 / DB を持たない | サーバー側永続ストアなし・状態は URL/Cookie/localStorage | ADR 0007 が「サーバー側ストアを持たない」「状態を要素ごとに振り分け」を決定として明記 | ✅ | `docs/adr/0007-no-database-client-side-state.md:1-13` |
| ADR 0011 / next-intl 不採用・middleware 不使用 | 「i18n は next.config.ts の redirects() で既定ロケールへ送る（middleware を使わない）」 | `next.config.ts:24` に `redirects()` あり、`middleware.ts` はリポジトリに存在しない。ADR 0011 が proxy.ts 不採用の経緯を記録 | ✅ | `next.config.ts:24` / `docs/adr/0011-i18n-routing-and-default-locale.md:1-17` |
| ADR 0004 の 2026-08-23 追記 | 「GitHub Actions が起動できない（D-23）ためテストゲートはセッションが main HEAD で npm run check を再実行する形で運用。§8.2 が正本」 | D-23 が GitHub Actions 制限とセッション実行切替の根拠と一致。§8.2 が「セッション実行の手順」として実在 | ✅ | `docs/02_requirements/open-questions.md:333`（D-23）/ `docs/03_design/infrastructure/cloudflare-infrastructure.md:555` |

検証項目はすべて実ファイル・実データと一致した（❌ 0 件）。今回の改稿で新しく書かれた数値・引用ファイルパス・コード参照に事実誤りは見つからなかった。

### `scope_guard` — 主張
<sub>2026-08-23T14:24:03+09:00</sub>

## scope_guard（スコープ・整合レンズ）

### ① 認証の再混入判定（差分ベース）

前提事実: `git diff origin/main...HEAD -- README.md` を確認したところ、環境変数表の OAuth 4 行（`GITHUB_OAUTH_CLIENT_ID`/`_SECRET`/`_CALLBACK_URL`/`SESSION_ENCRYPTION_KEY`）・その直後の「🔵 公開中の本番環境には…」1 行・ADR 索引表の `0012` 行は、**今回の改稿でいずれも 1 文字も変更されていない**（diff の context 行のまま、+/- が付いていない）。PR #469（`22828ca`）は「与件が対象外とした認証を上乗せした理由」節（`AR-5`）だけを削除し、D-39（`open-questions.md:350`）の反映先も明示的に「環境変数表の直後に本番の現況 1 行を残す」としている。

1. **環境変数表の 4 行 → 残す** [critical 判定・根拠あり]
   理由: D-39 自身がこれを残す前提で決定済み（`docs/02_requirements/open-questions.md:350`）。ユーザーの禁止対象は「過去に対応した認証の"上乗せ理由"の記載」（README「設計上の判断」に置かれていた正当化ロジック）であり、`.env.example` と 1:1 対応するセットアップ手順の事実列挙はこれに当たらない。消すと与件 §6 の「必要な環境変数」の完全性が崩れる。
2. **「🔵 公開中の本番環境には…現在ログイン導線は表示されない」1 行 → 残す** [critical 判定・根拠あり]
   理由: D-39 が明記した反映先そのもの。`site/README.md:52-53` にも同じ思想（「本番で無効化されている機能も『使える』と書かない」「いま観測できる事実だけを書く」）がプロジェクト規約として存在し、この 1 行はその規約に沿った「事実の開示」であって「上乗せの正当化」ではない。整合的。
3. **ADR 索引表の `0012` 行 → 残す（消すと機械ゲートが壊れる）** [critical 判定・根拠あり]
   理由: `tools/check_adr_coverage.py:93-98`（`check_readme_lists_all_adrs`）は「`docs/adr/` に存在する ADR が README の ADR 一覧から漏れていないこと」を検査し、漏れがあれば `NFR-32` 違反として `npm run check` を FAIL させる。`docs/adr/0012-optional-github-oauth.md` は実在するため、この行を消すと機械ゲートを壊す。D-39 も ADR 索引の削除は指示していない（反映先に含まれていない）。

**結論**: 今回の改稿は認証の記載を**一切増減させていない**（3 項目とも無変更）。ユーザー制約への抵触なし。トレードオフ（残すと使えない機能の説明が残る）は D-39 が既に検討・採用済みの帰結であり、本 PR の論点ではない。

### ② 他ドキュメントとの新規矛盾・SSOT 重複

- **[major] `minimum-requirements-checklist.md:128,176` vs 実際の `.github/workflows/`**: チェックリストは「`.github/workflows/` は存在しない（`D-23` により撤去済み）」と書いているが、`ls .github/workflows/` で `gem-pool-refresh.yml` が実在することを確認した（`D-40`・2026-08-23 付）。README の今回の改稿はこのワークフローの存在を前提にした記述（「GitHub Actions は Gem 候補プールの日次実行 + 週次反映にのみ使う」）を新規に追加しており、これはチェックリストの記述と食い違う。ただし **この齟齬はチェックリスト側の陳腐化であり README 側の誤りではない**（README の新記述は fact_check 確認済みの事実と一致）。spec の争点 F が挙げる既知候補と一致（roadmap.md の M-6 も同種）。→ **本 PR のスコープ外**（対象は README.md + ADR 0004 + docs/images/readme/ のみ）。別 Issue で `minimum-requirements-checklist.md` の `D-23`/`D-40` 追従を起票すべき。
- **[minor] `minimum-requirements-checklist.md:173` の「全 20 チェック」 vs README 新記述の「機械検査 30 本超」**: 数値が食い違う。README の数字が今回新規追加のため、fact_check に実数の裏取りを依頼したい（`tools/run_checks.sh` 内のチェック本数を機械的に数える）。事実なら checklist 側が古いだけで README 側は正しい可能性が高い → いずれにせよ checklist 修正は別 Issue（スコープ外）。
- それ以外（`docs/README.md` / `open-questions.md` / `site/README.md` / `NOTICE` / `LICENSE`）は今回の改稿と衝突する記述を確認できなかった。特に `site/README.md:52-53` の LP 側ポリシーは README の OAuth 非表示記述と整合している（新規矛盾なし）。

### ③ 相対リンクの実在確認

改稿後の README の相対リンク（`./docs/...` 等、アンカー部分を除いて実在確認）を機械チェックしたところ、**リンク切れは 0 件**（65 本中 65 本すべて実在。画像 4 枚の `src` も含む）。spec の事前確認（34 本・リンク切れゼロ）と矛盾しない（改稿で新規リンクが増えて 65 本になった）。

### ④ ADR 0004 への 1 行追記の妥当性

`git diff origin/main...HEAD -- docs/adr/0004-release-cycle-trunk-based.md` は「代償 | 緩和策」表の 1 セルに **2026-08-23 追記** を 1 文追加するだけ（既存文は変更なし・追記のみ）。内容は「GitHub Actions が起動できないため、このテストゲートは CI ではなくセッションが `main` HEAD で `npm run check` を再実行する形で運用している」。

**判定: スコープ内（README の記述の裏付けとして必要な矛盾解消）**。理由:
- 今回の README 改稿は「リリースサイクル: 常設の dev 環境を持たない（`D-21`）」節の本文を「CI ゲート」→「セッションが実行するゲート」へ書き換えている（`README.md` 該当箇所の diff で確認）。これは ADR 0004 の同じ「代償/緩和策」表の記述（「`main` マージ後のテストゲート」＝ CI 前提の書きぶりのまま）と食い違う状態を放置すると、README と ADR 0004 が同一事象について異なる実装（CI vs セッション実行）を主張したままになる。
- `docs/rules/sprint-development-rules.md` `SD-4` は「ドキュメント間が矛盾していたら権威順で解決し、矛盾自体を修正する（放置しない）」と明記。本追記は権威順（現行コード実態 = セッション実行）に沿った是正であり、README 側の書き換えが原因で新たに露呈した矛盾を同じ PR で埋めるものなので、`core-principles.md` CP-1 の「自分の変更が作ったコードパス上の壊れは即修正する」に相当する（無関係な既存の壊れを持ち込む＝ NG のケースには当たらない）。
- 変更が **1 行の追記のみ**（既存の決定文・却下案は一切書き換えていない）で、影響範囲が README の当該記述と完全に一致する主題に閉じている点も、スコープ逸脱でないことを裏付ける。

以上より、争点 C は「3 項目とも現状維持（残す）」、争点 F の ADR 0004 追記は「スコープ内」、`.github/workflows` 齟齬・チェック本数齟齬は「別 Issue（out_of_scope）」を推奨する。

### `reader_flow` — 主張
<sub>2026-08-23T14:24:23+09:00</sub>

## reader_flow（読者導線・ビジュアルレンズ）

### ① 新設『画面』節（4 枚を実際に Read して確認）

- **mobile-digest.webp**: ヘッダー（ロゴ + 日本語/English 切替）→ 石の山から青い宝石を虫眼鏡で見つけるイラスト（縦約 370px、全体 2392px の約 15%）→ キャッチコピー → 検索窓 → 「今日の Gem」見出し・説明文 → 1〜5 位のカード（利用パッケージ数・star 数）→ 末尾に Ecosyste.ms の出典と「2026/08/22 15:04 JST 頃」の注記。
- **mobile-search.webp**: ヘッダー → 検索窓（`yaml` 入力済み）→ 並び替え/表示件数チップ → 「61,909 件中 20 件を表示」→ Gem バッジの説明文 → 「Gem 候補を一覧で見る」リンク → 結果カード 6 件（go-yaml/yaml, jbeder/yaml-cpp, aaubry/YamlDotNet, symfony/yaml, eemeli/yaml, nodeca/js-yaml）。**jbeder・aaubry・eemeli は実写顔写真のアバター**が写り込んでいる。
- **mobile-gems.webp**: ヘッダー → 「「yaml」の Gem」見出し・74 件 → カード多数（pub.dev/npmjs.org/rubygems.org/packagist.org/pypi.org/hex.pm/proxy.golang.org など多レジストリ）。**alexei-sintotski・ota-meshi・j13k・sebastiandedeyne も実写顔写真のアバター**で、氏名（GitHub ユーザー名）とセットで表示されている。
- **mobile-detail.webp**: 戻るリンク → nodeca/js-yaml のヘッダー（言語・説明文）→ star/watcher/fork/issue の 2x2 タイル → 最終更新日 → README 見出しと本文レンダリング開始。4 枚中最短（1660px）で最も「作り込みの証拠」密度が高い。

**枚数・配置の評価 [minor]**: 4 枚は多すぎない。digest/search は LP（`shot-mobile.webp` / `shot-search.webp`）と主題が重なるが、gems 一覧・詳細は LP に存在しない新規機能（SP-19）の証拠であり削れない。2x2 の表 + `width="300"` という配置自体は GitHub のレンダリング幅では妥当。

**縦の長さが可読性を損なう点 [major]**: 表示幅 300px で元画像が 786x1660〜2500（アスペクト比約 1:2.1〜1:3.2）のため、GitHub 上では **1 セルが実測で約 630〜950px の縦長画像** になる（300 × 2392/786 ≈ 913px（digest）、300 × 2360/786 ≈ 901px（search）、300 × 2500/786 ≈ 954px（gems）、300 × 1660/786 ≈ 634px（detail））。2 行の表だが実質 4 枚分・合計 3400px 超の縦スクロールを README 冒頭に強いる。特に digest は宝石を見つけるイラストが約 372px（全体の 15%）を占め、README の文脈では装飾に過ぎず「セットアップ手順まで辿り着く前の無駄なスクロール」を増やしている。
- **具体的トリミング案**:
  - `mobile-digest.webp`: 出典注記（末尾約 y=1968〜2392、約 424px）をカットし **786×1968** へ。可能ならイラスト区間（y≈144〜516、約 372px）も除去し再撮影で **786×1600 程度**まで縮める。
  - `mobile-search.webp`: 5〜6 枚目のカード（symfony/yaml 以降、y≈1600〜2360）をカットし **786×1600** へ（見出し・フィルタ・Gem バッジ説明・カード 3〜4 枚は残す）。
  - `mobile-gems.webp`: 5 枚目以降のカード（y≈1500〜2500）をカットし **786×1500** へ。
  - `mobile-detail.webp`: そのまま（既に 4 枚中最短でトリミング不要）。

**alt の記述量 [minor]**: 4 枚とも 2〜4 文の説明的 alt で、画像内の数値（74 件・61,909 件・6,626 star 等）を含めて内容と一致している。長いが「スクリーンショットの内容を伝える」目的には適切な長さで、短縮は不要。

**保存先の二重管理 [minor]**: `docs/images/readme/`（README 用・モバイル・技術的証拠）と `site/assets/img/shot-*.webp`（LP 用・`shot-search` は PC 幅 1600px・マーケティングコピー付き）は用途が異なり、即座の統合は不要。ただし digest と search は概念的に同じ画面を指しており、将来 UI が変わった際に **どちらか一方だけ差し替えられてズレる**リスクがある。README に「LP のスクリーンショットとは別管理」である旨の一言注記があると親切だが、必須ではない。

**実在ユーザーのアバター・氏名 [minor・容認]**: 与件の前提（プロダクト自体が GitHub の公開データを表示しているだけであり、LP も同種のショットを掲載済み）に従えば問題にしない。ただし LP の `shot-search.webp` が実際に同程度のアバター露出を含むかは本レビューでは未確認（画像を開いていない）。次回 LP 側のスクリーンショットを更新する際に一貫性を確認する程度でよい。

**PC 幅が無いこと [minor・妥当]**: 「PC 幅を含む紹介は紹介ページを参照」という誘導が明記されており、README をモバイルに絞って軽量化する設計は妥当。LP との役割分担として機能している。

### ② 構成と分量（228 行・並び順）

- 冒頭 15 行で「何か（1-5 行）→ 使ってみるリンク・紹介ページリンク（7-8 行）→ 目次（10 行）」が揃っており、3 読者いずれにも到達性が高い。**目次行が実質的な救済策になっている**ため、後段の並び順（設計上の判断が 8 番目に配置）は致命的ではない。
- **[minor] 目次の過不足**: `技術スタック` が目次に無い。OSS 第三者・コントリビュータが最初に知りたい情報の 1 つであり、6 項目から 7 項目に増やしても目次の可読性は損なわれない。追加を提案する（`[技術スタック](#技術スタック)` を `[環境変数]` の直後に挿入）。
- **[minor]** 12 行目の「旧称: IndieGems」注記が目次の直後・画面節の直前に挟まり、実務情報の流れを一瞬止める。今回の改稿対象ではないため本 PR での移動は不要（現状維持でよい）。
- 並び順自体（画面→開発→デプロイ→環境変数→技術スタック→ドキュメント→設計上の判断→AI利用→ADR→ライセンス）は「動かす情報クラスタ」→「知る情報クラスタ」→「法務」の 2 段構成として一貫しており、3 読者のいずれにも破綻しない。**変更不要（現状維持）**。

### ③ LP との役割分担

`site/README.md` は LP のスクリーンショットを「マーケティングコピー付き・PC 幅中心・JS なしの単一ページ」向けと明記しており、README の『画面』節（モバイル幅・技術的注記の alt・グレーディング対象としての証拠提示）とは目的が異なる。digest/search の画面主題は重なるが、フォーマット・対象読者・付随情報が別物であり **問題になる重複ではない**。gems 一覧・詳細画面は LP に存在しない独自コンテンツで、むしろ相互補完的。**現状維持でよい**。

## ラウンド 2

### `req_gate` — 反論
<sub>2026-08-23T14:25:54+09:00</sub>

## req_gate（与件・要件充足レンズ）— round 2

### reader_flow のトリミング提案への回答（与件 §6 の観点）

`mobile-digest.webp` の出典注記（Ecosyste.ms・末尾約 424px）と、`mobile-search.webp`/`mobile-gems.webp` の後半カード群をカットする提案について、**与件 §6（セットアップ手順 / 設計上の判断 / AI 利用範囲）そのものへの抵触はない**。これらの要件は README の「本文」に対する記載義務であり、画像内にどこまで写っているかを規定していない。トリミングで NFR-29〜32 / AC-11 のいずれも損なわれない。

ただし **1 点、私のレンズから見て見落とせない副作用がある**: `mobile-digest.webp` の alt テキスト（README.md:20）は「末尾に Ecosyste.ms を出典とする注記がある」と明記しており、`mobile-search.webp` の alt（README.md:20）も「symfony/yaml, eemeli/yaml, nodeca/js-yaml」を含む後半カードの内容を含んでいる可能性がある（reader_flow の実見結果によれば search 画像は 6 件のカードを alt が説明）。**トリミングを採用するなら、alt テキストを実際に見える範囲だけに書き直す必要がある**（見えなくなった要素を alt に残すと「書いてあるが画像を見ても確認できない」= 事実と乖離した代替テキストになる）。これは新たな指摘として追加する。

- **[minor・条件付き]** reader_flow のトリミング案（digest 末尾カット / search・gems 後半カット）を採用する場合、対応する alt テキストからも切り取られた要素の説明を削除すること。トリミングしないなら本指摘は無効。

### scope_guard の「ADR 0012 行は残す」への回答（NFR-30/31 の観点）

**同意する。** ADR 索引表（README.md:199-215）の 0012 行は「設計上の判断」本文（採否理由を語る散文）ではなく、`NFR-32`（ADR 記録の完全性）に対応する機械検証対象の目次であり、そこに書かれているのは ADR ファイルの見出しをそのまま転記した中立的な 1 行（「任意の GitHub OAuth ログインを、与件が対象外とした認証に上乗せする」）にすぎない。ユーザー制約（「過去に対応した認証に関する記載は含めない」）が禁じているのは PR #469 で除去済みの **正当化ロジック**（AR-5 節）であり、ADR の存在自体を隠すことまでは要求していない ——というのが D-39 の read（scope_guard 提示）でもあり、私のレンズでも一致する。

むしろ **私のレンズからは追加の理由がある**: `NFR-32` は「技術的意思決定を ADR として記録する」ことを求め、PRD §12 の記録済み ADR 一覧（0012 含む）と README の索引を一致させる要求（`check_adr_coverage.py`）がある。0012 行を消すと、與件 §6 が求める「重要な技術的意思決定の記録」自体が README 上で **不完全になる**（NFR-32 違反）。scope_guard の「機械ゲートを壊す」という技術的理由に、「NFR-32 の充足を壊す」という要件面の理由を重ねて支持する。

### fact_check の「事実誤り 0 件」との整合性

**矛盾しない。両指摘とも維持する。** fact_check の検証は「今回書かれた個々の記述が実ファイル・実データと一致しているか」という **正確性** の軸であり、私の round 1 指摘 1・2 は「正確だが構成上どう配置されているか」という **要件充足・可読性（与件 §6 の『工夫した点』としての取捨選択）** の軸で、評価対象が異なる。

- **指摘 1（Gem Index 母集団の重複記述）**: fact_check は Hidden Gem 節の「62,483 は D-37 適用後」「D-36 は約 11 万」という数値を ✅ 判定しているが、これは Hidden Gem 節・Gem Index 母集団節の **どちらの記述も現時点では正確** であることを示しただけで、「同じ事実を 2 箇所に書く設計が妥当か」には答えていない。むしろ fact_check が両節の数値を個別に裏取りできた事実は、**2 箇所に同じ実測値が実在する**ことの追認であり、私の「重複している」という指摘の前提（2 箇所に同内容がある）を補強する。将来 D-36/D-37 の値が更新されたとき、2 箇所同時に直さなければ整合が崩れるという desync リスクは、現時点の正確性とは独立した論点として残る。**維持する（major）**。
- **指摘 2（ADR 0014 の昇格要求）**: fact_check の検証項目に ADR 0014（今日の Gem のダイジェスト設計）そのものへの言及はなく、「事実誤りが無い」という結論は「本文に書かれていない」ことの当否を判定していない。ADR 0014 が本文に無いのは「誤り」ではなく「欠落」であり、fact_check のスコープ（記述済み内容の正確性）とは独立した与件充足の論点。**維持する（major）**。

### `scope_guard` — 反論
<sub>2026-08-23T14:26:39+09:00</sub>

## scope_guard（round 2 反論・concession）

### 1. req_gate の ADR 0014 昇格要求・Gem Index 重複統合要求 → **どちらもスコープ内と判定（concession ではなく賛成側の追認）**

- 両方とも **README.md 内、しかも今回の PR がまさに新設・拡充している「設計上の判断」節そのもの**（現行 diff で 2 項目 → 8 項目へ拡充した箇所）に対する追加編集であり、PR スコープ（README.md + `docs/adr/0004` 追記 + `docs/images/readme/` に限定）の外に出るものではない。新しいファイル・新しい主題領域を持ち込む提案ではなく、**このPRが既に触っている節の中身を直す**話なので `core-principles.md` CP-1 の「スコープ外の改善は別 Issue」ルールには当たらない。
- むしろ Issue #513 原文の指示（「最低限の要件を満たす記載となっているかセルフレビューしてください」）が直接この種の是正を求めている。ADR 0014（今日の Gem＝README 冒頭で最初に紹介される目玉機能）が `NFR-30` の「設計上の判断」から漏れているのは与件未充足そのものであり、これを直さずマージすると `SD-4` の「読めば分かることを聞くのではなく、着手時に必読ドキュメントを読んで矛盾を解消する」を満たさないまま終わる。
- Gem Index 母集団の重複記述も **今回の diff が自ら作った重複**（Hidden Gem 節と Gem Index 節はどちらも今回新規追加の `+` 行）なので、CP-1 の「自分の変更が作ったコードパス上の壊れは即修正する」に相当し、是正は義務であって任意の磨き込みではない。
- 争点 A の「多すぎて要点が埋もれないか」という別軸の懸念（reader_flow・spec 双方が言及）とは対立しない: req_gate の提案は「9 項目に純増」ではなく「重複を 1 箇所に畳んでから ADR 0014 を追加する」という **正味では大きく膨らまない再構成**。分量判断そのものは req_gate/reader_flow の主担当領域であり、scope_guard としては「スコープ内であること」だけを保証する。

### 2. reader_flow のトリミング具体案 → **スコープ内の調整（別 Issue に回す理由がない）**

- `docs/images/readme/*.webp` は **今回の PR で新規追加されたファイル**（`git diff --stat` で `Bin 0 -> ...` の 4 枚）であり、PR スコープの明示リスト（`docs/images/readme/` を含む）に文字どおり含まれる。既存の本番コードや過去resourcesを磨き込む話ではなく、**このPR自身がまだマージされていない自分の成果物を仕上げる話**なので、「スコープ外の改善は別 Issue」ルールの対象外。
- ただし実行コストは text 編集とは異なる（スクリーンショットの再撮影・切り出しという運用作業が要る）。これは scope（やってよいか）の問題ではなく実行可能性の問題なので、synthesizer が「マージ前に対応必須（critical/major）」とするか「対応困難なら理由を添えて現状維持」とするかは reader_flow の指摘の重大度判定に委ねる。scope_guard としては **out_of_scope（別 Issue へ回す）という選択肢は不要**と判定する。

### 3. 自分が挙げた「`minimum-requirements-checklist.md` 陳腐化は別 Issue」への反論の有無 → **反論なし。むしろ req_gate が独立に同じ結論に到達しており、判定を維持する**

- req_gate の「補足（本 PR のスコープ外・参考情報）」が、`minimum-requirements-checklist.md:137`（設計上の判断が旧 2 項目のまま）・`:136`（環境変数「8 変数」表記が現状 9 行と食い違う）を **独立に検出したうえで「README 自体の充足性には影響しない」「checklist 側の desync として別 Issue 候補」** と、私と同じ結論を出している。fact_check・reader_flow からもこの結論への異論は無い。
- 私が round 1 で挙げた `.github/workflows/` 存在有無の食い違い（`:128,176`）・チェック本数の食い違い（`:173`「全 20 チェック」）と合わせると、**`minimum-requirements-checklist.md` の陳腐化は最低 4 箇所** に及ぶことが 2 レンズの独立検証で確認できた。これは「1 行の裏付け追記」だった ADR 0004 のケースとは性質が違う（4 箇所の実質的な書き直しが要り、対象ファイルも PR スコープの明示リストに含まれない）ので、**concession はしない。out_of_scope 判定を維持し、別 Issue（`minimum-requirements-checklist.md` の D-23/D-40/設計上の判断 8 項目化/環境変数 9 変数化への追従）を起票することを推奨する。**

### 4. 認証の再混入判定への他レンズの反応 → **反論・反証は皆無。違反ゼロと断定する**

- req_gate（NFR-29〜32・与件充足の専任）は環境変数節・ADR 索引を精査したが、OAuth 4 行や ADR 0012 行を問題視する指摘を一切出していない（充足項目の列挙にも指摘リストにも登場しない）。
- fact_check は「今回の改稿で新しく書かれた記述」を 19 項目にわたって実ファイルと突合したが、**OAuth・認証関連の新規記述は 1 件も検証対象に含まれていない**。これは fact_check の検証対象選定自体が「新規に書かれた記述」基準であり、私が round 1 で確認した「OAuth 関連 3 箇所は diff 上 1 文字も変更されていない」という事実と整合する（新規記述が無いから検証対象にも上がらなかった）。
- reader_flow はスクリーンショット 4 枚を実際に開いて確認しているが、画面内に認証・ログイン導線が写っている、あるいは画面節の説明文が認証に言及しているという指摘は無い（４画面はいずれも未ログイン状態の実画面）。
- 以上、4 レンズ全員の独立検証を突き合わせても、認証に関する記載を増やした形跡はどこにも無い。**round 1 の判定（環境変数 4 行・現況 1 行・ADR 索引 0012 行はすべて無変更で維持＝ユーザー制約「過去に対応した認証に関する記載は含めないまま」への違反はゼロ）を最終確定として断定する。**

### `reader_flow` — 反論
<sub>2026-08-23T14:26:59+09:00</sub>

## reader_flow（読者導線・ビジュアルレンズ）— Round 2

### 1. トリミング提案の修正（出典表示・帰属表示への配慮）

**`mobile-digest.webp` の「末尾出典をカットして 786×1968 へ」は撤回する**。末尾の Ecosyste.ms 出典注記（CC BY-SA 4.0 由来データの帰属表示）は画像内に残すべき情報であり、これを切り落とす提案は不適切だった。修正案は次の通り:
- digest は **出典注記を残したまま**、可能なら装飾イラスト区間（y≈144〜516・約 372px）だけを再撮影で除去する案に限定する。これは合成トリミングが必要で今回のセッションの手軽な作業ではないため、**必須要求ではなく nice-to-have** に格下げする。
- `mobile-search.webp` / `mobile-gems.webp` の「下位カードをカットして 786×1600 / 786×1500 へ」も **撤回する**。カード列は「多様なレジストリ・多数の実データ」という説得力そのものであり、途中で切ると逆に情報量の証拠性が下がる。フィラーではなく本体なので、勝手にトリミングする理由が弱い。

結果として、**トリミングの積極提案は取り下げ、[major] → [minor] に格下げ**する（後述 3）。

### 2. req_gate の ADR 0014 昇格要求について: 条件付き反対（全面昇格には反対、軽量な代替案を出す）

**反対理由（読者負荷の観点）**: 228 行はすでに 3 読者すべてにとって決して短くない分量で、自分の Round 1 指摘（設計上の判断が目次頼みでようやく辿り着ける 8 番目の節）も踏まえると、ここに他 6 項目と**同格のフルサイズの新規小節**（見出し + 箇条書き 3 点 + ADR 参照、他の項目は概ね 10〜15 行）を追加するのは、目玉機能の説明として正当ではあっても純増分が大きく、読者が「設計上の判断」全体を読み切る負荷をさらに押し上げる。

**req_gate の指摘 1（Hidden Gem 節と Gem Index 節の重複統合）には賛成する**。これは自分の [major]（縦の情報量が多すぎる）とも整合し、統合すればネットで数行〜1 段落分は減る。

**代替案**: ADR 0014 は独立節を新設せず、**既存の「Hidden Gem を...と定義した」節（README.md:115-122）の箇条書きに 1 行追記**する形で解決する。案文（例）:
> 「今日の Gem」もこの指標に基づくが、無限フィードや通知ではなく **日次の有限件数** に絞っている（[ADR 0014](./docs/adr/0014-zero-query-daily-digest.md)）。無限に見せると「今日はここまで」という区切りが消え、Hidden Gem の希少性という価値提案が埋没するため。

これなら新規に増える行数は 2〜3 行で済み、目玉機能の工夫も可視化でき、req_gate の指摘 1（統合）と指摘 2（ADR 0014 の不可視化）を **同時に、節を増やさずに** 解消できる。req_gate の指摘 3（ADR 0008 昇格・minor）には元から反対しない立場だが、同じ理由でこちらも独立節ではなく短い追記に留めることを推奨する。

### 3. scope_guard へ: トリミングはマージ前必須ではないと譲歩する（concession）

Round 1 の [major] 判定を撤回し **[minor] に格下げする**。理由:
- 4 枚とも実在の意味あるコンテンツで埋まっており（フィラーなのは digest のイラスト約 372px のみ）、読者が得られる情報を損なうものではない。「縦に長くてスクロールが増える」は実害としては軽微（GitHub の README で画像付きセクションが縦に伸びること自体は一般的で、致命的な離脱要因とまでは言えない）
- 是正には実際の画像編集（再撮影 or crop + webp 再エンコード）が要り、本レビューラウンドの範囲を超えた実装作業になる。他 3 名の誰からも blocking 判定が出ていないこととも整合する
- 結論: **本 PR ではトリミングを行わず現状維持**。将来 UI が変わって再撮影する機会に、digest のイラスト区間の圧縮だけ軽く検討する程度の扱いでよい（別 Issue 化するほどの重さもない・自然に次回更新時のついでで良い）

### 4. 目次への「技術スタック」追加提案: 維持する

こちらは撤回しない。**採用コストが目次 1 行の追加のみ**で、他の指摘（req_gate の圧縮要求・scope_guard のスコープ限定）と衝突せず、README.md 単体で完結する軽微な改善のため、他の議論の帰趨と独立に採用してよい。

### `fact_check` — 反論
<sub>2026-08-23T14:27:02+09:00</sub>

## fact_check（round 2・他 3 名への応答）

### 1. req_gate [major] 「Gem Index の母集団と配信」節が Hidden Gem 節と重複 — 部分同意（concession）

実際の本文を突き合わせた。

- Hidden Gem 節（README.md:115）: 「母集団を 12 レジストリ・約 11 万リポジトリへ拡大した実測を受けて `D-36` で再導入した（汚染フィルタとリポジトリ単位の重複排除を通した後の配信プールは 12 レジストリ・**62,483** リポジトリ・`D-37`）」
- 新設節（README.md:141）: 「汚染を落としてから数える: …同一リポジトリを指す複数パッケージはリポジトリ単位で重複排除する。この 2 段を通した結果が配信プールの **62,483** リポジトリ」

**「母集団拡大 → 汚染フィルタ + dedupe → 62,483」という同一の算出根拠が、ほぼ同じ言葉で 2 箇所に書かれている事実**は認める（req_gate の指摘は该当 1〜2 文について正しい）。

ただし完全な二重記述ではない。新設節にしかない情報が過半を占める:
- 「レジストリ別成層化・既定 15,000 件の固定枠」（母数比例だと npm が枠を占める、という設計理由）
- 「配信はレジストリ別シャードの静的アセット + isolate cold start 並列取得 + singleton promise」（配信方式の設計理由）

これらは Hidden Gem 節には存在しない。Hidden Gem 節は「指標の意味・どこで使われるか・何が未確定か」という **利用者向けの説明**、新設節は「母集団をどう作るか・どう配信するか」という **エンジニアリング判断の記録**（`NFR-30` が求める工夫点）であり、役割自体は分かれている。

**fact_check としての結論**: 「二重記述」という指摘は該当 1 文（62,483 の算出根拠を繰り返す部分）に限って事実。req_gate が要求する「レジストリ別成層化・シャード配信を ADR 参照 1 行へ格下げ」までは事実確認の範囲外（要件充足の判断は req_gate 領分）だが、**その 1 文を Hidden Gem 節側の参照リンクに置き換えても新設節の主張（成層化・固定枠・配信方式）は失われない**ことは事実として確認できる。

### 2. req_gate [major] ADR 0014 の設計判断を本文へ昇格 — 同意（concession）

ADR 0014（`docs/adr/0014-zero-query-daily-digest.md`）を確認した。決定の骨子は:
- 発見面をトップページに置き **キーワード非依存**（`A-8`）
- 提示形式は **有限件数（既定 5 件）の日次ダイジェスト。無限フィード・無限スクロールは採らない**
- **日付を唯一のシードとする決定論的生成**。サーバーに個人化状態を持たない。`?date=` で外部から上書きでき、URL 共有だけで再現できる

これに対し README の現行記述を突き合わせると:
- 冒頭（README.md:5）は「日次で入れ替わるダイジェスト『今日の Gem』」とだけ書き、**「なぜ有限 5 件か」「なぜ無限フィード/通知を採らなかったか」「なぜ日付シードで状態を持たないのか」への言及がない**
- Hidden Gem 節（README.md:115）は「① 日次ダイジェストの並び順」を Gem Index の適用先の 1 つとして挙げるのみで、ダイジェスト自体の設計判断（有限件数・決定論的生成）には触れていない

**fact_check としての結論**: 「今日の Gem」の設計判断（ADR 0014 の本体）は README 本文のどこにも実質的に書かれていない、という req_gate の前提は事実。既存記述による代替充足はなく、昇格要求は事実面から支持できる。

### 3. scope_guard [major] `minimum-requirements-checklist.md` の `.github/workflows/` 記載齟齬 — 事実確認・同意（concession）

`ls .github/workflows/` を実行し、`gem-pool-refresh.yml` が実在することを確認した。一方でチェックリスト側:

```
docs/02_requirements/minimum-requirements-checklist.md:128: 「.github/workflows/ は 存在しない（GitHub Actions がプラットフォーム側の制限で起動できず 2 本とも撤去・D-23）」
docs/02_requirements/minimum-requirements-checklist.md:176: 「ls .github/workflows | 存在しない（D-23 により撤去済み）」
```

**齟齬は事実**（scope_guard の指摘どおり）。`D-40`（2026-08-23）で `gem-pool-refresh.yml` を新設した経緯が checklist に反映されていない desync。

**README への波及は無し**: README の新規記述（「GitHub Actions は Gem 候補プールの日次実行 + 週次反映 [`.github/workflows/gem-pool-refresh.yml`] にのみ使う」）は round 1 で確認済みの実ファイル状態と一致しており、checklist の古い記述をそのまま引き写してはいない。scope_guard の「checklist 側の陳腐化であり README 側の誤りではない」という判定は事実と一致する。out_of_scope（別 Issue）への切り分けに異論なし。

### 4. reader_flow の画像トリミング案（digest から装飾イラスト・末尾出典をカット）— D-29 抵触なし、ただし alt 修正が必須（rebuttal + 注意喚起）

`D-29`（`open-questions.md:340`）を確認した。要求しているのは「配信 JSON のトップレベルに `source`/`license` 等のメタデータを含めること」と「**UI にも出典表示を出す運用**」——これは **本番アプリの実行時 UI** に対する要件であり、README のスクリーンショットという静的ドキュメント資産に出典を写り込ませることを義務付けるものではない。

- README 本文には Hidden Gem 節（README.md:120）に既に「被依存数は Ecosyste.ms のオープンデータに委ねる」という帰属の言及があり、画像をトリミングしても **本文からは Ecosyste.ms への言及が消えない**。
- 本番アプリの実際の出典表示（画面下部の注記）自体はコードで実装されているものであり、README のスクリーンショットをどう切り取るかとは無関係に存続する。

**結論**: reader_flow の提案（`mobile-digest.webp` の末尾出典部分 y≈1968〜2392 をカット）は D-29 / CC BY-SA 4.0 のいずれにも抵触しない。

**ただし 1 点、事実整合上の見落としを指摘する**: 現在の alt テキスト（README.md:20）は「末尾に Ecosyste.ms を出典とする注記がある」と明記している。この案どおり画像をトリミングすると **alt テキストが実際の画像内容と食い違う**（存在しない要素の説明が残る）ことになる。トリミングを採用するなら、同じ diff で alt テキストからも当該記述を削るか、「出典注記は画面下部（本 README では非表示）」等に修正する必要がある——これは D-29 とは別の、単純な事実整合の問題として round 3 までに解消すべき。

## ラウンド 3

### `lead` — 合意
<sub>2026-08-23T14:29:15+09:00</sub>

## lead — 合意（争点 A〜F の決着）

2 ラウンドの相互反論を突き合わせ、**マージ前に直すもの / 現状維持 / 別 Issue** を確定する。

### 合意した点（4 レンズが一致）

1. **事実誤りはゼロ**（争点 B）。fact_check が 19 項目を実ファイルで裏取りし ❌ 0 件。他 3 レンズからの反証もなかった。**今回の改稿で新しく書いた記述の修正は不要**。
2. **認証の再混入はゼロ**（争点 C）。scope_guard が diff ベースで「OAuth 4 行・現況 1 行・ADR 索引 0012 行はいずれも 1 文字も変更されていない」ことを確認し、req_gate が `NFR-32`（ADR 記録の完全性）の観点から「0012 行を消すと与件が求める技術的意思決定の記録が README 上で不完全になる」と要件面から追認、reader_flow が 4 画面すべて未ログイン状態であることを実見で確認した。**3 項目とも現状維持で、ユーザー制約への違反はない**。
3. **相対リンク切れゼロ**（65 本中 0 件・scope_guard 実測）。
4. **画像のトリミングは行わない**（争点 D）。reader_flow が round 2 で自ら提案を撤回し `[major]` → `[minor]` へ格下げした。撤回理由は 2 つあり、いずれも他レンズの指摘と整合する: ① digest 末尾の Ecosyste.ms 出典注記は `D-29`（CC BY-SA 4.0 の帰属表示）に関わるため画像から落とせない ② search / gems の下位カード列は「多様なレジストリ・多数の実データ」という説得力そのもので、切ると情報量の証拠性が下がる。**4 枚・2x2 配置・`width="300"`・保存先 `docs/images/readme/` はすべて現状維持**。トリミングしないため、req_gate と fact_check が条件付きで挙げた「alt テキストの整合修正」も発生しない。
5. **ADR 0004 への 1 行追記はスコープ内**（争点 F）。README 本文の書き換え（実在しない CI ゲート → セッション実行ゲート）と同じ齟齬の裏返しであり、`SD-4`（ドキュメント間の矛盾を放置しない）が要求する是正。scope_guard が「新しいファイル・新しい主題領域を持ち込む提案ではない」と判定した。

### 対立が残り、lead が決着させた点

#### 対立 1: ADR 0014（今日の Gem）の扱い

- **req_gate**（`[major]`・維持）: 他 6 項目と同格の独立節として昇格せよ。冒頭で最初に紹介する目玉機能なのに「なぜ有限件数か」「なぜ無限フィード・通知を採らなかったか」が本文にない。
- **fact_check**: ADR 0014 の骨子（有限 5 件・無限フィード不採用・日付を唯一のシードとする決定論的生成）は **README 本文のどこにも実質的に書かれていない**と事実確認し、req_gate の前提を支持。
- **reader_flow**（条件付き反対）: 228 行に他項目と同格のフルサイズ小節（10〜15 行）を足すのは読者負荷が大きい。既存の Hidden Gem 節へ 2〜3 行の追記で代替せよ。

**lead の決着**: 両者の妥当な部分を採る。**独立節は作るが、req_gate が求めた「ADR 0014 単独のフルサイズ節」ではなく、ADR 0014 と ADR 0008 を 1 つの判断としてまとめた短い節を新設する**。

- 根拠: ADR 0014 自身が frontmatter の関連に [ADR 0008](0008-pagination-over-infinite-scroll.md) を「無限スクロール不採用の既存決定」として挙げており、**「無限に流さず有限で区切る」という同一の設計思想の 2 つの現れ**である。別々の節にすると重複し、まとめれば 1 節で 2 つの ADR を可視化できる。
- これは req_gate が round 1 で `[minor]`（余裕があれば）とした ADR 0008 の昇格要求も同時に満たす。reader_flow も ADR 0008 について「独立節ではなく短い追記に留めることを推奨」としており、1 節への同居はこの推奨とも矛盾しない。
- reader_flow の読者負荷への懸念には、下記の対立 2（重複削除）でネットの増分を抑えることで応える。

#### 対立 2: Gem Index 母集団の記述の重複

- **req_gate**（`[major]`・維持）: Hidden Gem 節と Gem Index 節が「約 11 万 → 汚染フィルタ + repo 単位 dedupe → 62,483」という同一の算出根拠をほぼ同じ言葉で二重に書いている。将来 `D-36` / `D-37` の値が更新されたとき片方だけ直されて desync する。
- **fact_check**（部分同意）: 重複は **該当 1〜2 文（62,483 の算出根拠を繰り返す部分）に限って事実**。新設節にしかない情報（レジストリ別成層化・既定 15,000 件の固定枠・シャード配信 + isolate 内キャッシュ + singleton promise）が過半を占め、Hidden Gem 節は「指標の意味と適用先」、新設節は「母集団の作り方と配信方式」という **役割の分担は成立している**。
- **reader_flow**: 統合には賛成（縦の情報量が減る）。

**lead の決着**: **fact_check の切り分けを採る**。節そのものの統合（req_gate が求めた「シャード配信を ADR 参照 1 行へ格下げ」）は行わない — 役割が分かれていることを fact_check が確認しており、格下げすると `NFR-30` が求める工夫点の記録が薄くなる。**重複しているのは 62,483 の算出根拠を繰り返す 1 文だけなので、新設節側のその 1 文を削り、Hidden Gem 節に一本化する**。desync リスクは消え、新設節の主張（成層化・固定枠・配信方式）は失われない（fact_check が「置き換えても失われない」と明言）。

#### 対立 3: `minimum-requirements-checklist.md` の陳腐化

- **scope_guard**（out_of_scope・維持）: 陳腐化は最低 4 箇所（`.github/workflows/` の存在有無・チェック本数「全 20」・設計上の判断が旧 2 項目・環境変数「8 変数」）に及び、実質的な書き直しが要る。対象ファイルは PR スコープの明示リストに含まれない。
- **req_gate**: 独立に同じ 2 箇所を検出したうえで「README 自体の充足性には影響しない・別 Issue 候補」と同結論。
- **fact_check**: `ls .github/workflows/` を実行して実在を確認し、スコープ外判定に同意。

**lead の決着**: **out_of_scope を採る**（3 レンズが独立に同結論）。ADR 0004 の 1 行追記とは性質が違う — あちらは README 本文が書き換えた事実の裏返しで 1 行、こちらは 4 箇所の書き直しでスコープ外のファイル。**別 Issue を起票する**。

### 採用しない指摘

- **req_gate `[minor]` 4（Gem Index 節に実装ファイルへのリンクが無い）** → **採用する**（`src/infrastructure/platform/static-gem-index.ts` の実在を lead が確認済み）。低コストで「書いてあるが第三者に追えない」を解消できる。
- **reader_flow `[minor]`（目次に「技術スタック」を追加）** → **採用する**。目次 1 行の追加で、他のどの指摘とも衝突しない。
- **reader_flow の nice-to-have（digest の装飾イラスト区間 372px の除去）** → **採用しない**。本人が「必須要求ではない」「別 Issue 化するほどの重さもない・次回更新時のついでで良い」と格下げしている。

### マージ前に行う編集（4 件）

| # | ファイル | 節 | 内容 |
|---|---|---|---|
| 1 | `README.md` | 設計上の判断 | 新設: 「発見面と一覧の見せ方を『有限』で区切った（`ADR 0014` / `ADR 0008`）」。今日の Gem を日次・有限 5 件にした理由（日付を唯一のシードとする決定論的生成でサーバー状態を持たない）と、一覧に無限スクロールを採らずページネーションにした理由（キーボードで末尾に到達できる・現在位置を読み上げられる・戻り位置を URL の 1 値で復元できる）を 1 節にまとめる |
| 2 | `README.md` | Gem Index の母集団と配信の作り方 | 62,483 の算出根拠を繰り返す 1 文を削り、Hidden Gem 節への参照に置き換える |
| 3 | `README.md` | Gem Index の母集団と配信の作り方 | 配信方式の記述に `src/infrastructure/platform/static-gem-index.ts` へのリンクを足す |
| 4 | `README.md` | 冒頭の目次行 | 「技術スタック」を追加する |

### 別 Issue（本 PR では扱わない）

- `minimum-requirements-checklist.md` の陳腐化 4 箇所（`.github/workflows/` の存在・チェック本数・設計上の判断の項目数・環境変数の個数）を実態へ追従させる
- `docs/02_requirements/roadmap.md` の `M-6` が `SP-17`〜`SP-19` を含んでいない（事前調査で検出・既存 Issue #324 と同種）
- `NOTICE` に Ecosyste.ms（CC BY-SA 4.0）と Geist（SIL OFL 1.1）の帰属表示が無い（既存 Issue #366）

### `lead` — 判定
<sub>2026-08-23T14:30:24+09:00</sub>

{
  "decisions": [
    {
      "issue": "A（与件 §6 充足）",
      "decision": "README.md「設計上の判断」に短い節を 1 つ新設する: 「発見面と一覧の見せ方を『有限』で区切った（ADR 0014 / ADR 0008）」。ADR 0014 単独のフルサイズ節は作らない。",
      "rationale": "fact_check が「ADR 0014 の骨子（有限 5 件・無限フィード不採用・日付を唯一のシードとする決定論的生成）は README 本文のどこにも実質的に書かれていない」と事実確認し、req_gate の未充足指摘を支持した。一方 reader_flow の読者負荷への懸念も妥当なので、ADR 0014 が frontmatter で ADR 0008 を「無限スクロール不採用の既存決定」として関連付けている点を使い、『無限に流さず有限で区切る』という同一の設計思想として 1 節にまとめる。req_gate が minor で挙げた ADR 0008 の昇格要求も同時に満たす。",
      "rejected": "req_gate の「ADR 0014 を他 6 項目と同格の独立節へ」= 読者負荷を押し上げるうえ ADR 0008 と重複する。reader_flow の「Hidden Gem 節へ 2〜3 行追記」= 発見面の設計判断が Gem Index 指標の説明に埋没し、ADR 0008 を拾えない。"
    },
    {
      "issue": "A（与件 §6 充足・重複記述）",
      "decision": "README.md「Gem Index の母集団と配信の作り方」節から 62,483 の算出根拠を繰り返す 1 文を削り、Hidden Gem 節への参照に置き換える。節そのものの統合・シャード配信の ADR 参照 1 行への格下げは行わない。",
      "rationale": "fact_check の切り分けを採用。重複は該当 1〜2 文に限って事実で、新設節にしかない情報（レジストリ別成層化・既定 15,000 件の固定枠・シャード配信 + isolate 内キャッシュ + singleton promise）が過半を占め、両節の役割分担（指標の意味と適用先 / 母集団の作り方と配信方式）は成立している。1 文を削れば将来 D-36・D-37 の値が更新されたときの desync リスクは消え、新設節の主張は失われない（fact_check が明言）。",
      "rejected": "req_gate の「レジストリ別成層化・シャード配信を ADR 参照 1 行へ格下げ」= NFR-30 が求める工夫点の記録が薄くなる。"
    },
    {
      "issue": "A（第三者に追えるか）",
      "decision": "README.md「Gem Index の母集団と配信の作り方」節の配信方式の記述に src/infrastructure/platform/static-gem-index.ts へのリンクを足す。",
      "rationale": "req_gate の minor 指摘。エラー分類節が src/domain/errors.ts にリンクしているのに対しこの節だけ実装への導線が無く「書いてあるが第三者に追えない」。lead が実ファイルの存在を確認済み。",
      "rejected": "なし（反対意見なし）"
    },
    {
      "issue": "B（事実整合）",
      "decision": "修正なし。今回の改稿で新しく書いた記述はすべて実物と一致している。",
      "rationale": "fact_check が 19 項目（機械検査本数・Lighthouse a11y ゲート・Workers Builds 一次経路・ErrorKind 7 種・秘匿情報と非秘匿変数の読み手の層・SITE_URL 空文字での起動失敗・RATE_LIMIT_SALT の警告ログ・npm scripts 追加分・LP の gh-pages 配信・next/image 不使用と ?s=80 / ?s=128・62,483 と約 11 万の書き分け・index.json の totalCount と 12 シャード・レジストリ別 15,000 件固定枠ほか）を実ファイルの行番号付きで裏取りし ❌ 0 件。他 3 レンズからの反証もなかった。",
      "rejected": "なし"
    },
    {
      "issue": "C（認証の再混入）",
      "decision": "環境変数表の OAuth 4 行・その直後の「本番では表示されない」1 行・ADR 索引の 0012 行は、3 つとも現状維持。認証に関する記載の追加はしない。",
      "rationale": "scope_guard が diff ベースで 3 項目とも 1 文字も変更されていないことを確認し、D-39 が既に決めた維持方針そのものだと判定。req_gate が NFR-32 の観点から「0012 行を消すと与件が求める技術的意思決定の記録が README 上で不完全になる」と要件面から追認し、check_adr_coverage.py の機械ゲートを壊す点も一致。reader_flow は 4 画面すべてが未ログイン状態であることを実見で確認。ユーザー制約が禁じているのは PR #469 で除去済みの正当化ロジック（AR-5 節）であり、ADR の存在自体を隠すことまでは求めていない。",
      "rejected": "OAuth 4 行の削除 = .env.example との 1:1 対応とセットアップ手順としての完全性が崩れる。ADR 索引 0012 行の削除 = 機械ゲートと NFR-32 の両方を壊す。"
    },
    {
      "issue": "D（画面キャプチャ）",
      "decision": "4 枚・2x2 の表・width=300・保存先 docs/images/readme/ をすべて現状維持。トリミングは行わない。alt テキストも現状のまま。",
      "rationale": "reader_flow が round 2 で自らトリミング提案を撤回し major から minor へ格下げした。撤回理由は ① digest 末尾の Ecosyste.ms 出典注記は D-29（CC BY-SA 4.0 の帰属表示）に関わり画像から落とせない ② search / gems の下位カード列は多様なレジストリ・多数の実データという説得力そのもので、切ると情報量の証拠性が下がる。req_gate も「与件 §6 は画像内に何が写るかを規定していない」としつつトリミング時の alt 整合修正を条件付き指摘としており、トリミングしないため発生しない。実在ユーザーのアバターは製品自体が公開データとして表示しているもので LP も同種のショットを載せており、4 レンズから問題視なし。",
      "rejected": "digest を 786x1968 / 786x1600 へ、search / gems を 786x1600 / 786x1500 へ切る案 = 出典表示と証拠性を失う。digest の装飾イラスト 372px 除去 = 提案者自身が nice-to-have へ格下げ。"
    },
    {
      "issue": "E（構成と分量）",
      "decision": "冒頭の目次行に「技術スタック」を追加する。節の並び順は現状維持。",
      "rationale": "reader_flow の minor 提案。採用コストが目次 1 行のみで、他のどの指摘とも衝突しない。並び順については 4 レンズから変更要求が出なかった（目次行が導線を救済している）。",
      "rejected": "なし"
    },
    {
      "issue": "F（スコープ）",
      "decision": "ADR 0004 への 1 行追記は本 PR に含めたまま維持する。minimum-requirements-checklist.md の陳腐化 4 箇所は本 PR で扱わず別 Issue を起票する。",
      "rationale": "ADR 0004 は README 本文が書き換えた事実（実在しない CI ゲート → セッション実行ゲート）の裏返しで、SD-4 が要求する矛盾解消。scope_guard が「新しいファイル・新しい主題領域を持ち込む提案ではない」と判定。checklist は 4 箇所の実質的な書き直しが要りスコープの明示リスト外で、scope_guard・req_gate・fact_check の 3 レンズが独立に out_of_scope と結論した。",
      "rejected": "checklist を同じ PR で直す案 = CP-1 の「スコープ外の改善は別 Issue を立ててから着手する」に反する。"
    }
  ],
  "edits": [
    {
      "file": "README.md",
      "section": "設計上の判断",
      "action": "add",
      "detail": "短い節を 1 つ新設する: 「発見面と一覧の見せ方を『有限』で区切った（ADR 0014 / ADR 0008）」。今日の Gem を日次・有限 5 件にし、日付を唯一のシードとする決定論的生成でサーバー側に状態を持たせない判断と、一覧に無限スクロールを採らずページネーションにした判断（キーボードで末尾に到達できる・スクリーンリーダーで現在位置を提示できる・戻り位置を URL の 1 値で復元できる）を 1 節にまとめる。"
    },
    {
      "file": "README.md",
      "section": "Gem Index の母集団と配信の作り方",
      "action": "update",
      "detail": "「この 2 段を通した結果が配信プールの 62,483 リポジトリ」という 62,483 の算出根拠を繰り返す 1 文を削り、Hidden Gem 節への参照に置き換える。"
    },
    {
      "file": "README.md",
      "section": "Gem Index の母集団と配信の作り方",
      "action": "update",
      "detail": "配信方式の記述に src/infrastructure/platform/static-gem-index.ts へのリンクを足す。"
    },
    {
      "file": "README.md",
      "section": "冒頭の目次行",
      "action": "update",
      "detail": "「この README で分かること」に「技術スタック」を追加する。"
    }
  ],
  "critical": [],
  "out_of_scope": [
    "docs/02_requirements/minimum-requirements-checklist.md の陳腐化 4 箇所（.github/workflows/ の存在有無・チェック本数「全 20」・設計上の判断が旧 2 項目のまま・環境変数「8 変数」）を実態へ追従させる",
    "docs/02_requirements/roadmap.md の M-6 が SP-17〜SP-19 を含んでいない（既存 Issue #324 と同種）",
    "NOTICE に Ecosyste.ms（CC BY-SA 4.0）と Geist（SIL OFL 1.1）の帰属表示が無い（既存 Issue #366）",
    "docs/images/readme/mobile-digest.webp の装飾イラスト区間（約 372px）の圧縮（次回 UI 更新時のついでで足りる）"
  ],
  "open_questions": []
}
