<!-- discussion_whiteboard:auto -->
# 🧑‍🏫 議論ホワイトボード: 実装着手前の今、gem-hunter に画面遷移図などの UI/UX 設計成果物を用意すべきか

- 議題ID: `uiux-upfront-design-20260818`
- 論点: スプリント開発（ユーザーストーリーベース・途中軌道修正が前提）の gem-hunter で、実装着手前の今、画面遷移図・サイトマップ・ワイヤーフレーム・状態遷移図などの UI/UX 設計成果物を用意すべきかを、最新のトレンド／既存ドキュメントの実態／スプリント順序のリスク／コスト対効果の 4 レンズで敵対的に検証する。
- 参加者: `trend-researcher`, `doc-architect`, `sprint-risk-reviewer`, `yagni-cost-reviewer`
- 投稿数: 10
- 更新: 2026-08-18T09:16:51+09:00

> このファイルは `tools/discussion_whiteboard.py render` が自動生成する。直接編集せず `post` で追記すること（同時書き込み破損防止）。

## ラウンド 1

### `trend-researcher` — 主張
<sub>2026-08-18T09:10:37+09:00</sub>

## trend-researcher: 2026 年時点のトレンド調査結果

### 1. アジャイル/スプリントにおける「どこまで先に設計するか」の定説

- **BDUF（Big Design Up Front）は依然アンチパターン扱いが定説**。Wikipedia/DevIQ とも「変更コストが高い」「プロトタイピングなしに問題領域を予見できない」を根拠に批判を維持している。[Big design up front - Wikipedia](https://en.wikipedia.org/wiki/Big_Design_Up_Front) / [Big Design Up Front (BDUF): A Software Development Antipattern – DevIQ](https://deviq.com/antipatterns/big-design-up-front/)
- 一方で「Doing Agile Wrong: When is Big Up-Front Design Right?」のように **BDUF が正当化される条件**（規制産業・複数チーム間の契約的インターフェース・後戻りコストが極端に高い基盤部分）を論じる記事も 2026 年時点で存在し、「BDUF=常に悪」ではなく **文脈依存** という立場が主流。[Doing Agile Wrong – The Interconnected](https://theinterconnected.net/kirabug/doing-agile-wrong-when-is-big-up-front-design-right/)
- **Dual-track agile が 2026 年の主流モデル**: リサーチ/デザインと実装を並行 2 トラックで回し、デザイナーは実装の 1〜2 スプリント先を設計する（全部を先に固めない）。[UX Design and Agile: Dual-Track Done Right in 2026 | CorsoUX](https://courseux.com/ux-design-and-agile/)
- **Lean UX の位置づけ**: 成果物（ドキュメント）を作ることより「学習速度」を優先し、軽量 MVP で仮説検証する思想。Agile UX（ワイヤーフレーム等の構造化成果物を作る）と対比される。[What is Lean UX? — updated 2026 | IxDF](https://ixdf.org/literature/topics/lean-ux) / [9 UX Design Frameworks Every Product Team Should Know (2026)](https://www.uxpin.com/studio/blog/design-frameworks/)
- **結論**: 「Big Design Up Front はしない」が定説の骨格は変わっていないが、「Just Enough Design（次の 1〜2 スプリント分だけ先行）」が推奨形。個人開発・単独スプリント運用（本リポジトリ）では複数トラックの並行体制自体が過剰。

### 2. 画面遷移図/ユーザーフロー図/IA 図の現在の位置づけ

- **静的画像の遷移図は陳腐化しやすいと明言される一方、"diagram as code" として形を変えて残っている**。Mermaid のようにテキストで書きリポジトリにコミットし、PR でレビューし、コードと同じパイプラインで更新する運用が 2026 年の標準的推奨。[Mermaid Diagrams Quickstart and Cheatsheet for Developers](https://www.glukhov.org/documentation-tools/diagrams/mermaid-diagrams-quickstart-cheatsheet/) / [10 Copy-Paste Mermaid Diagram Examples for 2026 — GitDoc](https://gitdoc.ai/resources/mermaid-diagram-examples)
- ドリフト対策の要点は「**図をコードと同じ PR で更新する**」「**スキーマ/エンドポイント定義の隣に置く**」の 2 点に集約されている（本リポジトリの `artifact-diagramming` スキルが Mermaid をネイティブ対応しているのは追い風）。
- **含意**: 「先に一枚絵の画面遷移図を作って終わり」は推奨されない。作るなら Next.js の `app/` ディレクトリ構造＝ルーティング定義そのものを正本にし、Mermaid は補助的な可視化として **必要になった時点で・コードと同じ場所で** 生成する方が長持ちする。

### 3. AI コーディング時代の変化（最重要論点）

- **Spec-Driven Development（SDD）が 2026 年の主要潮流**: 「コードではなく実行可能な仕様（spec）を正本にする」考え方。vibe coding の失敗モード（もっともらしいが意図からドリフトするコード・API 幻覚）への直接的な反動として 2025 年に台頭し、2026 年半ばには Claude Code・Cursor・GitHub Spec Kit・AWS Kiro 等主要ツールが軒並み対応。[Spec-Driven Development with AI Coding Agents (2026)](https://zeroshot.ghost.io/spec-driven-development-with-ai-coding-agents/) / [Spec-Driven Development (SDD): The Definitive 2026 Guide](https://www.thebcms.com/blog/spec-driven-development/)
- **重要な非対称性**: SDD が求める「spec」は**振る舞い・受け入れ条件を記述したテキスト仕様**であり、**ビジュアルなワイヤーフレーム画像ではない**。AI エージェントへの入力として有効なのは「テキスト仕様」（本リポジトリの `prd.md` / `user-story-map.md` の AC）であって、静的ワイヤーフレーム画像はむしろ補助情報止まり。
- **Claude Design / Figma Make / v0 の位置づけ**: 「コードが最初のデザイン成果物になる」動きが加速。Claude Design はコードベースを読んでデザインシステムを抽出し、そのまま HTML として動く出力を返す＝**プロトタイプ＝実装コードの一種**になりつつある。[Claude Design (Anthropic): The Complete 2026 Guide](https://agence-scroll.com/en/blog/claude-design-anthropic-2026-guide) / [Claude Design vs Figma Make: Which AI Prototyping Tool Is Better?](https://alloy.app/library/figma-make-vs-claude-design)
- **AI エージェント実装前提での含意**: 人間チーム前提のセオリー（「エンジニアに渡す前に非エンジニアが読めるワイヤーフレームを用意する」）は、実装者が AI エージェント（このセッション自身）になる場合は動機が薄れる。AI エージェントは Figma のような画像よりテキスト仕様（AC・状態一覧・URL 設計）を直接消費できるため、**「絵を描く」コストを「テキストで仕様化する」コストに置き換えるほうが投資対効果が高い**。
- ただし「Vibe Coder Blog」の指摘（後述 §5）のとおり、**AI が生成した UI は状態設計を素で漏らす傾向がある** ため、"仕様が要らない" わけではなく "絵ではなくテキスト・チェックリストで仕様化すべき" という方向にシフトしている。

### 4. URL/ルーティング設計を先に固定することの扱い

- Next.js App Router では **ディレクトリ構造＝URL 構造** が直接対応するため、ルート設計は「別図に描く」のではなく **`app/` の設計そのものが正本**になる。route group の命名規約（`(marketing)` のような無意味な名前を避け `(dashboard)` 等の意味のある名前にする）が実務上の要点。[App Router Directory Design: Next.js Project Structure Patterns](https://dev.to/pipipi-dev/app-router-directory-design-nextjs-project-structure-patterns-31eo)
- **共有可能な URL（クエリパラメータでの状態保持）は事前に方針だけ決めておく価値がある**: リロード耐性・共有可能性のため、フィルタ/検索状態を URL クエリに载せるかどうかは早期に決めるべき設計判断（後から変えると壊れるため）。[mastering state in next js app router with url query parameters](https://medium.com/@roman_j/mastering-state-in-next-js-app-router-with-url-query-parameters-a-practical-guide-03939921d09c)
- **結論**: 「URL を図に描く」必要はないが、「検索クエリ・フィルタ条件を URL に持たせるか」という **1〜2 個の設計方針の言語化** は、画面遷移図よりずっと軽いコストで同じ目的（後戻りコスト回避）を達成できる。

### 5. 状態設計（loading/empty/error/初期）の一覧化

- 2026 年の実務記事群は一貫して **「Empty / Loading / Error / Success の 4 状態は全画面で洗い出すべき」** と明言。特に **AI 生成フロントエンドはこの 4 状態のうち初期状態以外を素で欠落させがち**、という直接的な指摘がある: 「Empty States, Loading States, Error States: The UX AI Forgets」。[Empty States Loading States Error States The UX AI Forgets](https://blog.vibecoder.me/empty-states-loading-states-error-states)
- ローディングは 2026 年時点でスピナーではなく **skeleton screen** が推奨（体感待ち時間を約 30% 短縮・レイアウトシフト防止）。[Nobody Designs the Empty State](https://medium.com/design-bootcamp/nobody-designs-the-empty-state-thats-exactly-why-your-app-feels-unfinished-da3396570de0)
- **含意**: これは gem-hunter（GitHub 検索アプリ）に直接刺さる。検索結果 0 件・API レート制限エラー・初回訪問時の空状態は、画面遷移図より **「各画面の状態一覧表（テキストのチェックリスト）」** の形で先に洗い出すほうが実装バグを防ぎやすく、AI エージェント（実装者）が読み落としにくい。

---

### trend-researcher の暫定結論

**先に作る価値がある（ただし絵ではなくテキスト/コードとして）**:
1. **状態一覧チェックリスト**（各画面 × loading/empty/error/success を表で洗い出す）— AI 実装エージェントが素で漏らす領域への直接対策。コストは低く効果が高い。
2. **URL/ルーティング設計方針の 1 ページ言語化**（`app/` 構造そのものを正本にし、フィルタ・検索語をクエリに持たせるかだけ明文化）。Next.js App Router では図よりディレクトリ構造が正本になるため、別途遷移図を描く投資対効果は低い。
3. 複雑なフロー（認証・多段階ウィザード等）が発生するスプリントに限り、**その場で Mermaid をコードと同じ PR に書く**（先に一括で全画面分を用意しない）。

**先に作る価値が低い/無駄になりやすいもの**:
- 全画面を網羅した静的ワイヤーフレーム集・画面遷移図（BDUF 的投資、AI エージェント実装では消費されない、スプリントで頻繁に変わる個人開発では陳腐化が確実）
- 実装前の高忠実度モックアップ（Claude Design 的にコード＝プロトタイプになる時代性を踏まえると、「動くコード」を先に出したほうが安い）

**根拠の要約**: 2026 年の潮流は「Big Design Up Front からの脱却」自体は変わらず継続しているが、AI コーディングエージェント時代に入り **「先に描くべきもの」がビジュアル成果物からテキスト仕様（spec-driven development・状態一覧・受け入れ条件）へシフトした** という点が新しい論点。gem-hunter は個人開発・単独セッションの実装なので、複数人チーム前提のドキュメント（画面遷移図・IA 図）よりも、実装エージェント自身が消費しやすい形式（AC・状態表・ルーティング方針）に投資する方が期待値が高い。

### `doc-architect` — 主張
<sub>2026-08-18T09:11:06+09:00</sub>

# doc-architect: 実証的判定（既存ドキュメントの現況調査）

## 1. サイトマップ（画面一覧）は導出できるか

**できる。画面は 2 つのみ**（`prd.md` §8.1 トップページ / §8.2 詳細ページ）。導出に必要なファイルは実質 1 つ（`prd.md` §8）で、`user-story-map.md` §1 のバックボーン図（A-1〜A-7）が行動レベルの補完情報を与える。画面数が 2 つしかないプロダクトでは「サイトマップ図」という成果物形式そのものが過剰投資になりやすい。

## 2. 画面遷移は既に書かれているか

**部分的に書かれているが、1 箇所に集約されていない。** 遷移の実体は `user-story-map.md` §5.3 の各 `SP-n` の「操作レビュー」手順に埋め込まれている（例: SP-3「カードを選ぶ→詳細ページへ遷移→戻る」、SP-2「`/`→`/ja` へリダイレクト」）。これは画面遷移図ではなく E2E テスト手順として書かれているが、**遷移そのものの情報量は網羅的**。

**抜けている遷移（実証済み）**:
- **ログイン/ログアウトの往復**: `prd.md` AR-5 詳細に「セッションは httpOnly Cookie」「ログアウト提供」と機能記述はあるが、GitHub OAuth への離脱→復帰という**画面をまたぐ遷移**を図示した箇所はゼロ。SP-8 の操作レビュー（`user-story-map.md` L373-377）も「ログインする→レート枠が切り替わる」の 1 行で、コールバック遷移の中間状態は書かれていない。
- **エラーからの復帰**: `prd.md` §7 とガイドライン §5.2 は「エラー種別 → 文言」の対応表であり、**画面から画面への遷移**ではなく **1 画面内の状態遷移**として書かれている（これは意図的でよい設計だが、「復帰」という語が示す "別の画面/状態へ移る" 経路までは図示されていない）。
- **404（詳細ページ）**: `prd.md` §7 に 1 行（Not Found 表示）はあるが、そこから一覧へ戻る導線が図示されているのは `ui-ux-guidelines.md` §6 の「一覧へ戻るを左上に固定」という**文章記述のみ**。
- **言語切替**: `US-2`（`user-story-map.md` L87）に 1 行あるのみで、切替前後の URL 形状（`/ja/...` ↔ `/en/...`）の遷移を図示した箇所はない。
- **ページ送り**: SP-7 操作レビュー（L362）にあるが単発で、他の状態（0 件・エラー）との組み合わせ遷移は書かれていない。

## 3. 🔴 URL 設計（パラメータ名・既定値・ロケールの置き場所）— 検証結果

**ロケールの置き場所は確定済み**: `prd.md` AR-4 詳細（L239）「`/ja/...` / `/en/...`」。これは実装者が迷わない粒度。

**しかしクエリパラメータの名前と既定値は、探した全ファイルのどこにも定義されていない。**

- `prd.md` §2.4（状態の置き場所表）は「検索キーワード・ページ・ソート・表示件数 → URL（`searchParams`）」としか書いておらず、キー名（`q`? `keyword`? `query`?）も既定値（`page=1`? `sort=best-match`?）も書いていない。
- `user-story-map.md` US-9（L99）は「S-0 の範囲はキーワード・ページ・ロケールの 3 つ」「ソート・件数は **パラメータ名と既定値だけを決めておく**」と明言しながら、**その決定を書く場所を一切指定していない**。「決めておく」という指示だけがあり、正本の置き場が存在しない。
- `ui-ux-guidelines.md` §4.1 も「すべての状態は URL の `searchParams` に反映する」という抽象論のみで、具体キー名には触れない。

→ **これは doc-architect の判定基準に照らして「本当に欠けている情報」である。** US-9 の指示を実行する場所が現状ゼロなので、このままでは SP-1/SP-2 の実装者が独自にキー名を決め、SP-7（ソート・件数追加）の実装者が別の命名慣習で追加する事故が起こり得る（`NFR-18`「キャッシュキー命名規約」と同種のリスクが URL パラメータ名にも存在するのに、こちらだけ正本がない）。

## 4. 状態（初期/読み込み中/0件/エラー種別）の網羅性

**トップページは十分**: `ui-ux-guidelines.md` §4.4 に 4 状態のマトリクスが明示され、`AC-8` / `prd.md` §7 と対応が取れている。

**詳細ページ側は抜けている**: `ui-ux-guidelines.md` §6（詳細ページ）は表示項目のみを規定し、**読み込み中・エラー（404 以外の 401/403/5xx 等）の状態表現に一切触れていない**。`prd.md` §7 のエラー種別表は画面を特定していないため、「404 は詳細ページに出る」ことは書いてあるが、「詳細ページでネットワーク到達不可が起きたらどう出すか」は不明。画面 × 状態のマトリクスとして見ると、**詳細ページ列がほぼ空欄**。

## 5. SSOT 分離とドリフト負債のリスク評価

本リポジトリは `user-story-map.md` §0.1 で「受け入れ条件を再掲しない」と明示するほど正本分離を厳格運用しており、新規ドキュメントの追加は次の 2 つのコストを常に伴う。

1. **保守コスト**: `prd.md` §2.5 未決事項・`open-questions.md` の決定ログ更新のたびに、新規ファイルとの整合を人力で取る必要が生まれる（現に `prd.md` と `user-story-map.md` の間だけでも 8 箇所以上の相互参照規律がある）。
2. **権威順の追加項目**: `intent-gate-rules.md` の権威順（ユーザー明示 > 仕様 > テスト > 現行コード）に「新規設計図」がどこに入るか不明確になり、矛盾時の解決コストが上がる。

**したがって「画面遷移図」を独立した正本ファイルとして新規作成することには反対する。** 情報のほとんどは既に存在し、欠けているのは「一覧性」であって「決定内容」ではない（例外は §3 の URL パラメータ名だけ）。

---

## 暫定結論（doc-architect）

**今作るべきもの**:
- ❶ `prd.md` §2.4 の拡充（新規ファイルではなく既存正本への追記）: クエリパラメータ名・既定値の表を 1 つ追加する。**正本は `prd.md`**（要件の正本であり、`NFR-18` のキャッシュキー命名規約と同じ性質の「後から変えると壊れる」情報のため）。US-9 が「決めておく」と要求している内容の置き場所を作るだけで、US-9 の指示自体は既に存在する。
- ❷ `ui-ux-guidelines.md` §6（詳細ページ）に状態表現の節を追記（§4.4 相当を詳細ページにも作る）。**正本は `ui-ux-guidelines.md`**（UI 実装判断基準の正本であるここに置くのが一貫する）。

**作らないもの**:
- 独立ファイルとしての画面遷移図・サイトマップ・ワイヤーフレーム。画面数が 2 つしかなく、遷移情報は `user-story-map.md` §5.3 の操作レビュー手順に実質的に存在する。新設すると「操作レビュー手順」と「遷移図」の 2 箇所が同じ事実を持つことになり、本リポジトリが最も警戒するドリフト（`D-8`）を自ら生む。
- ログイン/ログアウト・エラー復帰・404 の遷移は、**新しい図ではなく §5.3 の該当 SP（SP-8/SP-9）の操作レビュー手順自体に 1〜2 行足す形**で補う方が、正本を増やさずに済む。

**位置づけの一言**: 「欠けているのは絵ではなく、US-9 が要求した決定（URL パラメータ名表）そのもの」。絵を先に作ると本当の欠落（決定未了）を覆い隠すリスクがある。

### `sprint-risk-reviewer` — 主張
<sub>2026-08-18T09:11:27+09:00</sub>

# sprint-risk-reviewer: 上流 UI/UX 設計成果物は不要（結論: 作らない）

## 1. SP-2 で「入力となる設計」は既に文章で存在する

`SP-2`（`US-1`/`US-9`/`E-4`/`E-9`）が固定すべき境界は、実は **`user-story-map.md` 自身が既に文章で決めている**。

- URL 反映範囲: `US-9` 注記「S-0 の範囲はキーワード・ページ・ロケールの 3 つ（ソート・件数は `US-7`/`US-8`）」→ **決定済み**
- ロケール置き場所: `US-1`「`/` は `/ja` へリダイレクト」= パスプレフィックス方式 → **決定済み**（`AR-4`）
- 配色: `ui-ux-guidelines.md` §2.1 に **9 トークン×ライト/ダークの構成が確定済み**。未確定なのは「実測値」だけで、これは `E-9` = `SP-2` 本体の作業そのもの（ワイヤーフレームでは前倒しできない、実測が要る）

未確定で残るのはパラメータ名（`q` か `query` か）・詳細パスの形（`/ja/{owner}/{repo}` か `/ja/repo/{owner}/{repo}` か）程度で、これは `SD-3` の「実装手段・命名」に分類され **確認不要・自律決定・仮定記録**（`sprint-development-rules.md` §3）。ワイヤーフレームがあってもなくてもこの決定コストは変わらない。**SP-2 セッション内での即興決定で問題なし。**

## 2. 論点2: SD-3 割り込みは事前図で減るか — 名指し検証

| 分岐点 | SD-3 該当性 | 判定根拠 |
|---|---|---|
| 検索実行後の URL 形 | ❌ 実装手段 | パラメータ名は命名。範囲は §1 で既決 |
| 詳細ページのパス形 | ❌ 実装手段 | ユーザー体験に差が出ない純粋な URL 構造 |
| ページ番号 0/1 始まり | ❌ 実装手段 | 内部表現。GitHub API 慣習に合わせれば「最も単純な合理的解釈」で自律決定可 |
| ソート値の名前 | ❌ 命名 | 自律決定 |
| **言語切替時のクエリ保持** | ⚠️ 要検討 | `US-2` は locale 保持のみ明言。検索中のクエリ保持は未記載 → 2 通りの挙動がありうる |
| **ログイン後の戻り先** | ⚠️ 要検討 | `US-4`/`AR-5` に着地点の明記なし。同一ページ復帰 vs トップ遷移で成果物が変わる |

6 例のうち **4 件は元々 `SD-3` の確認対象ですらない**（実装手段）。事前に図を描いても描かなくても、この 4 件は自律決定で処理され割り込みは発生しない。図を描く動機として持ち出すのは論点のすり替え。

残り 2 件（言語切替時のクエリ保持・ログイン後の戻り先）は真に `2 通りの解釈で成果物が変わる` 候補だが、これは `SP-8` 実装時に初めて実装知識込みで判断材料が揃う（OAuth コールバック実装・i18n ルーティング実装が先に要る）。**事前の画面遷移図がこの 2 件を「なくす」のではなく「前倒しするだけ」**（設問4への回答が示す通り）。しかも前倒しした場合、`SP-2`/`SP-8` 実装前の抽象的な状態で聞くことになり、`SD-3` §「答えに依存しない作業を先に全部終わらせてから、まとめて聞く」の原則にも反する（早すぎる確認はむしろ悪手）。

## 3. 論点3: 先に描く害（本プロジェクトで具体的に起きる）

- **鮮度崩壊が確実**: `§5.5` で `SP-6`/`SP-7`/`SP-10` は自由入替可、`SP-1` は sp:8 で「1 セッションに収まらない場合はその場で分割」と明記。画面遷移図が SP 単位の到達順や画面構成を先取りして描けば、**最初の入替・分割で即座に陳腐化**する。
- **権威系統の外側に第三の正本が生まれる**: `user-story-map.md` §0.1 は「受け入れ条件を再掲しない、2 箇所がドリフトすると判定不能になる」と自ら明記している。ID トレーサビリティ（`US-n`/`AC-n`）に紐付かない独立したワイヤーフレームファイルを作ると、まさにこの警告が指す "第三の正本" になり、`intent-gate-rules.md` の権威順（ユーザー明示 > 仕様 > テスト > 現行コード）のどこにも位置づけられない孤児ドキュメントになる。
- **未決事項の上に絵を描くリスク**: `prd.md` §13 は「カードの情報密度・視覚的階層」（`US-12`）を `R-10` 未確定のまま開いている。ここでワイヤーフレームを描くと、`R-10` 確定前に確定したかのような誤ったシグナルを出し、`E-9` の配色同様「決めたつもりにしない」原則に反する。

## 4. 論点4: SP-6〜SP-11 の自由入替は「全体像不要」を支持する

支持する。自由入替が成立している理由は、**画面間の一貫性を担保する機構が既に別の場所（`ui-ux-guidelines.md` §2-3 のデザイントークン: 配色・タイポスケール・4px グリッド）にあり、それが SP の順序に依存しない**ため。画面遷移図は「どの順で作るか」の一貫性は保証できても「見た目・挙動が揃うか」は保証できない — トークン系が既にその役目を担っている。順序を固定しない設計に対して順序前提の成果物（多くの画面遷移図はフロー＝順序を暗黙に固定する）を足すのは、設計思想と逆行する。

## 5. 論点5: AI エージェント・セッション分断という前提の違い

人間チームの議論とは前提が違う点を明示する。

- 人間チームでは「口頭議論の記憶」が失われるため図が有効な外部化装置になるが、本プロジェクトは **`session-compression-rules.md` により口頭指示は圧縮で失われる前提** で、恒久情報は `CLAUDE.md`/`docs/rules`/Issue/PR コメントに書く運用が既に確立している。
- AI エージェントにとって最も鮮度が高い「画面の実態」は、`SD-1` が毎スプリント義務付けるプレビュー URL そのもの（実際に動くコード）であり、これは **絶対に陳腐化しない**（描いた瞬間から古くなる図と違い、実装と同期している）。次セッションは新規描画物を読むより先にプレビュー URL を触るべきで、それが `SD-1`/`SD-4` の設計そのもの。
- 「画面フローの全体像を素早く把握する」というニーズ自体は `user-story-map.md` §1 のバックボーン図（`A-1`〜`A-7` の ASCII 図）と §5.3 の SP 別テーブルで **既に文章として提供済み**。これは ID トレーサビリティに組み込まれ、§7 運用ルールで更新規律も定義されている。新しいワイヤーフレーム/画面遷移図を追加で作ることは、既にある正本を重複させ、更新規律のない孤児ファイルを増やすだけ。

## 暫定結論

**上流 UI/UX 設計成果物（画面遷移図・サイトマップ・ワイヤーフレーム・状態遷移図）は事前に作らない。**

- 根拠: (1) SP-2 の入力となる決定は既に `user-story-map.md`/`prd.md`/`ui-ux-guidelines.md` に文章で確定済み。(2) 名指しされた分岐点の大半は `SD-3` 対象外（実装手段）で図の有無と無関係に自律決定される。残り僅かな真の分岐は前倒しでなく実装知識が揃う該当スプリントで `AskUserQuestion` する方が精度が高い。(3) 自由入替可能な設計自体が「順序前提の全体像」より「トークン系による一貫性」を選んでいる。(4) AI エージェント運用ではプレビュー URL と `user-story-map.md` の記述が図より鮮度・権威性で優れる。
- **唯一のリスク低減策**: `SP-2` 実施時に、パラメータ名・詳細パス形などの命名決定を **1〜2 行のテキストで** `user-story-map.md` §8 相当の場所か最初のコミットメッセージに記録する（`SD-3` の仮定記録義務どおり）。これは「事前に描く」ではなく「決めた瞬間に書き残す」。
- 例外的に価値がありうるのは、`E-9` の配色トークンを **`SP-2` 内で実測して確定した後**、その実測値を `ui-ux-guidelines.md` §2.1 の表に追記する作業（=すでにルールで規定済み・追加の準備成果物ではない）。

### `yagni-cost-reviewer` — 主張
<sub>2026-08-18T09:11:52+09:00</sub>

# YAGNI 判定: UI/UX 設計成果物の必要性判定

## 現状確認: 既に存在する成果物

1. **ユーザーストーリーマップ** (`user-story-map.md`)
   - バックボーン（A-1 ～ A-7）と流れ図が ASCII アート + 構造化表で明示
   - ユーザーストーリー 27 本（US-1 ～ US-27）に分解完了
   - 各 US に参照要件 ID・スライス割り当て済み

2. **UI/UX ガイドライン** (`ui-ux-guidelines.md`)
   - **技術スタック決定**: Tailwind v4 / shadcn/ui / next-themes
   - **デザイントークン基本構想**: 9 トークン × ライト/ダーク（実値は SP-2 で確定）
   - **タイポ・スペーシング原則**: 5 段階 / 4px グリッド
   - **各ページのレイアウト詳細設計**:
     - トップページ: コントロール配置・カード要素順序（ASCII 図で視覚化）・4 状態の出し分け方
     - 詳細ページ: 統計行配置・メタ情報表現方法
     - エラー表示: 種別別文言設計・a11y 要件
   - **a11y 実装指針**: route announcer・aria-live・focus 管理

3. **要件定義書** (`prd.md`)
   - 与件・上乗せ要件・非機能要件が ID で体系化

---

## 候補成果物の評価

### 1. 画面遷移図（Mermaid）

| 項目 | 評価 |
|------|------|
| **作成コスト** | SP 1-2（user-story-map.md §1 の A-1～A-7 を Mermaid にシリアライズ） |
| **維持コスト** | **高** — US 追加・削除・分割のたびに同期。実装フェーズで図と実装がドリフト常態化 |
| **効果** | **中** — ASCII ステップフロー図（user-story-map.md）が既に A-1→A-2→A-3... の時系列を示している。Mermaid は「ビジュアル」という +α だけ |
| **既存で足りるか** | **足りる** — user-story-map.md §1 の ASCII 図で流れは明確 |
| **推奨** | **❌ やらない** |

**理由（YAGNI）**: 既存のテキスト図で時系列フローは把握可能。図の本来の価値「デザイナ↔エンジニア間のコミュニケーション」が、個人開発 AI 実装では存在しない（通路が最初からない）。維持コストを負うだけ無駄。

---

### 2. サイトマップ / 画面一覧表

| 項目 | 評価 |
|------|------|
| **作成コスト** | SP 1（ルーティング設計表と統合すると効率的） |
| **維持コスト** | **低** — ルーティング確定後はほぼ不変 |
| **効果** | **中** — US と画面の対応（US-1=`/[locale]`・US-16=`/[locale]/repos/...`）をいま 1 表にまとめるだけで、実装時に参照性が上がる |
| **既存で足りるか** | **部分的に足りる** — user-story-map.md が US ごとに「どの機能か」を含むが、**URL 構造が専用表として見えない** |
| **推奨** | **🟢 やる**（ただし URL 設計表とセット化） |

**理由**: S-0 項目（US-9 で「URL に反映」と明記）だから。**URL パス・クエリパラメータ名・既定値を 1 表に集約する**と、実装前の仕様解釈ズレを減らせる。コストは軽い（SP 1）。

---

### 3. URL・ルーティング設計表（パス・クエリパラメータ名・既定値）

| 項目 | 評価 |
|------|------|
| **作成コスト** | SP 1 — Next.js app router 前提での パス / `searchParams` 仕様を表にまとめるだけ |
| **維持コスト** | **低** — ルーティング確定後は極めて安定 |
| **効果** | **大** — S-0 項目。実装時の「クエリパラメータ名の揺らぎ」「既定値の曖昧さ」を事前に潰す |
| **既存で足りるか** | **足りない** — US-9 は「キーワード・ページ・ロケール」だけを名指ししているが、パラメータ名（`q`? `query`?）や `page` の既定値（1 ベース? 0 ベース?）が決まっていない |
| **推奨** | **🟢 やる（必須）** |

**理由**: S-0 でリポジトリ公開・共有リンク長期保持が要件。パラメータ設計が後付けコストになると既存 URL 全破壊。実装前に確定必須。

---

### 4. ワイヤーフレーム（低忠実度）

| 項目 | 評価 |
|------|------|
| **作成コスト** | SP 3-5 — Figma で low-fi sketch、またはオンラインツール |
| **維持コスト** | **中** — 状態追加・コントロール位置変更のたびに更新 |
| **効果** | **小～中** — ui-ux-guidelines.md が以下を既に記載: |
| | - 要素の順序（§4.2 の `┌─────┐` 図） |
| | - 各要素のフォント・色・高さ規定（§4.2 の表） |
| | - スペーシング原則（§2.3） |
| | - 状態遷移（§4.4・§5） |
| **既存で足りるか** | **十分に足りる** — ガイドラインがテキスト + ASCII 図で相対位置・余白・色を詳述済み |
| **推奨** | **❌ やらない** |

**理由（YAGNI）**: ガイドラインの記述密度が既に「ワイヤーフレーム並み」。追加図は「きれい」という付加価値だけだが、実装フェーズで図が古くなり、無視される常態化リスク大。同期コスト >> 実装サポート効果。

---

### 5. 画面 × 状態マトリクス（初期 / 読み込み / 0 件 / エラー等）

| 項目 | 評価 |
|------|------|
| **作成コスト** | SP 2 — ページごとに状態一覧 × UI 表現を記入 |
| **維持コスト** | **中** — 新しい状態・エラー種別追加のたびに更新 |
| **効果** | **中** — トップページの 4 状態は ui-ux-guidelines.md §4.4 で既に表定義。**詳細ページ・エラー詳細ページは状態設計が未記載** |
| **既存で足りるか** | **部分的** — トップページは足りる。詳細ページ・404 の「戻る導線」「状態変化」は？ |
| **推奨** | **🟡 任意**（優先度は低い） |

**理由**: メインシナリオ（検索→一覧→詳細）の状態は既に §4.4・§5・§6 で埋まっている。全ページ統合表は「落とし穴防止」には有効だが、エネルギーをかけるなら **URL 設計表（S-0・先行確保）の方が先**。 実装開始後の「あ、この場面の状態表現どうする？」はゴリ押しで対応可能（UI ガイドラインが詳しいから）。

---

### 6. 高忠実度デザインカンプ / プロトタイプ（Figma）

| 項目 | 評価 |
|------|------|
| **作成コスト** | SP 8+ — ビジュアルデザイン + インタラクション検証 |
| **維持コスト** | **極度に高** — 実装で細部が変わるたびに同期。最終ピクセル合わせを強制するとコストが爆発 |
| **効果** | **中** — ビジュアル + インタラクション確認。ただし実装が進むと図は無視される傾向 |
| **既存で足りるか** | **十分に足りる** — デザイントークン（色・フォント・スペーシング）と ui-ux-guidelines.md の記述で、実装レベルでの統一性は確保可能 |
| **推奨** | **❌ やらない** |

**理由（YAGNI）**: 個人開発 + AI 実装の文脈では、**Figma カンプ ↔ 実装の同期コストが成果物の再現価値を完全に上回る**。むしろ「実装を進めながら UI ガイドラインで例外対応・微調整」の方が現実的で軽い。

---

## 総括（推奨アクション）

### 🟢 **必ずやる**（S-0 先行確保）
- **URL・ルーティング設計表** (SP 1)
  - 記載内容: ページパス・クエリパラメータ名・既定値・制約
  - 例: `GET /[locale]/`、`GET /[locale]/repos/[owner]/[repo]`、`searchParams: { q, sort, limit, page }`
  - サイトマップとセット化（計 SP 1）

### 🟡 **やると便利だが省略可**
- **サイトマップ / 画面一覧表**（上記 URL 表とセット）
  - コスト軽い（SP 1）。実装時の参照性上昇。ただし必須ではない

### ❌ **やらない**（既存で十分・コスト > 価値）
- 画面遷移図（Mermaid）
- ワイヤーフレーム（低 / 高忠実度両方）
- 高忠実度デザインカンプ
- 統合 × 状態マトリクス（ui-ux-guidelines §4.4・§5 で既に定義）

---

## YAGNI の核となる原則（本プロジェクト）

1. **デザイナ ↔ エンジニアの通路が不在**
   - 個人開発で AI 実装。図の最大価値「職能間コミュニケーション」が消えている
   - 代わりに「実装が参照しやすい優先度順テキスト」の方が実用的

2. **後付けコスト > 事前図**（except S-0）
   - 実装フェーズで仕様微調整が必須（AI が仕様を照らし合わせて実装するため）
   - 古い図を同期するなら、その時間を「実装 → ガイドライン記述」に充てた方が効率的

3. **1 箇所しか使わない抽象化を先に作らない**（CLAUDE.md より）
   - トップページと詳細ページしかないのに「全ページ統合状態マトリクス」を先に作るのは YAGNI
   - 使い回す複数ページが出てから、共通パターンを抽象化

4. **S-0 は先行確保**（不可逆な破壊を避ける）
   - URL 設計だけは「後付けコスト = 既存リンク全破壊」だから先に決める
   - その他の図は「実装して確認」でも間に合う

## ラウンド 2

### `trend-researcher` — 反論
<sub>2026-08-18T09:13:32+09:00</sub>

## trend-researcher round 2: 反論・支持の整理

### 1. 「個人開発 + AI 実装だから図は不要」は妥当か → **支持（反証なし）**

`sprint-risk-reviewer` §5 と `yagni-cost-reviewer`「YAGNI の核となる原則 1」が挙げる「デザイナ↔エンジニアの通路が不在」は、round 1 で私が調べた 2 点と整合する。

- BDUF 批判記事（[Doing Agile Wrong](https://theinterconnected.net/kirabug/doing-agile-wrong-when-is-big-up-front-design-right/)）が挙げる BDUF 正当化条件は「規制産業」「複数チーム間の契約的インターフェース」であり、**gem-hunter（個人開発・単独セッション実装）はこの正当化条件のどれにも該当しない**。反証にならず、むしろ「BDUF を避けるべき典型例」に位置する。
- Claude Design / Figma Make の潮流（round 1 §3）は「コードそのものがプロトタイプになる」方向であり、`sprint-risk-reviewer` が言う「プレビュー URL（動くコード）が最も鮮度の高い画面の実態」という主張と同じ結論を別角度から補強する。**反証となる一次情報は見つからなかった**。ここは自分の round 1 結論を維持する。

### 2. 「URL 設計表だけで足りるか」→ **`yagni-cost-reviewer` の状態マトリクス評価に異議あり（rebuttal）**

`yagni-cost-reviewer` は画面 × 状態マトリクスを **🟡 任意・低優先**（「URL 設計表の方が先」）と格付けした。これに反論する。

round 1 で見つけた一次情報（[Empty States, Loading States, Error States: The UX AI Forgets](https://blog.vibecoder.me/empty-states-loading-states-error-states)）は、**「AI が生成するフロントエンドはこの 3 状態を素で欠落させる」ことをタイトルからして名指しで警告する記事**であり、これは「一般論として状態設計が大事」ではなく「**まさに本タスクの実装者（AI エージェント）がやらかす典型的失敗**」を指している。URL パラメータ命名ミス（後から直せる程度の実装手段）と、状態欠落（ユーザーがエラー時に空白画面を見る・UX 品質の直接劣化）は **リスクの性質が違う**。`doc-architect` も §4 で「詳細ページ列がほぼ空欄」と実証しており、これは "任意" と格付けするには重すぎる欠落だと考える。

- 修正提案: 詳細ページの状態表（`doc-architect` ❷ が提案する `ui-ux-guidelines.md` §6 への追記）は、URL 設計表と **同格の優先度（両方 SP-1 前後で対応）** に引き上げるべき。コストは `yagni-cost-reviewer` 自身の見積りでも SP 2 と軽量であり、「低コスト・高再発リスク」を "低優先" にするのはトレンド一次情報と整合しない。

### 3. 「散在情報 vs 一枚絵」→ **`doc-architect` の "既存 SSOT への追記" 方式を支持、`sprint-risk-reviewer` の "何も書かず口頭決定" には一部反論**

round 1 §2 で調べた通り、2026 年の diagram-as-code トレンドの核心は「**図や決定をコードと同じ場所・同じ PR で管理し、独立した第三の正本を作らない**」ことにある（[Mermaid Diagrams Quickstart](https://www.glukhov.org/documentation-tools/diagrams/mermaid-diagrams-quickstart-cheatsheet/)）。これは `doc-architect` の「新規ファイルではなく `prd.md`/`ui-ux-guidelines.md` への追記」という結論と完全に一致し、支持する。「散在した情報」自体が悪いのではなく、**決定そのものが存在しない**（`doc-architect` §3 が実証した URL パラメータ名の欠落）のが問題であり、それを一枚絵で覆い隠すのではなく正本に 1 行足すべき、という `doc-architect` の整理はトレンド的にも正しい。

一方 `sprint-risk-reviewer` §1 は「未確定で残るのはパラメータ名程度で SP-2 内での即興決定で問題なし」とし、専用の記載場所を作ることに消極的に見える。ここは round 1 §4 の知見（クエリパラメータは共有可能 URL・後方互換性に直結し「後から変えると壊れる」）から見て **軽い反論**を加える: 命名自体は実装手段（`SD-3` 対象外）で正しいが、**共有 URL の後方互換性という性質上、決定を「その場のコミットメッセージ」より発見しやすい正本（`prd.md` §2.4）に置く価値は URL に限っては高い**。これは `doc-architect`❶ の提案と同じ結論であり、`sprint-risk-reviewer` の「専用の置き場は不要」という含意とは軽く食い違う。

### round 1 結論の修正

round 1 で私は「URL/ルーティング設計方針の 1 ページ言語化」と書いたが、これは **新規ファイルと誤読されうる表現だった。訂正する**: 正しくは `doc-architect` ❶ の言う「`prd.md` §2.4 への追記（新規ファイルではなく既存正本への数行追加）」であり、私の round 1 「Just Enough Design＝テキストで最小限」という主張の実装形態としても、新規ファイルより既存 SSOT への追記の方が diagram-as-code トレンドに整合する。この点は `doc-architect` の分析に軍配を上げ、自分の表現を修正する。

状態マトリクスについては round 1 の優先度づけ（高い）を維持し、`yagni-cost-reviewer` の格下げには上記の通り異議を唱える。

### `yagni-cost-reviewer` — 反論
<sub>2026-08-18T09:13:52+09:00</sub>

# Round 2: YAGNI 判定の修正と譲歩

## 総評: doc-architect の指摘が核心。自分の「URL 設計表」案を修正する

3 名の意見を読んでわかったこと: **「新規ファイル作成」と「事前言語化」は違う問題である**。自分は前者に反対すべきだった。

---

## 各者への具体的反論・譲歩

### 1. trend-researcher への返答

**【完全同意】状態一覧チェックリストの理屈は正しい**

指摘「AI エージェントは Empty/Loading/Error 状態を素で漏らす」は事実で、gem-hunter（検索結果アプリ）に直結する。

**【反論の根拠を撤回】ただし既存で代替可能**

- `ui-ux-guidelines.md` §4.4: トップページの 4 状態（初期 / 読み込み中 / 0 件 / エラー）が表で明示済み
- §5.2: エラー種別別の文言設計で各状態の出し分け方を規定済み
- §6（詳細ページ）: **ただし「読み込み中」「その他エラー」の状態表現が記載なし**（これが穴）

**doc-architect が言う通り、新規ファイル作成ではなく `ui-ux-guidelines.md` §6 に「詳細ページの状態表現セクション」を追記するだけで足りる**（§4.4 相当を詳細ページにも作る）。趣旨に同意し、形式を修正する。

---

### 2. doc-architect への返答

**【ほぼ完全に同意。方針を統一する】**

❶ **パラメータ名・既定値の欠落指摘は的確**
- US-9 が「S-0 の範囲はキーワード・ページ・ロケール」「パラメータ名と既定値だけを決めておく」と指示しながら、その決定を書く場所が **本当にどこにもない**
- `prd.md` は「クエリに反映する」と言うだけで、`q`? `query`?　を決めていない
- これは「設計的な欠落」であり、新規ファイルでなく **既存の正本（`prd.md` §2.4）への追記で解決すべき**

❷ **新規ファイル作成への反対に同意**
- 独立した「画面遷移図」「サイトマップ」ファイルを作ると、SSOT 増加 → ドリフト → 権威系統が破綻
- 本リポジトリの「正本分離・重複排除」の厳格な設計思想（`D-8`）に対する背反

❸ **追記範囲の許容度**
- `prd.md` §2.4 にパラメータ名・既定値表（5〜10 行）
- `ui-ux-guidelines.md` §6 に詳細ページ状態表（8〜12 行）
- 合計 15〜20 行は十分吸収可能な追記規模

**修正: 自分の「URL 設計表」は新規ファイルではなく `prd.md` §2.4 への追記に切り替える**

---

### 3. sprint-risk-reviewer への返答

**【部分譲歩】ほぼ同意だが、URL パラメータ名は例外**

❶ **「実装知識が揃うまで待つ」は SD-3 の原則として正しい**
- 言語切替時クエリ保持・ログイン後戻り先は、実装スプリントで初めて判断材料が揃う
- 前倒しは精度を落とす（指摘通り）

❷ **ただし URL パラメータ名・既定値は実装知識と無関係**
- `q` か `query` か、`page=0` か `page=1` か、これは実装手段ではなく **インターフェース設計**
- S-0（共有可能な URL）の要件（`AC-2` / `US-9`）で先行確保すべき
- **「後から変えると既存リンク全破壊」という不可逆な破壊を避けるため、S-0 として **事前言語化** が必須**

❸ **新規ファイル作成には完全同意**
- 「画面遷移図・ワイヤーフレーム」の独立ファイルは不要
- 理由は完全に同意: 自由入替・プレビュー URL・`user-story-map.md` で既に十分

**修正: URL パラメータ名は新規ファイルでなく `prd.md` 追記で、S-0 先行確保として正当化する**

---

## 最終判定: 「1 つだけやるなら何か」

**唯一やるべき作業:**

### `prd.md` §2.4 への追記（8〜10 行）

```markdown
## 2.4（改訂）状態の置き場所

| 要素 | URL | 注記 |
|---|----|------|
| ロケール | `/ja/...` / `/en/...` | パスプレフィックス（AR-4） |
| キーワード | `?q=react` | [S-0] |
| ページ | `?page=1` | 1 ベース（既定値: 1） |
| ソート | `?sort=best-match` | [S-1] 既定値: best-match |
| 表示件数 | `?limit=20` | [S-1] 既定値: 20 |
```

**これだけで** :
- US-9 の「決めておく」指示を実行完了
- パラメータ名のゆらぎを回避
- SP-7 実装時に「ソート・件数追加」の基礎が確定
- 新規ファイルを増やさない

---

## 譲歩の総括

| 項目 | 元の立場 | 修正 | 理由 |
|------|--------|------|------|
| **状態チェックリスト** | 「既に定義済み・不要」 | 「追記で足す（詳細ページ §6）」 | trend-researcher の「AI が漏らす」実証指摘に説得力 |
| **URL 設計表** | 「SP 1 の新規ファイル」 | 「`prd.md` 追記（新規ファイルなし）」 | doc-architect の SSOT 厳格性・sprint-risk-reviewer の「図は不要」に同意 |
| **新規ファイル作成** | 「サイトマップ含めて検討」 | 「全て作らない」 | 本リポジトリの権威系統設計に照らし完全に不要 |

### `sprint-risk-reviewer` — 譲歩
<sub>2026-08-18T09:14:22+09:00</sub>

# sprint-risk-reviewer round2: 事実確認の結果、部分的に譲歩する

## 1. doc-architect への譲歩（事実確認済み）

`prd.md` §2.4（L141-150）・US-9（`user-story-map.md` L99）・`AR-4` 詳細（`prd.md` L237-242）を再確認した。**doc-architect の指摘は事実として正しい。** §2.4 は「検索キーワード・ページ・ソート・表示件数 → URL（`searchParams`）」としか書いておらず、キー名・既定値は本当にどこにもない。ロケールの置き場所（`/ja/...` プレフィックス）だけは `AR-4` 詳細に明記されている。

→ round1 の「SP-2 の固定対象は既に文章で確定済み」は **言い過ぎだった。撤回する範囲を明確にする**: 「範囲決定（キーワード/ページ/ロケールの3つ・US-9）」と「ロケール置き場所（AR-4）」は確定済みで正しいが、「パラメータ名・既定値」は確かに **正本の置き場所がゼロ**という doc-architect の指摘どおり。ここは round1 の主張精度不足として訂正する。

**ただし** これは「画面遷移図/ワイヤーフレームを事前に描くべき」を支持しない。doc-architect 自身が結論で「作らないもの: 独立ファイルとしての画面遷移図・サイトマップ・ワイヤーフレーム」と明言しており、私の暫定結論（上流デザイン成果物は作らない）とはむしろ一致している。争点は「絵を描くか」ではなく「`prd.md` §2.4 の表を埋めるか」であり、後者に異論はない（§3 で述べる）。

## 2. コーディネーターの問い: ページ 0/1・ソート値名・言語切替クエリ保持は本当に実装手段か

**3 つを一括りにしない。個別に判定する。**

- **ページ 0/1 始まり・ソート値の名前**: これは `sprint-development-rules.md` SD-3 表の「命名」に該当し、**依然として SD-3 対象外**。`user-confirmation-minimization.md` §0 は「確認してよいのは2系統だけ」と明言し、「実装手段・ライブラリ選定・**命名**は第2系統に含まない（不可逆リスクは不要）」と名指しで除外している。「URL が外部露出し後から変えられない」という不可逆性は事実だが、この不可逆性は `A-1〜A-6`（既約境界外リスト）のどの項目にも該当しない――A-1〜A-6 は main push・外部公開の即時手動実行・品質ゲート致命的NG・サーキットブレーカー・新規マイルストーン・アカウント設定に限定列挙されており、URL パラメータ命名は含まれない。つまり「不可逆だから確認すべき」という直感は、**本プロジェクトが `D-12` で意図的に却下した経路**（命名は不可逆性を問わず自律決定）。判定は変わらない。
- **言語切替時のクエリ保持**: これは命名ではなく **挙動の選択**（切替後に検索状態を保つか捨てるか）で、成果物のユーザー体験が明確に変わる。round1 で私は既にこれを「⚠️ 要検討」に分類済みで、`SD-3` 表の承認例「戻ったときページ番号も戻すのか維持するのか」と同型。ここは判定を変えない（= 元から SD-3 該当と見ていた）。

## 3. yagni-cost-reviewer「URL 設計表だけは作る」への回答: 賛成（ただし範囲限定）

賛成する。ただし置き場所は **新規ファイルではなく `prd.md` §2.4 の表を拡充する**（doc-architect の提案に同調）。理由: (1) §2.4 は既に「状態の置き場所」表として存在し ID トレーサビリティの内側にある、(2) `NFR-18`（キャッシュキー命名規約）と同種の「後から変えると壊れる決定」であり、正本は要件書であるべき、(3) 新規ファイルを作ると `user-story-map.md` §0.1 が自ら警告する「2 箇所が同じ事実を持つ」ドリフトを生む。

**これは画面遷移図・ワイヤーフレームの容認ではない**点を明確にする。URL パラメータ表は「決定を書き残す」作業であり、「絵を描いて将来の実装を予見する」設計成果物とは性質が異なる（前者はテキスト1行の決定記録、後者は視覚的な予見的成果物で維持コストとドリフトリスクを伴う）。

## 4. round1 結論の修正

- **修正**: URL パラメータ名・既定値は `prd.md` §2.4 に **`SP-2` 着手前（今）に追記してよい**（doc-architect / yagni-cost-reviewer に同意）。これは round1 で私が想定した「SP-2 セッション内での即興決定」より前倒しになる点を認める。
- **維持**: 画面遷移図・サイトマップ・ワイヤーフレーム・状態遷移図は依然として作らない。命名系の分岐（ページ index・ソート値名）は SD-3 対象外のまま。言語切替クエリ保持のような真の挙動分岐は、該当スプリント（`SP-8`）で `AskUserQuestion` する方針も維持。

### `doc-architect` — 反論
<sub>2026-08-18T09:15:04+09:00</sub>

# doc-architect: round 2（反論・決着）

## 1. trend-researcher への回答（一部是認・場所の指定で補完）

trend-researcher の「状態一覧チェックリストを先に洗い出すべき」（L51/L58）は **半分は既に満たされている**。`ui-ux-guidelines.md` §4.4（L130-142）にトップページの 4 状態（未検索/読み込み中/0件/エラー）が表で確定済み。

しかし **詳細ページ側は未着手**という指摘は正しい（doc-architect round1 §4 で実査済み、yagni-cost-reviewer L280-281 も独立に同じ穴を指摘）。3 者が同じ空白を別々に発見した＝実在するギャップと確定してよい。

**やること**: 新規チェックリストではなく、`ui-ux-guidelines.md` §6（詳細ページ、現状 L176-182 の 6 行のみ）に §4.4 と同形式の表を **6〜8 行追記**（読み込み中スケルトン / 404 は既述 / 401・403・5xx・ネットワーク到達不可時の表現）。新規ファイルは不要 — §4.4 の隣接節に対称形を足すだけで「画面×状態マトリクス」は完成する。

## 2. sprint-risk-reviewer への反論（事実は一致・分類が誤り）

**事実の食い違いはない。** sprint-risk-reviewer 自身も L142 で「未確定で残るのはパラメータ名（`q` か `query` か）...程度」と認めており、これは doc-architect round1 §3 の実査結果と完全に一致する。争点は「これを `SD-3` の "実装手段"（自律決定・確認不要）に分類してよいか」だけ。

**この分類には反対する。** `user-story-map.md` US-9 の理由づけ自身（L102）が根拠を潰している。

> 「URL の形（何をクエリに載せるか・パラメータ名・ロケールをどこに置くか）は**後から変えると既存の共有リンクがすべて壊れる**」

これは `NFR-18`（`prd.md` L312・キャッシュキー命名規約）と**全く同じ構造のリスク**（後方互換破壊）であり、`NFR-18` は commit message 止まりではなく **`prd.md` に正式な NFR として明文化**されている。パラメータ名だけを「実装手段」として commit message 送りにするのは、同種リスクを持つ `NFR-18` との扱いの非対称になる。

さらに sprint-risk-reviewer の記録先提案（L182「`user-story-map.md` §8 相当の場所か最初のコミットメッセージ」）にも異議あり。`sprint-development-rules.md` `SD-4` はスプリント着手時に読む文書として `prd.md` を指定しているが、過去スプリントの commit message は指定していない。**SP-7（ソート・件数パラメータ追加）の実装者が SP-2 の commit message を自然に読み返す経路は存在しない。** 一方 `prd.md` は毎スプリント読む対象（`SD-4` #2）なので、正本を `prd.md` §2.4 に置けば SP-7 実装時に自動的に参照される。

**決着**: パラメータ名の**決定タイミング**は sprint-risk-reviewer の言う通り SP-2 実施時でよい（round1 の「今すぐ決める」という含意は撤回する＝ concession）。ただし**決定後の記録先**は commit message ではなく `prd.md` §2.4 追記（1 行の表）とすべき — これは yagni-cost-reviewer の第 3 項目（L242-252、必須判定）とも整合する。

## 3. yagni-cost-reviewer への確認（独立収束・置き場所だけ補正）

yagni-cost-reviewer は「図はすべて不要」ではない（L304-318 を見ると URL 設計表は **🟢必ずやる**、サイトマップは 🟡任意、Mermaid 遷移図・ワイヤーフレームのみ ❌）。doc-architect round1 と **URL パラメータ表が最優先という結論は完全一致**（3 者が独立到達）。

差分は 1 点のみ: yagni-cost-reviewer は新規の「URL・ルーティング設計表」という**独立成果物**を SP1 で作る想定（L305-307）。doc-architect はこれに反対し、**`prd.md` §2.4 の既存表への追記**を推す。理由は sprint-risk-reviewer 自身が L162 で挙げた「権威系統の外側に第三の正本が生まれる」警告そのもの — 新規ファイルを作ると `US-9`（`user-story-map.md`）→ 新ファイル → `prd.md` の 3 点を同期する義務が生まれるが、`prd.md` §2.4 に足せば正本は 1 つのまま増えない。

yagni-cost-reviewer の「画面×状態マトリクス」🟡任意判定（L282-284）は、詳細ページ分だけなら §1 の通り数行で済むため **優先度を 🟢 へ格上げ**を提案する（同一スプリントで URL 表とセット追記すれば追加コストはほぼゼロ）。

---

## 結論の更新（round1 からの差分）

- 決定タイミング: 「今すぐ」→「SP-2 実施時でよい」に **譲歩**
- 記録先: `prd.md` §2.4 に**確定**（commit message 案・新規ファイル案の両方を却下する根拠を提示）
- 詳細ページの状態表現: `ui-ux-guidelines.md` §6 に追記（新規ファイルなし）— これは 3 者収束で確度が上がった

## ラウンド 3

### `lead` — 合意
<sub>2026-08-18T09:16:21+09:00</sub>

# lead: 合意点と対立点の整理

## 1. 全員が独立に到達した合意（4/4）

| 合意事項 | 到達者 |
|---|---|
| **独立ファイルとしての画面遷移図・サイトマップ・ワイヤーフレーム・デザインカンプは作らない** | 4 名全員 |
| 理由①: 画面は 2 つだけ（`prd.md` §8.1 / §8.2）で、遷移情報は `user-story-map.md` §5.3 の操作レビュー手順に実質存在する | doc-architect（実査）/ sprint-risk-reviewer / yagni-cost-reviewer |
| 理由②: 新規正本を作ると `D-8`（正本分離）が警告する「第三の正本」が生まれ、ドリフト負債になる | doc-architect / sprint-risk-reviewer / yagni-cost-reviewer（round 2 で明示同意） |
| 理由③: 図の最大の存在理由（デザイナ ↔ エンジニアの受け渡し）が個人開発 + AI 実装では存在しない。2026 年の BDUF 正当化条件（規制産業・複数チーム間の契約的インターフェース）にも該当しない | trend-researcher（一次情報）/ yagni-cost-reviewer |
| 理由④: 鮮度で図に勝るものが既にある = `SD-1` が毎スプリント義務付けるプレビュー URL（動くコード） | sprint-risk-reviewer / trend-researcher（Claude Design 等「コード＝プロトタイプ」潮流） |
| **本当に欠けているのは絵ではなく「URL クエリパラメータ名・既定値」という決定そのもの** | doc-architect（実査）→ sprint-risk-reviewer が round 2 で事実確認のうえ譲歩 / yagni-cost-reviewer が独立到達 / trend-researcher が支持 |
| **記録先は新規ファイルではなく `prd.md` §2.4 への追記**（5〜10 行） | 4 名全員（round 2 で収束） |
| **`ui-ux-guidelines.md` §6（詳細ページ）に状態表現の表が欠落している**（§4.4 の対称形が無い） | doc-architect / yagni-cost-reviewer が独立に同じ穴を発見 → trend-researcher が優先度引き上げを主張 → 4 名合意 |

## 2. 議論を経て解消した対立

| 対立 | 決着 |
|---|---|
| doc-architect「URL パラメータ名の決定が正本のどこにも無い」 vs sprint-risk-reviewer「SP-2 の固定対象は既に確定済み」 | **doc-architect が正しい**。sprint-risk-reviewer が `prd.md` §2.4 / `user-story-map.md` US-9 / `AR-4` を再確認して round 2 で撤回（範囲とロケール置き場所は確定済み、パラメータ名・既定値は未確定で置き場所もゼロ、が事実） |
| 記録先: 新規「URL 設計表」ファイル（yagni round 1）vs `prd.md` §2.4 追記（doc-architect） | **`prd.md` §2.4 追記**。yagni-cost-reviewer が round 2 で自案を撤回。根拠は `NFR-18`（キャッシュキー命名規約）が `prd.md` に明文化されているのと同種のリスク（後方互換破壊）であること、および `SD-4` が毎スプリント `prd.md` を読ませるのに対し過去スプリントの commit message を読む経路は存在しないこと |
| 決定タイミング: 今すぐ（doc-architect round 1）vs SP-2 セッション内（sprint-risk-reviewer） | **SP-2 実施時でよい**。doc-architect が round 2 で譲歩。ただし「決めた瞬間に `prd.md` §2.4 へ書く」ことは必須（commit message 止まりにしない） |
| 画面 × 状態マトリクスの優先度: 🟡 任意（yagni round 1）vs 高（trend-researcher） | **🟢 やる（詳細ページ分のみ）**。yagni-cost-reviewer が round 2 で格上げに同意。トップページ分は `ui-ux-guidelines.md` §4.4 で既に充足しており、追加するのは詳細ページの 6〜12 行のみ |

## 3. 残った（解消していない）対立

| 論点 | 状態 |
|---|---|
| ページ番号 0/1 始まり・ソート値の名前が `SD-3`（確認の発火ライン）に該当するか | sprint-risk-reviewer は「命名であり `D-12` が明示的に第 2 系統から除外している」として `SD-3` 対象外を維持。doc-architect / trend-researcher は「共有 URL の後方互換破壊という性質上、正本に記録する価値は高い」と主張。**ただし両者とも「ユーザー確認は不要・正本に記録は必要」で実質一致しており、成果物レベルの対立は残っていない** |
| 言語切替時のクエリ保持・ログイン後の戻り先 | 「2 通りの解釈で成果物が変わる真の分岐」であることは全員一致。ただし **事前に図を描いても分岐は消えず前倒しされるだけ**（`SD-3`「答えに依存しない作業を先に全部終わらせてからまとめて聞く」に反する）ため、該当スプリント（`SP-8`）で `AskUserQuestion` する方針で合意 |

## 4. 「途中軌道修正が前提」という前提への回答

軌道修正が前提であることは **先に全体像を描かない根拠を強める**（描いた図が最初の入替・分割で陳腐化する）。一方で **URL だけは軌道修正が効かない**（共有リンクが壊れる）ため、そこだけ先に固定する — これが 4 名の一致した結論。`user-story-map.md` §5.5 が `SP-6` / `SP-7` / `SP-10` を自由入替可としているのは、画面間の一貫性をデザイントークン（`ui-ux-guidelines.md` §2）で担保する設計であり、順序前提の成果物（画面遷移図）を足すのは設計思想と逆行する。

### `lead` — 判定
<sub>2026-08-18T09:16:51+09:00</sub>

{
  "verdict": "CREATE_MINIMAL",
  "create_now": [
    {
      "priority": 1,
      "artifact": "URL クエリパラメータの名前・既定値・制約の表",
      "target_file": "docs/02_requirements/prd.md",
      "section": "§2.4（状態の置き場所）",
      "size": "5〜10 行",
      "timing": "SP-2 実施時（決めた瞬間に書く。commit message 止まりにしない）",
      "reason": "US-9 が『パラメータ名と既定値だけを決めておく』と要求しているのに、その決定を書く正本がどこにも存在しない。後から変えると共有リンクが全部壊れる（NFR-18 と同種の後方互換破壊リスク）",
      "sp": 1
    },
    {
      "priority": 2,
      "artifact": "詳細ページの状態表現の表（読み込み中・404・401/403・5xx・ネットワーク到達不可）",
      "target_file": "docs/03_design/ui-ux/ui-ux-guidelines.md",
      "section": "§6（詳細ページ）に §4.4 と同形式で追記",
      "size": "6〜12 行",
      "timing": "SP-3 または SP-6 の着手時（詳細ページを触る最初のスプリント）",
      "reason": "§4.4 はトップページの 4 状態を定義済みだが詳細ページ側が空欄。AI 実装は loading/empty/error を素で欠落させる傾向が一次情報で指摘されている",
      "sp": 1
    }
  ],
  "defer": [
    {
      "item": "言語切替時のクエリ保持・ログイン後の戻り先",
      "action": "SP-8 実施時に AskUserQuestion で確認（事前の図では分岐は消えず前倒しされるだけ）",
      "note": "SD-3 の第 2 系統に該当する真の仕様分岐"
    },
    {
      "item": "Mermaid によるフロー図",
      "action": "複雑なフロー（OAuth コールバック等）が実装される SP-8 で、必要になったらコードと同じ PR に書く",
      "note": "先に一括で全画面分を用意しない（diagram-as-code の運用に合わせる）"
    }
  ],
  "reject": [
    {"item": "独立ファイルとしての画面遷移図", "reason": "画面は 2 つのみ。遷移情報は user-story-map.md §5.3 の操作レビュー手順に実質存在し、新設すると D-8 が警告する第三の正本になる"},
    {"item": "サイトマップ（独立ファイル）", "reason": "prd.md §8 から一意に導出できる。URL 表を §2.4 に足せば役割は満たされる"},
    {"item": "ワイヤーフレーム（低忠実度）", "reason": "ui-ux-guidelines.md §4.2 の ASCII 図 + 要素表が既にワイヤーフレーム相当の密度を持つ"},
    {"item": "高忠実度デザインカンプ / プロトタイプ", "reason": "同期コストが再現価値を上回る。デザイントークンで一貫性は担保済み。プレビュー URL（SD-1）が常に最新の実物を提供する"},
    {"item": "全画面統合の状態マトリクス（新規ファイル）", "reason": "トップページ分は §4.4 で充足済み。欠けているのは詳細ページ分だけで、既存節への追記で足りる"}
  ],
  "summary": "画面遷移図などの上流ビジュアル成果物は作らない（4 名全員が独立に到達）。軌道修正が前提であることは先に全体像を描かない根拠をむしろ強める。ただし『後から変えられない URL パラメータ名・既定値』と『詳細ページの状態表現』の 2 つだけは実在する欠落なので、新規ファイルではなく既存正本（prd.md §2.4 / ui-ux-guidelines.md §6）へ計 15〜20 行を追記して埋める。"
}
