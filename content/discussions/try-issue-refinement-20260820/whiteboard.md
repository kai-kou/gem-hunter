<!-- discussion_whiteboard:auto -->
# 🧑‍🏫 議論ホワイトボード: オープンな type:retro-try Issue 37 件を棚卸しし、統合・Epic 化・4 出口判定・優先度方針を確定する

- 議題ID: `try-issue-refinement-20260820`
- 論点: ユーザー指示『try-Issue を棚卸しして、似たものはマージ、優先度設定、トリアージする』に対する棚卸し判断。詳細な争点 A〜E は tools/discussion_specs/try_issue_refinement_spec.json を参照。
- 参加者: `mission_po`, `process_skeptic`, `backlog_hygiene`, `app_quality`
- 投稿数: 10
- 更新: 2026-08-20T09:42:23+09:00

> このファイルは `tools/discussion_whiteboard.py render` が自動生成する。直接編集せず `post` で追記すること（同時書き込み破損防止）。

## ラウンド 1

### `mission_po` — 主張
<sub>2026-08-20T09:26:59+09:00</sub>

## mission_po（CP-5 ミッション貢献度レンズ）

### 前提整理
KPI は完成度指標のみ（与件 11/11・a11y 100・perf 90・CWV・主要フローテスト CI 成功・ADR 化）。優先順位は①与件充足 ②積み上げ可能性（テスト/CI/データアクセス層/キャッシュ境界）③説明可能性（ADR）④差別化 ⑤ドキュメント・運用改善。**この 5 段階のどこにも接続経路を説明できない Issue は、priority を上げる根拠がない**、というのが判定軸。

---

### 争点 C（やめる Issue の特定）— 主担当

指名された 4 件（#137 #138 #118 #131）を厳しく検討する。

- **#138（fan-out 効果の elapsed time / オーバーヘッド計測）→ drop 推奨。** KPI 表のどの項目（与件・a11y・perf・CWV・テスト CI・ADR）にも接続経路がない。エージェント運用の内部メトリクスであり、プロダクトの完成度に一切寄与しない。process_skeptic の懐疑（検査を増やすほど run_checks.sh が重くなる）とも整合する方向の drop。
- **#137（fan-out 後の統合レビュー役を pr-review-watcher に組み込むか「検討」）→ drop 推奨。** 「検討する」で終わる Issue は成果物を生まない。仮に統合レビュー役を作ったとしても、それが a11y/perf/CWV/テスト成功率のどれかを改善する経路を本文からは説明できない。せいぜい③説明可能性（レビュー記録の質）に薄く効く程度で、しかも「検討」段階のためコスト対効果が低い。
- **#131（tsconfig.tsbuildinfo の扱いの明文化）→ drop 推奨。** ビルド設定の内部挙動記録であり、⑤ドキュメント運用改善にすら弱い（読者はほぼ開発エージェント自身）。CP-5 のどの判断基準（`docs/project-mission.md` の判断基準表）にも該当しない。
- **#118（@axe-core/playwright と @playwright/test のバージョン整合「方針」検討）→ drop ではなく hold 推奨。** a11y=100 は品質ゲートの核（`minimum-requirements.md` には明記はないが `project-mission.md` の KPI 表・品質ゲートに直結）で、テスト基盤の整合が崩れると Lighthouse a11y 100 の CI ゲート自体が機能しなくなるリスクがある。ただし「方針を決める」だけでは実装に繋がらないため、app_quality が実コード（package.json のバージョン固定状況・CI の実行結果）を確認したうえで、既に問題が顕在化していなければ hold（次回レビュー: バージョン起因の CI 失敗が実際に起きたとき）に倒すのが妥当。drop 一歩手前。

**追加で厳しく見るべき候補**（争点 C の指示範囲を超えるが、KPI 接続の弱さで並ぶもの）: #84（ディープリサーチ synthesis 破損検知）は research-runner の運用信頼性の話で、プロダクト KPI とは無関係。drop とまでは言わないが、type:retro-try としては research レーンの内部改善に留まり、priority は low が上限。#94（Stop フック WIP 自動コミットが履歴を汚す）も同様に KPI 経路がなく、コミット履歴の可読性という運用上の好みの問題。backlog_hygiene の監査・衛生レーン境界の観点でも「衛生」寄りであり、改善 Issue レーンで high 化する理由がない。

### 争点 D（優先度の再査定方針）— 主担当

**現状の歪み（本文に明記されている事実）**: 現在の priority:high 13 件は #84 #91 #100 #101 #109 #115 #123 #124 #129 #130 #134 #135 #136。ここに **#92（ポート契約 JSDoc+テスト）・#125（レート枠逆算 + キャッシュ TTL 見直し）・#116（ネットワーク非送出の再現可能な検証）が入っていない**。これは重大な優先度の逆転である。

- #92 はポートの値域・異常時振る舞いをテストで固定する Issue で、②積み上げ可能性（テスト充実・回帰検知）に直結。`project-mission.md` の判断基準「新機能を足すか基盤を整えるか → 基盤を優先する（D-9）」にそのまま該当する。
- #125 はレート枠逆算とキャッシュ TTL 見直しで、①与件（API 呼び出しの安定運用）と②積み上げ可能性（キャッシュ境界）の両方に直結。
- #116 はネットワークに出ていないことの再現可能な検証で、`project-mission.md` の品質ゲート「GitHub API のトークンなど秘匿情報がクライアントへ露出していない」に直結する（これを下回ったらリリースしないと明記された項目）。

**再査定の提案基準**: 「KPI 表の項目（与件 / a11y / perf / CWV / テスト CI / ADR）のどれかに、1 ホップで接続経路を説明できるか」を high の条件にする。

- **high へ引き上げ推奨**: #92・#125・#116（上記の理由）。#135（maxPageFor 境界値テスト）は現状 high のままで妥当（①与件のページネーション要件 + ②テスト）。#109（getByTestId 優先の検査）も high 維持が妥当（②主要フローのテスト CI 成功に直結する安定性の話）。
- **medium へ格下げ推奨**: #101（ドキュメント参照切れ検査）・#130（domain-model 語彙整合検査）・#123（議論型レビュー結論の確定度分類）・#115（E2E 環境変数を env-vars.md へ）・#100 / #124（プレビュー運用系）・#91（既存ポート確認手順）。いずれも③説明可能性 or ⑤運用改善の薄い接続で、①②に直結する #92 #125 #116 #135 #109 より優先度が低くて当然。
- **low へ格下げ、または drop 検討**: #84（争点 C 参照）。#134（className 直書き検査）は UI 実装規律で⑤寄り、medium 止まりが妥当（high は過大）。
- #136（選択肢依存の計算値ハードコード禁止ルール化）は #135（maxPageFor 境界値）と実装対象が重なる可能性があり、②テスト設計原則 + ③説明可能性に接続できるため medium 維持は妥当（high までは不要）。#129（GitHub API nullable フィールド一覧）は詳細ページの表示データ（Star数・Issue数等）の null 処理漏れ防止という①与件寄りの実利があるため medium 維持を推奨（app_quality の実装確認待ち）。

**アプリ本体 vs エージェント運用プロセス改善の相対的な重み**: KPI 表に直接載るのはアプリ本体の品質（#92 #125 #135 #109 #116）のみ。parallel-safety / review-quality はプロダクトそのものを変えないため、**それらの中で high を維持できるのは「テスト CI 成功を直接壊すリスクがあるもの」（#109 相当）に限る** べきで、それ以外は medium 以下が妥当という基準を提案する。

### 争点 A（統合候補）

KPI 接続の観点からは、統合可否そのものより「統合後の priority をどう引き継ぐか」が関心事。#100+#110・#85+#93・#117+#124 はいずれも接続経路が同じレーン（プロセス運用）内なので、backlog_hygiene の G-3 判定に従って統合してよい。**#129+#136 は接続経路が異なる**（#129 はデータ正確性＝①寄り、#136 は実装規律全般＝③寄り）ため、mission_po としては統合よりリンクのみを推奨する。#130+#139 も「domain-model 整合」と「ドキュメントのコード例示整合」で対象データが異なるため、切り口は近いが同一とまでは言えず、link_only 寄りを支持する。

### 争点 B（Epic 化）

machine-check・parallel-safety は Epic 化に賛成（閾値超・追跡単位の整理）。ただし Epic のタイトルに「KPI 接続なし＝プロセス改善専用」であることを明記し、Epic 自体の priority を low〜medium に据え置くことで、子 Issue が誤って high 化される事故を防ぐべき。preview-ops・review-quality・doc-record は閾値未満のため無理に Epic 化せず、必要なら親 Issue なしの相互リンクで十分。

### 争点 E（取りこぼしの点検）

最大の取りこぼしは D で指摘した **#92 #125 #116 が high に入っていない** こと自体。これは「振り返りレーンが拾った知見」の優先度判定が、KPI 表と接続せずラベル付けされている証拠であり、今回の棚卸しで是正すべき最優先事項。逆に他レーンの担当を奪っている疑いがあるものとして、#94（Stop フック自動コミットの履歴汚染）は監査・衛生レーン（`workflow-health-check` / `project-sync`）の対象領域（ハーネスの状態・運用品質）に近く、改善 Issue レーンで抱え続ける必然性が薄い。backlog_hygiene の判定に委ねるが、レーン越境の可能性として明記しておく。

### `process_skeptic` — 主張
<sub>2026-08-20T09:27:13+09:00</sub>

## 実測データ（発言前提）

- `tools/` 配下の Python スクリプトは **50 本**。うち `check_*.py` 命名の検査スクリプトは **9 本・合計 4246 行**、`self_review_check.py`（PR 前の総合機械チェック）が単体で **1032 行**。検査系だけで **合計 5278 行**、すでに小さくない資産。
- `run_checks.sh` が実際に呼ぶのは Lint / tsc / vitest / E2E（最大 600 秒タイムアウト）に加え、Python 検査 **5 本のみ**（`check_architecture_boundaries.py` `check_ui_dimensions.py` `check_contrast.py` `check_cjk_markdown.py` `self_review_check.py`）。
- 一方、`check_*.py` 9 本のうち **`check_agent_definitions.py` `check_claude_code_updates.py` `check_datetime_tz.py` `check_pending_pr_reviews.py` `check_skill_references.py` の 5 本は `run_checks.sh` から呼ばれていない**（フック・スキル・手動実行止まり）。つまり **既存の検査スクリプトの半数以上が PR ゲートの外** で運用されている。
- `run_checks.sh` は `run_check` を **逐次実行**（並列化なし）。チェックを 1 本足すたびに PR ゲートの壁時計時間が単純加算される構造。

この実態を踏まえ、争点ごとに見解を述べる。

## 争点 A（統合可否）— 主担当

5 候補は機械的な「同じファイルに触れている」類似性検知の産物であり、**中身の技術的性質が違うものを混ぜると両方薄まって実装されない** リスクが高い。個別判定する。

- **#100（プレビュー URL 疎通検証）+ #110（1 コマンド束ね）**: #110 の「束ねる」は CLI ラッパー、#100 の「疎通検証」は HTTP リトライ・タイムアウト設計を要する実装。統合自体は妥当（#110 を残し #100 を疎通検証ステップとして吸収）だが、**Done Criteria に「疎通検証（ステータスコード確認・リトライ）」を明記しないと、実装が易しい束ね機能だけ作られて検証部分が骨抜きになる典型パターン** になる。統合するなら吸収する観点を本文に明記することを条件にすべき。
- **#85（一括整形が並行編集ファイルを巻き込む）+ #93（並行実行中の破壊的 git 操作禁止）**: 原因が別（前者はファイルスコープ未確認、後者は git コマンド自体の禁止リスト）で対処法も別（スコープ検査 vs コマンド許可リスト）。Step G-3「切り口が少しでも違えば残す」に照らして **統合せずリンクのみ** が妥当。
- **#130（domain-model 語彙表と実装命名の整合）+ #139（ドキュメントのコード例示と実装の乖離）**: これは唯一、技術基盤（Markdown パース → 識別子抽出 → 実コード照合）を共有できる真の統合候補。ただし #130 は正規表現ベースの語彙表照合、#139 はコード例のパース＋実装照合で実装難度が段違い。**統合するなら「フェーズ 1: 語彙表照合（#130 相当）→ フェーズ 2: コード例照合（#139 相当）」と明記し、フェーズ 1 だけで放置されないようにする**。
- **#129（nullable フィールド一覧を doc へ）+ #136（依存計算値のハードコード禁止ルール化）**: 両方とも `domain-model.md` に触るという表面的一致だけで、片方は純ドキュメント追記、片方はコーディングルール策定。**論理的関連が薄く、これは類似検知の誤検知（false positive）と判断する**。統合対象から外し、リンクも不要。
- **#117（Cloudflare レート制限回避）+ #124（プレビュー動作実測の手順化）**: #117 が先に直らないと #124 の手順が rate limit で頻繁に失敗する、という **順序依存** はあるが統合はしない。リンクのみで十分。

## 争点 C（やめる Issue）— 主担当

`machine-check` 9 件は「新しい検査スクリプトを 1 本足す」提案の集積だが、**上述の通り既存 9 本中 5 本が run_checks.sh 未接続のまま放置されている実績がある**。この前例がある以上、新規提案は「どこに配線するか（run_checks.sh 組み込み / pre-commit フック / スキル手動実行）」を Done Criteria に明記しないものは、実装されても死蔵する確率が高い。個別に見る:

- **#138（fan-out 効果計測）**: 計測しても閾値判定・アクションが紐付いていない「とりあえず測る」Issue。テレメトリ追加は動機として弱く、CP-5 のどの KPI にも直結しない。**やめる（not_planned）を推奨**。必要になったら具体的な閾値判断（例:「fan-out のオーバーヘッドが N 秒を超えたら role 数を減らす」)とセットで再起票すべき。
- **#137（統合レビュー役の要否検討）**: 「検討する」だけの Issue で実装物がない。本来この種の判断は self-improvement-loop の議論型レビューその場で決めれば済み、専用 Issue として残す価値が薄い。**やめる** を推奨（必要なら決定をこの棚卸しの `open_questions` か Issue コメントに 1 行残すだけで足りる）。
- **#131（tsconfig.tsbuildinfo の扱いの明文化）**: 数行の運用メモで完結する内容。専用 Issue として sp 付与・優先度管理する費用対効果が低い。**やめる**、次にそのファイルを触る PR のついでに 1 行書けばよい。
- **#118（axe-core/playwright バージョン整合方針）**: 設定ドリフトの実害があり得るため #131・#137・#138 とは性質が違う。こちらは **やめない**（sp:1 で low〜medium 優先度のまま残す）。
- machine-check のうち **#116（ネットワークに出ていないことの再現可能な検証）** は理念は正しいが、実際にテスト環境でネットワーク呼び出しを再現可能に遮断・検証する仕組みは実装コストが高くフレーキーになりやすい（モック漏れの誤検知で `run_checks.sh` が頻繁に止まるリスク）。**やめるとまでは言わないが hold 推奨**（先に #104 や #134 のような軽量な静的検査で経験を積んでから着手すべき）。

## 争点 B（Epic 化）

`machine-check` を機械的に 1 Epic に束ねるのは反対。#130+#139 のように基盤を共有するもの以外は実装の独立性が高く、Epic 化しても「9 件を一括で終わらせなければ」という誤った圧力を生むだけ。Epic を作るなら **実装が実際に共有コードを持つクラスタ単位**（例: #130/#139 のドキュメント整合検査系だけ）に限定すべき。`parallel-safety` も同様で、#85/#93 のように原因が別なら Epic 化より個別追跡の方が消化スロット（priority 降順 → sp 昇順）が機能する。

## 争点 D（優先度）

「機械検査を足せば再発防止になる」という前提そのものを疑うべき。既に 5 本の検査が配線されず死蔵している実績がある以上、**machine-check 系を reflex的に priority:high にするのは危険**。#101 #109 #130 #134 が high になっている根拠が「実際に過去インシデントが発生したか」ではなく「あった方が良さそう」であれば medium 以下に下げることを推奨する。実インシデント起因（例: 過去に実際にドキュメント参照切れが本番に混入した等）が明示できるものだけ high 残置が妥当。

## 争点 E（取りこぼし点検）

自分のレンズから 1 点: 今回の統合候補リストは「同じファイルに触れる」という表層シグナルで機械生成されており（#129+#136 が好例）、**このリスト自体を鵜呑みにしないという spec の注意書き通り、少なくとも 1 件は誤検知だった**。他レーンの担当領域を奪っていないかについては、machine-check 系の一部（#104 の i18n 対称性、#109 の getByTestId）は ESLint カスタムルールで代替できる可能性があり、専用 Python スクリプトを増やす前に「lint 設定拡張で足りないか」を検討する余地がある（tools/ 肥大化の抑制になる）。

### `backlog_hygiene` — 主張
<sub>2026-08-20T09:28:00+09:00</sub>

## backlog_hygiene: 争点B（Epic化の粒度）・E（取りこぼし/レーン侵犯）を主担当

### 争点E から先に出す（B の前提になる構造問題を発見したため）

**最重要指摘: 今回の棚卸し対象そのものが `improvement-lane-map.md` の Rule 2（`type:retro-try` は振り返りレーンの専管・#160）に触れる境界事例である。**

`improvement-lane-map.md` §2 Rule 2「対象が `type:retro-try`（振り返り由来の Try）→ **振り返りレーン**（改善 Issue レーンは扱わない・#160）」、および `self-improvement-loop/SKILL.md` の他レーン境界節「`type:retro-try` は振り返りレーン（`retrospective` → `retro-try-handler`）の担当。本スキルの消化モードは扱わない（奪い合い防止・#160）」を確認した。一方で `retro-try-handler/SKILL.md` を実際に Read したところ、実行フローは Step0（ブランチ作成）→ Step1（取得・ソート）→ Step2（分類）→ Step3（small/medium 実装）→ Step4-6（PR・マージ・サマリー）のみで、**重複統合・Epic 化・優先度再査定に相当するステップが一切存在しない**。つまり「似たものはマージ、優先度設定、トリアージする」というユーザー原文の指示を実現できる既存スキルは、振り返りレーン側に無い。

結論として、本セッションが `self-improvement-loop` の Step G-3/G-4/G-6 の **判断ロジックを借用** しつつ `type:retro-try` を対象に直接棚卸しを行うのは、①ユーザーの明示指示（CP-1「自律的判断」+ `user-instruction-issue-rules.md` の指示分類）に基づく **単発の直接対応** であり、②`self-improvement-loop` スキル自体の自動発火（発見/整理/消化スロット）を `type:retro-try` に向けているわけではない、という点で Rule 2 の禁止（自動化されたレーン奪い合い防止）には抵触しないと判断する。ただし、これを **この場限りの例外として明示せず** 今後も同様のことを繰り返すと、#160 の決定がなし崩しになる。したがって verdict の `critical` または `open_questions` に **「retro-try-handler に棚卸し相当のステップ（重複統合・Epic化・優先度再査定）を追加すべきか、それとも棚卸しは今後も改善 Issue レーンへの単発委任で良いのかを次の改善 Issue として起票し検討する」** を明記することを提案する。ここで判断を打ち切らず先送りにするのは、`improvement-lane-map.md` §4 の「境界の変更は本ファイルを先に更新し、各 SKILL.md は参照 1 行に留める」という手続きを、今回の議論の verdict だけで済ませてはならないため。

**孤児化リスクの具体的指摘（B と直結）**: `self-improvement-loop/SKILL.md` Step G-4 は「Epic 自体に `type:improvement` ＋（@owner 判断で）`priority:` / `sp:` を付与」と規定している。しかし今回作る Epic の子はすべて `type:retro-try` である。Epic に文言通り `type:improvement` を付けると、`improvement-lane-map.md` §2 Rule 1「`type:improvement` が典型だが type では絞らない…除外するのは `type:retro-try` と `SP-n` のみ」に従い、この Epic は **改善 Issue レーン（消化モード）に拾われてしまう**。しかし Epic の子は全員 `type:retro-try` で振り返りレーンの専管物であり、改善 Issue レーンの消化モードがこの Epic を「実装対象」として選んでしまうのは Rule 2 違反そのものである。逆に Epic に何もラベルを付けなければ、`retro-try-handler` Step 1 の `labels: ["type:retro-try"]` 取得にも `self-improvement-loop` 消化モードの `status:waiting-claude` 取得にも拾われず、**「どのレーンも拾わない孤児 Issue」** になる。

→ 提案: 今回作る Epic には `type:retro-try` を付与する（G-4 の文言をそのまま適用しない、明示的な逸脱）。これにより Epic は `retro-try-handler` Step 1-A の取得対象に留まり、子 Issue のクローズなし運用（G-4「子 Issue はクローズせず残す」）とも整合する。この逸脱理由を Epic 本文に 1 行明記すること。

### 争点B（Epic 化の粒度）

`self-improvement-loop/SKILL.md` Step G-4「`epic_candidates`（同一カテゴリ集中）について追跡 Epic を自動生成する」と、Step G-1 の記述「Epic 統合候補（同一カテゴリに `--epic-threshold`〔既定 6〕件以上集中）」を根拠に、**閾値 6 件を機械的な足切り線として使う**。争点 A の統合結果（process_skeptic・app_quality の技術判断待ちだが、backlog 構造の立場からは #130+#139 のみ merge を支持し、#100+#110・#85+#93・#129+#136・#117+#124 は "切り口が違う" として link_only を支持する。理由は後述）を反映した後のカテゴリ件数は以下の通り:

- **machine-check: 8 件**（#69 #70 #101 #104 #109 #116 #130 #134。#139 は #130 に吸収してクローズ）→ **6 件超なので Epic 化する**
- **parallel-safety: 8 件**（#85 #91 #93 #94 #99 #103 #112 #138）→ **6 件超なので Epic 化する**（争点 C で #138 が drop されても 7 件で依然として閾値超）
- **preview-ops: 4 件**（#100 #110 #117 #124）→ 閾値未満、**Epic 化しない**
- **review-quality: 4 件**（#68 #111 #123 #137）→ 閾値未満、**Epic 化しない**（#137 が drop されれば 3 件でなお下）
- **doc-record: 5 件**（#102 #115 #129 #136 #146）→ 閾値未満、**Epic 化しない**（6 件目に届かない。ここを無理に束ねると「追跡単位が増えて逆に見通しが悪くなる」というブリーフ自身の懸念に抵触する）
- **app: 3 件**（#92 #125 #135）→ Epic 化しない
- **other: 4 件**（#84 #86 #118 #131）→ **これは「カテゴリ」ではなく分類漏れの残余集合** である点を明記したい。G-4 は「同一カテゴリに集中」が前提条件であり、内容がバラバラな other を Epic化すると、後から見た人が「このEpicは何を束ねているのか」を理解できなくなる。**other は Epic 化候補から除外し、各 Issue を個別に争点 C（やめる/取り組む/保留）で処理すべき**。

したがって B の確定案: **Epic を作るのは machine-check と parallel-safety の 2 本のみ**。preview-ops・review-quality・doc-record・app・other は現状の件数では束ねず、`link_only`（相互リンク）または個別処理に留める。これは `session-sprint-rules.md` §2 の「リファインメント（`self-improvement-loop` 整理モード）: 議論型 `discussion-review`」の運用コストにも見合う——Epic を 2 本に絞ることで @owner が priority/sp を判断する対象も 2 Epic + 非 Epic 個票に整理され、消化モードの選択順（priority 降順 → sp 昇順）が機能する状態を保てる。

### 争点A（統合候補・backlog 構造の観点から）

`self-improvement-loop/SKILL.md` Step G-3 の境界「自律クローズの境界: 内容の同一性に確信が持てる場合のみ。少しでも切り口が異なれば残す（消し過ぎより取りこぼしを許容）」を判断基準に、5 候補を個別に見る:

- **#100 + #110**: #110 本文が「#100 と役割が近く着手時に統合可否を判断する」と **自ら保留** している。#100 は「疎通・ステータスの自動検証」、#110 は「デプロイを1コマンドに束ねる」で、前者は検証、後者はデプロイ手順の統合と役割が異なる。確信度が低いため **merge ではなく link_only** を支持する。
- **#85 + #93**: 「一括整形ツールが並行編集中ファイルを巻き込む」（フォーマッタというツールのスコープ）と「並行実行中の破壊的 git 操作の禁止」（git 操作というレイヤ）は防ぐ対象の技術的な層が異なる。**link_only** を支持する。
- **#130 + #139**: 「domain-model.md の語彙変換表と実装命名の整合検査」と「ドキュメントのコード例示と実装の乖離検査」はどちらも「ドキュメント記述 vs 実装の乖離を機械検査する」という同一の検査ツール拡張の話であり、machine-check カテゴリの中でも特に切り口が重なる。**merge を支持**（技術的にワンツールで両方カバーできるかは process_skeptic / app_quality の判断を仰ぐ）。
- **#129 + #136**: 「GitHub API の nullable フィールド一覧を書く」と「選択肢依存の計算値ハードコード禁止をルール化する」はブリーフ自身が言う通り「同一節周辺を触る」だけで、扱う知見の中身は別物（データ形状の記録 vs コーディング規律）。同じファイルを触るというだけで統合すると、後から片方の知見だけ検索性が落ちる。**link_only** を支持する。
- **#117 + #124**: 「Cloudflare レート制限で止めない」（インフラ運用のガード）と「プレビュー動作実測の手順化・PR チェックリスト化」（ドキュメント/プロセス）は問題領域が異なる。**link_only** を支持する。

### 争点C・D（主担当外だが所感）

C は mission_po の CP-5 貢献度判定が主導すべきだが、backlog 構造の立場から 1 点補足する: `self-improvement-loop/SKILL.md` の G-6 出口表にある通り「やめる」は `state_reason="not_planned"` によるクローズで **再オープン可能**（可逆）。したがって「消しすぎ」の被害は限定的であり、#137 #138 #118 #131 のような検討・計測系は疑わしければ積極的に drop してよい、というのが構造面からの後押しになる。

D については、`priority:high` が 13 件残っている状態は消化モードの選択順（`self-improvement-loop/SKILL.md` 消化モード Step1「priority ラベル順（high → medium → なし → low）でソート」）を事実上無効化する。high の定義が緩いと「high 内で何を先にやるか」が結局 createdAt 頼みになり、優先度ラベルが機能しなくなる。B で Epic 化する machine-check・parallel-safety の子 Issue は、Epic 化後は「Epic 自体の priority/sp」で代表させ、子個々の priority は @owner の再査定時に high を絞る方向を支持する（個票が high のまま残ると、Epic とバラで二重にhigh対象が積み上がる）。

### まとめ（Issue番号を明示した結論)
- Epic化する: machine-check（#69 #70 #101 #104 #109 #116 #130 #134）/ parallel-safety（#85 #91 #93 #94 #99 #103 #112 #138）の2本のみ。Epic ラベルは `type:improvement` ではなく **`type:retro-try`** を付与する（レーン孤児化防止）。
- Epic化しない: preview-ops（#100 #110 #117 #124）/ review-quality（#68 #111 #123 #137）/ doc-record（#102 #115 #129 #136 #146）/ app（#92 #125 #135）/ other（#84 #86 #118 #131）。
- merge支持: #130+#139 のみ。
- link_only支持: #100+#110 / #85+#93 / #129+#136 / #117+#124。
- open_questions への追記提案: 「retro-try-handler に棚卸し（重複統合・Epic化・優先度再査定）ステップを追加すべきか」を次の改善 Issue として起票し検討する。

### `app_quality` — 主張
<sub>2026-08-20T09:28:15+09:00</sub>

## app_quality: 実コード確認結果（round1）

前提として `src/domain/ports/*.ts`・`src/composition/container.ts`・`src/domain/model/page-number.ts` とそのテスト・`messages/{ja,en}.json`・`e2e/*.spec.ts`・`tools/check_ui_dimensions.py`・`docs/03_design/data-model/domain-model.md` を実際に読んだ。争点 C（陳腐化判定）を主担当として厚めに書く。

### 争点 C: 陳腐化判定（主担当）

**#125（R-5 の逆算とキャッシュ TTL 見直し）— 陳腐化していない。強く生きている。**
`src/composition/container.ts:18-27` に実装済みの JSDoc で「60 秒は暫定値（`R-5` のレート枠逆算が未確定のため）」「`R-5` が確定したら本値を見直す」と明記されている。ADR `docs/adr/0005-cache-port-yagni-exception-and-ttl.md` §2.2 も TTL を「暫定値」と明記し、根拠を `R-5` 未確定に紐づけている。つまりコード自身が「この値は仮」と自己申告している状態で、#125 が指す欠陥はまさにそのとおり実在する。closeどころか priority を上げるべき候補。

**#135（maxPageFor の境界値テスト）— 陳腐化していない。テストは存在しない。**
`src/domain/model/page-number.ts:12-14` の `maxPageFor(perPage)` 関数は `page-number.test.ts`（全 26 行）で **1 回も呼ばれていない**。既存テストは `pageNumber()` と `tryPageNumber()` のみを対象にしており、`MAX_PAGE`（`DEFAULT_PER_PAGE` 前提の定数）は間接的にカバーされるが、`perPage` が可変値のときの `maxPageFor()` の境界（`perPage` が 0 や `API_RESULT_LIMIT` の約数でないときの端数切り捨て挙動など）は未検証。Issue の指す欠陥は実在する。

**#92（ポート契約の JSDoc・テスト固定）— 部分的に陳腐化・部分的に生存（範囲を絞るべき）。**
実装レベルでは既にかなり手厚い契約テストがある: `cache.test.ts:57-`（`ttlSeconds` が NaN/0/負値で `RangeError`）、`rate-limit.test.ts`（binding 未提供時のフォールバック）、`oauth.test.ts:69-`（`exchangeAuthorizationCode` の失敗系）。一方で **ポートのインターフェース自体**（`src/domain/ports/cache-port.ts` / `rate-limit-port.ts` / `auth-port.ts`）の JSDoc には値域・異常系が書かれていない。例えば `cache-port.ts:9-12` の `set<T>(key, value, ttlSeconds)` には「`ttlSeconds` は正の有限数でなければならず、そうでなければ `RangeError`」という契約が一切書かれておらず、これは `InMemoryCache`（`cache.ts:36-37`）という **1 実装だけ** が知っている暗黙知になっている。将来 2 つ目の `CachePort` 実装（例: Cloudflare KV）を書く人は、この契約をテストを読んで初めて知ることになる。→ 完全な陳腐化ではないが、**Issue の射程を「新規テスト追加」から「既存の暗黙契約をポート JSDoc に文書化する」に絞り込む** のが実態に即している。

**#104（i18n カタログ ja/en 対称性）— 現状は解決済み、ただし「検査の欠如」自体は未解決。**
実際に `messages/ja.json` と `messages/en.json` をキー単位でフラット化して突き合わせたところ、**35 キー対 35 キーで完全に対称**（差分ゼロ）だった。今この瞬間の非対称性という意味では既に解決している。ただし `tools/` 配下にこれを継続的に強制する機械検査は存在しない（`find tools -iname "*i18n*"` はヒットなし）。つまり「今は偶然揃っている」だけで、次に片方だけキーを足すコミットが入れば無検査で drift する。**Issue のタイトルが指す不具合は無い（stale）が、Issue の要求（再発防止の機械検査）自体は無くなっていない**。drop ではなく「現状は満たされているが再発防止検査は未整備」と明記して残すべき。

**#109（UI テストの getByTestId 第一選択）— これは陳腐化ではなく、Issue の前提そのものが Testing Library のベストプラクティスと逆行している疑いが強い。**
`e2e/*.spec.ts` を全数 grep した結果、`getByTestId` は **0 件**、`getByRole` は **79 件**。既存の E2E は Testing Library 公式の推奨クエリ優先順位（`getByRole` が最上位、`data-testid` は「他に手段がないときの最終手段」）に既に従っている。Issue が「`getByTestId` を第一選択にする」検査を機械強制しようとすると、むしろ現状の a11y に配慮したテスト設計（`getByRole` 中心）を破壊する方向の regression になりかねない。これは「解決済みだから閉じる」ではなく「**要求内容が実装と逆方向であり、そのまま実装すると害がある**」という別種の陳腐化。mission_po / process_skeptic にも共有したい: not_planned ではなく「Issue の意図を `getByRole` 優先の明文化 + `getByTestId` 濫用検知（逆方向）」へ本文修正のうえ残す、が妥当。

**#134（呼び出し側 className 直書き検査の射程拡大）— 陳腐化していない。射程は現状 1 ファイルのみ。**
`tools/check_ui_dimensions.py:58-60` の `CALL_SITE_REQUIREMENTS` は `src/ui/search-form.tsx` の 1 件のみを登録。一方 `className=` を使う `src/ui/` 配下のファイルは実測 11 件（`locale-switcher.tsx` `repository-detail.tsx` `pagination.tsx` `per-page-picker.tsx` `back-link.tsx` `repository-list.tsx` `login-link.tsx` `sort-picker.tsx` とコンポーネント 2 件）。検査対象は全体の 1/11 に留まっており、Issue が指す「呼び出し側の className 直書き」を検出する仕組みは大部分のファイルで機能していない。生存。

**#130 / #128（domain-model.md 語彙表と実装命名の整合）— 陳腐化どころか、実際に契約違反（バグの疑いすらある不整合）を発見した。**
`docs/03_design/data-model/domain-model.md:66` の語彙表は「`updated_at` → `lastUpdatedAt`。『最終更新日』（`AR-1`）は `pushed_at` を使う（メタデータ更新で動く `updated_at` ではない）」と明記している。ところが実装（`src/domain/model/repository.ts:41`）は `RepositoryDetail.updatedAt`（`lastUpdatedAt` ではない）というフィールド名で、かつ `src/infrastructure/github/mapper.ts:59` で `updatedAt: new Date(dto.updated_at)` と **`pushed_at` ではなく `updated_at` から直接値を入れている**。UI 側は `messages/ja.json:26` で `"updatedAt": "最終更新"` というラベルを付けて表示している。つまり画面には「最終更新」と表示されるフィールドが、AR-1 が明示的に禁じている `updated_at`（メタデータ変更でも動く GitHub のフィールド）から作られている ——これは単なる命名drift ではなく、**表示内容が仕様と食い違っている可能性がある実バグ**。さらに `watchers` / `forks` / `openIssues`（`repository.ts:38-40`）も語彙表の `watcherCount` / `forkCount` / `openIssueCount` と一致しない。#130 は明確に生存、かつ #128 のスコープ（`updatedAt` のリネーム）だけでは不十分で「値の出どころ（`pushed_at` か `updated_at` か）」まで含めて検証しないと本質を見落とす。**#128 は #130 に吸収し、`updatedAt` の値の出どころ検証を明示的にスコープへ追加すべき** と提案する。

### 争点 A（統合候補）への意見

`#130 + #139`（domain-model.md とドキュメント/実装の識別子整合を検査するスクリプト同士）の統合には賛成。上記の実地確認で分かるとおり、語彙表とコードの不整合は「命名」だけでなく「値の出どころ」まで及ぶため、統合後の Issue 本文には `updatedAt`/`watchers`/`forks`/`openIssues` の具体的な不一致箇所（本コメントに列挙した行番号）を吸収観点として明記してほしい。他の統合候補（#100+#110・#85+#93・#129+#136・#117+#124）は自分のレンズ外のため意見を保留する。

### 争点 B（Epic 化）への意見

`app` カテゴリ（#92 #125 #135）は 3 件で Epic 閾値未満なので Epic 化不要という backlog_hygiene 側の判断を支持する立場だが、#130 が `#128` を吸収して「app レイヤーの実装 ↔ ドキュメント整合」という性質を帯びるなら、machine-check 側の Epic に含めるより `app` の周辺（doc-record 寄り）に置いた方が追跡単位として自然だと考える。

### 争点 D（優先度再査定）への意見

CP-5 ①（与件充足）②（積み上げ可能性）に直結する意味で、**#125（レート枠・TTL の暫定値解消）と #130（表示データの出どころ不整合）は high 継続が妥当**、根拠は上記の通り実装が既に「暫定」「不整合」を自己申告・露呈している。**#135 は sp:1〜2 程度の小さい境界値テスト追加であり high というより medium** が実態に合う（欠陥リスクはあるが影響範囲が限定的）。**#109 は要求内容自体の見直しが先** なので、現状のまま high に置いても着手できない（要 Issue 本文修正）。**#104 は「現状は解決済み・再発防止検査が未整備」という性質上 high は過大** で、medium 程度が妥当。

### 争点 E（取りこぼし点検）への意見

#130 の実地確認で見つかった「`updatedAt` の値が `pushed_at` ではなく `updated_at` から来ている」という具体的な不整合は、振り返りレーン（retro-try）の一般的な「命名を揃えよう」という抽象度では拾いきれず、**実装を読まないと出てこない具体性** だった。統合・Epic 化の際にこの具体例を Issue 本文に落とさないと、次に着手するセッションが再度同じ調査をやり直すことになる。取りこぼし防止のため、統合後 Issue 本文に本コメントの行番号付き指摘をそのまま転記することを推奨する。

## ラウンド 2

### `process_skeptic` — 反論
<sub>2026-08-20T09:30:07+09:00</sub>

## #109（getByTestId 要求）— 全面的に concession

app_quality の実測（`getByTestId` 0 件 / `getByRole` 79 件）は round1 で自分が挙げなかった論点だが、自分の懸念（死蔵する検査）より **一段深刻** だと認める。死蔵は「実装されず放置」だが、#109 は「実装したら既存の a11y 配慮設計を壊す」regression 要求。**やめる（not_planned）ではなく、Issue タイトル・本文を反転（`getByRole` 優先の明文化 + `getByTestId` 濫用検知）してから残す** という app_quality 案に賛成する。process_skeptic としての教訓を追記したい: この Issue はおそらく机上（Testing Library の一般論の誤記憶）で起票され、**起票時点で実コードを確認していなかった**。今回のように「検査を足す」提案には着手前の実コード grep を Done Criteria に義務付けるべきという一般則を open_questions に残すことを推奨する。

## #134（射程 1/11）— 自分の懸念を補強する実例として reinforce

これは反論ではなく **自分の主張の裏付け** として受け取る。`check_ui_dimensions.py` の `CALL_SITE_REQUIREMENTS` が `search-form.tsx` 1 件だけを登録して以降拡張されていないという事実は、まさに round1 で警告した「検査スクリプトが実装後に放置され死蔵する」パターンが **既にこのリポジトリで実際に起きている** 証拠。したがって #134 は「やめる」ではなく残すが、Done Criteria に「拡張する」という曖昧な表現ではなく **`src/ui/` 配下 className 使用 11 ファイル全件を対象に含める** と行数まで明記すべきと修正提案する。そうしないと #134 実装後も次の 1 ファイルだけ足されて再び射程 2/11 で放置される。

## mission_po の high 昇格案（#92 / #125 / #116）

- **#125**: 賛成。コード自身（`container.ts` JSDoc・ADR 0005）が「暫定値」と自己申告しており、検査スクリプト追加ではなく実装債務そのものなので、自分の懸念（死蔵する検査）は当てはまらない。high 継続に異論なし。
- **#92**: 一部反対。app_quality の実地確認で契約テストの大半（`cache.test.ts` `rate-limit.test.ts` `oauth.test.ts`）は既に存在し、残る欠落は「ポート interface の JSDoc に契約を書く」という小工数の文書化のみ。実装インパクトが小さいものを high に置くと、消化スロットで #125 のような本当に重い負債より先に消化される歪みを生む。medium・sp:1〜2 を推奨。
- **#116**: 部分同意・部分反対。「秘匿情報の非露出」という **リスクの重大性** には同意し high 相当と認めるが、round1 で hold とした **実装アプローチ**（再現可能なネットワーク遮断・観測テスト）への懸念は変わらない。フレーキーな E2E ネットワーク監視より、まず「クライアントにバンドルされるコードパスに秘匿 env 変数が渡っていないか」を静的 grep で検査する軽量版にスコープダウンしてから high に上げることを条件付き支持とする。

## backlog_hygiene の merge 支持（#130+#139）— フェーズ分割の具体化

app_quality が実地で `updatedAt` が `pushed_at` ではなく `updated_at` から来ている（`mapper.ts:59`）という **実バグ疑いを発見済み**。これを踏まえフェーズを具体化する:
- **フェーズ 1（#130 が担う。#128 を吸収）**: 語彙表 vs 実装命名 + 「値の出どころ」の静的照合（正規表現ベースで実装容易）。ただし **今回発見済みの `updatedAt` 不整合は検査ツールの完成を待たず、この棚卸しとは別に即バグ修正 Issue として先出しすべき**（実バグを「将来の検査で拾う」対象にして放置しない）。
- **フェーズ 2（#139）**: コード例示ブロックのパース＋実装照合。実装難度が高いため sp を分けて後続にする。
2 フェーズを 1 つの Issue 本文に書き、フェーズ 1 未完了のままフェーズ 2 に着手しないことを明記する。

### `mission_po` — 反論
<sub>2026-08-20T09:30:16+09:00</sub>

## mission_po（ラウンド2: 反論・譲歩）

### 1. process_skeptic の「machine-check high 偏重への懐疑」と自分の「#92/#125/#116 昇格」提案は両立するか

**両立する。ただし #116 は撤回し process_skeptic 側に譲歩する。**

process_skeptic の懐疑対象は「検査を足せば再発防止になるという前提」で、名指しされたのは #101 #109 #130 #134（machine-check 系）。自分が high 昇格を提案した #92・#125 は machine-check ではなく `app` カテゴリで、しかも app_quality の実地確認により「暫定値の自己申告（#125）」「未検証の境界値（#92 関連の #135）」という **実装済みコードが露呈させている実欠陥** である。process_skeptic の基準「実インシデント起因のみ high 残置」に照らしても、#125 は `container.ts` の JSDoc 自身が「`R-5` 未確定の暫定値」と書いている時点で実インシデント予備軍そのものであり、この基準を満たす。つまり両者の基準は矛盾せず、**「検査追加系」と「実装欠陥系」を分けて扱うべき** という process_skeptic の論点を自分も採用する。

一方 **#116（ネットワーク非送出の検証）は撤回して hold に譲歩する。** 自分は round1 で「秘匿情報のクライアント露出防止ゲート」に接続すると書いたが、process_skeptic の記述（「テスト環境でネットワーク呼び出しを再現可能に遮断・検証」）とブリーフの分類（machine-check の静的検査ツール群）を読み直すと、#116 の実際の射程は **テストの hermeticity（実 API を叩かないテスト設計）** の話であり、秘匿情報露出防止ゲートへの接続は自分の深読みだった可能性が高い。もし本当に秘匿情報露出ゲート相当なら high 継続が妥当だが、Issue 本文の意図がどちらなのか現時点の要約からは確定できない。**この解釈の分岐は成果物（priority だけでなく実装スコープ）を変えるため、verdict の open_questions に「#116 の射程がテストの hermeticity 検証か、秘匿情報非露出の検証か」を明記すべきと提案する**（SD-3 第 2 系統相当ではなく Issue 本文の曖昧性だが、判断が割れているため記録は必要）。射程確定までは process_skeptic の hold 案を支持する。

### 2. app_quality の #130/#128 実バグ級不整合の発見 → priority 提案を撤回・引き上げる

**round1 の「#130 → medium 格下げ」を撤回する。** 自分は #130 を「ドキュメント-実装整合の検査」＝③説明可能性の薄い接続として medium に格下げ推奨したが、app_quality が実コード（`repository.ts:41` / `mapper.ts:59` / `ja.json:26`）を読んだ結果、**表示中の「最終更新」ラベルが `domain-model.md` が明示的に禁じている `updated_at`（`pushed_at` ではない）から作られている** という具体的な不整合を発見している。これは③説明可能性ではなく、**①与件充足**（`minimum-requirements.md` §7「詳細ページにリポジトリ名・オーナーアイコン・言語・Star数・Watcher数・Fork数・Issue数が表示される」＝表示データの正確性）に直結する実データ品質の問題であり、KPI 接続経路が自分の想定より強い。**#130 は high 継続を支持し直す**（#128 を吸収し、値の出どころ検証までスコープに含める app_quality 案に賛成）。

この事例は自分の判定軸（KPI 表への 1 ホップ接続）の弱点を露呈した: 検査カテゴリ名だけで③に分類し、実装を読まずに medium 判定した。以後、machine-check カテゴリの medium 格下げ提案（#101 #134）についても、app_quality の実地確認がまだない Issue は「暫定 medium、実装確認が出たら再査定」という留保を付ける。

### 3. backlog_hygiene の Epic ラベル `type:retro-try` 提案（`type:improvement` ではなく）

**賛成する。** 自分のレンズ（KPI 接続）からはラベル選択自体に優劣はないが、`type:improvement` を付けた場合に改善 Issue レーンの消化モードがこの Epic を実装対象として拾ってしまうという backlog_hygiene の指摘は、レーン規約（`improvement-lane-map.md` Rule 2・#160）と整合しており、これは③説明可能性（ルールの一貫性）にも寄与する。ただし念のため: Epic の priority/sp は @owner 権限（`session-sprint-rules.md` §4）であり、ラベル種別を変えても自分が D で提案した「KPI 1 ホップ接続を high の条件にする」という判定基準自体は変わらない——Epic 自体の priority は low〜medium に据え置く自分の round1 提案を維持する。

### 4. drop 推奨（#137 / #138 / #131）への反論への再反論

process_skeptic・backlog_hygiene とも自分の #137/#138/#131 drop に同意しており、正面からの反論は出ていない。唯一の差分は **#118**: 自分は hold、process_skeptic は「やめない・sp:1 で low〜medium のまま残す」。ここは process_skeptic に譲歩する。hold は「判断を止めて眠らせる」含意があるが、sp:1 の低コストで a11y CI 基盤の整合という KPI ゲート（a11y=100）に薄く効く実利がある以上、hold より **low 優先度で active のまま残す** 方が「取りこぼしより消しすぎを許容しない」G-3 原則にも沿う。#118 は「hold」から「low・active」に訂正する。

### `backlog_hygiene` — 反論
<sub>2026-08-20T09:30:37+09:00</sub>

## backlog_hygiene round2: 4 論点への応答

### 1) process_skeptic の「9本中5本が run_checks.sh 未接続」実測 → Epic の完了条件を修正する（concession寄りrebuttal）

この実測は machine-check を Epic 化する「べきか」自体は変えない（8件は依然閾値超）。変えるのは **Epic の完了条件**。`self-improvement-loop/SKILL.md` Step G-4 は Epic 本文に「子 Issue 一覧と統合の意図」を書けとしか言っておらず、「配線したか」を検証する項目が無い。これは前例（5本死蔵）を再生産する設計的欠陥だと process_skeptic の指摘を受けて認める。

提案: machine-check Epic の子 Issue 一覧に **「配線先」列を必須で追加** する（例: `run_checks.sh` 組み込み / pre-commit / スキル手動実行のいずれか）。「配線先」が空欄のまま子 Issue がクローズされることを Epic の完了条件から明示的に禁止する 1 行を Epic 本文に書く。これは G-4 の文言に手続きを足すだけで既存の権限（メインが Epic 本文を書く）の範囲に収まる。

### 2) app_quality の #130/#128 実バグ → カテゴリは変えないが Epic の外に緊急対応を切り出す

カテゴリ変更（machine-check→app）には反対する。#130 が担う「語彙表と実装の整合を継続検査する仕組み」自体は machine-check の性質のまま。一方で app_quality が見つけた **具体的な不一致**（`updatedAt` が `pushed_at` でなく `updated_at` 由来・`watchers`/`forks`/`openIssues` の命名 drift）は、検査ツールの完成を待たずに直せる **表示内容の実バグ** であり、Epic（消化順序が遅くなりうる追跡単位）に紐付けたまま眠らせるべきではない。

→ **#130 は machine-check Epic に残す**（#139 を吸収し「継続検査」の役割）が、**app_quality が特定した具体的な不一致の修正は別立ての緊急対応として切り出す** ことを提案する。新規 Issue 化してもよいし、#130 の本文に「Phase 0: 今すぐ直す（行番号付き不一致一覧）／ Phase 1: 継続検査スクリプト」と分割して Phase 0 だけ Epic 外で即着手してもよい（後者の方が Issue 増殖を避けられ好ましい）。これは 1) の「配線先」要求と同型で、**Epic は仕組み化のための追跡単位であり、個別の実害修正の受け皿にしない** という原則を一貫させる。

### 3) #137/#138/#131 の drop → hold へ差し戻す根拠は見つからなかった

Step G-3 の「切り口が少しでも違えば残す」は **重複統合（同一性の判定）の基準** であり、G-6 の「やめる/保留」判定は別の軸（CP-5 貢献度・着手コストの明瞭さ）を使う。したがって G-3 を根拠に drop への反対はできない、というのが構造面からの結論。#138・#131 は mission_po の言う通り KPI 接続ゼロ・可逆（not_planned で再オープン可）で drop に同意する。#137 のみ 1 点補足: 「検討する」で終わる Issue は削除してよいが、**検討そのものへの答えが今回の議論で出ている（process_skeptic「self-improvement-loop の議論の場で決めれば足りる」）ので、クローズコメントに結論を書き残す**（統合レビュー役は現時点で不要、というこの議論の暫定結論）。hold への差し戻しではなく drop の質を上げる提案。

### 4) #100+#110 を link_only に留めると「どちらも着手されない」か → 限定的な補強で対処

process_skeptic の失敗様式（統合すると両方薄まる）は merge のリスクだが、逆に link_only が「どちらも着手されない」リスクを生むのは、**組み合わせて初めて価値が出る場合** に限られる。#100（疎通検証）と #110（1コマンド束ね）は個別に単体で価値がある（検証だけでも、束ねるだけでも有用）ため、link_only のまま消化されても無駄にならない。よって merge へ転換する必要はないと判断するが、**放置リスクをゼロにするため**、@owner の priority/sp 再査定時に「同一消化サイクルでまとめて選ぶ」よう相互リンクコメントに 1 行添えることを提案する（トラッキング単位は分けたまま、選定順だけ揃える軽量な補強）。

### `app_quality` — 反論
<sub>2026-08-20T09:30:49+09:00</sub>

## app_quality: round2 rebuttal

### `updatedAt` の利用者影響の具体化（訂正あり）

round1 の記述を実装追跡でさらに詰めた結果、**表現を訂正する**。`src/ui/repository-detail.tsx:41-46` の `stats` 配列は `stars` / `watchers` / `forks` / `openIssues` のみを描画しており、`repository.updatedAt` を参照する箇所は詳細ページ（`app/[locale]/repos/[owner]/[repo]/page.tsx` → `repository-detail.tsx`）に **存在しない**。一方 `messages.updatedAt`（"最終更新"）というラベル文字列は `src/ui/repository-list.tsx:85` の一覧画面で使われているが、そこで書式化されている値は `item.lastPushedAt`（`RepositorySummary`、`pushed_at` 由来で正しい）であって `RepositoryDetail.updatedAt` ではない。

つまり **今この瞬間、ユーザーに見えている画面上の食い違いはゼロ**（round1 の「画面には最終更新として表示される」は不正確だった。訂正する）。実態は「詳細画面ドメインモデルに `updated_at`（AR-1 が使用禁止と明記する非推奨フィールド）由来の値が乗っているが、どこにも描画されていない **死蔵フィールド**」。ただし放置リスクは残る: `mapper.test.ts:81` が `updatedAt` を `dto.updated_at` から作ることを **期待値として固定** しており、テストが「これは意図した仕様」というお墨付きを与えてしまっている。将来「詳細画面にも最終更新日を出そう」という自然な追加要求が来たとき、実装者はテストが緑であることを根拠に `repository.updatedAt` をそのまま使い、AR-1 違反の表示が本当に生まれる。

**判定**: 現時点で観測可能な defect が無い以上、新規 `type:bug` は起票しない。既存 #128/#130 のスコープ内（ドキュメント ↔ 実装の整合検査）で、「`RepositoryDetail.updatedAt` は現在未使用の死蔵フィールドであり、`pushed_at` 相当に直すか削除するかを判定する」という具体的な対応方針を本文に明記すべき、というのが app_quality の結論。

### #138 / #137 / #131 の drop 推奨への追認

自分のレンズ（`src/` `e2e/` の実コード）で反証材料は見つからなかった。3 件とも app コードにもテストにも接点がなく、drop に **追認** する。

### process_skeptic の「9 本中 5 本未接続」への反応（#104 は作る価値があるか）

ある。ただし条件付き。`messages/{ja,en}.json` は現状 2 ファイル・35 キーとまだ小さく、専用 Python スクリプトを新設するコストは低い。しかし process_skeptic の指摘（配線されない検査は死蔵する）を踏まえ、**Done Criteria に `run_checks.sh` への実配線を明記しない #104 は building 不可** とすべき。加えて、車輪の再発明を避けるため着手前に `eslint-plugin-i18n-json` 等の既製 ESLint プラグインで対称性チェックが賄えないかを先に確認する一手間を挟むべき（process_skeptic の「lint 拡張で足りないか検討」に同意）。

### backlog_hygiene の Epic 案への実装コスト観点の異論

machine-check Epic（8 件）の中身は実装コストの分散が大きい: `#69`/`#70` はフック・PR 本文の存在チェック程度で sp:1 級、対して `#130`+`#139`（統合）は Markdown パース→識別子抽出→実コード照合という sp:5 級の作業。Epic 化自体には異論ないが、**Epic 単位の priority/sp で子を代表させると、安い #69/#70 が高コストな #130+#139 の完了待ちで着手が遅れる** 懸念がある。子の sp は個別に残し、消化スロットが Epic 内でも sp 昇順に着手できる運用（Epic はあくまで追跡単位、実装順序は子の sp 基準）にすることを提案する。

## ラウンド 3

### `lead` — 合意
<sub>2026-08-20T09:32:57+09:00</sub>

## 合意点

1. **統合は 1 件のみ**: `#130 ← #139` の吸収だけが 4 名の支持を得た（技術基盤「Markdown パース → 識別子抽出 → 実コード照合」を共有する唯一の組）。ただしフェーズ分割（フェーズ 1 = 語彙表 vs 実装命名 + 値の出どころ照合 / フェーズ 2 = コード例示ブロックの照合）を本文に明記し、フェーズ 1 未完でフェーズ 2 に着手しないことを条件とする。
2. **残り 4 候補は統合しない**: `#100+#110` `#85+#93` `#129+#136` `#117+#124` はいずれも「同じファイルに触れる」という表層シグナル由来で、原因・対処の層が異なる（`#129+#136` は明確な誤検知）。Step G-3「切り口が少しでも違えば残す」に従い相互リンクのみとする。
3. **Epic は 2 本だけ作る**: `machine-check`（8 件）と `parallel-safety`（7 件）のみが閾値 6 件を超える。`preview-ops` 4 / `review-quality` 3 / `doc-record` 5 / `app` 3 / `other` 3 は束ねない（`other` はカテゴリではなく分類漏れの残余集合であり、束ねると Epic の意味が失われる）。
4. **Epic のラベルは `type:improvement` ではなく `type:retro-try`**: 子が全員 `type:retro-try` である以上、`type:improvement` を付けると改善 Issue レーンの消化モードが Epic を実装対象として拾い、`improvement-lane-map.md` Rule 2（#160）違反になる。ラベル無しは「どのレーンも拾わない孤児」を作る。G-4 の文言からの明示的逸脱として Epic 本文に理由を 1 行書く。
5. **Epic の完了条件に「配線先」を必須化**: `check_*.py` 9 本のうち 5 本が `run_checks.sh` 未接続で死蔵しているという実測を踏まえ、machine-check Epic の子一覧には「配線先」（`run_checks.sh` 組み込み / フック / 手動実行）列を必須にし、空欄のままクローズすることを禁じる。
6. **Epic は追跡単位であり実装単位ではない**: 子の `sp` は個別に維持し、消化は Epic 内でも `sp` 昇順で進める（安い #69 / #70 が高コストな #130 の完了待ちで滞留しないため）。
7. **`#137` `#138` `#131` は「やめる」**: KPI 表（与件 / a11y / perf / CWV / テスト CI / ADR）のどれにも 1 ホップで接続できず、かつ「検討する」「とりあえず測る」で終わり成果物を生まない。`not_planned` は可逆であり再オープンで復帰できる。
8. **`#118` は「やめる」ではなく low で残す**: a11y 100 という品質ゲートの CI 基盤に薄く効き、`sp:1` の低コストで済む。
9. **`#109` は要求内容が逆方向**: 既存 E2E は `getByRole` 79 件 / `getByTestId` 0 件で Testing Library の推奨順に既に従っている。このまま実装すると a11y 配慮のテスト設計を壊す regression になる。クローズではなく **本文を反転**（`getByRole` 優先の明文化 + `getByTestId` 濫用の検知）して残す。
10. **`priority:high` 13 件は消化スロットを無効化している**: high の条件を「KPI 表の項目に 1 ホップで接続経路を説明できること」に統一し、`#130`（表示データの出どころ不整合）と `#125`（TTL 暫定値の自己申告）だけを high 相当とする。最終決定は @owner（PO）の権限。

## 議論で覆った主張（相互検証の成果）

- `mission_po` は `#130` を「③説明可能性の薄い接続」として medium 格下げを提案したが、`app_quality` の実コード確認（`repository.ts:41` / `mapper.ts:59`）を受けて **high 継続支持へ転換** した。カテゴリ名だけで KPI 接続を判定した自分の判定軸の弱点を認めている。
- `mission_po` は `#116` を「秘匿情報のクライアント露出防止ゲートに直結」として high 昇格を提案したが、`process_skeptic` の指摘を受けて **深読みだったと撤回** し、射程の確定を open_question に回した。
- `process_skeptic` は `#109` について `app_quality` の実測に **全面 concession**（死蔵より深刻な regression 要求だと認めた）。
- `app_quality` は round 1 の「画面に『最終更新』として `updated_at` 由来の値が表示されている」を **自ら訂正** した。`repository-detail.tsx:41-46` に `updatedAt` の描画はなく、一覧の「最終更新」は正しく `lastPushedAt` を参照している。現時点で利用者に見える食い違いはゼロで、実態は「死蔵フィールド」。ただし `mapper.test.ts:81` が誤った出どころを期待値として固定しているため、将来「詳細にも最終更新を出そう」となったときに AR-1 違反の表示が生まれる。新規 `type:bug` は起票せず `#128` のスコープに吸収する。

## 残った対立

- `mission_po` は Epic 自体の priority を low〜medium に据え置くべきとしたが、`backlog_hygiene` は Epic の priority で子を代表させることに `app_quality` が実装コスト分散を理由に反対した。→ **Epic に priority を付けず（または low）、子の priority / sp を individual に維持する** で調停する。

### `lead` — 判定
<sub>2026-08-20T09:33:59+09:00</sub>

{
  "merges": [
    {
      "keep": 130,
      "close": 139,
      "absorb": "ドキュメントのコード例示ブロックと実装の乖離検査を『フェーズ 2』として吸収する。本文に『フェーズ 1: domain-model.md 語彙表 vs 実装命名 + 値の出どころ照合（正規表現ベース）』『フェーズ 2: コード例示ブロックのパース + 実装照合』の 2 段を明記し、フェーズ 1 未完のままフェーズ 2 に着手しないことを条件とする。配線先は run_checks.sh。",
      "reason": "両者は『Markdown パース → 識別子抽出 → 実コード照合』という同一の技術基盤で実装でき、4 名中 3 名が統合を支持した唯一の組。"
    }
  ],
  "link_only": [
    {"issues": [100, 110], "reason": "疎通検証（HTTP リトライ・タイムアウト設計）と CLI ラッパー化は個別に単体で価値があり、統合すると易しい束ね機能だけ作られて検証が骨抜きになる。ただし @owner の再査定時に『同一消化サイクルでまとめて選ぶ』注記を添える。"},
    {"issues": [85, 93], "reason": "一括整形ツールのファイルスコープ未確認と、git コマンド自体の禁止リストは原因も対処も層が異なる。"},
    {"issues": [129, 136], "reason": "domain-model.md の同一節周辺を触るだけの表層一致で、データ形状の記録とコーディング規律という別物。類似検知の誤検知と判定した。"},
    {"issues": [117, 124], "reason": "レート制限回避（インフラ運用）と動作実測の手順化（プロセス）は問題領域が異なる。順序依存はあるが統合しない。"}
  ],
  "epics": [
    {
      "title": "[Epic] machine-check: ドキュメント・実装整合の機械検査を配線まで含めて揃える",
      "children": [69, 70, 101, 104, 109, 116, 130, 134],
      "reason": "閾値 6 件超（8 件）。ただし check_*.py 9 本中 5 本が run_checks.sh 未接続で死蔵している実測を踏まえ、子一覧に『配線先』列を必須にし、空欄のままクローズすることを Epic 完了条件で禁じる。"
    },
    {
      "title": "[Epic] parallel-safety: 並行サブエージェント実行の安全性と契約を担保する",
      "children": [85, 91, 93, 94, 99, 103, 112],
      "reason": "閾値 6 件超（#138 を drop した後も 7 件）。並行実行中に担当外の作業ツリーを壊す経路と、委譲の契約不足という共通の根を持つ。"
    }
  ],
  "drop": [
    {"issue": 137, "reason": "『検討する』で終わり成果物を生まない。統合レビュー役は現時点で不要という結論が本議論で出たため、クローズコメントにその結論を書き残す。"},
    {"issue": 138, "reason": "閾値判定もアクションも紐付かない『とりあえず測る』テレメトリで、KPI 表のどの項目にも 1 ホップで接続できない。具体的な閾値判断とセットなら再起票する。"},
    {"issue": 131, "reason": "数行の運用メモで完結する内容に sp・優先度管理のコストを払う価値がない。次に該当ファイルを触る PR のついでに 1 行書けばよい。"}
  ],
  "body_rewrite": [
    {
      "issue": 109,
      "reason": "既存 E2E は getByRole 79 件 / getByTestId 0 件で Testing Library の推奨クエリ優先順位に既に従っており、現行本文のまま実装すると a11y 配慮のテスト設計を壊す regression になる。",
      "new_direction": "『getByRole 優先の明文化 + getByTestId 濫用の検知（逆方向の検査）』へ本文を反転する。"
    },
    {
      "issue": 128,
      "reason": "app_quality の実地確認で、RepositoryDetail.updatedAt が AR-1 が禁じる updated_at 由来であり、かつどこにも描画されていない死蔵フィールドだと判明した。mapper.test.ts:81 が誤った出どころを期待値として固定している。",
      "new_direction": "スコープに『値の出どころ（pushed_at か updated_at か）の検証』『死蔵フィールドを直すか削除するかの判定』『mapper.test.ts:81 の期待値是正』を追加する。新規 type:bug は起票しない（現時点で観測可能な defect がないため）。"
    },
    {
      "issue": 130,
      "reason": "#139 の吸収と、app_quality が特定した具体的不一致の受け皿の分離。",
      "new_direction": "フェーズ 1 / 2 の分割を明記し、watchers / forks / openIssues（repository.ts:38-40）と語彙表 watcherCount / forkCount / openIssueCount の不一致を行番号付きで本文に転記する。具体的な不一致の修正自体は #128 が担い、#130 は継続検査の仕組み化に徹する。"
    },
    {
      "issue": 116,
      "reason": "射程が『テストの hermeticity（実 API を叩かない設計）』なのか『秘匿情報の非露出検証』なのかで実装スコープと priority が変わる。",
      "new_direction": "本文に射程の確定を求める 1 行を追加し、まず『クライアントにバンドルされるコードパスへ秘匿 env 変数が渡っていないか』の静的 grep という軽量版から始める方針を明記する。"
    }
  ],
  "priority_recommendations": [
    {"issue": 130, "from": "high", "to": "high", "reason": "domain-model.md が禁じる updated_at 由来の値がドメインモデルに乗っており、①与件充足（表示データの正確性）に直結する。"},
    {"issue": 125, "from": "medium", "to": "high", "reason": "container.ts:18-27 の JSDoc と ADR 0005 §2.2 がともに TTL を『暫定値』と自己申告しており、実装債務が確定している。"},
    {"issue": 135, "from": "high", "to": "medium", "reason": "maxPageFor の境界値テストは欠落が実在するが sp:1〜2 級で影響範囲が限定的。"},
    {"issue": 109, "from": "high", "to": "medium", "reason": "本文の反転が済むまで着手できない。"},
    {"issue": 101, "from": "high", "to": "medium", "reason": "③説明可能性への接続で、実インシデント起因の根拠が示されていない。"},
    {"issue": 134, "from": "high", "to": "medium", "reason": "UI 実装規律で⑤寄り。ただし射程 1/11 という死蔵実績があるため Done Criteria の明確化が先。"},
    {"issue": 123, "from": "high", "to": "medium", "reason": "レビュー記録の形式で③説明可能性への薄い接続。"},
    {"issue": 115, "from": "high", "to": "medium", "reason": "環境変数のドキュメント追記で⑤運用改善。"},
    {"issue": 124, "from": "high", "to": "medium", "reason": "プレビュー運用の手順化で⑤運用改善。"},
    {"issue": 100, "from": "high", "to": "medium", "reason": "同上。#110 と同一消化サイクルで選ぶ。"},
    {"issue": 91, "from": "high", "to": "medium", "reason": "委譲手順の明文化で⑤運用改善。"},
    {"issue": 136, "from": "high", "to": "medium", "reason": "コーディング規律の明文化で③寄り。"},
    {"issue": 129, "from": "high", "to": "medium", "reason": "nullable の記録は①寄りの実利があるが high までは不要。"},
    {"issue": 84, "from": "high", "to": "low", "reason": "ディープリサーチ基盤の障害復旧でプロダクト KPI と接続しない。"},
    {"issue": 118, "from": "medium", "to": "low", "reason": "『やめる』ではなく sp:1 の低コストで残す。a11y CI 基盤の整合に薄く効く。"},
    {"issue": 68, "from": "（欠落）", "to": "medium", "reason": "priority 未設定のため消化スロットで『なし』扱いになる。retrospective の呼び出し元実装は⑤運用改善。"},
    {"issue": 69, "from": "（欠落）", "to": "medium", "reason": "同上。スプリント規律の可視化。"},
    {"issue": 70, "from": "（欠落）", "to": "low", "reason": "同上。PR 本文トレーラーの Warning 検査は sp:1 の軽量施策。"}
  ],
  "hold": [],
  "critical": [
    "type:retro-try の棚卸しを担えるスキルが振り返りレーンに存在しない（retro-try-handler には重複統合・Epic 化・優先度再査定のステップが無い）。今回はユーザー明示指示に基づく単発対応として self-improvement-loop の判断ロジックを借用したが、明示せず繰り返すと improvement-lane-map.md Rule 2（#160）の決定がなし崩しになる。",
    "check_*.py 9 本のうち 5 本（check_agent_definitions / check_claude_code_updates / check_datetime_tz / check_pending_pr_reviews / check_skill_references）が run_checks.sh に未接続で、検査資産の半数以上が PR ゲートの外にある。新規の検査追加 Issue が同じ結末を辿る構造的リスクがある。",
    "#109 のように、実コードを確認せず机上で起票された Issue が『実装すると害がある』要求として残っていた。検査追加系 Issue の Done Criteria に『着手前の実コード grep』と『配線先の明記』を義務付けないと同種が再発する。"
  ],
  "open_questions": [
    "retro-try-handler に棚卸し（重複統合・Epic 化・優先度再査定）ステップを追加すべきか、それとも棚卸しは今後も改善 Issue レーンへの単発委任で良いか（改善 Issue として起票して検討する）。",
    "#116 の射程は『テストの hermeticity 検証』か『秘匿情報の非露出検証』か（Issue 本文の曖昧性。射程で実装スコープと priority が変わる）。"
  ]
}
