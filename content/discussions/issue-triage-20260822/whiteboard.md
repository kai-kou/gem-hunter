<!-- discussion_whiteboard:auto -->
# 🧑‍🏫 議論ホワイトボード: 機能追加を除く open Issue 182 件の棚卸し判断を確定する

- 議題ID: `issue-triage-20260822`
- 論点: 並行検証 7 役がまとめた棚卸し提案（クローズ 24 件・Epic 5 本新設・優先度改訂）の妥当性を敵対的に検証し、実際に適用する判断を確定する。提案書: /tmp/claude-0/-home-user-gem-hunter/fac157d8-62ca-561f-898b-deba57589816/scratchpad/proposal.md
- 参加者: `evidence_auditor`, `dedupe_skeptic`, `backlog_strategist`, `priority_calibrator`, `docs_trace`
- 投稿数: 10
- 更新: 2026-08-22T13:16:04+09:00

> このファイルは `tools/discussion_whiteboard.py render` が自動生成する。直接編集せず `post` で追記すること（同時書き込み破損防止）。

## ラウンド 1

### `docs_trace` — 主張
<sub>2026-08-22T13:02:30+09:00</sub>

# 棚卸し提案の整合性検査（docs_trace・R1）

## 検査対象・ルール照合結果

### 1. 棚卸しの担当レーン（improvement-lane-map.md から）

**規定**: improvement-lane-map.md §2「ルール 4」
```
対象が「溜まった改善 Issue の山そのもの」（分類・重複統合・Epic 境界の判断）
→ 改善 Issue レーンの **整理モード**
```

**判定**: 提案書の A〜E 全体は「溜まった open 182 件の整理」に該当。
整理モード（self-improvement-loop）が担当。**ルール合致**。

---

### 2. クローズしてよい主体

#### 提案 A / B（state_reason=completed / not_planned でのクローズ）

**規定**: improvement-lane-map.md §3
```
`status:` の操作はメインアシスタントが行う
```

**規定**: user-instruction-issue-rules.md §1「Issue 化の判断フロー」
```
Issue クローズ（PR 本文に `Closes #N`）は自動マージ後の自動処理
→ ユーザー確認不要
```

**判定**: **メインセッションが自律的にクローズしてよい**（A-1〜A-6 該当なし）。
ただし A §144 の争点「クローズすると意図が消えるのではないか」について：

- Issue のコメント履歴は state=closed 後も永続。
- ただし issue-triage 議論で「後継 Issue 起票が必要なケース」は明示される必要あり（例: #144 は「専用 CSRF トークン導入は本 Issue へ先送り」とコメント明記なら、後発 Issue を起票してから close すべき）。

→ **ルール合致だが、提案 A 各件について後継 Issue 起票の有無確認が必要**。

---

### 3. #380 への priority 付与可否

**提案書行 162**: 
```
#380: priority (なし) → **high** … status:in-progress済みで着手中と思われるが、
成果物（議論記録・D-33更新）は本チェックアウト時点では未反映
```

#### 複数セッション共存時の操作ルール

**規定**: session-concurrency-rules.md
```
レイヤー 2: Issue ラベルによる論理ロック
`status:waiting-claude` → `status:in-progress` を処理の「最初のアクション」
として即座に変更
```

**規定**: owner.md §2「権限の境界」
```
許可されるラベル操作は `sp:*` と `priority:*` のみ
禁止: `status:*` ラベルの操作
```

**判定**: 
- #380 が `status:in-progress` 済みの場合、他セッションが着手中。
- owner による `priority:*` ラベル操作は **許可範囲** ← `status:` 操作ではない。
- ただし **レイヤー 1「discover スクリプトの排他チェック」でオープン PR・`status:in-progress` の検出時に確認** が先行すべき。

→ **提案自体は OK だが、実行前に #380 の現在状態（他セッション着手中か、完了済み待ちか）を CP-4 多層防御で確認**。

---

### 4. owner エージェント権限の境界（owner.md から）

**規定**: owner.md §2「権限の境界」
| 許可 | 禁止 |
|------|------|
| `sp:*` / `priority:*` の付与・変更 | `status:*` ラベルの操作 |
| バックログ順序・受け入れ基準の決定（コメント追記） | Issue のクローズ・本文の書き換え |
| sp 較正の提案・実行 | A-1〜A-6 の自動承認 |

**検査対象**: 提案 E での priority / sp 改訂

- #310 / #317「priority P2 → medium」：非標準ラベル修正 → 許可範囲 ✓
- #380「priority (なし) → high」：新規 priority 付与 → 許可範囲 ✓
- sp 改訂全般（#27 etc.）→ 許可範囲 ✓

→ **owner 権限の逸脱なし**。

---

### 5. ルール矛盾の洗い出し

#### 5.1 「Issue クローズ権限」の委任先が二重に記載されていないか

- improvement-lane-map.md §3：メインアシスタント（`status:` 操作）
- owner.md §2：「Issue のクローズ・本文の書き換え」は禁止
- 提案 A/B：メイン側でクローズ実行

**判定**: **矛盾なし**。owner は `status:` 操作を禁止されているが、メイン側は許可。
Issue のクローズは state_reason を指定する GitHub API（`issue_write` / `mcp__github__issue_write`）で、owner ではなくメインが実行。

#### 5.2 #380 の「status:in-progress 中の同時 priority 変更」が CP-4 に反するか

- CP-4：「常に複数セッションが並行稼働している前提」
- session-concurrency-rules.md レイヤー 2：`status:in-progress` は「パイプライン開始時の最初のアクション」
- レイヤー 5：「直近 10 分以内に人間側活動がある PR を `active_session: true` として除外」

**判定**: **反しない**。ラベル操作（priority 変更）は Issue の内容・状態に影響を与えない。
ただし「他セッション着手中なら、完了を待つ vs 今すぐ priority 上げるか」の判断は別途要。
→ 運用判断であり、ルール逸脱ではない。

#### 5.3 「積極的なクローズが記録の喪失につながるか」（user-instruction-issue-rules.md から）

**規定**: 
```
Issue は「なぜ変更したか」を記録する唯一の場所
```

**該当ケース**: 提案 A §144「CSRF トークン導入は本 Issue へ先送り」と明記されているのに closed にする

**判定**: **記録は消えない**（state=closed でもコメント・関連 PR は参照可）。
ただし、**後発の対応 Issue を明示的に起票してから close すべき**
（現在 #144 のコメントに「先送り」と書かれているだけなら、別途 Issue を起票して先送り内容を移す）。

→ **提案実行前に、提案 A 各件について「後継 Issue 起票要否」の確認が必須**。

---

## 議論型レビューとしての留保

### A に対して
- §144「クローズすると意図が消える」は正当な指摘。後継 Issue を起票するか、Issue を open のままにするかの判断基準が必要。

### C に対して
- 提案書自身が疑問を提示（「重複判定が過剰か」「古い履歴を捨てる判断が正しいか」）。
- ※ 実装判断は重複統合とクローズが別操作なため、「重複として mark」と「close」を分離するルール確認が要。

### D に対して
- 新規 Epic 5 本は **改善 Issue レーン・整理モードの責務** で、ルール上は OK。
- ただし「#153 / #377 の先行解消論」は運用判断（優先順位）であり、ルール逸脱ではない。

### E に対して
- priority 分布の上限（high 件数キャップ）は **本ルールに記載がない**。
- 提案値「high 追加 7 件で総数 48 件」が「機能する分布」かは、owner の判断軸次第。
- **ルール矛盾ではなく、見積もり・優先度システム設計の問題**。

---

## 結論

1. **棚卸し自体は改善 Issue レーン・整理モードの担当** ✓
2. **A/B クローズはメイン側が自律実行可** ✓（ただし後継 Issue 起票確認必須）
3. **#380 への priority 付与は owner 許可範囲** ✓（CP-4 多層防御の確認後）
4. **owner ラベル権限の逸脱なし** ✓
5. **ルール矛盾（コンフリクト）なし** ✓

**留意**: 提案 A の「意図の喪失」は **ルール違反ではなく、実装品質の判断**。
クローズ前に後継 Issue 起票の有無を確認すべし（user-instruction-issue-rules.md の「Why」の永続化）。

### `backlog_strategist` — 主張
<sub>2026-08-22T13:02:36+09:00</sub>

## backlog_strategist: 争点D（Epic 化）検証結果

### 1. 既存 Epic #151 / #152 の消化実績（実測）

`target_issues.json`（open 182 件のスナップショット）に対し、両 Epic の子 Issue 番号を全件突合した。

- **#151**（2026-08-20 作成・8 件束ねる文面だが実子は 9 件記載: #321,#256,#257,#299,#320,#178,#191,#206,#207）
  → **9/9 件が今も open（クローズ 0 件）**
- **#152**（2026-08-20 作成・7 件: #322,#319,#260,#215,#216,#200,#314）
  → **7/7 件が今も open（クローズ 0 件）**

**両 Epic 合計 16/16 件、Epic 作成から 2 日間で消化ゼロ**。さらに 16 件中 **15 件が `type:retro-try` ラベル** を持つ（唯一の例外は #191＝`type:improvement` のみ）。Epic 自身も `type:retro-try` ラベル。

### 2. #377 の主張の検証（自分で行番号を確認した）

`.claude/skills/sprint-cycle-router/SKILL.md`（399 行）を grep した。

- `retro-try-handler` という文字列は **ファイル中 1 箇所のみ** 出現（L151）: 「`type:retro-try` は振り返りレーン（`retro-try-handler`）の担当なので従来どおり除外する」— これは **Step 5（改善Issue消化）の除外規定** であり、決定木 Step 1〜9（§3、L98〜）のどのブランチにも `retro-try-handler` への委譲呼び出しは存在しない。
- `docs/routines/sprint-cycle-routine.md` を確認: 「別 cron・別ルーティンを追加で作らない」と明記、cron は `0 0/2 * * *` の **単一ルーティンのみ**。
- `retro-try-handler/SKILL.md` のトリガーは「日次の消化スロット（プロジェクト定義）」だが、その日次スロットは単一ルーティンへ統合済みで、統合先（sprint-cycle-router の決定木）に retro-try 用ブランチが無い。

→ **#377 の主張は完全に一致・検証済み**。既存 Epic の消化実績ゼロは机上の懸念ではなく、実際に起きている構造的滞留の直接的な証拠。

### 3. 提案 D「新規 Epic 5 本」への賛否: **反対**

新規 5 Epic の子候補 28 件のラベルを同様に突合すると:

| 新規 Epic 案 | type:retro-try 件数 |
|---|---|
| URL 疎通・実測パイプライン | 5/5 |
| deploy_gate 堅牢化 | 2/6 |
| Workers ランタイムテスト | 1/5 |
| 制作系品質検証 | 7/7 |
| tools/ 検査基盤棚卸し | 3/5 |
| **合計** | **18/28（64%）** |

既存 Epic で実証済みの「type:retro-try は束ねても誰も拾わない」という欠陥が、新規 Epic の 64% の子にもそのまま当てはまる。新規 Epic 作成は **滞留の解消ではなく滞留の階層化**（#377 の懸念どおり）であり、Issue 数を 182→187 に増やして体感の「整理感」を出すだけで、実消化には無寄与。**反対**。

### 4. 推奨する打ち手: **Epic 化ではなく #377 の実装を最優先し、それが完了するまで新規 Epic 作成を凍結する**

根拠:
- 182 件を実際に減らせるのは「Issue を束ねる」ことではなく「束ねた Issue を消化する経路を開通させる」ことのみ。今回の実測（16/16 open）はこの命題を直接裏付ける。
- #377 は既に `priority:high` / `sp:3` で起票済み・完了条件も明確（決定木への retro-try ブランチ追加 + 既存 107 件棚卸し + 再発検知の機械検査）。**車輪の再発明は不要で、実装するだけでよい**。
- #377 自身が「経路を開けた瞬間に消化スロットが 107 件で溢れる」と警告しており、**今回の棚卸し（重複統合・不要クローズ・Epic 化検討）は #377 の完了条件の一部として位置づけるべき**。順序は: ① 今回の棚卸しで type:retro-try 107 件を最大限圧縮（重複統合・前提消滅クローズ）→ ② #377 実装で経路開通 → ③ 開通後の残量に対して初めて Epic 化の要否を判断する。

### 代案の評価（他候補との比較）

- ラベルによる束ね（`epic:machine-check` 等、Issue を増やさない）: **#377 解消後なら有効な選択肢**。Epic Issue という「拾われないコンテナ」を増やさずに済む。ただし現時点では経路自体が無いので、ラベルで束ねても同じ結果（滞留）になる点は Epic 化と同じ。
- type:retro-try 109 件の起票基準の見直し: 中長期的には妥当（レトロが Try を出すたびに拾えない Issue が増える逆インセンティブは #377 も指摘）が、今回のスコープ外（争点 D の即応策にはならない）。
- 棚卸し自体を定期ルーティンに載せる: #377 の完了条件 4 番目（再発検知の機械検査）と実質同義。#377 実装に含めるべきで独立の打ち手にしなくてよい。
- 何もしない: 不可（CP-3 違反が継続し、レトロを回すほど悪化する構造をそのまま放置することになる）。

**結論**: 争点 D の答えは「Epic 化は無効、#153（棚卸しレーン担当決定）・#377（経路修復）を先に解消すべき」という提案書内の順序論（D の争点 2 行目）を支持する。新規 Epic 5 本は見送り、今回のセッションでは A/B/C（クローズ・重複統合）による retro-try 母数の圧縮と #377 の優先度確認に絞るべき。

### `dedupe_skeptic` — 主張
<sub>2026-08-22T13:03:51+09:00</sub>

# dedupe_skeptic: 9 クラスタの実本文突合結果

全 9 クラスタとも「本文を実際に読み比べた」上での判定。結論: **9 組中 6 組は統合可能だが、うち 4 組は keep 側本文に dup 側の固有要求を追記しないと要求が消える**。1 組（#159/#258）も同様。2 組（#357/#100・#182/#292）は安全な重複。**#77/#63 は重複判定そのものより深刻な問題（両方陳腐化の疑い）がある**。

## クラスタ判定

### 1. keep=#322 dup=[94, 341] — 重複だが keep 側に注記が必要
3 件とも「並行サブエージェント稼働中に Stop フックがコミット要求/WIP 自動コミットを出す」で同一事象。#322 は #341 の上位互換（mutation_guard.sh 統合可否を PR に明記させる要求を追加）で完了条件は他 2 件を包含している。ただし **#94 はコメント 13 件・priority:high・最古**（08-19）で、3 件の中で唯一実運用の議論履歴を持つ。close 時に #94 のコメント要旨を #322 側へ一言リンクしておかないと、なぜこの Issue が 3 回も立て直されたか（＝ retro が起票前に既存 Issue を検索していない疑い）の経緯が追えなくなる。
**追記すべき固有要求**: なし（#322 が完全上位互換）。ただし #94 への参照リンクは残すこと。
**別懸念**: 同一問題が 08-19 / 08-20 / 08-21 と 3 回別々に起票されている＝retro-issue-creation が起票前に重複検索していない疑い。これは dedupe 対象というより **プロセス欠陥** として別途 Issue 化を検討すべき。

### 2. keep=#321 dup=[104] — **重複ではない可能性あり（スコープ不一致）**
#321 は「ja.json/en.json のキー集合・プレースホルダ一致・空文字/未翻訳検知」＝ **ロケール間の対称性** のみを検査対象にしている。
#104 の対応方針には #321 に無い 2 項目がある: 「**実装が参照するキーがカタログに存在するか**」（コード→カタログの逆引き）と「**カタログに未使用キーがないか**」（未使用キー検出）。これは ja/en 対称性とは別の失敗モードで、#321 の対応方針・完了条件のどこにも現れない。
**判定**: 重複統合可・ただし **#321 の本文に「未使用キー検出」「コード参照存在チェック」を明記追記しないまま #104 を close すると、この 2 要求が消える**。

### 3. keep=#221 dup=[81] — 重複だが keep 側に注記が必要
#221 は #81 の後継版（#219 で対象ファイルが 2→3 に増えたのを反映）で概ね上位互換。ただし **#81 は `lineno_at()` の共通化も対象に含めている** が、#221 の対応方針は `strip_comments` / `EXCLUDE_DIR_PARTS` / `MAX_FILE_BYTES` / ファイル収集ロジックのみで **`lineno_at()` に一切触れていない**。
**追記すべき固有要求**: `lineno_at()` の共通化を #221 の対応方針に明記して statement しないと、実装時にこの関数だけ重複が残る。

### 4. keep=#319 dup=[344] — 重複だが keep 側に注記が必要
両方「ホワイトボード投稿漏れの検知」だが実装方針が異なる: #319 は新規 `verify` サブコマンド実装、#344 は既存 `list --json` の運用強化。#344 固有の完了条件 2 点が #319 に無い: ①「欠落者には **自動で1回だけ** 再指示する」という具体的な回復動作、②「参加者プロンプトの『最終出力は1行サマリーのみ』という指示が **post の代わりに返り値へ書く誤解を生んでいないか** 見直す」という **プロンプト文言側の原因究明**。#319 の完了条件は検知ツール実装に閉じており、この 2 点は範囲外。
**追記すべき固有要求**: 自動 1 回再指示のロジック、参加者プロンプト文言の見直し、を #319 の完了条件に追加。

### 5. keep=#350 dup=[201] — 重複だが **keep 側の情報量が少ない**（要検討）
#350 は「検査ごとの見る/見ない一覧化」という **枠組み** の提案（SP-16 起票、具体例は check_contrast.py の CSS 変数追随漏れ 1 件のみ）。
#201 は SP-10 の **実インシデント**（フォーカスリングのコントラストが 3 層防御を全部すり抜けた）を持ち、加えて #350 に一切登場しない **3 つの死角**（Playwright `setViewportSize` が `<meta name="viewport">` に依存しない／Next.js 16.3.1 が `viewport` export 無しでも meta を自動出力する／クライアント実装が SSR 実装をマスクする hydration 上書き）を記録している。
**判定**: keep=#350 のまま統合すると #201 が持つ 3 つの具体的死角事例が失われる。**#201 の「実例」節をそのまま #350 の本文へ転記するか、逆に #201 を keep にして #350 の広い対象範囲（axe/Lighthouse/vitest/E2E 含む一覧化要求）を追加するかのどちらかが必要**。どちらでも良いが「統合本文を作る」を怠ってはいけない具体例。

### 6. keep=#357 dup=[100] — 安全な重複（統合可）
#357 は #100 の完全な後継・拡張。対象パス・失敗時の挙動・呼び出し元（pr-review-watcher Step6/7-3.5）まで具体化されており、#100 の要求（プレビューURL疎通検証・PR本文に結果記載）を包含する。唯一 #100 にあって #357に明記が無いのは「検証パスを `user-story-map.md` の操作レビュー手順から導出する」という方法論的示唆だが、これは完了条件ではなく実装手段の提案であり、失われても実害は小さい。
**判定**: 統合可・追記不要（軽微な方法論メモのみ、必須ではない）。

### 7. keep=#182 dup=[292] — 安全な重複（統合可）
同一バグ（LoadingIndicator の `text-muted-foreground` + `animate-pulse` によるコントラスト未達・フレーク）。#182 の方が原因分析・完了条件（5 回連続実行・`check_contrast.py` 側の検出漏れ修正）とも #292 を包含している。#292 固有の「これはフレークではなく実際の a11y リスクである」という主張は #182 の記述内容（4.41:1 で既に基準未達＝パルス関係なく壊れている）と論旨として一致しており、実質的に失われていない。
**判定**: 統合可・追記不要。

### 8. keep=#159 dup=[258] — 重複だが keep 側に注記が必要
#159 は publish-sync 記述の問題 **と** run_checks 見出し文字列要件の欠落、の 2 件を扱う広いスコープで #258 を包含する。ただし #258 には #159 に無い 2 点がある: ①参照修正の対象に **`session-concurrency-rules.md`** も含めている（#159 の対応方針は `pr-review-flow-summary.md` / `pr-review-flow.md` の 2 ファイルのみ）、②完了条件が `grep -rn 'check_publish_drift\|publish-sync' docs/ .claude/` という機械検証可能な形で書かれている（#159 は「参照が残っていない」という曖昧な記述）。
**追記すべき固有要求**: 対象ファイルに `session-concurrency-rules.md` を追加、完了条件に grep コマンドを明記。

### 9. keep=#77 dup=[63] — **重複ではあるが、両方とも陳腐化の疑いが強い（最優先で要確認）**
事象自体（GitHub Actions のワークフローが起動しない）は同一で、#77（後・広範囲・A-6 結論）が #63（先・deploy-preview のみ・原因未確定）を包含する重複そのものは妥当。
しかし **本セッションの `CLAUDE.md` / `pr-review-flow-summary.md` に「🔴 GitHub Actions は制限中で使えない（ジョブが数秒・ログ0バイトで失敗）。ワークフロー2本は撤去済み」と明記されている**。つまり #63/#77 が対象にしている `.github/workflows/deploy-preview.yml` 等は **既に撤去され存在しない** 可能性が高く、両 Issue の完了条件（「deploy-preview が PR で成功しプレビューURLがコメントされる」）は **現状のアーキテクチャでは原理的に達成不能**（ワークフロー自体を使わない運用に切り替わったため）。
**判定**: 重複統合の是非を判断する前に、`evidence_auditor` に `.github/workflows/` の実在確認を依頼すべき。撤去済みが確認できたら、この組は「keep を選んで統合」ではなく **両方 close（wontfix/obsolete）** が正しい処理になる。

## keep 選択の一般基準（提案）

「常に新しい方」「常に履歴のある方」のどちらの単純ルールも本レビューで破綻した（#94 は最古・最多コメントだが #322 の方が仕様として優れている／#201 は #350 より古いが実例が濃い）。したがって:

> **keep は「対応方針・完了条件の要求が最も広い（他方を部分集合として包含する）方」を機械的に選び、close 前に必ず dup 側の本文と diff を取って keep 側にしか無い要求がないか確認する。片方にしかない要求（完了条件・対象範囲・具体的死角事例）を見つけたら、close 前に keep 側の本文へ 1 行以上追記してから統合する。コメント履歴が豊富な側を close する場合は、keep 側の本文末尾に `関連: #{dup番号}（コメント{N}件の議論履歴あり）` の 1 行リンクを残す。**

9 組中 4 組（#321/#104・#221/#81・#319/#344・#159/#258）と要検討 1 組（#350/#201）で、この「diff して追記」を怠ると要求が消えることを確認した。単純な「新しい方を残す」運用は危険。

## 全件横断の「重複ゼロ」報告について
本レビュー対象の 9 組はいずれも `[Retro][SP-n]` 形式ではなく `improvement:` / `fix:` / `bug:` 系タイトルで、レキシカル類似度に頼らず実質同一事象と判定できるものだった。むしろ **retro 由来の Issue が同一問題を日を跨いで複数回起票している実例（#94→#322→#341、#201→#350）を複数確認しており**、「retro 起票は重複検索をしていない」傾向がある。この事実は `[Retro][SP-n]` 形式の重複がゼロという報告への直接反証にはならないが、**retro 起票プロセス自体に重複検索の欠落という共通の脆弱性がある** ことを示すため、その報告の前提（機械的なレキシカル類似度だけで重複ゼロと判定したのか、実際に本文を読み比べたのか）を再確認する価値はある。

### `evidence_auditor` — 主張
<sub>2026-08-22T13:04:28+09:00</sub>

# evidence_auditor: A/B クローズ根拠の実測監査（Round 1）

全 14 件をリポジトリ実測で再確認した。**うち 3 件が CLOSE 不可、1 件が要再スコープ**。残り 10 件は根拠を確認できた。

## 提案 A（対応済みクローズ）

### #55 — **CLOSE 不可**（提案書の根拠が誤り。むしろ問題が再発中）
- 確認箇所: `docs/rules/token-optimization-rules.md:75-86`、実測 `wc -c .claude/rules/*.md`
- 提案書は「2026-08-19 エントリ (91,960B) で一本化・2026-08-20 エントリ (98,265B) も整合して継続記録」と書くが、**実際には整合していない**。増減ログの最新行（:84, 2026-08-20, **98,265 B**）の直後にある本文要約（:86）は依然「現行の実測は **~92.0KB**（+12,888B, +16%）」＝ 2026-08-19 の 91,960B を指したまま更新されていない。さらに **今この場で `wc -c .claude/rules/*.md` を実測すると 100,890 B**（14 ファイル）で、ログ最新値 98,265B とも +2,625B 乖離している。
- つまり現在「増減ログ最新（98,265B）」「本文要約（~92.0KB=91,960B 相当）」「実測（100,890B）」の **3つの異なる数値が併存** しており、これは #55 の完了条件②「増減ログ最新 = 本文要約 = 実測」に **正面から違反** する状態。Issue が懸念していた「未記録の増分が溜まる」構造的問題が、まさに一度目の対応後に再発している。
- 判定: CLOSE 不可。再オープンするか、この再発を扱う後継 Issue が要る。

### #85 — **CLOSE 不可**（完了条件 3 項目中 1 項目が未達）
- 確認箇所: `tools/check_cjk_markdown.py:197-213,321-335,369-389`（`--under` 実装・self-test あり、確認）
- しかし完了条件「委譲プロンプトの定型（`agent-team-summary.md` 等）に必要な注意が入っている」は **未達**。`docs/rules/agent-team-summary.md` / `agent-team.md` を grep しても `--under` への言及は一切ない。さらに `CLAUDE.md:246` 自身が模範コマンドとして今も `python3 tools/check_cjk_markdown.py --fix --changed`（`--under` なし）を掲げており、並行委譲時に「担当ファイルだけを整形する」運用は明文化されていない。ツールの機能は追加されたが、再発防止の運用側の手当てが欠落している。
- 判定: CLOSE 不可（または完了条件を「ツール実装のみ」に縮小した上で別 Issue へ運用手当てを切り出すなら再スコープ可）。

### #108 — CLOSE 可
- 確認箇所: `src/ui/site-header.tsx:1-60`、`app/[locale]/page.tsx:290-299`、`app/[locale]/repos/[owner]/[repo]/page.tsx:78,101,111`、`app/[locale]/repos/[owner]/[repo]/not-found.tsx:40,49`
- 一覧・詳細・404 の 3 ページ全てで `SiteHeader` 使用を確認。`h1`（アプリタイトル）は `SiteHeader` にのみ存在し、詳細ページの `h2`（`${owner}/${repo}`）と重複しない見出し階層を確認。`site-header.test.tsx` あり。
- 提案書の「実装形態は layout.tsx 直置きでなく page 単位の共有コンポーネント」は事実だが、完了条件（両ページでタイトル表示・見出し階層に重複なし・テストあり）は実測で全て満たされている。争点として残すのは妥当だが CLOSE 判断自体は支持できる。

### #144 — **CLOSE 不可**（コードが明示的に本 Issue への先送りを宣言している）
- 確認箇所: `app/api/auth/logout/route.ts:12-15`、`git log --oneline -- app/api/auth/logout/route.ts`（コメント追加コミット `ae6c195`, PR #219, 2026-08-20）
- コメント原文: 「CSRF 対策: セッション Cookie は `sameSite: 'lax'`...のため、クロスサイトからの POST 送信では Cookie が付与されず攻撃は成立しない。**専用の CSRF トークン導入は Issue #144 に残る。**」
- 重要なのは、この一文は #144 が起票された **後**（PR #219, 2026-08-20）に追記されたということ。つまり当時のセッションは `sameSite:lax` の防御効果を認識した上で、なお「専用トークン導入」を #144 に意図的に残す判断をしている（同 PR は #145/#213/#82 を close したが #144 は close していない）。完了条件の文言（「POST化 or 同等のCSRF対策」）は字面上満たせるが、コードの一次情報（コメント）は Issue の作者自身が「まだ残作業がある」と明記しており、これを覆す新しい判断根拠が提案書にはない。
- 判定: CLOSE 不可。close するなら、先にこのコメントを更新して #144 への forward reference を外す（さもないと closed issue を指す dangling reference が残る）か、"sameSite:lax で十分と判断し直した" 旨をコメントと Issue 両方に記録すること。

### #182 — CLOSE 可
- 確認箇所: `app/globals.css:87`（`--muted-foreground: oklch(0.5 0 0)`）、`python3 tools/check_contrast.py` 実行 → 全 22 判定 PASS（`--color-fg-muted` ライト 6.00:1 / ダーク 7.63:1）、`src/ui/loading-indicator.tsx:21-25,49` に `animate-pulse` 不使用を確認、`e2e/sp-9-a11y.spec.ts:74-80` 現存。
- 完了条件を実測で満たしている。

### #288 — CLOSE 可（ただし残存する未確認点あり）
- 確認箇所: `docs/02_requirements/open-questions.md:342-343`（D-31/D-32, 2026-08-21）、`docs/03_design/infrastructure/cloudflare-infrastructure.md:600-783`（接続手順・実際に踏んだ罠 4 件・API 実測込みで詳細に記録）、`package.json:23-24`（`deploy` / `deploy:ci` 配線確認）、`tools/workers_build_deploy.sh`（fail-closed 設計を確認）。
- 罠 3・罠 4 の記述（ダッシュボード実機での警告・初回ビルドボタン不在の観測）から、Cloudflare 側の実接続が実際に行われたことは強く裏付けられる。
- ただし、ドキュメント内に「接続後の初回本番デプロイが実際に成功した」という直接的な確認文（ビルド ID・成功ログ等）は見当たらなかった（§8.2.3 は設定・罠・検証すべき 3 点の記述までで終わっている）。「デプロイ経路が完走する状態が整った」は支持できるが、「本当に完走することを確認した」は一次証跡が薄い。CLOSE は支持するが、Issue コメントに「初回実デプロイの成否」を一言残すことを推奨。

### #292 — CLOSE 可（#182 と同一事象・根拠は #182 と同じ）
- 確認箇所は #182 と同一。`animate-pulse` 除去とコントラスト改善で、点滅の谷による瞬間的な閾値割れという#292固有の原因も解消されている。

### #346 — CLOSE 可
- 確認箇所: `src/ui/site-header.tsx:39`（`<header>` が `LocaleSwitcher` を内包）、`app/[locale]/page.tsx:290,299`（`SiteHeader` → `<main>` の順で兄弟配置）、詳細ページ・404 ページも同型を確認（`SiteHeader` → `<main>` の順）。`nav`（`locale-switcher.tsx:38`）は `main` の外にある。

## 提案 B（前提消滅クローズ）

### #63 — CLOSE 可
- 確認箇所: `.github/workflows/` ディレクトリ不在を実機確認（`ls` → No such file or directory）、`docs/03_design/infrastructure/cloudflare-infrastructure.md:472,506`「撤去済み（D-23）」を確認、`open-questions.md:333`（D-23）で GitHub Actions 撤去の飼い主指示を確認。前提が構造的に消滅している。

### #77 — CLOSE 可
- 確認箇所: 同上 + `open-questions.md:342-343`（D-31/D-32）。A-6 として依頼していた課金確認自体、GitHub Actions 経由デプロイをそもそも採らない方針に切り替わったため意味を失っている。

### #282 — CLOSE 可
- 確認箇所: `src/infrastructure/feed/`・`app/api/digest/rss/`・`src/composition/digest-feed.ts` いずれも実機で不在確認。`open-questions.md:345`（D-34, 2026-08-21）が撤去範囲・理由・`DAILY_DIGEST_LIMIT` の移設先（`container.ts:47`）まで明記し、実ファイル状態と完全一致。

### #283 — CLOSE 可（#282 と同根拠）
- 確認箇所は #282 と同一。「トップと RSS で同じ内容」という不変条件の対象（RSS 側）自体が消滅している。

### #295 — CLOSE 可
- 確認箇所: `e2e/sp-16.spec.ts` 不在を実機確認、`open-questions.md:344`（D-33, 2026-08-21）で `sort=gem-index` 撤去を確認。

### #305 — **要再スコープ**（撤去は事実だが、指摘していた設計欠陥は別経路で現存する）
- 確認箇所: `src/usecases/search-repositories.ts` に `GemDigestPort`/`listCandidates`/`gem-index`/`gemIndex` の参照 0 件（grep 確認・提案書の記述どおり）。
- ただし唯一の呼び出し元になった `src/usecases/get-daily-digest.ts:46` は今も `await deps.port.listCandidates()` を **try/catch なし** で呼んでいる。Issue が問題視した「ポートが throw すると呼び出し側に何の防御もない」という設計欠陥そのものは、search-repositories.ts から get-daily-digest.ts へ **場所を変えて現存** している。
- 実際には `app/[locale]/page.tsx:278-284` が `.catch(() => null)` で二重防御しており（コメントで「二重防御」と明記）、500 化という最悪の実害は現状回避されている。しかし完了条件②「その分岐の単体テストがある（例外を投げるフェイクポートで検証）」・③「テストが修正前なら落ちる」は **どこにも実装されていない**（`get-daily-digest.test.ts` に throw/catch のテストケースなし、page.tsx 側にも該当テストなし）。`.catch` は page.tsx 側の作法に依存した防御であり、将来誰かがこの1行を消しても機械的に検知できない。
- 判定: 提案書の争点（「単純クローズか後継 Issue か」）の答えは **後継 Issue が必要**。「sort=gem-index の 500 化」という当初の症状は消えたので not_planned で閉じてよいが、「GemDigestPort の例外安全性が構造的に担保されていない」という本質的懸念は別 Issue として残すべき（closeする場合はその旨をコメントに明記）。

## 提案書の根拠が誤り・不十分だった箇所（名指し）

1. **#55**: 「2026-08-20 エントリも整合して継続記録されている」は誤り。実際は本文要約が最新ログ行に追随しておらず、かつ実測値がログよりさらに +2,625B 進んでいる（3 値不一致が現在進行形）。
2. **#85**: 完了条件 3 項目のうち「委譲プロンプトへの注意追記」を未検証のまま「完了済み」と判定していた（ツール機能の実装だけを見て運用面を見ていない）。
3. **#144**: 争点として認識はしていたが、コメントが「Issue #144 に残る」と明記した **時期**（#144 起票後）まで確認しておらず、「意図が消えるのでは」という疑問止まりだった。実際は当時の担当者が意図的に close しなかった記録であり、疑問ではなく明確な反証。
4. **#305**: 争点で「留保している」とは書いていたが、get-daily-digest.ts 側に実際に try/catch が無いこと・page.tsx 側の `.catch` がテストで担保されていないことまでは踏み込んでいなかった。

### `priority_calibrator` — 主張
<sub>2026-08-22T13:05:27+09:00</sub>

# priority_calibrator: 優先度の情報量設計（争点E）検証結果

実測（`target_issues.json` 182 件 / `merged.json` 7 役統合）:
- `priority:high` 41(22.5%) / `medium` 84(46.2%) / `low` 12(6.6%) / 欠損 43(23.6%) / `P2` 2(310,317)
- `sp:2` 94(51.6%) / `sp:3` 56 / `sp:1` 24 / `sp:5` 5 / `sp:8` 1 / 欠損 2
- 検証7役の独立推奨 priority 合計: high 57(31%) / medium 95(52%) / low 30(16%) — **検証役自体が high 側に張る傾向がある**（個別issueの正当性は見るが、バックログ全体の情報量は見ていないため）。これを鵜呑みにしない。

## 1. high を増やすと優先度は無意味化するか（数値で判定）

**閾値: high は全体の 25〜30% を超えると「順序付けの情報」として機能しなくなる**（一般的なトリアージ運用の経験則。「最優先」バケットが 1/4〜1/3 を超えると、次に何から手を付けるかの判断材料にならず、実質「medium の言い換え」になる）。

現状 41/182=22.5% は **既にこの帯域の上限に近い**。提案書 E 節どおり無条件で +7 すると 48/182=26.4%、さらに欠損43件からの high 推奨10件を無審査で足すと 58/182=31.9% となり **閾値超過（無意味化）**。
→ **昇格は同数以上の降格・クローズとセットでなければ承認しない。**

## 2. high/medium/low の目標件数と判定軸

| 優先度 | 目標件数(全体182比) | 判定軸 |
|---|---|---|
| **high** | **35〜42件（19〜23%）** | 軸①（ユーザーに見える不具合・データ不整合）**または** 軸②（開発ワークフローが現に壊れている・エビデンスに実害の記述がある）に該当し、かつ **具体的な実害・再現手順がある** もの |
| **medium** | **95〜105件（52〜58%）** | 軸③（品質ゲートの穴・予防的検査）④（重複排除・共通化）⑤（ドキュメント整合）。実害の記述がなく「あると安全」レベルのものはデフォルトここ |
| **low** | **40〜50件（22〜27%）** | 影響範囲が局所・頻度が稀・代替手段あり・美観のみ |

**軸③④⑤は「実害の証拠（grep/実測でヒットした具体的事象）」がない限り high に上げない**。verifier の evidence 欄に「〜の可能性がある」「〜すべき」だけで、実際に発生した事象・再現手順が書かれていないものは medium 止まり。

## 3. 欠損43件の埋め方（一律mediumは不採用）

一律 medium 案は medium が 84+43=127件(70%)に膨張し、medium 自体が無意味化するため **不採用**。

実測: 検証7役は欠損43件を high 10 / medium 18 / low 15 と自然に分岐させている（一律mediumではなく実データに基づく判定が既に存在する）。ただし上記②の軸を厳格適用すると high 10 のうち実害エビデンスありは 4 件のみ（下記）。

**機械的に適用できる規則**:
1. `title` が `fix:`/`bug:` で始まり、body に grep/実測による具体的な失敗事象の記述がある → **high**
2. それ以外で `improvement:`/`docs:`/検査追加・重複排除・ドキュメント整合の提案 → **medium**（既定）
3. body に「低頻度」「代替手段あり」「影響範囲は限定的」等の記述がある → **low**

欠損43件のうち high 昇格候補（規則1で確認・4件）: **#365**(レート上限エラー画面が本番に無いログインを案内=ユーザー可視バグ) **#338**(不正な日付文字列で一覧/詳細が500=ユーザー可視バグ) **#341**(並行委譲中にStopフックがコミット要求=ワークフロー実害進行中) **#354**(スキップリンク欠如=WCAG違反で一部ユーザーに実害)。残り39件は medium(約24件)/low(約15件)に規則2/3で機械的に振り分け可能。

## 4. sp:2 偏在（96→実測94件・53%)は見積もり機能不全か

**機能不全ではない。実測で否定する**: 検証7役の独立推奨 sp 分布は `sp:2`=94 / `sp:3`=54 / `sp:1`=26 / `sp:5`=5 / `sp:8`=1 と、**現行ラベルの分布とほぼ完全一致**（sp:2 は 94 対 94）。ラベル値と検証役推奨値の不一致は 182件中 **11件（6%）のみ**。7人の独立検証役が同じ帯域に着地するのは、見積もり基準が曖昧で当てずっぽうになっているサインではなく、**このバックログの実態（単一ファイル・単一スキルの小改善が過半数を占める）を正しく反映している** ことの裏付け。`session-sprint-rules.md` §3 の Dynamic 補正（要リサーチ・仕様未確定 → +1〜2SP）はこの11件の不一致（#377 3→5, #306 2→3, #130 3→5 等の上振れ、#207 #196 #135 2→1 等の下振れ）にのみ適用対象を絞ればよく、**分布全体を作り直す必要はない**。

## 5. 具体的な数と、high 41件のうち降格候補

**high の目標件数: 42件**（181件中23.2%。#288はDONEにつきクローズし母数から除外）。内訳: 現行41件 − 降格/クローズ8件 + 昇格候補9件(下記) = 42件。

### 現行 high 41件のうち降格・クローズ候補（8件・最低要求5件を上回る実数で提示）

| # | 処理 | 理由（実測） |
|---|---|---|
| **#288** | クローズ | verdict=DONE。「Workers Builds経路が実装・接続完了。クローズ推奨」（evidence実測） |
| **#182** | クローズ | verdict=DONE。「4.5:1確保・animate-pulse除去は既に満たされている」（evidence実測） |
| **#205** | high→medium | CHROME_PATH未設定は開発時の不便であり、ユーザー可視でも進行中の実害でもない |
| **#202** | high→medium | E2Eヘルパーの汎用化はテスト基盤の改善で、現に何かが壊れているわけではない |
| **#281** | high→medium | 「Closesを書けないよう機械検査」は運用手順の穴防止（軸③予防的）で実害事例の記述なし |
| **#257** | high→medium | 「D-nがやらないことリストと矛盾しないか機械検査」は予防的（軸③）、実際に矛盾が発生した事例の記述なし |
| **#130** | high→medium | 語彙変換表とコードの命名整合性検証は軸⑤（ドキュメント整合）、ユーザー影響なし |
| **#299** | high→medium | 「正本ドキュメントとコードの許容値ずれ」機械検査も同様に軸③予防的 |

### 欠損43件からの昇格候補（4件・§3参照）
#365 #338 #341 #354

## 6. 提案書E節「medium→high」7件への賛否（1件ずつ）

| # | 判定 | 理由 |
|---|---|---|
| **#227** | **賛成** | run_checks.shがバックグラウンド実行でLint直後に打ち切られる＝CIが偽の green を返しうる。軸②の実害（回避策・再現条件の記録すら無いとevidence実測） |
| **#236** | **賛成** | デプロイゲートが投稿者権限を検証せず、コメント本文だけで判定＝権限昇格に近い実害ポテンシャル。軸②③の複合で実害度が高い |
| **#245** | **賛成** | マージ直前のmain変化を再検証しない＝壊れた合成状態のままデプロイされうる。軸②（デプロイの安全性） |
| **#247** | **反対（medium据え置き）** | D-19のコスト監視未実装は「約束したのに未実装」だが、現時点で予算超過が実際に発生した記録はない。軸③相当で実害エビデンス無し |
| **#259** | **賛成** | MAX_SYNCABLE_SPハードコードによりSP-12〜16が同期対象から実際に漏れている（grep実測で現在進行形のデータ不整合）。軸② |
| **#272** | **賛成** | per_page改ざんで到達不能ページがGitHub APIまで到達＝外部入力によるデータ不整合/コスト実害。軸① |
| **#278** | **反対（medium据え置き）** | バッチスクリプトのテスト可能化はrefactor目的で、現に発生した実害の記述がない。軸③相当 |

→ 7件中 **5件賛成（227,236,245,259,272）・2件反対（247,278）**。提案書は「7件全部+7」という無条件加算だが、それでも同時に §5 の8件降格とセットにしないと 48/182=26.4% で閾値に接近する。**5件賛成+missing43から4件+downgrade8件で合計42件（23%）に収める案を推奨する。**

## ラウンド 2

### `evidence_auditor` — 反論
<sub>2026-08-22T13:07:29+09:00</sub>

# evidence_auditor: Round 2 rebuttal/concession

## #77 / #63（dedupe_skeptic への回答）
`.github/workflows/` は実機確認済み（`ls` → No such file or directory）。dedupe_skeptic が依頼した「両方陳腐化の疑い」の裏付けは取れている。

**結論: duplicate ではなく、両方 not_planned で個別に閉じるべき。**
`duplicate` という state_reason は「片方が生きていて、もう片方の要求をそちらが引き受ける」関係を表す。しかし #63/#77 はどちらも生きていない——.github/workflows/ 自体が撤去され、GitHub Actions 経由のデプロイという **両者共通の前提そのものが構造的に消滅した**（D-23/D-31/D-32）。#77 を keep にして #63 を duplicate で閉じると、「#77 は今も有効で追跡すべき Issue」という誤った含意が残る。実際には #77 が要求する「Actions の課金確認」自体が無意味（Actions 経路をそもそも使わない設計に切り替わったため）。**両方 not_planned・relates-to のコメントリンクのみで十分**（dedupe_skeptic の keep/dup 判定枠組みはこの組には適用しない）。

## #182 / #292（dedupe_skeptic への回答・一部譲歩）
Round 1 では両方を独立に CLOSE 可（completed）とだけ判定したが、dedupe_skeptic の「#182 が #292 を包含する安全な重複」という分析には **譲歩する**。

**結論: #182 を completed、#292 は duplicate（#182 参照）で閉じるのが正しい。** 両方を別々に completed で閉じると、同一コミット（`app/globals.css` `f53bbbf` 相当・`loading-indicator.tsx` の animate-pulse 除去）を指す「完了記録」が 2 つ並立し、後から見た人が「どちらが正の記録か」を混乱する。#182 の方が原因分析が深い（不透明時点で既に 4.41:1 未達という根本原因まで特定）ため keep として妥当。

## #55 / #85 / #144 / #305（docs_trace への回答・1 件ずつ結論）

docs_trace の原則（「後継 Issue 起票後に close」）に一律賛成はしない。**このパターンが適切なのは #305 のみ** で、#55・#85・#144 は性質が異なる。

### #55 — **close しない（open のまま）。後継 Issue も不要**
Round 1 で確認した通り、この Issue の完了条件そのものが **今この瞬間も未達**（増減ログ最新 98,265B / 本文要約 ~92.0KB(91,960B相当) / 実測 100,890B の 3 値不一致が現在進行形）。これは「別の新しい問題」ではなく「この Issue が元々測っていた問題」そのものが継続している。後継 Issue を立てると同じ完了条件を持つ Issue が 2 つ並ぶだけで、docs_trace が懸念する「なぜ Issue が立て直されたか経緯が追えなくなる」問題を自ら作ることになる。**この Issue を open のまま残し、今回の実測差分（98,265→100,890）をコメントで追記するのが正しい。**

### #85 — **close しない（open のまま）。後継 Issue も不要**
残作業（`agent-team-summary.md` / `CLAUDE.md` への `--under` 使用の明記）は完了条件の 3 項目中 1 項目であり、かつ対象ファイルへの 1〜数行の追記で完結する軽微な残作業。CP-1 の「スコープ外の改善は別 Issue を立ててから着手する」はここでは逆方向に働く——この残作業は #85 の完了条件そのものであり、スコープ外の改善ではない。**新規 Issue に切り出すのは Issue 管理コストが残作業量に見合わない。#85 を open のまま、未達項目だけをチェックリストに残すべき。**

### #144 — **close しない（open のまま）。後継 Issue も不可（同じ問題を再生産するだけ）**
docs_trace は「#144 は先送りコメントが明記されているなら後継 Issue を起票してから close すべき」と述べたが、この組み立ては #144 には当てはまらない。**コード側のコメントが名指ししている先送り先は、まさに #144 自身**（`app/api/auth/logout/route.ts:14-15` 「専用の CSRF トークン導入は Issue #144 に残る」）。ここで #144 を close して新しい Issue #N を起票しても、コード内のコメント文字列を `#144` → `#N` に書き換えない限り同じ「closed issue への dangling reference」問題が形を変えて再発するだけで、何も解決しない。**正しい処理は #144 を open のまま残すか、close するなら「先にコード側のコメントを編集して forward reference を外し、sameSite:lax を正式に十分な対策と再判定した」という決定を #144 のコメントと両方に記録してから close する**、のどちらか。後継 Issue の起票は不要（起票してもコード側の参照を書き換える手間は同じで、番号が変わるだけ）。

### #305 — **close（not_planned）+ 後継 Issue 起票、が正しい（docs_trace のパターンが唯一当てはまる）**
#144 と違い、#305 は当初の症状（`search-repositories.ts` の `sort=gem-index` 経路が 500 化する）が **コードごと物理的に消滅** しており、そのファイルには関連する参照が 0 件（grep 確認済み）。一方で発見した設計欠陥（`GemDigestPort` の例外安全性）は **別ファイル**（`get-daily-digest.ts`）の **別の呼び出し経路** に移動している。#144 と違い、コード側に「Issue #305 へ先送り」という明示的な forward reference は無い（`get-daily-digest.ts` にはそのような一文がない）ため、#305 を close しても dangling reference にはならない。**#305 は not_planned で close し、「`GemDigestPort` 例外安全性のテスト整備（`get-daily-digest.ts` 側、`page.tsx` の `.catch` に対する回帰テスト欠如）」を新規 Issue として起票するのが正しい。**

## priority_calibrator への確認
#288・#182 を「クローズ前提で high から除外」とする判定は、evidence_auditor の実測結果（両方とも完了条件を満たしファイル・テストで確認済み）と **整合する**。異論なし。ただし #182 を close する際は上記の通り #292 を duplicate として道連れにすること（priority 側の処理対象からも #292 を outcome=closed(duplicate) として除外するのを忘れずに）。

### `backlog_strategist` — 反論
<sub>2026-08-22T13:07:48+09:00</sub>

## backlog_strategist R2: rebuttal + concession

### 1. 「#377 実装はスコープ外」への応答 — 今回できることを Epic 抜きで列挙する

指摘は妥当。R1 の「唯一有効な打ち手は #377 実装」は言い過ぎで、**このセッションの手札（ラベル追加・本文追記・優先度・クローズ）だけで今すぐ retro-try 母数を減らせる分** を過小評価していた。実測（`target_issues.json` に対する再突合）:

| 提案書の操作 | retro-try ラベル付き件数 |
|---|---|
| 提案 A（completed クローズ・8 件） | **1 件**（#85） |
| 提案 B（not_planned クローズ・6 件） | **2 件**（#282, #283） |
| 提案 C（duplicate クローズ・dup 側 10 件） | **7 件**（#94, #341, #104, #344, #201, #100, #258） |
| **合計** | **10 件を今日クローズできる** |

→ **Epic を作らずとも、A/B/C をそのまま実行するだけで retro-try 109 件 → 99 件（-9%）に減らせる**。これは #377 の解消を待たない、今回のセッションで完結する具体的な打ち手。ここは自分の R1 の主張を修正する（`concession`）: 「#377 実装が唯一有効」ではなく「A/B/C 実行が即効・#377 実装が根治」の 2 段構え。

### 2. dedupe_skeptic「4 組で固有要求が失われる」への応答 — 保全先は Epic ではなく keep 側の本文追記

同意する（`concession`）。Epic は保全先として不適切（#151/#152 の運用ルール自体が「子はクローズしない＝Epic 本文を編集する運用ではない」）。コーディネータが列挙した 4 手札のうち **「本文追記」がそのまま答え**:

- #321 keep 側に「未使用キー検出」「コード参照存在チェック」を追記 → #104 close
- #221 keep 側に `lineno_at()` 共通化を追記 → #81 close
- #319 keep 側に「自動1回再指示」「プロンプト文言見直し」を追記 → #344 close
- #159 keep 側に `session-concurrency-rules.md` と grep 完了条件を追記 → #258 close

**Epic 抜きでも固有要求は消えない**。むしろ Epic に逃がすと「Epic 本文を編集 → 子は close しない」運用のせいで要求が Epic 側に散り、keep 側 Issue 単体を見た人が要求全体を把握できなくなる（今回実測した #151/#152 の消化ゼロと同じ「見えるが拾われない」構造を dedupe 要求にまで広げるだけ）。本文追記のほうが optionally 安全。

### 3. priority_calibrator「high 42 件（23%）」への応答 — 比率だけでは機能しない。到達可能性という直交軸が要る

反論（`rebuttal`）。42/182 という比率は「high の中でどれから着手するか」の情報量としては機能するが、**182 件のうち 109 件（60%）が type:retro-try＝現状どの priority を付けても構造的に到達不能** という事実の前では、比率設計だけでは着手順を決められない。

具体例: priority_calibrator の昇格候補 4 件（#365 #338 #341 #354）のうち **#341 は type:retro-try**（R1 で確認済み・#94/#322 と三重統合対象でもある）。high に昇格しても sprint-cycle-router の決定木には乗らない（#377 実証済み）。一方 #227（同じく賛成の昇格候補）は type:retro-try ではなく `type:bug` のみなので、priority:high 昇格が実際に着手順へ反映される。

→ **priority 軸だけでは「high の中に到達可能なものと不可能なものが混在する」状態を解消できない**。到達可能性（reachable via 現行ルーティン / #377 待ち）を priority とは別の判定軸として持たないと、high 42 件の中身が「上げても動かない」ものだらけになりかねない。今回の手札の範囲で言えば、**retro-try ラベルが付いている Issue には priority を上げる前に「#377 解消待ち」である旨を本文へ 1 行追記する** 運用が現実的な代替（新規ラベル追加は今回のスコープ判断に委ねる）。

### 4. docs_trace への確認 + 恒久策の提案

docs_trace の「棚卸しは改善 Issue レーン・整理モードの担当」の確認に同意する（`concession`）。その上で、**次回以降この棚卸しセッション自体を繰り返さないための恒久策を 1 つに絞る**:

> **#377 の完了条件 4 番目（「同種の経路断絶を再発検知する機械検査」）を、`workflow-health-check`（監査・衛生レーン）の定期監査項目として実装する。** 具体的には `improvement-lane-map.md` の各レーンの「主な起動」欄に書かれたスキル名が、実際に `sprint-cycle-router` の決定木 Step 1〜9 のいずれかから呼ばれているかを機械照合するスクリプト（`tools/check_lane_reachability.py` 相当）。

理由: 今回発見できた「Epic #151/#152 が 2 日間 16/16 件消化ゼロ」は、**人間（セッション）が手動で番号突合しないと見つからない** 状態だった。この検知を自動化すれば、次に同種の「ドキュメント上はレーンがあるが実装が呼ばない」欠陥が発生しても、次回の棚卸しセッションを待たずに検出できる。これは retro-try に限定されない汎用チェックなので、workflow-health-check の週次ゲートに載せるのが lane-map 上も筋が通る（監査・衛生レーンの担当範囲＝「Issue/PR の状態」の一種として「到達可能性」も含められる）。

### `dedupe_skeptic` — 譲歩
<sub>2026-08-22T13:07:50+09:00</sub>

# dedupe_skeptic: Round 2 rebuttal/concession

## 論点1: #182/#292 は「片方を duplicate」か「両方 completed」か → **evidence_auditor に譲歩。両方 completed が正しい**

R1 では「keep=#182・dup=#292（重複統合）」としたが、これは **まだ両方が未完了の作業項目である前提** での判断だった。evidence_auditor が実測で示したのは「両方ともコード上すでに直っている（`animate-pulse` 除去済み・`check_contrast.py` 全 PASS）」という事実で、これは私の R1 の前提を覆す。

未完了同士なら「重複だからどちらかに寄せて作業を1本化する」が正しいが、**両方すでに完了している場合に片方を『duplicate』で閉じるのは記録として不正確** になる。理由:
- GitHub の `duplicate` は「まだ残っている作業が別 Issue に引き継がれる」ことを意味する。今回は引き継ぐべき残作業が無い。
- #182（不透明時 4.41、静的トークン起因の恒常的未達）と #292（`animate-pulse` 中の谷 4.23、動的フレーク）は **測定条件が異なる 2 つの独立した検証** であり、これは重複ではなく「同じ修正が 2 つの異なる負荷条件で有効だったことの二重確認」という価値を持つ。duplicate 扱いにすると、#292 固有の 4.23 という実測値・フレーク再現条件が「重複」というラベルの下に埋もれ、将来同種のリグレッション（アニメーション途中の実効コントラスト低下）が起きたときに検索で拾われにくくなる。
- **結論**: 両方 `state_reason=completed` で個別クローズが正しい。R1 の統合判定を撤回する。

## 論点2: #77/#63 の not_planned 双方クローズに同意するか → **同意する（自分の R1 予測どおり）**

R1 で「重複判定以前に両方陳腐化している可能性が高く、evidence_auditor に `.github/workflows/` の実在確認を依頼すべき」と書いた。evidence_auditor が実際に `ls .github/workflows/` 不在・`cloudflare-infrastructure.md`（D-23）・`open-questions.md`（D-23/D-31/D-32）で前提消滅を裏付けた。**これは重複統合ではなく、独立した2件の not_planned クローズが正しい**（#77 は #63 の上位互換だから keep にする、という R1 の重複整理の枠組み自体が今回は不要）。「A-6 として課金確認を依頼していた点も、GitHub Actions 経由デプロイを採らない方針に切り替わったため意味を失った」という evidence_auditor の追加指摘（#77 の A-6 待ちも解消）にも異議なし。

## 論点3: Epic を使わず固有要求を保全する具体策 → **keep 側 Issue へのクローズ前コメント追記で確定**

backlog_strategist の「Epic は拾われない箱を増やすだけ」（既存 Epic #151/#152 が 16/16 open で消化ゼロ）という実測に同意する。**私が指摘した 4 組の「失われる固有要求」は、Epic ではなく keep 側 Issue 本文/コメントへの 1〜2 行追記で保全できる。以下を dup クローズ時にそのまま使う文言として確定する。**

1. **keep #321 ← dup #104**: #321 の対応方針に項目 4 を追加「実装コードが参照するメッセージキーがカタログ（ja/en）に存在すること、およびカタログ内の未使用キーを検出すること（#104 統合）」。完了条件に `- [ ] 未使用キー検出と実装参照キーの存在検査が check_i18n_parity.py に含まれている（#104 統合）` を追加。
2. **keep #221 ← dup #81**: #221 の対応方針に「`lineno_at()` の共通化も対象に含める（#81 統合）」を追加。完了条件に `- [ ] lineno_at() が共通モジュールへ1本化されている` を追加。
3. **keep #319 ← dup #344**: #319 の完了条件に `- [ ] 欠落検知時に自動で1回だけ再指示するロジックがある（#344 統合）` と `- [ ] 参加者プロンプトの「最終出力は1行サマリー」という表記が post ではなく返り値へ書く誤解を生んでいないか見直し済み（#344 統合）` を追加。
4. **keep #159 ← dup #258**: #159 の対応方針の対象ファイルに `docs/rules/session-concurrency-rules.md` を追加。完了条件を `grep -rn 'check_publish_drift\|publish-sync' docs/ .claude/` の結果が実在ファイルのみを指すこと（#258 統合・機械検証可能な形に精密化）に置き換える。

これは Epic 化でも新規 Issue 起票でもなく、**close 前に keep 側 Issue の本文を 1 回編集するだけ** で完結する。backlog_strategist の「新規コンテナを増やさない」方針と整合する。

## 論点4: #350/#201 の最終 keep 決定 → **keep を #201 に変更する（R1 から反転）**

R1 では判断保留（要検討）としたが、priority_calibrator が示した「軸③④⑤（予防的検査・重複排除・ドキュメント整合）は **実害の証拠（grep/実測でヒットした具体的事象）がない限り high に上げない**」という基準を援用すると、この 2 件の実質的な価値の差も同じ軸で測れる。

- #201: SP-10 の実インシデント（フォーカスリングが3層防御を全部すり抜けた）+ 3つの具体的死角（`setViewportSize` が meta viewport に非依存／Next.js 16.3.1 の meta 自動出力／hydration によるSSR上書き）— **すべて実際に踏んだ事象**。
- #350: 「同じ穴が他の領域にもあるかもしれない」という推測ベースの一般化提案 + #348 由来の1事例（check_contrast.py の CSS 変数追随漏れ）。

**証拠密度で #201 が優る** ため、keep=#201・dup=#350 に確定する（R1 の keep=#350 を撤回）。close 前に #201 の対応方針へ「axe / Lighthouse / vitest / E2E を含む全検査層の一覧化」（#350 の対象範囲）と「check_contrast.py が `:root`/`.dark` の固定パースのみで新規トークン・派生変数（`--tw-prose-*` 等）を追随できていない」（#350/#348 由来の事例）を追記し、完了条件に「洗い出した穴を Issue として起票する（ゼロなら『無かった』ことを記録する）」を追加する。

### `priority_calibrator` — 反論
<sub>2026-08-22T13:08:01+09:00</sub>

# priority_calibrator: Round 2 rebuttal（R1 の自分の投稿とホワイトボードのみで応答）

## 1. evidence_auditor: クローズ14→10 で high 42件の目標は変わるか → **変わらない（軽微な上振れのみ）**

私が high 42件の計算で「クローズ」扱いにしたのは **#288・#182 の2件のみ**（#55/#85/#144/#305 はそもそも私の現行 high 41件リストに含まれていない）。evidence_auditor の判定では #288・#182 はどちらも **CLOSE可**（実測確認済み）なので、この2件に関する私の計算は無傷。

影響があるのは分母の方: 提案書のクローズ 14 件のうち実行できるのは 10 件（#55・#85・#144 は CLOSE 不可、#305 は要再スコープで open のまま）。182 − 10 = **172** が正しい分母。
→ high 目標を 42/172 で再計算すると **24.4%**（前回は 42/182=23.1% としていた）。25〜30% 閾値の範囲内であることに変わりはなく、**high の目標件数（42件）はそのまま維持する**。ただし #55・#85・#144 は open のまま残るため、この3件の priority 再判定（現状ラベル値の確認）を次ラウンドで誰かが拾う必要がある — 私の側では現行 high 41件リストに含まれていないことのみ確認済み。

## 2. backlog_strategist: Epic #151/#152 の子16件が0件消化 → 「priority再配分は無意味」への反論

**無意味ではない。ただし単独では不十分、という指摘は受け入れる（concession）。**

backlog_strategist が実測した「16/16 open・消化ゼロ」は **#377 が主張する症状そのもの**（type:retro-try が自走ルーティンのどのブランチからも拾われない構造的欠陥）。これは「priorityラベルの値が悪い」ではなく「priorityラベルを読みに行く経路が存在しない」という **別レイヤーの故障** であり、私の R1 で #377 を high 昇格対象（軸②・実害進行中）として既に採用済み。

論理的に整理すると:
- priority 再配分 = **正しい入力データを用意する作業**
- #377 実装 = **その入力データを実際に読んで着手順を決める経路を作る作業**

経路（#377）が壊れている今、priority を直しても着手順には反映されない — この点は backlog_strategist の指摘どおり。**しかし** だからといって priority 再配分を後回しにすると、#377 が実装され経路が開通した瞬間に「high 41件中8件は本来降格対象」「欠損43件は未分類」という **汚れたデータのまま自動着手が始まる**。経路修復と同時にデータが正しくなければ、直った経路が誤ったものを最初に拾う。

→ **順序ではなく並行** を提案する: 今回のセッションで priority 再配分（本ラウンドの結論）を確定 → #377 は backlog_strategist の主張どおり最優先 high として別途実装 → #377 完了後に「経路が正しい優先度を読んでいるか」を smoke test で確認する完了条件を #377 に追加する（未追加なら追記を推奨）。**「意味があるか」ではなく「経路修復と対にしないと効果が出ない」が正確な結論** であり、この点は backlog_strategist に譲歩する。

## 3. 降格候補6件（#205 #202 #281 #257 #130 #299）の判定軸を1文で明示

> **evidence 欄に「実際に発生した failure・データ不整合・事故」の実測記述（grep/実行結果に基づく具体的事象）が無く、「〜すべき」「整合していない可能性」「検知できるようにする」という予防的・提案的表現に留まるものは、high から medium へ降格する。**

（#288・#182 は上記軸ではなく「verdict=DONE→クローズ」という別軸のため、この6件とは切り分けて扱う。evidence_auditor の実測でも両者とも CLOSE可が支持されており、この軸自体への異論はまだ出ていない。）

## 4. 欠損43件（昇格4件を除く39件）の埋め方 — if-then で確定

優先度の高い順に評価し、最初に一致した規則を適用する（1つの issue には1規則のみ適用）。

```
1. IF title が "fix:" または "bug:" で始まる
   AND body/evidence に grep・実行結果・再現手順など「実際に確認された失敗事象」の記述がある
   THEN high

2. ELSE IF body に "低頻度" "稀" "代替手段あり" "影響範囲は限定的" のいずれかの記述がある
   THEN low

3. ELSE IF category が CHECK（予防的検査の追加提案）
        OR RULE（ドキュメント整合）
        OR BACKLOG（将来計画・設計検討）
   AND 上記1の「実測された失敗事象」の記述が無い
   THEN medium

4. ELSE（ルール1〜3のどれにも一致しない・判定材料不足）
   THEN medium（既定値。次ラウンドで個別確認が必要な旨をコメントに1行残す）
```

このルールを欠損39件に適用すると、R1 の検証7役推奨（missing43で high10/medium18/low15、うち4件を昇格確定済み）とほぼ整合する分布になる想定。厳密な件数は次ラウンドで該当39件へ機械的に適用してから確定する。

## ラウンド 3

### `lead` — 判定
<sub>2026-08-22T13:16:04+09:00</sub>

# lead: 棚卸し判断の確定（verdict）

5 レンズの claim / rebuttal を突き合わせ、争点 A〜E の判断を確定し **実際に適用した**。

## 争点 A: 対応済みクローズ — 8 件提案 → **5 件に縮小して適用**

`evidence_auditor` が実測で 3 件の完了条件未達を示したため、提案から外した。

| 判断 | Issue |
|---|---|
| `completed` でクローズ | #108 #182 #288 #292 #346 |
| **クローズせず open 継続**（残作業をコメント記録） | #55（増減ログ・本文・実測の 3 値が現在も不一致）/ #85（`--under` の委譲プロンプトへの周知が未了）/ #144（コード側コメントが本 Issue 自身を名指しする forward reference） |

**#182 / #292 は両方 `completed` で個別クローズ**（lead 裁定）。`evidence_auditor` は R2 で「#292 を duplicate に」と譲歩し、`dedupe_skeptic` は R2 で「両方 completed に」と譲歩して主張が入れ替わったため lead が決めた。採用理由は `dedupe_skeptic` R2 の論拠 — `duplicate` は「残作業が別 Issue へ引き継がれる」ことを含意するが今回は引き継ぐ作業が無く、#292 固有の実測値（animate-pulse 中の谷 4.23）と再現条件が `duplicate` の下に埋もれると将来の同種リグレッションで検索に拾われない。

**却下**: `docs_trace` の「クローズ前に一律で後継 Issue を起票する」原則。`evidence_auditor` R2 の反論を採る — #144 は先送り先がまさに本 Issue 自身であり、新 Issue を立てるとコード内参照が dangling になるだけで何も解決しない。この原則が当てはまるのは #305 のみ。

## 争点 B: 前提消滅クローズ — **6 件すべて適用**（うち 1 件は後継 Issue 起票つき）

`not_planned` でクローズ: #63 #77 #282 #283 #295 #305

- **#63 / #77 は `duplicate` ではなく両方 `not_planned`**（`evidence_auditor` R2 を採用）。`duplicate` は「片方が生きていて要求を引き受ける」関係を表すが、両者共通の前提（GitHub Actions 経由デプロイ）が D-23 / D-31 / D-32 で構造的に消滅しており、どちらも生きていない。#77 を keep にすると「#77 は今も追跡すべき」という誤った含意が残る。
- **#305 のみ後継 Issue を起票**（#392）。当初症状はコードごと消滅したが、設計欠陥（`GemDigestPort` の例外安全性）が `get-daily-digest.ts` の別経路に残っており、コード側に本 Issue への forward reference が無いため dangling にならない。

## 争点 C: 重複統合 — **8 件を `duplicate` でクローズ**（提案 10 件から 2 件を除外）

| keep | dup | 統合時の本文追記 |
|---|---|---|
| #322 | #94, #341 | 追記なし（完全上位互換）。#94 のコメント 13 件への参照リンクを残した |
| #321 | #104 | 未使用キー検出・実装参照キーの存在検査を追記 |
| #221 | #81 | `lineno_at()` の共通化を追記 |
| #319 | #344 | 自動 1 回再指示ロジック・参加者プロンプト文言の見直しを追記 |
| **#201** | **#350** | **keep を R1 提案から反転**。`dedupe_skeptic` R2 の証拠密度基準（#201 は実インシデント由来の死角 3 件、#350 は推測ベースの一般化）を採用。全検査層への拡大と `check_contrast.py` の追随漏れを #201 へ追記 |
| #357 | #100 | 追記なし（包含関係） |
| #159 | #258 | `session-concurrency-rules.md` と grep ベースの完了条件を追記 |

除外: #292（→ 争点 A で `completed`）/ #63（→ 争点 B で `not_planned`）。

**keep 選択の一般基準を確定**（`dedupe_skeptic` R1 提案を採用）: 「対応方針・完了条件の要求が最も広い（他方を部分集合として包含する）方」を選び、close 前に必ず dup 側と diff を取り、**片方にしかない要求は keep 側の本文へ追記してから統合する**。コメント履歴が豊富な側を close する場合は keep 側末尾に参照リンクを残す。「常に新しい方」「常に履歴のある方」の単純ルールは本レビューで 2 度破綻した（#94 / #322、#201 / #350）。

## 争点 D: Epic 化 — **新規 Epic 5 本は全て見送り。既存 Epic へのメンバー追加も行わない**

`backlog_strategist` の実測が決定打:

- **既存 Epic #151 / #152 の子 16 件が、Epic 作成から 2 日間で 1 件も消化されていない**（16/16 open）。16 件中 15 件が `type:retro-try`
- `.claude/skills/sprint-cycle-router/SKILL.md` の決定木 Step 1〜9 のどのブランチにも `retro-try-handler` への委譲が無い（`retro-try-handler` の出現は Step 5 の **除外規定** 1 箇所のみ・L151）
- 新規 Epic 5 本の子候補 28 件のうち 18 件（64%）が `type:retro-try` で、同じ穴に落ちる

→ Epic 増設は滞留の解消ではなく **滞留の階層化**。Issue 数を増やして体感の整理感を出すだけで実消化に無寄与。

**代わりに採ったこと**（`backlog_strategist` R2 の concession「A/B/C 実行が即効・#377 実装が根治」の 2 段構え）:

1. A/B/C の実行だけで `type:retro-try` を 109 → 100 件に圧縮（クローズ 19 件のうち 10 件が retro-try）
2. 恒久策として **#394 を起票** — `improvement-lane-map.md` のレーン定義が実際に `sprint-cycle-router` から呼ばれているかを機械照合する（`tools/check_lane_reachability.py`）。今回の「16/16 消化ゼロ」は人が手で番号突合しないと見つからなかった
3. **#393 を起票** — 同一問題の複数回起票（#94→#322→#341、#201→#350、#104→#321、#81→#221、#319→#344、#100→#357）の原因は retro が起票前に既存 Issue を検索していないこと。`retrospective` スキルへ重複検索を必須ステップとして入れる

**却下**: 「ラベルによる束ね（`epic:*`）」— 経路（#377）が無い現状ではラベルで束ねても Epic と同じ結果（滞留）になる。#377 解消後に再検討する。

## 争点 E: 優先度 — **`priority_calibrator` の設計を採用し、昇格と降格をセットで適用**

`priority_calibrator` の閾値設計（high が全体の 25〜30% を超えると順序付けの情報として機能しなくなる。目標 high 42 件前後）を採用。提案書の「7 件を無条件で +7」は却下した。

| 操作 | Issue |
|---|---|
| high → medium（降格 6 件） | #205 #202 #281 #257 #130 #299 |
| medium → high（昇格 5 件） | #227 #236 #245 #259 #272 |
| 昇格却下（medium 据え置き） | **#247**（コスト監視未実装だが予算超過の実績記録なし）/ **#278**（refactor 目的で実害記述なし） |
| 欠損 36 件を if-then 規則で補完 | high 5（#365 #338 #354 #352 #27）/ medium 31 |
| `priority:P2` の表記ゆれを解消 | #310 #317 → medium |
| `status:` 欠損 30 件 | `status:waiting-claude` を付与 |

**降格の判定軸を確定**（`priority_calibrator` R2）: 「evidence 欄に **実際に発生した failure・データ不整合・事故** の実測記述が無く、『〜すべき』『〜の可能性がある』『検知できるようにする』という予防的・提案的表現に留まるものは high → medium」。

**欠損補完の if-then 規則を確定**: ① `fix:` / `bug:` 始まり **かつ** 実測された失敗事象の記述あり → high ② 「低頻度 / 稀 / 代替手段あり / 影響範囲は限定的」の記述あり → low ③ category が CHECK / RULE / BACKLOG で実測失敗事象なし → medium ④ それ以外 → medium（既定）。

**`sp:2` 偏在（94 件・53%）は見積もりの機能不全ではない** と確定。検証 7 役の独立推奨 sp 分布が現行ラベルとほぼ完全一致（`sp:2` は 94 対 94、不一致は 182 件中 11 件のみ）で、バックログの実態（単一ファイル・単一スキルの小改善が過半）を正しく反映している。Dynamic 補正はこの 11 件にのみ適用対象を絞る。

**採用した concession**（`priority_calibrator` R2 → `backlog_strategist`）: priority 再配分は「正しい入力データを用意する作業」、#377 実装は「そのデータを読んで着手順を決める経路を作る作業」。**順序ではなく並行**。経路修復と同時にデータが正しくなければ、直った経路が誤ったものを最初に拾う。

## 適用後の実測（GitHub から再取得して確認）

- open Issue: 182 → **168 件**（`type:feature` を除く。クローズ 19・新規起票 4）
- `type:retro-try`: 109 → **100 件**
- `priority` 欠損: 43 → **1 件**（#391 のみ。他セッションが `status:in-progress` で着手中のため CP-4 により触れていない）
- `status` 欠損: 34 → **0 件**
- `priority:high`: 44 件（26.2%）

## critical（議論を経ても残った真の問題）

1. **`type:retro-try` 100 件は今も自走ルーティンから到達不能**。A/B/C は母数を 9% 減らしただけで、経路そのものは #377 の実装まで開通しない。この棚卸しは対症療法であり、#377 を実装しない限り次のレトロで再び増える
2. **`priority:low` が 12 件（7%）しか使われていない**。`priority_calibrator` の目標分布は low 22〜27% だが、if-then 規則の②（「低頻度 / 稀 / 代替手段あり」の記述）に該当する Issue が実データに 1 件も存在しなかった。**Issue 本文が「これは後回しでよい」と書く文化が無い** ため、機械規則では low を埋められない。medium が 111 件（66%）に膨らんだままで、medium 内の順序付けは依然として情報を持たない
3. **既存 Epic #151 / #152 自体の処遇が未決**。子を追加しない方針は決めたが、消化ゼロの Epic 2 本を open のまま残すか閉じるかは #377 の実装結果を見てから判断する

## open_questions

なし（A-1〜A-6 該当なし。仕様解釈の分岐も生じなかった）。
