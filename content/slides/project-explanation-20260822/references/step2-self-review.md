# Step 2: スライド構成セルフレビュー記録（2026-08-22）

参照ワークフロー（kai-kou/qiita-bash-lt-2026 の `slides` スキル）Step 2 の実施記録。
必達原則 1（セルフレビューをスキップしない・結果を必ず残す）・2（MECE 観点必須）・
4（ファクトチェック）・5（最新情報リサーチ）に対応する。

## 実施方法

`doc-review` スキル（参照リポジトリ・本ディレクトリの `doc-review.SKILL.md`）の **7 軸並列レビュー** を
Batch 1（軸 1〜4）→ Batch 2（軸 5〜7）の順にサブエージェントで実行した。
軸 2 が MECE、軸 7 がファクトチェックを担う。

構成そのものは、これに先立って **議論型レビュー**（`content/discussions/project-slides-20260822/`・
5 レンズ × 2 ラウンド + 合意形成）で決めている。本記録はその成果物に対する 7 軸レビューである。

## 軸ごとの主な指摘と対応

| 軸 | 主な指摘 | 対応 |
|---|---|---|
| 1 戦略・目的 | ① Gem Index のスライドがどのアウトライン項目にも属さず宙に浮き、しかも 4 枚あとで「撤去した」と否定される ② (a)「なぜ作ったのか」が観測事実だけで、初期コンセプトの **作り手の動機** が出てこない ③ まとめが汎用的な設計論だけで主題を回収していない | ① Gem Index を **13 の直前（新 12）へ移動** し、定義 → 撤去を地続きにした ② スライド 4 の 1 点目に「自分が使うライブラリを選ぶとき star 以外に判断材料がない」という動機を追加 ③ まとめの 1 項目目を主題（実利用起点）の回収に差し替え、4 項目構成にした |
| 2 論理・MECE | ① 9（定義）と 13（撤去）の間に無関係な 4 枚が挟まる ② `CachePort` の説明が 6 と 15 でほぼ同一文言で重複 ③ Gem の定義が 4 と 9 で二度出る ④ 「ADR 15 本」が 5 と 17 で重複 ⑤ スライド 7 だけ体言止めの列挙で粒度が違う | ① 並び替えで解消（軸 1 と同じ対応） ② 6 は「唯一の例外がある」に留め、詳細は 15 に一元化 ③ 4 は「指標が要る」という仮説提示に留め、定義は 12 に一元化 ④ 5 から本数を落とし 17 に残した ⑤ 7 の各点に選定理由を書き、他スライドと同じ文体に揃えた |
| 3 実行設計 | **テキスト量が新基準（本文 250〜300 字）の 35% しかなく、全 17 枚が薄い**（ユーザー指示でテキスト量を倍に引き上げたため） | 全スライドを **本文 5〜7 点・各 40〜60 字・合計 250〜300 字** に増量。スライド 13（90〜100 秒枠）は 7 点 322 字に厚くした。長すぎた 2 点（スライド 8 の 1 点目・15 の状態列挙）は 2 点に分割した |
| 4 ポジション視点 | `Gem Index` / `A-1〜A-6` / `CachePort` / `criticality_score` / `OpenNext` / `ADR` が未定義のまま出てくる | 初出箇所に 1 行の説明を追加（`Gem Index` はスライド 3 に「被依存数と star の順位差。定義は後半」、`A-1〜A-6` は「main への直接 push・課金設定など不可逆な操作」、`CachePort` は「アーキテクチャで触れた TTL 付きキャッシュ層のインターフェース」、`criticality_score` は「OpenSSF の既存指標」、`OpenNext` は「Next.js を Workers に載せるアダプタ」、`ADR` は正式名称を併記） |
| 5 可読性 | ① 見出し「層を分けるのは W-1〜W-3 を守るときだけ」は未定義記号が見出しに出る ② スライド 7 の見出しが 38 字で上限超過 ③ スライド 12 の見出しが 25 字で上限（24 字）を 1 字超過 ④ `3MB` だけ単位前スペースがない ⑤ 主語述語が離れた長文が 4 箇所 | ① 「層を分けるのは、3 つの規律を守るときだけ」に変更 ② 「Next.js 16 を Workers の上で動かす」（27 字）に短縮 ③ 「20 項目の機械検証を通してからマージする」（21 字）に短縮 ④ `3 MB` に統一 ⑤ 4 箇所とも書き直した（結論を先に出す・因果を「ため」で補う・助詞を補う） |
| 6 人間らしさ | 開発プロセス（旧 10〜12）と認証の判断（14）が制度の説明に終始し、迷った跡が見えない。スライド 13 に「何を諦めたか」がない | 各スライドに判断の跡を 1 点ずつ追加（「最初は各段階で確認を挟んでいたが待ち時間がボトルネックになった」「テストを後回しにした回は後から直すコストの方が高くついた」「2 回続くと設計判断が要るサイン」「最初は Fine-grained PAT で十分だと思っていた」「差別化の見せ場を自分で外す決定だったが、実測の数字の前では迷う余地がなかった」） |
| 7 リスク・ファクトチェック | **主要な数値・固有名詞は原典と全件一致**（偽 star 約 600 万・`debug_inspector` 25 star / 111,000 以上・Gem Index の定義・227 リポジトリ・3 MB・3〜8 秒 → 0.5〜1 秒・`:path` の 500・404/200・5 回失敗 2 回成功・6 軸と拒否権・DB レスの却下案・技術スタック・`W-1`〜`W-3`）。指摘は ① スライド 4 の出典に ADR 0009 が漏れている ② 「PR 90 件」「20 項目」はレビュアー側で未検証 | ① 出典に ADR 0009 §2.1 を追加 ② 数値の出典（GitHub 検索の実測日・PR #361 本文）をスライド本文と出典欄に明記した |

## ファクトチェックで確認した一次資料

`docs/00_concept/initial-concept.md` / `lean-canvas.md`（`P-1` / `P-2`）/ `docs/adr/0002` `0003` `0005` `0007` `0009` `0012` `0014` /
`docs/02_requirements/open-questions.md`（`D-33`）/ `docs/03_design/architecture/application-architecture.md`（`W-1`〜`W-3`）/
`docs/04_development/testing-strategy.md` / `docs/rules/lessons/cloud-environment.md`（`L-129` / `L-130`）/ `README.md` /
`docs/rules/session-sprint-rules.md` / `pr-review-flow-summary.md` / `user-confirmation-minimization.md`。

数値の実測: マージ済み PR **90 件**（GitHub 検索 `repo:kai-kou/gem-hunter is:merged` の `total_count`・2026-08-22 取得）/
`npm run check` の **20 項目**（PR #361 本文の結果表）/ 実 UI の並び替え・表示件数の選択肢（撮影したスクリーンショットの実物）。

## 載せないと決めたもの（必達原則 4）

- 「N 倍速くなった」「開発効率が向上した」の類 — 比較対象の一次データが存在しない
- `git log` のコミット数 — このセッションの clone は shallow で総数を表さない
- 「ユーザーリサーチを実施した」— `lean-canvas.md` 自身が顧客セグメント未検証（n=0）と明記している
- `lean-canvas.md` の `P-3`（ニーズの強さ）— 原典が未検証と明記。聴衆の欲求を断定する表現も使わない
- 「検索結果が Gem Index 順に並ぶ」— `D-33` で撤去済み。ダイジェスト限定であることを 2 箇所で明示した

## テキスト量の基準（ユーザー指示で 2 倍に引き上げ・2026-08-22）

| 項目 | 旧 | 新 |
|---|---|---|
| 本文の点数 | 4 点 | **5〜7 点** |
| 1 点あたり | 25〜30 字 | **40〜60 字** |
| 本文合計 | 150 字 | **250〜300 字**（スライド 13 のみ 300〜400 字） |
| 見出し（新規生成画像） | 24 字以内 | 変更なし（画像描画の物理的制約） |
| 見出し（既存画像流用） | 32 字以内 | 変更なし |

## 結果

7 軸あわせて **26 件の指摘のうち 24 件を反映**。残り 2 件は意図的に不採用とした。

- 不採用 1: 「見出し 12・16・17 の体言止めを言い切りに統一する」（軸 5・重要度低） — 定義・まとめ・索引の 3 枚は体言止めの方が機能する
- 不採用 2: 「運用・監視（障害検知・ログ・コスト）のスライドを足す」（軸 2・漏れ指摘） — ユーザー提示のアウトライン 7 項目の外側であり、17 枚 20 分の尺にも収まらない。スコープ外として見送る

---

## 追加改訂（2026-08-22・ユーザー指示 2 件）

1. **「セルフレビュー・PR 運用・`/code-review` によるレビューの徹底」を説明したい**
2. **「アーキテクチャ（クリーンアーキテクチャ）についても説明したい」**

いずれも既存スライドの中に埋もれていたため、独立スライドへ分割して厚くした。**17 枚 → 20 枚**。

| 追加・分割 | 新しいスライド | 内容 |
|---|---|---|
| アーキテクチャを 2 枚に分割 | 6「依存は内側へ。中心のドメインは何も知らない」 | 7 層構成（domain / usecases / infrastructure / ui / composition / app / shared）・Dependency Rule・依存性逆転・`composition/` だけが実装を束ねる・ポートは 5 つ |
| 同上 | 7「層を足す条件を、3 つに決めてある」 | `W-1`〜`W-3`・「クリーンアーキテクチャだから」は理由にならない・キャッシュ層が唯一の例外・`ARCH-1`〜`ARCH-7` の機械検査 |
| 旧 11（機械検証 + PR 運用）を 3 枚に分割 | 12「機械検証を通さないと PR を出せない」 | Layer 0。`npm run check` の 20 項目の内訳・PR 本文への結果表貼付が必須・フックが PR 作成をブロック |
| 同上 | 13「レビューは、他人の PR として読み直す」 | Layer 1。外部 AI レビュアー廃止・`/code-review` の 3 段（観点別ファインダー 5〜7 体の並列 → 敵対的検証 → 記録）・CONFIRMED / PLAUSIBLE・全件インラインコメント |
| 同上 | 14「実装からマージまで、確認なしで走り切る」 | PR 運用。PR 本文の必須項目・`Session-Id` による所有判定・サーキットブレーカー（2 回超で停止）・マージ済み PR 90 件 |

### 追加分のファクトチェック（一次資料で確認済み）

- 7 層の名前と役割・`ARCH-1`〜`ARCH-7`・ポート 5 つ（`RepositoryQueryPort` / `CachePort` / `RateLimitPort` / `ClockPort` / `AuthPort`）
  — `docs/03_design/architecture/application-architecture.md` §1.1〜§1.3・§2 / `docs/rules/architecture-rules.md` §2
- `src/` の実ディレクトリが 7 層とおおむね一致すること — `find src -maxdepth 2 -type d` の実行結果
- `ARCH-4` / `ARCH-5` は `// arch-ok` で抑止できないこと — `architecture-rules.md` §2 の表と §「抑止」節
- `npm run check` が **20 項目** であること — `tools/run_checks.sh` の実体を読んで列挙・PR #361 本文の結果表と一致
- `/code-review` の 3 段構成・観点 5〜7 系統・CONFIRMED / PLAUSIBLE の区別・指摘ゼロでもレビュー 1 件投稿（#461）
  — `.claude/skills/code-review/SKILL.md` Step 1〜3・Step 3-A
- 外部 AI レビュアー（Copilot・Gemini）の廃止 — `docs/rules/ai-reviewer-strategy.md`
- サーキットブレーカー（修正サイクル 2 回超で停止） — `docs/rules/pr-review-flow.md`

### 尺への影響

20 枚 × 60〜70 秒 = **20〜23 分**。当初の想定（15〜20 分）を上回るため、短縮が要るときは以下の順で削る想定にしておく。

1. スライド 7（層を足す条件）を 6 に統合して 1 枚減らす
2. スライド 12（機械検証ゲート）を 14（PR 運用）に統合して 1 枚減らす
3. スライド 5（ドキュメント全体像）を口頭のみにして 1 枚減らす

---

## 2026-08-23 改訂版（21 枚）の Step 2 記録

19 枚 → **21 枚** への改訂（`slides_plan.json` `decisions` の "H: 2026-08-23 改訂"）に対する 2 回目の Step 2。
新スライド 3「検索語をそのまま、Gem だけの一覧へ」（実 UI）と新スライド 18「母集団を広げたら、撤去の根拠が消えた」の
追加、および事実修正 10 件（ポート 7 つ / 機械検証 38 項目 / GitHub Actions の用途 / マージ済み PR 120 件 /
被依存数の取得元 12 レジストリ / 健全性指標が未実装である旨 / テスト実数 / 本番ビルドの install スクリプト /
Gem データも静的 JSON / 「今日の Gem」の用途の訂正）がレビュー対象。

### 実施方法（前回との差分）

前回同様 `doc-review` の **7 軸レビュー**（軸 2 = MECE・軸 7 = ファクトチェック）を実施した。
ただし今回は **サブエージェントによる並列実行ができず、単一レビュアーが 7 軸を順に実施した**
（`Task` / `Agent` ツールがこのセッションで無効化されていたため）。軸の定義・観点・出力様式は前回と同一。
軸 7 は一次資料を **実行して**（`grep` / `python3` によるカウント・GitHub API の `total_count`）突き合わせた。

### 軸ごとの主な指摘と対応

| 軸 | 主な指摘 | 推奨する対応（構成の正本 `slides_plan.json` 側で反映する） |
|---|---|---|
| 1 戦略・目的 | ① スライド 18 の 1 点目が撤去理由を「候補が 227 件しかないこと」だけに要約しており、`D-33` が実際に書いた **「npm 限定という構造的限界」** を落としている。撤去判断の誤りを小さく見せる形になり、**「言い訳」に読める最大の原因** ② 18 に「なぜ `sort=gem-index` を戻さないのか」の理由がなく、被覆率が回復したのに戻さないのが強情に見える ③ 3 が「star のわりに使われている」という **スライド 5 の主題を先に消費** している | ① 1 点目を「当時は『npm 限定だから母集団を広げても非 JS 系には効かない』と構造的限界だと書いた。そこが間違っていた」に差し替える ② 「戻さない理由は被覆率ではない。上位 100 件の 68 件に値が無く、有名なものを下げる指標を主ソートにすると検索の正しさが壊れる」を追加 ③ 3 の 1 点目を「検索結果のカードには、後半で定義する Gem Index が高い候補に「Gem」の印が付く」に差し替え、理由の説明は 5 と 16 に譲る |
| 2 論理・MECE | ① **Gem Index の説明が 3・4・16 の 3 か所に分散** し、4 の 5 点目「この並びに使う指標が Gem Index（被依存数と star の順位差。定義は後半で扱う）」と 3 の 1・3 点目が同じ内容を別の言葉で述べている ② 4 の 6 点目「Gem Index は検索結果の並び順を変えない。順位づけに使うのはこの並びと Gem 一覧だけ」が **17・18 の結論の先出し** になっており、18 の 7 点目とも重複する ③ 18 だけが持つ固有の論点（＝撤去根拠の消滅）が、16（定義）・17（撤去）の再説明に埋もれている ④ **CE 違反**: 18 に「戻さない理由」が無い（軸 1 ②と同一） ⑤ 20 の 5 点目と 18 の 6 点目が「静的アセットで配る」で重複 | ① Gem Index の **名前の初出を 3 に一本化** し、4 の 5 点目は「この並びにも同じ Gem Index を使う」に短縮する ② 4 の 6 点目を削除する（17・18 が本来の持ち場） ③ 18 の見出し「母集団を広げたら、撤去の根拠が消えた」は保つが、本文は「当時の根拠 → 何が変わったか → それでも戻さないもの」の 3 段だけに絞る ⑤ 20 の 5 点目を「Gem のデータも静的 JSON で、サーバー側に状態を持たない点は他と同じ」に変えて理由の再説明を落とす |
| 3 実行設計 | ① **尺の記載が過少**。20 枚 × 60〜70 秒 + 17 の 90〜100 秒 = **21.5〜25.0 分** で、冒頭の「想定尺: 21〜24 分」と合わない ② **スライド 17 が 9 点・348 字**（他は 5〜7 点）。90〜100 秒枠でも 9 個の箇条書きは assertion-evidence の原則から外れる ③ 実 UI スクリーンショットが 2・3・4 と **3 枚連続** し、「なぜ作ったか」が 5 まで来ない ④ **素材が未整備**: `images/` に `shot-03`（Gem 一覧）も `new-14`（広げる前と後の対比）も存在しない（Step 9 の前に撮影・生成が要る） ⑤ 7（310 字）・17（348 字）が上限超過、11（228 字）・21（178 字）が下限割れ | ① 冒頭を「想定尺: 22〜25 分」に直す ② 17 の 2・3 点目（候補プール 294 件 / ユニーク 227 件）を「当時の候補プールは npm だけの上位 294 件で、リポジトリ単位ではユニーク 227 件しかなかった」の 1 点に統合して 8 点にし、さらに削るなら 1 点目（作る前の想定）を口頭に回す ③ 短縮が必要になったときの削り順を **「3 を 2 に統合 → 8 を 7 に統合 → 6 を口頭のみ」** に更新する（前回の 3 案は番号がずれて無効） ④ 撮影・生成タスクを Step 9 の前に積む ⑤ 21 に 1 点足して 5 点 230 字前後まで厚くする |
| 4 ポジション視点 | ① **`sort=gem-index` が 17 で初出**。ここまで一度も画面にも本文にも出ておらず（2 は「関連度・star 数・更新日時」しか挙げない）、聴衆は **見たことのないものを「撤去した」と言われる** ② 「過小評価の度合い」（3）・「単語境界一致」（3）・「1 レジストリ支配」「固定枠」（18）が未定義 ③ 3 の 5 点目「印が付いた候補は URL に同伴させて渡すので、印と一覧で判定基準がずれない」が、実装を知らないと意味が取れない ④ 2 の「GitHub API のレート枠」と 3 の「レート制限の枠」が **別物なのに同じ語** | ① 17 の 6 点目を「採用: 検索結果を Gem Index 順に並べ替える `sort=gem-index` という並び順を用意していたが、これを撤去した（指標の定義そのものは撤回しない）」に差し替える ② 3 の 4 点目を「絞り込みはリポジトリ名とパッケージ名を語の区切りで照合し（部分一致では拾いすぎるため）、0 件のときだけ 1 語に緩める」に、18 の 4 点目を「特定のレジストリが枠を独占することはなく、最大の npm でも全体の 15.5% に収まる」に差し替える ③ 「検索結果で印が付いた候補は一覧側へそのまま引き渡すので、印と一覧で判定基準がずれない」に平易化 ④ 3 の 7 点目を「この一覧は GitHub API を叩かないが、利用者どうしで枠を食い合わないよう検索とは別のレート制限枠を用意した」に直す（因果の訂正も兼ねる・軸 7 参照） |
| 5 可読性 | ① **見出しは全 21 枚が上限内**（新規生成 24 字以内・実 UI / 既存流用 32 字以内。最長は 7 の 21 字、新 3 は 17 字・新 18 は 18 字）— 指摘なし ② 18 の 5 点目「一般語 24 語で測り直すと、検索上位 100 件との被覆率は平均 34.5%・0 件の語はなし」は 1 文に数字が 4 つ入り読点で息が続かない ③ 13 の 3・4 点目（38 項目の内訳）が数字の羅列で、口頭で追えない | ② 「一般語 24 語で測り直すと、検索上位 100 件のうち平均 34.5% に Gem Index が付いた。0 件の語はひとつもない」の 2 文に割る ③ 内訳（14 点 / 19 点）を落として「検査は 38 項目。lint・型・テスト・E2E・Lighthouse に、依存規則や表記ゆれの静的検査と運用ツール自身の self-test を重ねてある」の 1 点に統合する（軸 7 の内訳ずれも同時に解消する） |
| 6 人間らしさ | **スライド 18 に判断の跡が 1 つも無い**。他の主要スライド（10・11・12・15・17・19）はすべて「最初はこう思っていた」「後から高くついた」の 1 点を持つのに、18 は事実の羅列だけで終わる。これが軸 1 の「言い訳に見える」印象を決定的にしている | 18 の最終点として「自分が実測で殺した判断を、自分の実測でひっくり返す作業だった。気持ちのいいものではないが、記録が残っていたから測り直せた」を追加する（21 の 4 点目への接続にもなる） |
| 7 リスク・ファクトチェック | **一次資料で確認できたもの（下記「確認した一次資料」に全件）。裏が取れなかった / 食い違ったのは 4 件**: ① 13 の内訳「静的検査 14 点 / 残る 19 点は self-test」— 合計 38 は正しいが、`--self-test` を名に持つ検査は **18 点**、それ以外の静的検査は **15 点**。`test_wip_commit_guard.sh`（回帰テスト）を self-test 側に数えたときだけ 14 / 19 になる ② 3 の「この一覧は GitHub API を叩かない **ので**、レート制限の枠を検索とは別に用意した」— 別枠である事実（`gems:` 接頭辞）は確認できたが、コード上の理由は「正常な利用者どうしが枠を食い合うため」で、**因果が一次資料と違う** ③ 18 の「各 15,000 件の固定枠で集め直した」— `D-37` の既定値 15,000 は正しいが、実収集は 147,527 件（各レジストリ 7,306〜14,510 件）で **15,000 に届いたレジストリは 0**。上限であることが伝わらない ④ 12 の「単体・結合が 779 ケース」— 静的カウントでは `src/` 配下 774。`it.each` の展開差の範囲だが **再現手順が記録に無い** | ① 内訳を落として「38 項目」だけにする（軸 5 ③ と同じ対応） ② 「GitHub API を叩かないが、利用者どうしで枠を食い合わないよう検索とは別枠にした」へ（軸 4 ④ と同じ対応） ③ 「各レジストリから被依存数の多い順に最大 15,000 件ずつ、あわせて 14.7 万パッケージを集め直した」へ ④ 出典欄に数え方（`npx vitest run` の集計値であること）を明記する |

### ファクトチェックで確認した一次資料（2026-08-23 実行）

| 主張（スライド） | 一次資料 | 結果 |
|---|---|---|
| ポートは 7 つ（7） | `src/domain/ports/`（`auth` / `cache` / `clock` / `gem-digest` / `gem-index` / `rate-limit` / `repository-query`） | ✅ 7 ファイル |
| 機械検証 38 項目（13） | `tools/run_checks.sh`（`run_check` 36 件 + E2E + Lighthouse） | ✅ 38（内訳のみ要修正） |
| Actions は Gem 候補プールの日次生成のみ・PR まで（13） | `.github/workflows/gem-pool-refresh.yml`（workflow は 1 本のみ・`cron: 17 21 * * *`・main 直 push と自動マージを明示的に禁止） | ✅ |
| マージ済み PR 120 件（15） | GitHub API `repo:kai-kou/gem-hunter is:merged` → `total_count: 120` | ✅ |
| 12 レジストリ / ユニーク 62,483 / npm 15.5%（18） | `public/data/gem-index/index.json`（`shards` 12 件・`totalCount` 62483・`registryShare.npmjs.org` 15.49） | ✅ |
| 一般語 24 語・平均 34.5%・0 件なし（18） | `docs/02_requirements/open-questions.md` `D-36` の `SP-17` 実測追記 | ✅ |
| 汚染フィルタは star 下限 5・健全性指標は未実装（16） | `tools/generate_gem_digest.mjs`（`DEFAULT_MIN_STARS = 5`・採用根拠の実測表）/ `docs/adr/0009` §2.1 の役割表（健全性＝足切り・自作しない） | ✅（`criticality_score` を参照するコードは存在しない） |
| Gem Index ＝ 被依存数と star のパーセンタイル順位の残差（16） | `docs/adr/0009` §2.1 / §3.4 | ✅ |
| 印・一覧・単語境界一致・1 語への緩和・「付かない＝低評価ではない」（3） | `messages/ja.json`（`home.gemBadge.*` / `home.gemListLink.label` / `gems.unmatchableQuery` / `gems.relaxedNotice` / `gems.includedFromSearch`） | ✅ |
| Gem 一覧は 20 件ずつ（3） | `src/domain/model/per-page` の `DEFAULT_PER_PAGE`（`src/ui/gem-list.tsx`） | ✅ |
| Gem 一覧は検索と別のレート制限枠（3） | `src/composition/rate-limit.ts`（`enforceGemListRateLimit` / キー接頭辞 `gems:`） | ✅ ただし理由が違う（上表 ②） |
| E2E 106 ケース（12） | `e2e/*.spec.ts` の `test(` 119 − `test.describe(` 13 = 106 | ✅ |
| 227 件 / 3 MB / 10 リクエスト 3〜8 秒 → 1 回 0.5〜1 秒（17） | `open-questions.md` `D-30` / `D-33` | ✅（前回から変更なし） |
| 静的アセット配信（18・20） | `open-questions.md` `D-38` | ✅ |

🔴 **副次的に見つかったリポジトリ側の不整合（スライドは正しい）**: `docs/adr/0009` §2.3 の追記と
`open-questions.md` `D-36` 本文は母集団を **109,469 リポジトリ**（実現可能性調査時点の数字）と書いているが、
実際に生成された `public/data/gem-index/index.json` は **62,483**（汚染フィルタ `minStars=5` 適用後）。
スライド 18 は後者を採っており正しいが、**ドキュメント側が古い**。質疑で突かれると答えられないため、
別 Issue（`type:docs`）を起票してドキュメント側を実測値へ揃えることを推奨する。

### 載せないと決めたもの（必達原則 4・今回の追加分）

- 「Gem 一覧の被覆率が上がったので発見体験が良くなった」— 被覆率（34.5%）は測ってあるが、**発見体験への効果は測っていない**。効果の断定にあたるため書かない
- 「12 レジストリ化で非 JS 系にも届くようになった」の **割合表現**（`D-36` の「検索ヒットの 52% が非 npm」）— この 52% は 109,469 件時点の実測で、現行の 62,483 件では測り直していない。数字を伴う主張としては使わない
- 各レジストリの内訳（npm 9,677 / PyPI 9,326 / Go 8,427 …）— 1 枚の情報量を超え、`15.5%` という「支配が起きていない」証拠だけで論点は足りる
- テストの網羅率（カバレッジ）— 計測していない
