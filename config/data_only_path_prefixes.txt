# tools/detect_pr_diff_type.py が読む「自動生成データのみ」パス接頭辞リスト。
# ここに列挙したプレフィックスで始まるファイルは、コード拡張子であっても
# コード変更とみなさず、PR レビューの Layer 2（敵対的多観点議論）をスキップする対象にする。
#
# 1 行 1 プレフィックス。`#` で始まる行・空行は無視する。
# ファイルが無い/空でも動作する（その場合コードの組み込み既定値にフォールバックする）。
# 自プロジェクト固有の自動生成データ置き場（例: 動画パイプラインの生成 TS ファイル等）を
# 追加したい場合は、このファイルに行を追加するだけでよい（コード変更不要）。

content/
