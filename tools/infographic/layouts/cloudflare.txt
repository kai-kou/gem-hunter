a left-to-right request-flow diagram: browser -> Workers Static Assets / Workers Caching -> Worker (OpenNext adapter) -> GitHub API / GitHub OAuth, with a Rate Limiting binding attached to the Worker. Label the two cache outcomes correctly and do not swap them: the arrow that goes from Workers Caching INTO the Worker is the cache miss and must be labelled 「MISS時」, while the arrow that returns from Workers Caching BACK to the browser without touching the Worker is the cache hit and must be labelled 「HIT」. Below the Worker box, stack three cache layers and label them with exactly the three L1 / L2 / L3 lines written in the 「キャッシュ階層」 block below — copy those Japanese lines verbatim, do not translate them into English. Do NOT draw a separate environment lane or deploy lane inside the diagram: the 「環境構成」 and 「デプロイ」 blocks below already carry that content, and drawing them twice would repeat a heading.